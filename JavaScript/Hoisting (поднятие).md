## 📈 Hoisting (поднятие)

**Hoisting** — механизм, при котором объявления переменных и функций поднимаются в начало области видимости перед выполнением кода.

Это позволяет использовать переменные и функции до их фактического объявления в коде.

---

### ⚠️ Поведение разных типов объявлений:

#### `var` и объявления функций (function declaration)
Поднимаются **целиком**.

```js
console.log(x); // undefined (не ошибка!)
var x = 5;

foo(); // работает!
function foo() {
  console.log('Hello');
}
```

#### `let` и `const`
Поднимаются, но находятся в **temporal dead zone** (временная мёртвая зона).

Доступ к ним до инициализации вызовет ошибку **`ReferenceError`**.

```js
console.log(a); // ❌ ReferenceError: a is not defined
let a = 10;

console.log(b); // ❌ ReferenceError: b is not defined
const b = 20;
```

---

### 🧠 Итог:

- `var` и `function` — поднимаются, можно использовать до объявления
- `let` и `const` — поднимаются, но недоступны до инициализации (temporal dead zone)
