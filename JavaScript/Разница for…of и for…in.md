## 🔄 Разница `for…of` и `for…in`

---

### 🔹 `for...in`

Перебирает **ключи** (имена свойств) объекта.

- ✅ Работает с объектами и массивами
- ⚠️ Может проходить по **наследуемым свойствам**
- ⚠️ Порядок ключей не гарантируется

---

### 🔹 `for...of`

Перебирает **значения** итерируемых объектов (массивы, строки, Map, Set и т.д.).

- ✅ Порядок итерации определяется самим итератором (например, у массива — по индексам)
- ❌ Не работает с обычными объектами (без итератора)

---

### 📝 Пример для массива:

```js
const arr = ['a', 'b', 'c'];

for (const i in arr) console.log(i);   // 0, 1, 2  — ключи
for (const v of arr) console.log(v);   // a, b, c  — значения
```

---

### 📝 Пример для объекта:

```js
const obj = { a: 1, b: 2 };

for (const key in obj) {
  console.log(key);          // a, b  — ключи
}

for (const val of Object.values(obj)) {
  console.log(val);          // 1, 2 — значения
}
```
