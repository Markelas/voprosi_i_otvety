## 🌐 Что такое `fetch` и как работает

**`fetch`** — встроенная в браузер функция для выполнения HTTP-запросов. Возвращает [[Promise]], который разрешается объектом `Response`. Замена старого `XMLHttpRequest`.

---

### 🔧 Синтаксис

```js
fetch(url, options?)
```

- **`url`** — адрес запроса (строка или объект URL)
- **`options`** — объект с настройками: метод, заголовки, тело запроса

---

### 📊 Как работает

`fetch` возвращает `Promise`, который:

- **`resolve`** → когда сервер вернул ответ (объект `Response`), **не обязательно успешный**
- **`reject`** → при сетевой ошибке

**Важно:** при HTTP-ошибках (404, 500) промис **не отклоняется** — нужно проверять `response.ok`.

---

### ⚙️ Основные опции в `options`

| Параметр | Описание | Пример |
|----------|----------|--------|
| **`method`** | HTTP-метод | `'GET'`, `'POST'`, `'PUT'`, `'DELETE'` |
| **`headers`** | Заголовки запроса | `{ 'Content-Type': 'application/json' }` |
| **`body`** | Тело запроса | `JSON.stringify({...})`, `FormData`, строка |
| **`credentials`** | Отправка cookies | `'include'`, `'same-origin'` |
| **`mode`** | Режим CORS | `'cors'`, `'no-cors'`, `'same-origin'` |

По умолчанию cookies не отправляются — для этого указывают `credentials: 'include'`.

---

### 📦 Объект Response

**Методы чтения тела:** `.json()`, `.text()`, `.blob()`, `.arrayBuffer()`, `.formData()` — каждый возвращает Promise.

**Полезные свойства:** `response.ok` (true при статусе 200–299), `response.status`, `response.statusText`, `response.headers`.

---

### ✅ Обработка ответа и ошибок

Проверять `response.ok` и при необходимости бросать ошибку:

```js
fetch(url)
  .then(response => {
    if (!response.ok) throw new Error('HTTP ' + response.status);
    return response.json();
  })
  .then(data => console.log(data))
  .catch(err => console.error(err));
```

---

### 🛑 Отмена запроса (AbortController)

Запрос можно отменить через [[AbortController - отмена запроса]]: передать `signal: controller.signal` в `options`, вызвать `controller.abort()` для отмены.

---

### 📤 Отправка данных

**JSON:** `method: 'POST'`, `headers: { 'Content-Type': 'application/json' }`, `body: JSON.stringify({...})`.

**FormData:** `body: new FormData()` с `append('key', value)` или файлом — без явного Content-Type.

---

### 🔄 Async/await

```js
const response = await fetch(url);
if (!response.ok) throw new Error('HTTP ' + response.status);
const data = await response.json();
```

---

### 💡 Итого

- ✅ `fetch` асинхронный, возвращает `Promise`
- ⚠️ Не бросает ошибки на HTTP 404/500 — проверять `response.ok`
- ✅ Отмена через `AbortController`
- ✅ Замена `XMLHttpRequest`
