## 🔄 Event Loop

**Event Loop** — механизм в JavaScript, который управляет выполнением кода, обработкой событий и выполнением асинхронных операций, работая по принципу очередей задач.

Event Loop — это механизм, который управляет порядком выполнения кода в JavaScript.

Он позволяет **однопоточному JS работать асинхронно** — не блокируя выполнение других задач.

---

### ⚙️ Основные части механизма:

#### 1. **Call Stack** (стек вызовов)
Сюда попадает и выполняется **синхронный код** (функции, выражения).

#### 2. **Web APIs**
Внешние браузерные механизмы (не часть движка JS), которые выполняют асинхронные задачи:

`setTimeout`, `fetch`, DOM events, `MutationObserver`, `WebSocket`, `Geolocation` и т.д.

Когда JS встречает что-то вроде `setTimeout` или `fetch`, сам JS не выполняет эти задачи — он передаёт их в **Web API браузера**. Когда задача завершена — колбэк возвращается обратно в очередь (в зависимости от типа — в микро- или макро).

#### 3. **Callback Queue** (очередь макротасок)
Сюда возвращаются колбэки из Web APIs, когда готовы.

#### 4. **Microtask Queue** (очередь микротасок)
Очередь задач с **более высоким приоритетом**.

---

### 🔁 Как работает Event Loop

1. Выполняется весь **синхронный код** (из стека).
2. Потом — все **микротаски**, накопленные за время выполнения.
3. Затем — **одна макротаска**, например `setTimeout`.
4. После неё снова — все **микротаски**.

И так по кругу.

---

### 🧩 Разница между микро- и макротасками:

| Тип | Когда выполняется | Примеры |
|-----|-------------------|---------|
| **Микротаски** (Microtasks) | Сразу после текущего кода, до следующей макротаски | `Promise.then()`, `catch`, `finally`, `queueMicrotask()`, `MutationObserver`, `process.nextTick()` |
| **Макротаски** (Macrotasks) | После микротасков, по одной за цикл Event Loop | `setTimeout`, `setInterval`, `setImmediate`, обработчики DOM-событий, I/O, `fetch`, `WebSocket` |

---

### 📝 Важные детали:

**[[Mutation Observer]] — микротаска**  
Потому что его callback попадает в очередь микротасок, выполняясь до следующей макротаски, но после текущего синхронного кода. Это важно, если в коде нужно реагировать на изменения DOM максимально быстро.

**Создание (инициализация) промиса — это синхронная операция.**  
А выполнение колбэков `resolve` / `reject` — уже асинхронное, и попадает в очередь микротасок.

```js
const promise = new Promise((resolve, reject) => {
  console.log('1'); // выполняется сразу, синхронно
  resolve(); // ЕСЛИ resolve() НЕ БУДЕТ, ТО НЕ ВЫПОЛНИТСЯ then()
});

promise.then(() => console.log('2')); // микротаска
```

---

### 📊 Разница между стеком вызовов (call stack) и очередью задач (Task Queue)

| Параметр | Стек вызовов (Call Stack) | Очередь задач (Task Queue / Callback Queue) |
|----------|---------------------------|----------------------------------------------|
| Что это | Структура, где выполняется синхронный код | Очередь, где хранятся асинхронные колбэки, ожидающие выполнения |
| Как работает | **LIFO** (последний зашёл — первый вышел) | **FIFO** (первый зашёл — первый вышел) |
| Примеры кода | Вызовы функций, обычные выражения, циклы | Колбэки `setTimeout`, `fetch`, событий DOM и т.д. |
| Когда выполняется | Сразу, пока стек не пуст | После того, как стек опустел |
| Кто управляет | Движок JS (например, V8) | Event Loop |

---

### ⚠️ Важно:

**Длинные синхронные операции блокируют Event Loop** — он не может обработать асинхронные задачи.
