## 📦 Что такое Promise

**Promise** — это встроенный объект в JavaScript для работы с асинхронными операциями.

Он представляет значение, которое может быть доступно **сейчас**, **позже** или **никогда**.

---

### 📊 Состояния Promise:

1. **`pending`** — ожидание результата (по умолчанию).
2. **`fulfilled`** — операция завершена успешно (результат доступен в `.then`).
3. **`rejected`** — операция завершилась с ошибкой (ошибка обрабатывается в `.catch`).

---

### 🔧 В коллбеке параметры:

- **`resolve`** – результат успешной операции (направит в `then`)
- **`reject`** – возвращает ошибку (направит в `catch`)

---

### 🎯 Методы:

#### • **`.then(onFulfilled)`**
Выполняется при успешном завершении.

Здесь также можно обработать ошибку, передав второй коллбэк `.then((success) => {}, (error) => {})`, но это менее очевидно.

#### • **`.catch(onRejected)`**
Выполняется при ошибке.

#### • **`.finally(onFinally)`**
Выполняется **всегда**, вне зависимости от результата.

⚠️ Если стоит в цепочке раньше других, то и выполнится раньше.

---

### 🚀 Статические методы:

#### `Promise.all`
Ждёт, пока **все промисы выполнятся успешно**, и возвращает массив результатов.

Если хотя бы один промис отклонён — весь `Promise.all` падает с ошибкой.

**Возвращает:** массив успешных значений, либо ошибку.

```js
Promise.all([promise1, promise2, promise3])
  .then(values => console.log(values)) // [value1, value2, value3]
  .catch(error => console.error(error));
```

---

#### `Promise.allSettled`
Ждёт завершения **всех промисов**, даже если часть — с ошибкой.

**Возвращает:** массив с объектами:
- Если успешно: `[{status: 'fulfilled', value}, ...]`
- Если ошибка: `[{status: 'rejected', reason}, ...]`

```js
Promise.allSettled([promise1, promise2])
  .then(results => {
    results.forEach((result, index) => {
      if (result.status === 'fulfilled') {
        console.log(`Promise ${index}:`, result.value);
      } else {
        console.log(`Promise ${index} failed:`, result.reason);
      }
    });
  });
```

---

#### `Promise.race`
Возвращает результат **первого завершившегося промиса** — успех или ошибка, без разницы.

**Возвращает:** значение, либо ошибку первого промиса.

💡 **Хак:** можно использовать для ограничения времени запроса:

```js
Promise.race([
  fetch('/api'),
  new Promise((_, reject) => 
    setTimeout(() => reject('Timeout'), 3000)
  )
]);
```

---

#### `Promise.any`
Возвращает **первый успешно выполненный промис** (значение). Ошибки игнорирует, пока не упадут все — тогда вернёт `AggregateError` (это массив из ошибок `[Error: Ошибка 1, Error: Ошибка 2]`).

**Где можно использовать:** например, пользователь зашёл на сайт и мы делаем редирект на зеркало, ищем для него первое успешное подключение.

---

#### `Promise.resolve(value)`
Возвращает промис, уже находящийся в состоянии `fulfilled`.

#### `Promise.reject(reason)`
Возвращает промис в состоянии `rejected`.

---

### 🔄 Промисификация (promisification)

**Промисификация** — это процесс оборачивания функции с колбэком в функцию, возвращающую Promise.

Нужно, чтобы использовать старые callback-функции в современном `async/await`-коде.