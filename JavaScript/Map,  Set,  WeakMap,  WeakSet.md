## 🧭 `Map`  
Коллекция **ключ → значение**, где **ключи могут быть любого типа**.    
Сохраняет **порядок вставки** элементов.  
  
  🔧 Основные методы  
```js  
const map = new Map();  
  
map.set('name', 'Alice');  
map.get('name');     // 'Alice'  
map.has('name');     // true  
map.delete('name');  
map.clear();  
map.size;            // количество элементов  
```  
---  
### ⚖️ Отличия `Object` vs `Map` 

| Свойство           | Object                    | Map                                    |
| ------------------ | ------------------------- | -------------------------------------- |
| Ключи              | Только строки и `Symbol`  | Любой тип                              |
| Порядок            | Частично сохраняется      | Всегда сохраняется                     |
| Размер             | `Object.keys(obj).length` | `.size`                                |
| Методы             | `in`, `delete`, `obj.key` | `set`, `get`, `has`, `delete`, `clear` |
| Производительность | Лучше при малом объёме    | Быстрее при частых изменениях          |

---  
🔁 Преобразования  
  
```js  
// Object → Map  
const obj = { name: 'Alice', age: 25 };  
const mapFromObj = new Map(Object.entries(obj));  
  
// Map → Object  
const map = new Map([['name', 'Alice'], ['age', 25]]);  
const objFromMap = Object.fromEntries(map);  
```  
  
---  
🧩 Вложенность  
  
```js  
const users = new Map();  
users.set('user1', new Map([  
  ['name', 'Anna'],  ['age', 25]]));  
  
console.log(users.get('user1').get('name')); // "Anna"  
```  
  
---  
## 🔹 `Set`  
Коллекция **уникальных значений**, без ключей.    
Сохраняет порядок вставки.  
  
### 🔧 Основные методы  
```js  
const set = new Set();  
  
set.add('a');  
set.add('b');  
set.add('a'); // не добавится повторно  
  
set.has('a'); // true  
set.delete('b');  
set.clear();  
set.size; // количество элементов  
```  
  
---  
  
### ⚖️ Отличия `Set` vs `Array`  
  
| Свойство     | Array                             | Set                                    |
| ------------ | --------------------------------- | -------------------------------------- |
| Уникальность | ❌ допускает дубликаты             | ✅ только уникальные                    |
| Доступ       | по индексу                        | без индексов                           |
| Поиск        | O(n)                              | O(1) (в среднем)                       |
| Методы       | много (`map`, `filter`, `reduce`) | мало (`add`, `has`, `delete`, `clear`) |
| Порядок      | сохраняется                       | сохраняется                            |
| Применение   | универсальные списки              | уникальные множества                   |
  
```js  
const unique = [...new Set(['a', 'b', 'a'])];  
console.log(unique); // ['a', 'b']  
```  
  
---  
  
## 🧷 `WeakMap` и `WeakSet`  
  
### 🧱 `WeakMap`  
- Ключи **только объекты**, примитивы запрещены ❗  - Не итерируется, нет `.size`.  
- Объекты автоматически удаляются при сборке мусора 🧹.  
- Подходит для хранения **временных данных** (например, кэш или метаданные DOM-элементов).  
  
```js  
const wm = new WeakMap();  
const el = {};  
wm.set(el, { data: 42 });  
```  
  
---  
  
### 🧱 `WeakSet`  
- Хранит **только объекты**.  
- Не итерируется.  
- Используется, чтобы **отмечать** уже обработанные объекты без риска утечек памяти.  
  
```js  
const ws = new WeakSet();  
const obj = {};  
ws.add(obj);  
console.log(ws.has(obj)); // true  
```  
  
---  
  
### 🧠 Краткое сравнение  
  
| Коллекция | Ключи          | Значения | Уникальность | Сборщик мусора | Итерация |
| --------- | -------------- | -------- | ------------ | -------------- | -------- |
| Map       | любой тип      | любые    | ❌            | ❌              | ✅        |
| WeakMap   | только объекты | любые    | ❌            | ✅              | ❌        |
| Set       | значения       | значения | ✅            | ❌              | ✅        |
| WeakSet   | только объекты | объекты  | ✅            | ✅              | ❌        |
