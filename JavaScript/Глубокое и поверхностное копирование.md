## 📋 Глубокое и поверхностное копирование

---

### 🔸 Поверхностное копирование

**Вложенные объекты/массивы остаются ссылками.**

#### 1. Через spread оператор:
```js
const copy = {...obj};
const arrCopy = [...arr];
```

#### 2. `Object.assign({}, obj)`:
```js
const copy = Object.assign({}, obj);
```

---

### 🔹 Глубокое копирование

Создаёт **полностью независимую копию**, включая вложенные объекты и массивы.

#### 1. `JSON.parse(JSON.stringify(obj))`
❌ Не работает с функциями, датами, `Map`, `Set` и т.д.

```js
const deepCopy = JSON.parse(JSON.stringify(obj));
```

#### 2. `structuredClone(obj)` (современный вариант)
✅ Копирует все, **кроме функций**

```js
const deepCopy = structuredClone(obj);
```

#### 3. `cloneDeep(obj)` из библиотеки `lodash`
✅ Универсальное решение

```js
import { cloneDeep } from 'lodash';
const deepCopy = cloneDeep(obj);
```

#### 4. Написать самому функцию
Рекурсивное копирование с обработкой всех типов данных.

```js
function deepCopy(obj) {
  // Если это примитив или функция, возвращаем как есть
  if (obj === null || typeof obj !== 'object') {
    return obj;
  }

  // Если это массив
  if (Array.isArray(obj)) {
    return obj.map(item => deepCopy(item));
  }

  // Если это объект
  const copy = {};

  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      copy[key] = deepCopy(obj[key]);
    }
  }

  return copy;
}
```
