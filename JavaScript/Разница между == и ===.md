### 🔸 `==` — Нестрогое сравнение

Сравнение **по значению** с **приведением типов** (type coercion).

Перед сравнением пытается преобразовать значения к одному типу.

```js
5 == "5";    // true (строка преобразуется в число)
null == undefined; // true
false == 0;  // true
"" == 0;     // true
```

---

### 🔹 `===` — Строгое сравнение

Сравнение **по значению и типу**, **без преобразования**.

```js
5 === "5";   // false (разные типы)
null === undefined; // false
false === 0; // false
"" === 0;    // false
```