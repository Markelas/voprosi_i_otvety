## 🔄 IIFE (немедленно вызываемые функции)

**IIFE** (Immediately Invoked Function Expression) — это функция, которая создаётся и **сразу же вызывается**.

---

### 🎯 Для чего используется:

- ✅ Создание **отдельной области видимости** и предотвращение загрязнения глобального пространства переменных
- ✅ Использование **замыканий** для приватных данных
- ✅ Выполнение кода **один раз** при загрузке

---

### 💡 Пример:

```js
var globalVar = "Я глобальная";

(function() {
    var localVar = "Я локальная";
    globalVar = "Изменено внутри IIFE";
    
    console.log(localVar);  // Я локальная
    console.log(globalVar); // Изменено внутри IIFE
})();

console.log(globalVar); // Изменено внутри IIFE
// console.log(localVar); // ❌ Ошибка! localVar недоступна вне IIFE
```

---

### 🕰️ Почему IIFE раньше использовали часто:

Раньше в JS не было модулей и **блочной области видимости** (`let`, `const`), поэтому IIFE был основным способом создать локальные переменные и "приватность".

---

### ✅ Почему сейчас почти не используют:

Сейчас это решается проще и безопаснее:

- **ES-модули** (`import` / `export`) — каждый модуль имеет собственную область видимости
- **Блочная область видимости** (`let`, `const` с фигурными скобками `{}`)

---

### ❓ Где сейчас используется IIFE?

В современном коде **редко**, в основном в старом JS без модулей — для изоляции кода и создания приватных переменных.

Сейчас те же задачи решают ES-модули и блочная область видимости.

*(С ES6 ECMAScript 2015 появились `let`, `const` и блочная область видимости)*
