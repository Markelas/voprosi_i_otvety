## 🍛 Каррирование (Currying)

**Каррирование** — это техника функционального программирования, при которой функция с несколькими аргументами преобразуется в последовательность функций, каждая из которых принимает один аргумент.

Вместо вызова `f(a, b, c)` мы вызываем `f(a)(b)(c)`.

---

### 🎯 Основная идея

```js
// Обычная функция
function add(a, b, c) {
  return a + b + c;
}

add(1, 2, 3); // 6

// Каррированная версия
function curriedAdd(a) {
  return function(b) {
    return function(c) {
      return a + b + c;
    };
  };
}

curriedAdd(1)(2)(3); // 6
```

---

### 📝 Синтаксис

#### Классический способ

```js
function multiply(a) {
  return function(b) {
    return function(c) {
      return a * b * c;
    };
  };
}

const result = multiply(2)(3)(4); // 24
```

#### Стрелочные функции

```js
const multiply = a => b => c => a * b * c;

const result = multiply(2)(3)(4); // 24
```

#### Частичное применение

```js
const multiply = a => b => c => a * b * c;

// Сохраняем промежуточные функции
const multiplyBy2 = multiply(2);        // функция b => c => 2 * b * c
const multiplyBy2And3 = multiplyBy2(3);  // функция c => 6 * c

console.log(multiplyBy2And3(4)); // 24
```

---

### 💡 Практические примеры

#### Пример 1: Математические операции

```js
// Каррированная функция сложения
const add = a => b => a + b;

const add5 = add(5);
console.log(add5(10)); // 15
console.log(add5(20)); // 25

// Каррированная функция умножения
const multiply = a => b => a * b;

const double = multiply(2);
const triple = multiply(3);

console.log(double(5));  // 10
console.log(triple(5));  // 15
```

#### Пример 2: Работа с массивами

```js
// Каррированная функция для фильтрации
const filter = predicate => array => array.filter(predicate);

const isEven = n => n % 2 === 0;
const filterEven = filter(isEven);

const numbers = [1, 2, 3, 4, 5, 6];
console.log(filterEven(numbers)); // [2, 4, 6]

// Каррированная функция для map
const map = fn => array => array.map(fn);

const double = n => n * 2;
const doubleAll = map(double);

console.log(doubleAll([1, 2, 3])); // [2, 4, 6]
```

#### Пример 3: Логирование

```js
// Каррированная функция для логирования
const log = level => message => data => {
  console.log(`[${level}] ${message}:`, data);
};

const logError = log('ERROR');
const logInfo = log('INFO');

logError('Ошибка подключения')({ code: 500 });
// [ERROR] Ошибка подключения: { code: 500 }

logInfo('Пользователь зашёл')({ userId: 123 });
// [INFO] Пользователь зашёл: { userId: 123 }
```

#### Пример 4: Обработка событий

```js
// Каррированная функция для обработчиков событий
const handleClick = buttonId => event => {
  console.log(`Кнопка ${buttonId} была нажата`, event);
};

const handleSubmit = handleClick('submit');
const handleCancel = handleClick('cancel');

// Использование
button.addEventListener('click', handleSubmit);
```

---

### 🔧 Универсальная функция каррирования

Можно создать функцию, которая каррирует любую функцию:

```js
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) {
      return fn.apply(this, args);
    } else {
      return function(...nextArgs) {
        return curried.apply(this, args.concat(nextArgs));
      };
    }
  };
}

// Использование
function add(a, b, c) {
  return a + b + c;
}

const curriedAdd = curry(add);

console.log(curriedAdd(1)(2)(3));     // 6
console.log(curriedAdd(1, 2)(3));     // 6
console.log(curriedAdd(1)(2, 3));     // 6
console.log(curriedAdd(1, 2, 3));     // 6
```

---

### ✅ Преимущества каррирования

1. **Частичное применение** — можно создавать специализированные функции
2. **Переиспользование** — одна функция для разных случаев
3. **Композиция** — легко комбинировать функции
4. **Читаемость** — код становится более декларативным

```js
// Без каррирования
const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(n => n * 2);
const filtered = doubled.filter(n => n > 4);

// С каррированием
const map = fn => array => array.map(fn);
const filter = predicate => array => array.filter(predicate);
const double = n => n * 2;
const greaterThan = x => n => n > x;

const result = filter(greaterThan(4))(map(double)(numbers));
// или с композицией
const pipe = (...fns) => x => fns.reduce((acc, fn) => fn(acc), x);
const process = pipe(map(double), filter(greaterThan(4)));
const result = process(numbers);
```

---

### ⚠️ Важные моменты

1. **Количество аргументов** — каррирование работает лучше с функциями фиксированного числа аргументов
2. **Производительность** — создание множества функций может быть дороже, чем один вызов
3. **Читаемость** — не всегда каррирование делает код понятнее

```js
// Иногда обычная функция понятнее
function add(a, b) {
  return a + b;
}

// Чем каррированная
const add = a => b => a + b;
```

---

### 🆚 Каррирование vs Частичное применение

| Каррирование | Частичное применение |
|--------------|---------------------|
| `f(a)(b)(c)` | `f(a, b, c)` → `f(a)(b, c)` |
| Всегда по одному аргументу | Может принимать несколько аргументов |
| `curry(f)` | `f.bind(null, arg1, arg2)` |

```js
// Каррирование
const add = a => b => c => a + b + c;
add(1)(2)(3); // 6

// Частичное применение
function add(a, b, c) {
  return a + b + c;
}

const add1 = add.bind(null, 1);
const add1And2 = add.bind(null, 1, 2);

add1(2, 3);      // 6
add1And2(3);     // 6
```

---

### 💡 Итог:

- **Каррирование** — преобразование функции с несколькими аргументами в цепочку функций с одним аргументом
- **Синтаксис:** `f(a)(b)(c)` вместо `f(a, b, c)`
- **Преимущества:** частичное применение, переиспользование, композиция
- **Использование:** когда нужно создавать специализированные функции из общих





