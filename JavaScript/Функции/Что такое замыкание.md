## 🔒 Что такое замыкание (closure)

**Замыкание** — это когда функция запоминает переменные из внешней области видимости, даже после того, как внешняя функция завершила выполнение.

---

### 💡 Пример

```js
function createCounter() { 
  let count = 0;
  return () => ++count; 
} 

const counter = createCounter(); 
counter(); // 1
counter(); // 2
counter(); // 3
```

---

### 🎯 Для чего нужно:

- ✅ Чтобы **сохранять состояние** между вызовами функции
- ✅ Чтобы **ограничивать доступ к данным** (инкапсуляция)
- ✅ Для создания **фабрик функций** и **обработчиков событий**
- ✅ **Отсутствие глобальных переменных**
- ✅ **Повторное использование** — можно создавать множество счётчиков для разных кнопок, каждый со своим независимым состоянием

---

### 📍 Где используется:

- В **колбэках** и **обработчиках событий**
- При работе с **таймерами**
- В **функциональном программировании** (каррирование, частичное применение)
