## 🧹 Сборщик мусора (Garbage Collector)

**Сборщик мусора** — это встроенный механизм в JavaScript-движке (V8, SpiderMonkey и др.), который автоматически освобождает память, когда до объекта уже нет ссылок.

✅ Не нужно вручную управлять памятью (как в C или C++).

---

### 🔧 Как работает:

#### 1. Управление памятью

- Когда создаёшь объект (`{}`, `[]`, `new Date()`, функция и т.д.) → для него выделяется память.
- Когда на объект нет доступных ссылок → он становится **«мусором»**.
- GC освобождает память под этот объект.

#### 2. Основной алгоритм — **mark-and-sweep** (пометка и зачистка)

- Сборщик начинает с **корневых объектов** (root): `window`, `document`, текущий стек вызовов.
- Рекурсивно **помечает** все объекты, на которые можно добраться из этих корней (reachable).
- Всё остальное считается **недостижимым** (unreachable) → удаляется.

---

### 💡 Пример:

```js
let a = { name: "JS" };
let b = a;

a = null; // объект всё ещё достижим через b
b = null; // теперь ссылок нет → GC очистит
```

---

### 🧠 Итог:

**Сборщик мусора в JS работает по принципу «достижимости объектов».** Всё, что недостижимо из корней, удаляется. Ты не управляешь этим вручную, только косвенно — убирая ссылки и очищая ресурсы.
