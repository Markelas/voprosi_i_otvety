## 🔧 Унарные операторы

| Оператор | Пример | Описание | Результат |
|----------|--------|----------|-----------|
| **`+`** (унарный плюс) | `+true` | Приводит к числу | `1` |
| **`-`** (унарный минус) | `-3` | Меняет знак числа | `-3` |
| **`++`** (инкремент) | `let a=1; ++a` | Увеличивает на 1 | `2` |
| **`--`** (декремент) | `let a=1; a--` | Уменьшает на 1 | `0` |
| **`!`** (логическое отрицание) | `!true` | Приводит к boolean и инвертирует | `false` |
| **`~`** (побитовое НЕ) | `~5` | Инвертирует биты (даёт `-(x+1)`) | `-6` |
| **`typeof`** | `typeof "abc"` | Возвращает тип значения | `"string"` |
| **`delete`** | `delete obj.prop` | Удаляет свойство объекта | `true/false` |
| **`void`** | `void 0` | Выполняет выражение и возвращает `undefined` | `undefined` |

---

### 📊 Разница между префиксными и постфиксными `++` и `--`

| Оператор | Действие | Возвращает | Пример | Результат |
|----------|----------|------------|--------|-----------|
| **`++a`** | Увеличивает `a` на 1 сразу | **Новое значение** | `let a=5; let b=++a;` | `a=6`, `b=6` |
| **`a++`** | Увеличивает `a` на 1 после | **Старое значение** | `let a=5; let b=a++;` | `a=6`, `b=5` |
| **`--a`** | Уменьшает `a` на 1 сразу | **Новое значение** | `let a=3; let b=--a;` | `a=2`, `b=2` |
| **`a--`** | Уменьшает `a` на 1 после | **Старое значение** | `let a=3; let b=a--;` | `a=2`, `b=3` |
