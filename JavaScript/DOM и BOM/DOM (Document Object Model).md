### 📄 **DOM** (Document Object Model)

**Объектная модель HTML-документа.**

Браузер превращает HTML в дерево узлов (объектов), с которыми можно работать через JavaScript.

<font color="#e36c09">**DOM (Document Object Model)**</font> — это объектное представление <font color="#974806">HTML-документа</font>, с которым можно работать через JavaScript.  
Браузер при загрузке <font color="#974806">HTML</font> строит из него <font color="#c00000">**DOM-дерево**</font>, где каждый элемент страницы — это <font color="#4f6128">объект (узел)</font>.

DOM позволяет **читать, изменять, добавлять и удалять элементы** страницы динамически, например, при получении информации с сервера, можно изменить содержание страницы

---

### Как формируется DOM

1. Браузер получает HTML-файл.
    
2. Парсер HTML последовательно создает объекты, соответствующие тегам.
    
3. Эти объекты выстраиваются в иерархическое **DOM-дерево**.
    
4. На основе DOM строится **Render Tree** (с учетом CSS и стилей).
    
5. После этого выполняется отрисовка страницы.
    

---

## Что под капотом DOM

DOM-дерево состоит из различных типов узлов (**nodes**), каждый из которых выполняет свою роль:

### 🧱 **Элементные узлы (Element Nodes)**

- Представляют HTML-теги — `<html>`, `<body>`, `<div>`, `<h1>` и т. д.
    
- Обладают свойствами и методами:
    
    - `nodeName` — имя тега;
        
    - `childNodes` — дочерние элементы;
        
    - `innerHTML`, `outerHTML` — содержимое элемента.
        

Пример:

```js
const div = document.querySelector('div');
console.log(div.nodeName); // "DIV"
```

---

### 📝 **Текстовые узлы (Text Nodes)**

- Содержат текст между тегами, например `Hello` в `<h1>Hello</h1>`.
    
- Доступ через свойство `textContent`.
    

```js
const h1 = document.querySelector('h1');
console.log(h1.firstChild.textContent); // "Hello"
```

---

### 🏷️ **Атрибутные узлы (Attribute Nodes)**

- Представляют атрибуты элементов (`id`, `class`, `src`, `href`).
    
- Управляются методами:
    
    - `getAttribute('id')`,
        
    - `setAttribute('class', 'active')`,
        
    - `removeAttribute('disabled')`.
        

---

### 📄 **Документные узлы (Document Nodes)**

- Корневой узел — объект `document`.  Представляет весь HTML-документ.
    
- Через него начинается работа с DOM:
    
    - `document.querySelector()`,
        
    - `document.getElementById()`,
        
    - `document.createElement()`.
        

---
### 🤔 **Что такое reflow и repaint?**

⚙️<font color="#8064a2"> **Reflow (пересчёт рендера, layout)**</font>

Это процесс, при котором браузер <u><font color="#953734">**пересчитывает положение и размеры** </font></u>элементов на странице, когда изменяется структура DOM или стили, влияющие на геометрию.

**Reflow вызывается при изменениях, которые влияют на:**

- размер элемента (`width`, `height`, `padding`, `margin`, `border`);
    
- положение элемента (`position`, `display`);
    
- добавление / удаление узлов DOM;
    
- изменение шрифта или содержимого (текста);
    
- изменение размера окна (`resize`).
    

**Reflow дорог по производительности**, потому что требует пересчёта расположения всех затронутых элементов (и их потомков).

**Пример:**

```js
element.style.width = '200px'; // вызовет reflow
```

---

🎨 <font color="#9bbb59">**Repaint (перерисовка)**</font>

Это процесс, при котором браузер <u><font color="#4f6128">**перерисовывает внешний вид**</font></u> элемента, <u><font color="#4f6128">**не меняя его геометрию**</font></u>.

**Repaint вызывается при изменениях:**

- цвета (`color`, `background-color`);
    
- тени (`box-shadow`);
    
- прозрачности (`opacity`);
    
- изменения видимости (`visibility`, но не `display`).
    

**Repaint дешевле Reflow**, так как не требует пересчёта размеров и расположения.

**Пример:**

```js
element.style.backgroundColor = 'red'; // вызовет repaint
```

---

#### 💡 Как минимизировать reflow / repaint

|Практика|Что делает|Почему помогает|
|---|---|---|
|**Изменять DOM "оптом"**|Работать с фрагментами (`DocumentFragment`)|Изменения применяются один раз|
|**Использовать `classList` вместо inline-стилей**|Меняет класс, а не отдельные свойства|Уменьшает количество обращений к layout|
|**Избегать частого чтения layout-свойств** (`offsetHeight`, `getBoundingClientRect`)|Они принудительно вызывают reflow|Использовать кэширование|
|**Скрывать элемент перед серией изменений** (`display: none`)|Все изменения выполняются без рендера|Reflow произойдёт один раз при возврате элемента|
|**Использовать `requestAnimationFrame`**|Синхронизирует визуальные изменения с рендером|Снижает лаги и лишние пересчёты|

---


- ✅ Представляет **структуру страницы**
- ✅ Позволяет **изменять, добавлять, удалять** элементы
- ✅ Это часть стандарта **W3C** (не завязана на конкретный браузер)
