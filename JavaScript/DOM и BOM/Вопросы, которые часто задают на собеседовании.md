## DOM

### **Делегирование событий**
**Делегирование событий (Event Delegation)** — это приём, при котором обработчик события вешается **не на каждый элемент**, а **на общего родителя**, используя **всплытие событий**.

---

#### 🔹 Пример без делегирования

```js
document.querySelectorAll('button').forEach(btn => {
  btn.addEventListener('click', () => console.log('click'));
});
```

→ Если кнопок 100 — это 100 обработчиков.

---

#### 🔹 Пример с делегированием

```js
document.querySelector('.container').addEventListener('click', (event) => {
  if (event.target.tagName === 'BUTTON') {
    console.log('button clicked:', event.target.textContent);
  }
});
```

✅ Обработчик **один**, но срабатывает для всех кнопок внутри `.container`.  
✅ Работает даже для **динамически добавленных элементов**.  
✅ Использует фазу **всплытия**.

🔹 Применение: Списки (`ul > li`); Таблицы; Меню, карточки, кнопки и т.п.; SPA-интерфейсы с динамическим контентом

---

#### 🔹 Как работает:

1. Клик по элементу всплывает вверх по DOM.
    
2. Родитель перехватывает событие (`event.target` — конкретный элемент).
    
3. Проверяем, подходит ли цель (`matches`, `closest`, `tagName`).
    

Пример с `closest`:

```js
container.addEventListener('click', (e) => {
  const btn = e.target.closest('.btn');
  if (!btn) return;
  console.log('Нажата кнопка', btn.dataset.action);
});
```

---

#### 🔹 Типичные вопросы на собеседовании:

|Вопрос|Краткий ответ|
|---|---|
|Почему делегирование эффективнее?|Меньше обработчиков → меньше памяти и нагрузок|
|Когда делегирование не сработает?|Если событие **не всплывает** (например, `blur`, `focus`)|
|Как это обойти?|Использовать `focusin`, `focusout` — они всплывают|
|Что будет, если вызвать `stopPropagation()` в дочернем элементе?|Делегирование “сломается” — событие не поднимется до родителя|


### **Как поменять в браузере название вкладки?**
С помощью `document.title`

### **Что такое Shadow DOM**

**Кратко:**  
Shadow DOM — это механизм изоляции DOM и CSS, встроенный в браузер.  
Он позволяет компоненту иметь собственное “теневое” DOM-дерево, которое не влияет на внешний документ и не зависит от него.

**Пример:**

```js
const div = document.createElement('div');
const shadow = div.attachShadow({ mode: 'open' });
shadow.innerHTML = `
  <style>p { color: red }</style>
  <p>Это внутри Shadow DOM</p>
`;
document.body.append(div);
```

→ `<p>` изнутри **невидим** обычному `document.querySelector('p')`.

**Ключевые особенности:**

- Shadow DOM **инкапсулирует** структуру, стили и поведение.
    
- Внешние CSS не применяются к теневому DOM, и наоборот.
    
- Используется в **Web Components** (например, `<input>`, `<video>`, `<dialog>` — все уже используют shadow root).
    
- `mode: 'open'` → доступен через `.shadowRoot`,  
    `mode: 'closed'` → скрыт даже из JS.
    

**Зачем нужен:**  
✅ Защита компонентов от внешних стилей  
✅ Повторное использование без конфликтов  
✅ Создание независимых UI-элементов

---

### 🎯 **Разница между capture и bubble фазами событий**

В браузере каждое событие проходит **3 фазы**:

|Фаза|Описание|Пример|
|---|---|---|
|**1. Capturing (погружение)**|Событие идёт сверху вниз — от `window` → `document` → родителя → цели|используется редко|
|**2. Target**|Событие достигает самого элемента (цели)|`event.target`|
|**3. Bubbling (всплытие)**|Событие идёт снизу вверх — от цели обратно к `window`|поведение по умолчанию|

**Пример:**

```js
document.body.addEventListener('click', () => console.log('body'), true);  // capture
document.querySelector('button').addEventListener('click', () => console.log('button'));
```

Если кликнуть по кнопке, вывод будет: `body button`

→ потому что `true` включает **capture**, и обработчик `body` срабатывает на фазе погружения.   Если убрать `true`, сработает в порядке: `button body`

→ это **всплытие** (по умолчанию).

---

### 🛑 **Что делает `event.stopPropagation()`**

**Назначение:**  
Останавливает дальнейшее **всплытие события** вверх по DOM.  
(При этом само событие всё ещё выполняется на текущем элементе.)

**Пример:**

```js
document.querySelector('.parent').addEventListener('click', () => {
  console.log('Parent clicked');
});

document.querySelector('.child').addEventListener('click', (e) => {
  e.stopPropagation();
  console.log('Child clicked');
});
```

Если кликнуть `.child`, выведется только: `Child clicked` — событие не поднимется к `.parent`.

**Важно:**

- `stopPropagation()` не влияет на другие обработчики на этом же элементе.
    
- Чтобы остановить **всё**, включая другие обработчики на том же элементе, используют `stopImmediatePropagation()`.
    

---

### 🚫 **Что делает `preventDefault()`**

**Назначение:**  
Отменяет стандартное поведение браузера для элемента, если событие поддерживает отмену (`cancelable: true`).

**Примеры:**
Не переходить по ссылке: 
```js 
document.querySelector('a').addEventListener('click', e => { e.preventDefault(); });
```

Не отправлять форму: 
```js
form.addEventListener('submit', e => { e.preventDefault(); });
```

Не выделять текст при mousedown:
```js
element.addEventListener('mousedown', e => e.preventDefault());
```


### ↕️ **Что такое `getBoundingClientRect()`**?
Метод **`element.getBoundingClientRect()`** возвращает объект с координатами и размерами элемента **относительно видимой области (viewport)** браузера.
```js
const rect = element.getBoundingClientRect();

```

|Свойство|Описание|
|---|---|
|`top`|расстояние от верхнего края окна до верхнего края элемента|
|`left`|расстояние от левого края окна до элемента|
| `right`   | координата правого края элемента          |
| `bottom`  | координата нижнего края элемента          |
| `width`   | ширина элемента (равно `right - left`)    |
| `height`  | высота элемента (равно `bottom - top`)    |
| `x` / `y` | дубликаты `left` / `top` (новый стандарт) |

Вопросы
- **В чём разница между `offsetTop` и `getBoundingClientRect().top`?**  
    → `offsetTop` измеряется **от ближайшего позиционированного родителя**,  
    а `getBoundingClientRect()` — **от окна (viewport)**.
    
- **Когда пересчитываются координаты?**  
    → При изменении layout (reflow): изменение размеров, шрифтов, добавление элементов и т. д.
    
- **Влияет ли transform?**  
    → Да. CSS `transform`, `scale`, `rotate` изменяют реальные визуальные границы, и метод возвращает уже преобразованные координаты.
## BOM
### 1. Что такое BOM и чем он отличается от DOM?

**Ответ:**  
BOM (Browser Object Model) — это объектная модель браузера.  
Она предоставляет JavaScript доступ к функциям браузера, не связанным напрямую со структурой HTML-документа.  
Пример: управление вкладкой, адресной строкой, историей переходов, размером окна и т.д.  
Главный объект BOM — `window`.

**Разница с DOM:**

|Сравнение|DOM|BOM|
|---|---|---|
|Что описывает|HTML-документ|Окружение браузера|
|Главный объект|`document`|`window`|
|Примеры объектов|`body`, `div`, `p`|`location`, `navigator`, `screen`, `history`|
|Используется для|Изменения контента страницы|Управления браузером и вкладкой|

**Связь:**  
`window.document` — это DOM, вложенный в BOM.

**Доп. вопрос:**

> Что произойдёт, если вызвать `window.close()`?  
> **Ответ:**  
> Закроется текущее окно, **если оно было открыто скриптом** (`window.open()`). Для вкладок, открытых вручную, вызов будет проигнорирован.

**Доп. вопрос:**

> Как из BOM получить доступ к DOM?  
> **Ответ:**  
> Через свойство `window.document`. Например:

```js
window.document.body.style.background = 'red';
```

---

### 2. Что делает объект `location`?

**Ответ:**  
`location` управляет адресной строкой браузера и позволяет перенаправлять пользователя, перезагружать страницу и получать данные о текущем URL.

**Основные свойства:**

|Свойство|Назначение|Пример|
|---|---|---|
|`href`|Полный URL|`location.href`|
|`protocol`|Протокол (`http:` / `https:`)|`location.protocol`|
|`host`|Домен + порт|`location.host`|
|`pathname`|Путь|`location.pathname`|
|`search`|Строка запроса|`location.search`|
|`hash`|Хеш (после #)|`location.hash`|

**Методы:**

|Метод|Что делает|Пример|
|---|---|---|
|`assign(url)`|Переход по адресу (добавляет в историю)|`location.assign('https://example.com')`|
|`replace(url)`|Переход без добавления в историю|`location.replace('https://example.com')`|
|`reload()`|Перезагружает страницу|`location.reload()`|

---

### 3. Что делает объект `history`?

**Ответ:**  
`history` хранит историю переходов в пределах текущей вкладки.

**Методы:**

|Метод|Описание|Пример|
|---|---|---|
|`back()`|Переход назад|`history.back()`|
|`forward()`|Вперёд|`history.forward()`|
|`go(n)`|Перейти на n шагов (отрицательное — назад)|`history.go(-2)`|
|`pushState()` / `replaceState()`|Меняют URL без перезагрузки (используется в SPA)|`history.pushState({}, '', '/home')`|

**Вопрос:**

> В чём разница между `back()` и `go(-1)`?  
> **Ответ:**  
> Разницы нет — `go(-1)` является эквивалентом `back()`.

---

### 4. Что делает объект `navigator`?

**Ответ:**  
`navigator` предоставляет информацию о браузере и устройстве пользователя.  
Используется для определения платформы, языка, онлайн-статуса и доступа к API (например, `geolocation`).

**Примеры:**

```js
navigator.userAgent;     // информация о браузере
navigator.language;       // текущий язык
navigator.onLine;         // true / false
navigator.geolocation.getCurrentPosition(console.log);
```

**Вопрос:**

> Можно ли использовать `navigator.userAgent` для точного определения браузера?  
> **Ответ:**  
> Нежелательно — строка `userAgent` может быть изменена пользователем, поэтому она ненадёжна. Лучше использовать feature-detection (проверку доступных API).

---

### 5. Что делает объект `screen`?

**Ответ:**  
`screen` содержит информацию о мониторе пользователя: разрешение, доступную ширину и высоту, глубину цвета.

**Пример:**

```js
console.log(screen.width, screen.height); // размер экрана
console.log(screen.availWidth, screen.availHeight); // доступная область без панели задач
```

**Вопрос:**

> Для чего это может понадобиться?  
> **Ответ:**  
> Для адаптивных интерфейсов, анализа статистики устройств, настройки всплывающих окон с нужным размером.
