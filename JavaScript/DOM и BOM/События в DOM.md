## 🎯 События в DOM

**События** позволяют реагировать на действия пользователя (клики, нажатия клавиш, наведение мыши) и делать страницы динамичными и интерактивными.

---

### 📝 Что такое событие?

**Событие** — это активность или действие в браузере, которое можно "поймать" с помощью JavaScript.

Когда событие происходит, оно передаётся объекту события, который затем может быть использован для выполнения определённых действий.

**Пример:** пользователь нажимает на кнопку или перемещает мышь — это событие, на которое можно подписаться и реагировать.

---

### 🔧 Основные типы событий

| Категория | События | Описание |
|-----------|---------|----------|
| **Клики** | `click`, `dblclick` | Одиночный и двойной клик |
| **Клавиши** | `keydown`, `keyup`, `keypress` | Нажатие и отпускание клавиш |
| **Мышь** | `mouseover`, `mouseout`, `mousemove`, `mousedown`, `mouseup` | Действия с мышью |
| **Фокусировка** | `focus`, `blur` | Получение и потеря фокуса |
| **Изменение значений** | `change`, `input` | Изменение в полях ввода |
| **Загрузка** | `load`, `DOMContentLoaded` | Загрузка страницы и DOM |
| **Формы** | `submit`, `reset` | Отправка и сброс формы |

---

### 🎯 Работа с событиями

#### 1. Регистрация события

##### ✅ `addEventListener()` (рекомендуемый метод)

```js
const button = document.querySelector('button');

button.addEventListener('click', function() {
  alert('Кнопка нажата!');
});
```

**Преимущества:**
- Можно добавить несколько обработчиков на одно событие
- Можно удалить обработчик через `removeEventListener()`
- Больше контроля над фазой события (capture/bubble)

##### ❌ Атрибуты событий (не рекомендуется)

```html
<button onclick="alert('Кнопка нажата!')">Нажми меня</button>
```

**Недостатки:**
- Смешивает HTML и JavaScript
- Можно добавить только один обработчик
- Сложнее удалить

---

#### 2. Удаление события

```js
const button = document.querySelector('button');

function handleClick() {
  alert('Кнопка нажата!');
}

// Добавляем обработчик
button.addEventListener('click', handleClick);

// Удаляем обработчик
button.removeEventListener('click', handleClick);
```

**Важно:** Для удаления нужна **та же функция**, что и при добавлении (нельзя использовать анонимную функцию).

---

#### 3. Объект события (`event`)

Объект события содержит информацию о произошедшем событии.

**Основные свойства:**

| Свойство | Описание |
|----------|----------|
| **`event.type`** | Тип события (`'click'`, `'keydown'` и т.д.) |
| **`event.target`** | Элемент, на котором произошло событие |
| **`event.currentTarget`** | Элемент, на котором установлен обработчик |
| **`event.clientX`, `event.clientY`** | Координаты мыши относительно окна браузера |
| **`event.key`** | Клавиша, которая была нажата |
| **`event.preventDefault()`** | Отменяет стандартное поведение браузера |
| **`event.stopPropagation()`** | Останавливает всплытие события |

**Пример:**

```js
const button = document.querySelector('button');

button.addEventListener('click', function(event) {
  console.log(event.type);        // 'click'
  console.log(event.target);      // элемент button
  console.log(event.clientX);     // координата X мыши
});
```

---

#### 4. `preventDefault()` — отмена стандартного поведения

```js
const form = document.querySelector('form');

form.addEventListener('submit', function(event) {
  event.preventDefault(); // Отменяем отправку формы
  alert('Форма не будет отправлена');
});
```

**Когда использовать:**
- Отмена отправки формы
- Отмена перехода по ссылке
- Отмена контекстного меню

---

### 🖱️ События мыши

| Событие | Когда срабатывает |
|---------|-------------------|
| **`click`** | При клике |
| **`dblclick`** | При двойном клике |
| **`mouseover`** | Когда указатель мыши перемещается над элементом |
| **`mouseout`** | Когда указатель мыши покидает элемент |
| **`mousemove`** | При перемещении мыши по элементу |
| **`mousedown`** | При нажатии кнопки мыши |
| **`mouseup`** | При отпускании кнопки мыши |

**Пример:**

```js
const element = document.querySelector('div');

element.addEventListener('mouseover', function() {
  element.style.backgroundColor = 'yellow';
});

element.addEventListener('mouseout', function() {
  element.style.backgroundColor = '';
});
```

---

### ⌨️ События клавиатуры

| Событие | Когда срабатывает |
|---------|-------------------|
| **`keydown`** | При нажатии клавиши (любой) |
| **`keyup`** | При отпускании клавиши |
| **`keypress`** | При нажатии клавиши, которая вводит символ (устарел) |

**Пример:**

```js
document.addEventListener('keydown', function(event) {
  console.log('Нажата клавиша:', event.key);
  
  if (event.key === 'Escape') {
    console.log('Нажат Escape');
  }
});
```

---

### 🔄 Делегирование событий

**Делегирование** — назначение одного обработчика на родительский элемент для отслеживания событий на его потомках.

**Преимущества:**
- Экономит ресурсы (один обработчик вместо многих)
- Работает с динамически добавленными элементами
- Упрощает код

**Пример:**

```js
const list = document.querySelector('ul');

// Обработчик на родительском элементе
list.addEventListener('click', function(event) {
  if (event.target.tagName === 'LI') {
    alert('Кликнут элемент: ' + event.target.textContent);
  }
});
```

**Как работает:**
1. Событие происходит на дочернем элементе (`<li>`)
2. Событие всплывает до родителя (`<ul>`)
3. Обработчик проверяет `event.target` и выполняет действие

---

### 📊 Всплытие и погружение событий

#### Всплытие (Bubble) — по умолчанию

Событие распространяется от целевого элемента вверх к родителям.

```js
// HTML: <div><button>Click</button></div>

document.querySelector('div').addEventListener('click', function() {
  console.log('Клик на div');
});

document.querySelector('button').addEventListener('click', function() {
  console.log('Клик на button');
});

// При клике на button выведется:
// 1. "Клик на button"
// 2. "Клик на div" (всплытие)
```

#### Погружение (Capture)

Событие обрабатывается сначала на родителе, затем на потомке.

```js
document.querySelector('div').addEventListener('click', function() {
  console.log('Погружение на div');
}, true); // третий параметр true включает capture

document.querySelector('button').addEventListener('click', function() {
  console.log('Клик на button');
});

// При клике на button выведется:
// 1. "Погружение на div" (capture фаза)
// 2. "Клик на button" (target фаза)
```

**Фазы события:**
1. **Capture** (погружение) — от `document` к целевому элементу
2. **Target** (целевая) — на целевом элементе
3. **Bubble** (всплытие) — от целевого элемента к `document`

---

### 🛑 Остановка всплытия

```js
button.addEventListener('click', function(event) {
  event.stopPropagation(); // Останавливает всплытие
  console.log('Клик на button');
});
```

---

### 💡 Практические примеры

#### Пример 1: Обработка формы

```js
const form = document.querySelector('form');

form.addEventListener('submit', function(event) {
  event.preventDefault();
  
  const formData = new FormData(form);
  console.log('Данные формы:', Object.fromEntries(formData));
});
```

#### Пример 2: Отслеживание изменений в input

```js
const input = document.querySelector('input');

input.addEventListener('input', function(event) {
  console.log('Текущее значение:', event.target.value);
});
```

#### Пример 3: Загрузка страницы

```js
// Когда DOM готов (быстрее)
document.addEventListener('DOMContentLoaded', function() {
  console.log('DOM загружен');
});

// Когда всё загружено (медленнее)
window.addEventListener('load', function() {
  console.log('Страница полностью загружена');
});
```

---

### 🎯 Итог:

- **События** — действия пользователя, на которые можно реагировать
- **`addEventListener()`** — основной метод для работы с событиями
- **Объект `event`** содержит информацию о событии
- **Делегирование** — эффективный способ обработки событий на множестве элементов
- **Всплытие** — событие поднимается от элемента к родителям
- **Погружение** — событие обрабатывается сначала на родителе






