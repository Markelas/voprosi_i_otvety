## 👁️ Mutation Observer

**Mutation Observer** — это интерфейс, позволяющий отслеживать изменения в DOM, такие как добавление или удаление элементов, изменение атрибутов или текста.

Он предоставляет более эффективный и мощный способ наблюдать за изменениями, чем старые методы вроде `Mutation Events`, так как не блокирует поток выполнения и может отслеживать сразу несколько изменений.

---

### 🎯 Когда использовать Mutation Observer

- ✅ Отслеживать изменения в структуре DOM (добавление или удаление элементов)
- ✅ Реагировать на изменения атрибутов элементов
- ✅ Мониторить текстовое содержимое элементов (динамически обновляемые данные)

---

### 🔧 Основы работы

**Этапы:**

1. **Создаётся экземпляр `MutationObserver`** с функцией-колбэком
2. **Настраивается конфигурация** наблюдения
3. **Начинается наблюдение** за целевым элементом
4. **Останавливается наблюдение** при необходимости

---

### 📝 Создание Mutation Observer

```js
const observer = new MutationObserver((mutationsList) => {
  mutationsList.forEach((mutation) => {
    console.log(mutation);
  });
});
```

**Колбэк получает:**
- `mutationsList` — массив записей (`MutationRecord`), описывающих изменения

---

### 📊 Объект MutationRecord

Каждый объект `mutation` содержит информацию о произошедшем изменении:

| Свойство | Описание |
|----------|----------|
| **`type`** | Тип изменения: `'childList'`, `'attributes'`, `'characterData'` |
| **`target`** | Целевой элемент, где произошло изменение |
| **`addedNodes`** | Список добавленных узлов (если `type === 'childList'`) |
| **`removedNodes`** | Список удалённых узлов (если `type === 'childList'`) |
| **`attributeName`** | Имя изменённого атрибута (если `type === 'attributes'`) |
| **`oldValue`** | Старое значение (если включено в конфигурации) |

---

### 🎯 Начало наблюдения

```js
const targetNode = document.getElementById('targetElement');

observer.observe(targetNode, {
  childList: true,        // следить за добавлением и удалением дочерних элементов
  attributes: true,       // следить за изменением атрибутов
  characterData: true,    // следить за изменением текстового содержимого
  subtree: true,            // отслеживать изменения во всех потомках
  attributeOldValue: true, // сохранять старое значение атрибута
  characterDataOldValue: true // сохранять старое значение текста
});
```

---

### ⚙️ Параметры конфигурации

| Параметр | Описание |
|----------|----------|
| **`childList`** | Отслеживает добавление и удаление дочерних элементов |
| **`attributes`** | Отслеживает изменения атрибутов у целевого элемента |
| **`characterData`** | Отслеживает изменения текстового содержимого |
| **`subtree`** | Если `true`, отслеживает изменения не только у целевого элемента, но и у всех его потомков |
| **`attributeOldValue`** | Сохранять старое значение атрибута в `mutation.oldValue` |
| **`characterDataOldValue`** | Сохранять старое значение текста в `mutation.oldValue` |
| **`attributeFilter`** | Массив имён атрибутов для отслеживания (например, `['class', 'id']`) |

---

### 💡 Пример 1: Отслеживание изменений в списке

```js
const itemList = document.getElementById('itemList');

// Создаём MutationObserver
const observer = new MutationObserver((mutationsList) => {
  mutationsList.forEach((mutation) => {
    if (mutation.type === 'childList') {
      console.log('Дочерние элементы изменились');
      
      if (mutation.addedNodes.length > 0) {
        console.log('Добавлены элементы:', mutation.addedNodes);
      }
      
      if (mutation.removedNodes.length > 0) {
        console.log('Удалены элементы:', mutation.removedNodes);
      }
    }
  });
});

// Настраиваем наблюдение за списком
observer.observe(itemList, { 
  childList: true 
});

// Добавление нового элемента
document.getElementById('addItemBtn').addEventListener('click', () => {
  const newItem = document.createElement('li');
  newItem.textContent = `Item ${itemList.children.length + 1}`;
  itemList.appendChild(newItem);
  // MutationObserver автоматически зафиксирует изменение
});
```

---

### 💡 Пример 2: Отслеживание изменений атрибутов

```js
const targetElement = document.getElementById('targetElement');

// Создаём MutationObserver для отслеживания изменений атрибутов
const observer = new MutationObserver((mutationsList) => {
  mutationsList.forEach((mutation) => {
    if (mutation.type === 'attributes') {
      console.log(`Атрибут ${mutation.attributeName} был изменён`);
      console.log('Старое значение:', mutation.oldValue);
      console.log('Новое значение:', targetElement.getAttribute(mutation.attributeName));
    }
  });
});

// Начинаем наблюдение за изменениями атрибутов
observer.observe(targetElement, { 
  attributes: true,
  attributeOldValue: true, // сохранять старое значение
  attributeFilter: ['class', 'data-status'] // отслеживать только эти атрибуты
});

// Меняем атрибут элемента
document.getElementById('changeAttrBtn').addEventListener('click', () => {
  targetElement.setAttribute('data-status', 'updated');
  // MutationObserver автоматически зафиксирует изменение
});
```

---

### 💡 Пример 3: Отслеживание изменений текста

```js
const textNode = document.querySelector('#content');

const observer = new MutationObserver((mutationsList) => {
  mutationsList.forEach((mutation) => {
    if (mutation.type === 'characterData') {
      console.log('Текст изменился');
      console.log('Старое значение:', mutation.oldValue);
      console.log('Новое значение:', mutation.target.textContent);
    }
  });
});

observer.observe(textNode, {
  characterData: true,
  characterDataOldValue: true,
  subtree: true // отслеживать изменения во всех потомках
});
```

---

### 🛑 Остановка наблюдения

```js
observer.disconnect(); // Останавливает наблюдение
```

**Когда использовать:**
- Когда больше не нужно отслеживать изменения
- При очистке ресурсов (например, при размонтировании компонента)

---

### 🔄 Отличие от Mutation Events

| Параметр | Mutation Observer | Mutation Events (устарел) |
|----------|-------------------|---------------------------|
| **Производительность** | Не блокирует поток выполнения | Блокировал выполнение |
| **Группировка** | Может отслеживать несколько изменений сразу | Одно событие на изменение |
| **Поддержка** | Современный стандарт | Устарел, не рекомендуется |

---

### 🎯 Практические применения

1. **Отслеживание динамического контента** — когда элементы добавляются через AJAX
2. **Валидация изменений** — проверка структуры DOM
3. **Логирование изменений** — для отладки
4. **Автоматическое обновление UI** — реакция на изменения в DOM

---

### 💡 Важные моменты

- ✅ Mutation Observer — **микротаска**, выполняется после текущего кода
- ✅ Можно отслеживать несколько элементов одним observer
- ✅ Нужно явно останавливать через `disconnect()` для освобождения ресурсов
- ✅ Более эффективен, чем периодическая проверка через `setInterval`

---

### 🎯 Итог:

- **Mutation Observer** — современный способ отслеживания изменений в DOM
- **Не блокирует** выполнение кода
- **Группирует** изменения для эффективной обработки
- **Гибкая конфигурация** — можно отслеживать только нужные типы изменений






