## 🔹 Что такое BOM

**BOM (Browser Object Model)** — это объектная модель браузера,  
которая позволяет JavaScript взаимодействовать **с окном браузера, вкладкой, историей, адресной строкой и т.п.**

📘 Если DOM отвечает за **страницу (HTML)**, то BOM — за **окружение страницы (браузер)**.

---
## 🔹 Основные объекты BOM

|Объект|Описание|
|---|---|
|**window**|Глобальный объект браузера. Содержит всё — DOM, BOM, таймеры, историю, и т.д.|
|**navigator**|Информация о браузере, ОС, устройстве пользователя|
|**location**|Информация о текущем URL и методы для навигации|
|**history**|Управление историей переходов (вперёд/назад)|
|**screen**|Информация о параметрах экрана|
|**alert / confirm / prompt**|Диалоговые окна браузера|
|**setTimeout / setInterval**|Таймеры и задержки выполнения|

---
## 🧱 1. Объект `window`

Это **глобальный объект**, доступный везде.  
Все переменные и функции, объявленные в глобальной области, становятся его свойствами.

```js
console.log(window.innerWidth);  // ширина окна
console.log(window.document === document); // true
```

**Методы `window`:**

|Метод|Назначение|
|---|---|
|`alert(msg)`|Показывает сообщение|
|`confirm(msg)`|Диалог с выбором OK / Cancel|
|`prompt(msg, default)`|Ввод текста пользователем|
|`open(url, name, params)`|Открывает новое окно / вкладку|
|`close()`|Закрывает текущее окно|
|`focus()` / `blur()`|Активирует / деактивирует вкладку|

---

## 🧭 2. Объект `navigator`

Содержит информацию о браузере и платформе.  
Можно использовать для определения устройства, языка и т.п.

```js
console.log(navigator.userAgent);
console.log(navigator.language);
console.log(navigator.onLine); // true / false
```

|Свойство|Описание|
|---|---|
|`userAgent`|Информация о браузере|
|`platform`|ОС (например, "MacIntel")|
|`language`|Язык интерфейса|
|`onLine`|Подключен ли пользователь к сети|
|`geolocation`|Объект для работы с геолокацией|

📍 **Пример:**

```js
navigator.geolocation.getCurrentPosition(
  pos => console.log(pos.coords.latitude, pos.coords.longitude),
  err => console.error(err)
);
```

---

## 🌍 3. Объект `location`

Управляет текущим URL.

```js
console.log(location.href);   // Полный адрес
console.log(location.hostname); // example.com
```

|Свойство|Пример|Описание|
|---|---|---|
|`href`|`https://example.com/page?id=1`|Полный URL|
|`protocol`|`https:`|Протокол|
|`host`|`example.com:443`|Хост и порт|
|`pathname`|`/page`|Путь|
|`search`|`?id=1`|Параметры запроса|
|`hash`|`#section`|Якорь|

**Методы:**

```js
location.assign('https://google.com'); // переход
location.replace('https://google.com'); // переход без возврата
location.reload(); // перезагрузка
```

---

## 🔙 4. Объект `history`

Позволяет управлять историей переходов в браузере.

|Метод|Назначение|
|---|---|
|`back()`|Переход на предыдущую страницу|
|`forward()`|Вперёд|
|`go(n)`|Переход на n шагов (например `history.go(-2)`)|
|`pushState(data, title, url)`|Добавить запись в историю (SPA)|
|`replaceState(data, title, url)`|Заменить текущую запись истории|

📘 Используется в **одностраничных приложениях (SPA)** для управления адресами без перезагрузки страницы.

---

## 🖥️ 5. Объект `screen`

Содержит информацию о параметрах экрана пользователя.

```js
console.log(screen.width, screen.height);
console.log(screen.availWidth, screen.availHeight);
```

|Свойство|Описание|
|---|---|
|`width`, `height`|Полный размер экрана|
|`availWidth`, `availHeight`|Размер без панели задач и т.д.|
|`colorDepth`|Глубина цвета (в битах)|

---

## ⏰ 6. Таймеры: `setTimeout`, `setInterval`, `clearTimeout`, `clearInterval`

|Функция|Назначение|
|---|---|
|`setTimeout(fn, delay)`|Выполнить через `delay` мс|
|`setInterval(fn, delay)`|Повторять каждые `delay` мс|
|`clearTimeout(id)`|Отменить отложенное выполнение|
|`clearInterval(id)`|Остановить интервал|

📘 Пример:

```js
const id = setTimeout(() => console.log('Once!'), 2000);
clearTimeout(id);
```

---

## ⚡ Разница DOM и BOM

|Критерий|DOM|BOM|
|---|---|---|
|Что описывает|Структуру HTML-документа|Окружение браузера|
|Главный объект|`document`|`window`|
|Работа с|Элементами страницы|Окном, вкладками, навигацией|
|Пример|`document.querySelector('div')`|`window.open('https://example.com')`|
