В JavaScript классы работают на основе **прототипного наследования**. Когда вы создаёте класс и объявляете методы, под капотом происходит следующее:

---

### 🎯 Основные шаги:

1. **Создание функции-конструктора** — JavaScript создаёт функцию-конструктор
2. **Добавление методов в прототип** — все методы добавляются в `prototype` функции-конструктора
3. **Статические методы** — добавляются непосредственно к функции-конструктору

---

### 📝 Пример класса:

```js
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  introduce() {
    return `Привет, меня зовут ${this.name}, мне ${this.age} лет.`;
  }

  static describe() {
    return 'Класс Person описывает человека.';
  }
}
```

---

### 🔍 Что происходит под капотом:

#### 1. Создание функции-конструктора

```js
// JavaScript создаёт функцию-конструктор
function Person(name, age) {
  this.name = name;
  this.age = age;
}
```

#### 2. Добавление методов в прототип

```js
// Метод introduce добавляется в Person.prototype
Person.prototype.introduce = function() {
  return `Привет, меня зовут ${this.name}, мне ${this.age} лет.`;
};
```

#### 3. Добавление статических методов

```js
// Статический метод добавляется к самой функции
Person.describe = function() {
  return 'Класс Person описывает человека.';
};
```

---

### 🎬 Что происходит при создании экземпляра?

```js
const person1 = new Person('Алексей', 30);
```

**Пошагово:**

1. **Создаётся новый пустой объект** `{}`
2. **Прототипом становится** `Person.prototype`
3. **Вызывается конструктор** `Person('Алексей', 30)`
4. **Инициализируются свойства** `this.name` и `this.age`
5. **Возвращается объект** и присваивается `person1`

---

### 💡 Использование:

```js
// Создание экземпляра
const person1 = new Person('Алексей', 30);

// Вызов метода экземпляра (из прототипа)
console.log(person1.introduce()); // "Привет, меня зовут Алексей, мне 30 лет."

// Доступ к свойствам
console.log(person1.name); // "Алексей"
console.log(person1.age);  // 30

// Вызов статического метода (на самом классе)
console.log(Person.describe()); // "Класс Person описывает человека."
```

---

### 🔗 Связь с прототипами:

```js
// Проверка связи
console.log(person1.__proto__ === Person.prototype); // true
console.log(person1 instanceof Person);              // true

// Метод находится в прототипе
console.log(person1.hasOwnProperty('introduce'));    // false
console.log('introduce' in person1);                 // true
```

---

### 📊 Структура:

```
Person (функция-конструктор)
  ├── prototype
  │   └── introduce()  ← методы экземпляров
  └── describe()       ← статический метод

person1 (экземпляр)
  ├── name: "Алексей"
  ├── age: 30
  └── __proto__ → Person.prototype
```

---

### 🎯 Итог:

- **Класс** = синтаксический сахар над функцией-конструктором
- **Методы класса** = методы в `prototype`
- **Статические методы** = свойства самой функции-конструктора
- **`new`** создаёт объект и связывает его с `prototype`
