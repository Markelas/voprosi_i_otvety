## 📝 Объявление класса и constructor

---

### 🔧 Как объявить класс?

```js
class ClassName {
  // тело класса
}
```

---

### 🏗️ Основные блоки класса:

1. **<font color="#e36c09"> Constructor (конструктор)</font>** - Специальный метод, вызываемый при создании экземпляра через `new`.
   
2. **<font color="#0070c0">Методы экземпляра</font>** - Обычные методы класса, которые принадлежат экземплярам, а не самому классу. Доступны всем экземплярам через this. <u>Не требуют ключевого слова</u> `static`. Например, в `class User {}` укажем функцию, которую будем присваивать каждому новому экземпляру.

3. **<font color="#76923c">Статические методы и свойства</font>** (`static`) - Методы и свойства, принадлежащие самому классу, а не его экземплярам. Используются для: — утилитарных функций, фабричных методов, констант. Экземпляры не имеют доступа к статическим членам. Могут обращаться к другим статическим членам через `this`.

4. **<font color="#7030a0">Геттеры и сеттеры</font>** (`get`, `set`) - Специальные методы для чтения и записи значений свойств как будто напрямую, но с логикой под капотом. Позволяют контролировать доступ и валидацию. Вызов без () — выглядит как обычное свойство. Часто применяются с приватными свойствами (#).

5. **<font color="#c00000">Приватные свойства</font>** (`#`) - Свойства, недоступные вне класса. Они начинаются с `#` и защищают внутреннее состояние от прямого доступа. Нельзя обратиться извне (`obj.#name` → ошибка). Используются для инкапсуляции. Доступны только внутри методов того же класса.

---

### 💡 Пример полного класса:

```js
class User {
  // 1. Приватное свойство
  #password = '';
  
  // 2. Constructor
  constructor(name, email) {
    this.name = name;
    this.email = email;
  }
  
  // 3. Метод экземпляра
  introduce() {
    return `Меня зовут ${this.name}`;
  }
  
  // 4. Геттер
  get info() {
    return `${this.name} (${this.email})`;
  }
  
  // 5. Сеттер
  set password(value) {
    this.#password = value;
  }
  
  // 6. Статический метод
  static createAdmin(name) {
    return new User(name, 'admin@example.com');
  }
}
```

---

### 🎯 Для чего нужен `constructor`?

**`constructor`** — специальный метод, который:

1. **Вызывается автоматически** при создании экземпляра через `new`
2. **Инициализирует свойства** объекта
3. **Принимает параметры** для настройки экземпляра

```js
class Person {
  constructor(name, age) {
    this.name = name;  // инициализация свойства
    this.age = age;    // инициализация свойства
  }
}

const person = new Person('Иван', 25);
// constructor вызывается автоматически с аргументами 'Иван' и 25
```

**Если `constructor` не указан:**
- JavaScript создаёт пустой `constructor` автоматически
- Он просто создаёт объект без инициализации

---

### 🔄 Что возвращает вызов класса?

При вызове класса с `new`:

1. **Создаётся новый объект** `{}`
2. **Вызывается `constructor`** с переданными аргументами
3. **Объект связывается с прототипом** класса
4. **Возвращается созданный объект**

```js
class User {
  constructor(name) {
    this.name = name;
  }
}

const user = new User('Анна');
console.log(user); // { name: 'Анна' }
```

**Важно:**

- Если `constructor` возвращает **примитив** → игнорируется, возвращается объект
- Если `constructor` возвращает **объект** → возвращается этот объект (не созданный автоматически)

```js
class User {
  constructor(name) {
    this.name = name;
    return 'игнорируется'; // примитив — игнорируется
  }
}

class Admin {
  constructor(name) {
    this.name = name;
    return { role: 'admin' }; // объект — вернётся он
  }
}

const user = new User('Иван');
console.log(user); // { name: 'Иван' } (не строка!)

const admin = new Admin('Петр');
console.log(admin); // { role: 'admin' } (не { name: 'Петр' }!)
```

---

### 📊 Структура класса:

``` js
class User {
  // Приватные свойства
  #privateField
  
  // Constructor
  constructor() { }
  
  // Методы экземпляра
  method() { }
  
  // Геттеры/сеттеры
  get property() { }
  set property(value) { }
  
  // Статические методы (Метод для самого класса)
  static staticMethod() { }
}
```

---

### 🎯 Итог:

- **Класс объявляется** через `class ClassName { }`
- **Constructor** инициализирует экземпляр при создании
- **Вызов с `new`** возвращает новый объект, связанный с прототипом класса







