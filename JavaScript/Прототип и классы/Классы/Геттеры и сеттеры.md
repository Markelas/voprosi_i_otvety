## 🔑 Геттеры и сеттеры

**Геттеры** и **сеттеры** — специальные методы для доступа к свойствам, которые позволяют обращаться к ним как к обычным свойствам, но с дополнительной логикой.

---

### 🎯 Для чего нужны?

- ✅ **Вычисляемые свойства** — значение вычисляется при обращении
- ✅ **Валидация** — проверка данных при установке значения
- ✅ **Инкапсуляция** — контроль доступа к данным
- ✅ **Логирование** — отслеживание изменений свойств

---

### 📝 Геттер (`get`)

Позволяет получить значение как свойство, но с вычислением.

```js
class User {
  constructor(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
  }
  
  get fullName() {
    return `${this.firstName} ${this.lastName}`;
  }
}

const user = new User('Иван', 'Петров');
console.log(user.fullName); // "Иван Петров" (вызывается как свойство!)
```

**Особенности:**
- Вызывается без скобок `()`
- Вычисляется каждый раз при обращении
- Не может принимать параметры

---

### 📝 Сеттер (`set`)

Позволяет установить значение как свойство, но с валидацией или обработкой.

```js
class User {
  constructor(age) {
    this._age = age;
  }
  
  set age(value) {
    if (value < 0 || value > 150) {
      throw new Error('Некорректный возраст');
    }
    this._age = value;
  }
  
  get age() {
    return this._age;
  }
}

const user = new User(25);
user.age = 30;  // вызывается сеттер
console.log(user.age); // 30

user.age = 200; // Error: Некорректный возраст
```

---

### 💡 Пример: Полный класс с геттером и сеттером

```js
class Rectangle {
  constructor(width, height) {
    this._width = width;
    this._height = height;
  }
  
  // Геттер для площади
  get area() {
    return this._width * this._height;
  }
  
  // Сеттер для ширины с валидацией
  set width(value) {
    if (value <= 0) {
      throw new Error('Ширина должна быть положительной');
    }
    this._width = value;
  }
  
  // Геттер для ширины
  get width() {
    return this._width;
  }
}

const rect = new Rectangle(10, 5);
console.log(rect.area);    // 50 (вычисляется автоматически)
rect.width = 20;           // используем сеттер
console.log(rect.area);    // 100 (пересчиталось)
```

---

### 🔍 Отличие от обычных методов:

```js
class User {
  // Обычный метод
  getFullName() {
    return `${this.firstName} ${this.lastName}`;
  }
  
  // Геттер
  get fullName() {
    return `${this.firstName} ${this.lastName}`;
  }
}

const user = new User('Иван', 'Петров');

// Обычный метод — нужны скобки
console.log(user.getFullName()); // "Иван Петров"

// Геттер — без скобок, как свойство
console.log(user.fullName);      // "Иван Петров"
```

---

### 🎯 Итог:

- **Геттеры** (`get`) — для получения вычисляемых значений как свойств
- **Сеттеры** (`set`) — для установки значений с валидацией или обработкой
- Обращение к ним **без скобок**, как к обычным свойствам
- Полезны для **инкапсуляции** и **валидации** данных







