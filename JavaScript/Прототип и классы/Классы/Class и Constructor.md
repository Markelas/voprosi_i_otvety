<font color="#6425d0">class</font> — это синтаксический сахар в JavaScript для создания функций-конструкторов и организации прототипного наследования. Класс сам по себе — это специальная форма определения конструктора и его прототипа.
<font color="#953734">constructor</font> — специальный метод внутри `class`, который вызывается при создании нового экземпляра через `new`. Он отвечает за инициализацию свойств объекта.

```js
class User {
  // constructor — вызывается при new User(...)
  constructor(name, age) {
    this.name = name; // свойство экземпляра
    this.age = age;
  }

  // метод на prototype
  greet() {
    return `Hello, ${this.name}`;
  }
}

const u = new User("Anna", 28);
console.log(u.name);      // "Anna"
console.log(u.greet());   // "Hello, Anna"
```

**Пояснение:**

- `class User { ... }` создаёт функцию-конструктор `User` и объект `User.prototype`
- Метод `greet` автоматически помещается в `User.prototype`, поэтому он общий для всех экземпляров (не копируется в каждый объект)
- `constructor` выполняет инициализацию полей: `this` — это только что созданный объект

---

### 🔧 Статические методы и свойства

**Статические** члены принадлежат классу, а не экземплярам.

```js
class Counter {
  static count = 0;        // статическое свойство
  
  static increment() {     // статический метод
    Counter.count++;
  }
  
  static getCount() {
    return Counter.count;
  }
}

Counter.increment();
console.log(Counter.getCount()); // 1

const c = new Counter();
console.log(c.count); // undefined — у экземпляра нет доступа
```

**Важно:** Статические члены хранятся на самом классе, а не в `prototype`.

---

### 📝 Геттеры и сеттеры

Позволяют обращаться к вычисляемым значениям как к свойствам.

```js
class User {
  constructor(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
  }
  
  get fullName() {           // геттер
    return `${this.firstName} ${this.lastName}`;
  }
  
  set fullName(value) {      // сеттер
    [this.firstName, this.lastName] = value.split(' ');
  }
}

const user = new User('Иван', 'Петров');
console.log(user.fullName);        // "Иван Петров"
user.fullName = 'Петр Сидоров';
console.log(user.firstName);      // "Петр"
```

---

### 🔄 Наследование: `extends` и `super`

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
  
  speak() {
    return `${this.name} makes a sound.`;
  }
}

class Dog extends Animal {
  constructor(name, breed) {
    super(name);      // вызывает constructor базового класса
    this.breed = breed;
  }
  
  speak() {           // переопределение метода
    return `${this.name} barks.`;
  }
}

const d = new Dog("Rex", "Shepherd");
console.log(d.speak());              // "Rex barks."
console.log(d instanceof Animal);    // true
```

**Важно:**
- `extends` устанавливает прототипную связь: `Dog.prototype.__proto__ === Animal.prototype`
- `super(...)` в `constructor` обязателен, если в наследнике есть `constructor`
- `super.method()` позволяет вызвать метод родителя

---

### 🔒 Приватные свойства (`#property`)

Приватные свойства с префиксом `#` недоступны вне класса.

```js
class Account {
  #balance = 0;  // приватное свойство
  
  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
    }
  }
  
  getBalance() {
    return this.#balance;
  }
}

const acc = new Account();
acc.deposit(100);
console.log(acc.getBalance());  // 100
console.log(acc.#balance);      // SyntaxError — нельзя обратиться напрямую
```

**Особенности:**
- Не видны через `Object.keys()`, `for...in`
- Это единственный способ обеспечить настоящую приватность в JavaScript

---

### 🛡️ Защищённые свойства (`_property`)

Условная договорённость (не синтаксическая защита). Обозначаются `_` и считаются внутренними.

```js
class Employee {
  _salary = 0; // защищённое (условно)
  
  setSalary(value) {
    if (value > 0) {
      this._salary = value;
    }
  }
}

const emp = new Employee();
emp.setSalary(5000);
console.log(emp._salary); // 5000 — доступ есть, но не рекомендуется
```

---

### 🔍 `instanceof`

Проверяет, был ли объект создан определённым конструктором (или наследует его прототип).

```js
class User {}
class Admin extends User {}

const u = new User();
const a = new Admin();

console.log(u instanceof User);   // true
console.log(a instanceof Admin); // true
console.log(a instanceof User);  // true (Admin наследует User)
console.log(a instanceof Object); // true
```

**Как работает:** Проверяет наличие конструктора в цепочке прототипов.

---

### ⚖️ Отличия `class` от function-конструкторов

| Параметр | `class` | `function`-конструктор |
|----------|---------|------------------------|
| **Строгий режим** | Автоматически | Нужно указывать |
| **Вызов без `new`** | Ошибка | Работает (но неправильно) |
| **Методы** | Неперечислимые | Перечислимые |
| **Читаемость** | Более явный синтаксис | Менее явный |

---

### ⚠️ Важные моменты

- ❌ Не вызывать `constructor` без `new` — будет ошибка
- ❌ Не добавлять методы в `constructor` — лучше в тело класса (экономят память)
- ❌ Избегать расширения встроенных классов без надобности
- ✅ Использовать `super()` в `constructor` наследника, если есть `constructor`