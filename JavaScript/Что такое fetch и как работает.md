## 🌐 Что такое `fetch` и как работает

**`fetch`** — это встроенная в браузер функция для выполнения HTTP-запросов.

Возвращает [[Promise]], который разрешается объектом `Response`.

---
### 🔧 Синтаксис:

```js
fetch(url, options?)
```

- **`url`** — адрес запроса
- **`options`** — объект с настройками: метод (GET, POST и т.д.), заголовки, тело запроса
---
### 📊 Как работает:

`fetch` возвращает `Promise`, который:
- **`resolve`** → когда сервер вернул ответ (объект `Response`), **не обязательно успешный**
- **`reject`** → если произошла сетевая ошибка

---
### 📦 Объект Response

Имеет методы для чтения данных:

- **`.json()`** — преобразует тело ответа в JSON
- **`.text()`** — как текст
- **`.blob()`, `.arrayBuffer()`, `.formData()`** — для других типов данных
---
### 💡 Пример:

```js
fetch('https://api.example.com/data')
  .then(response => {
    if (!response.ok) throw new Error('Ошибка HTTP ' + response.status);
    return response.json(); // преобразуем в объект
  })
  .then(data => console.log(data))
  .catch(err => console.error(err));
```

---
### 📝 Итого:

- ✅ `fetch` всегда асинхронный, работает через `Promise`
- ⚠️ **Не бросает ошибки на HTTP ошибки** (404, 500) — нужно проверять `response.ok`
- ✅ Можно отменять запрос через `AbortController`
- ✅ Используется для запросов к API, замена старого `XMLHttpRequest`
