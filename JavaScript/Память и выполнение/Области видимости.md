<font color="#c00000">В JavaScript есть **3 основные области видимости**:</font>

---

### 🌍 Глобальная

Переменные видны **везде** и объявляются **вне функций**.

```js
let globalVar = "Глобальная переменная";
```

---

### 🔹 Функциональная

Переменные видны только **внутри функции**. Создаётся каждой функцией.

```js
function f() { 
  let b = 2; // доступна только внутри функции
}
```

---

### 📦 Блочная

Работает с `let` и `const`, видна только в пределах `{}` (например, `if`, циклы).

```js
if (true) { 
  const c = 3; // доступна только внутри блока
}
```

---

### 💡 Важно:

- `var` имеет функциональную область видимости (не блочную!)
- `let` и `const` имеют блочную область видимости

## 🔹 Контекст выполнения (Execution Context)

Это среда, в которой выполняется конкретный кусок кода (функция, модуль, глобальный код).  
Она определяет:

- какие **переменные** доступны;
    
- какое значение имеет **this**;
    
- где именно программа **сейчас выполняется**.
    

---

### Виды контекстов выполнения:

1. **Глобальный контекст**
    
    - Создаётся один раз при запуске скрипта.
        
    - Доступны глобальные переменные и функции.
        
    - `this` в браузере указывает на `window`.
        
2. **Контекст функции**
    
    - Создаётся при вызове функции.
        
    - У каждой функции — свой контекст (со своими переменными, параметрами, `this`).
        
3. **Контекст eval** (используется редко)
    
    - Внутренний, создаётся при выполнении `eval()`.
        

---

## 🔹 Лексическое окружение (Lexical Environment)

Это структура, где **хранятся идентификаторы** (переменные, функции) и ссылки на внешнее окружение.  
Проще: «область видимости», созданная при объявлении.

Каждое лексическое окружение состоит из:

1. **Environment Record** — таблица, где хранятся пары «имя → значение».
    
2. **Ссылка на внешнее окружение** — позволяет искать переменные «наружу», если не найдены внутри.
    

---

### Пример:

```js
let a = 10;

function foo() {
  let b = 20;

  function bar() {
    let c = 30;
    console.log(a, b, c);
  }

  bar();
}

foo();
```

Разбор:

- **Глобальное окружение:** `{ a: 10, foo: <function> }`
    
- **Окружение `foo`:** `{ b: 20, bar: <function> }`, ссылка на глобальное
    
- **Окружение `bar`:** `{ c: 30 }`, ссылка на `foo`
    

Когда вызывается `console.log(a, b, c)`, движок ищет переменные:

- `c` → в `bar`
    
- `b` → в `foo`
    
- `a` → в глобальном
    

---

## 🔹 Разница:

|Понятие|Что это|Когда создаётся|
|---|---|---|
|**Контекст выполнения**|Процесс выполнения кода, где есть `this`, переменные и стек вызовов|При запуске (глобально или при вызове функции)|
|**Лексическое окружение**|Область, где хранятся переменные и ссылка на внешнюю область|При **объявлении** функции или блока кода|
