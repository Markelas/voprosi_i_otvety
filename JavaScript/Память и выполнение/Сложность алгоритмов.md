## 📊 Сложность алгоритмов

#### 1. <font color="#fac08f">O(1) — Константная</font>
Время **не зависит** от размера данных.

**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** `arr[0]`, `obj.key`, `map.get(key)`
#### 2. <font color="#b2a2c7">O(log n) — Логарифмическая</font>
Делим задачу пополам.

**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** бинарный поиск, операции с отсортированными структурами (например, деревья)
#### 3. <font color="#92cddc">O(n) — Линейная</font>
Проход по массиву или объекту **один раз**.
**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** `for…of arr`, `arr.reduce()`, `for (let key in obj)`
#### 4. <font color="#b2a2c7">O(n log n) — </font><font color="#92cddc">Линейно</font>-<font color="#b2a2c7">логарифмическая</font>
Эффективные сортировки.
**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** `arr.sort()`
#### 5. <font color="#d99694">O(n²) — Квадратичная</font>
Два вложенных цикла.
**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** поиск пар элементов в массиве

#### 6. <font color="#c3d69b">O(n³) — Кубическая</font>
Три вложенных цикла.
#### 7. <font color="#938953">O(2ⁿ) — Экспоненциальная</font>
Рекурсия, где ветвление удваивается.

**<span style="background:rgba(74, 82, 199, 0.2)">ПРИМЕР В JS:</span>** простой рекурсивный Fibonacci
#### 8. <font color="#e36c09">O(n!) — Факториальная</font>
Все перестановки или комбинаторика.

---

### 💡 JS-особенности:

- **`.push()/.pop()`** — O(1)
- **`.shift()/.unshift()`** — O(n)
- **`.splice()`** — O(n)
- **`Map/Set`** доступ по ключу — O(1), перебор — O(n)
