## 🔗 Что такое прототип в JavaScript

В JavaScript <font color="#d99694">прототип</font> — это объект, у которого другой объект может брать свойства и методы, если у него самого их нет. Так работает **<font color="#d99694">прототипное наследование</font>**.

<font color="#31859b">Преимущества прототипов:</font>
- <font color="#31859b">Эффективность памяти</font>: Методы и свойства, определенные в прототипе, хранятся в одном месте в памяти и используются всеми экземплярами, что экономит память.
- <font color="#31859b">Легкость обновления</font>: Обновление методов и свойств в прототипе автоматически отражается на всех экземплярах, что облегчает поддержание кода.
- <font color="#31859b">Динамичность</font>: Прототипы позволяют динамически добавлять новые свойства и методы к существующим объектам.

**<font color="#d99694">Прототипное наследование</font>** — это механизм, с помощью которого объекты могут получать свойства и методы других объектов. В JavaScript каждый объект имеет скрытую ссылку на **прототип**, откуда он может наследовать свойства и методы.

---
### 📝 Основные концепции:

- Каждый объект в JS имеет скрытую ссылку **`[[Prototype]]`** (доступна как `__proto__`), указывающую на другой объект или `null`.

- Когда мы обращаемся к свойству, которого нет в объекте, движок ищет его в **прототипе** и по **цепочке прототипов** (prototype chain).

- У функций есть свойство **`prototype`** — это объект, который будет прототипом для всех экземпляров, созданных через `new`.

---
### Как присвоить прототип объекту?

### 🔄 Прототипная связь

**Прототипная связь** — это механизм, при котором один объект может наследовать свойства и методы другого через свойство `[[Prototype]]` (доступно как `__proto__`).

В JS её можно создать разными способами:

- **`Object.create(proto)`** — создаёт объект с указанным прототипом.
- Через **функции-конструкторы** и `prototype` — для создания цепочки наследования между экземплярами.
- Синтаксис **`class`** — упрощённая запись над прототипным наследованием.

---
<font color="#e36c09">Функции-конструкторы</font> — это <font color="#366092">механизм создания однотипных объектов с общими методами.</font>
Ключевое слово `new` <font color="#c00000">автоматически связывает создаваемый объект с прототипом функции-конструктора</font>.
Свойство `Function.prototype` используется для хранения методов, общих для всех экземпляров, а цепочка прототипов обеспечивает наследование поведения между разными типами объектов.

---
### 🔑 Разница между `prototype` и `__proto__`

| Свойство | Где находится | Назначение |
|----------|---------------|------------|
| **`prototype`** | У функции-конструктора | <font color="#e36c09">Используется как шаблон (прототип), откуда объекты наследуют методы</font>. Определяет, какие методы и свойства будут доступны всем объектам, созданным этой функцией. Он является основой прототипного наследования в JavaScript. |
| **`__proto__`** | У любого объекта |<font color="#76923c"> Используется как ссылка на свой прототип (на объект `prototype` конструктора)</font>. Это не стандартное свойство (это «геттер/сеттер» для `[[Prototype]]`) |

<font color="#e36c09">Связь <code>__proto__</code> и <code>prototype</code></font>

Когда объект создаётся через функцию-конструктор, его внутреннее свойство `[[Prototype]]` (`__proto__`) ссылается на объект функции `prototype`.

```js
console.log(bob.__proto__ === Person.prototype); // true
```

Разбор:
Объект <code>bob</code> наследует методы и свойства из<code>Person.prototype</code>
<code>__proto__</code> указывает на объект прототипа функции-конструктора, связывая экземпляр с его прототипом.

---
### Общая структура прототипов
В JavaScript каждый объект наследует свойства и методы своего прототипа. Встроенные объекты, например `Array`, `String` или `Object`, также имеют собственные прототипы, через которые доступна стандартная функциональность языка. 

```js
const arr = [1, 2, 3];

console.log(arr.__proto__ === Array.prototype); // true
console.log(Array.prototype.__proto__ === Object.prototype); // true
```
Разбор:
- `arr` — это массив, созданный через литерал `[1,2,3]`.
- У любого массива в JS прототипом является `Array.prototype`.
- Именно там лежат все стандартные методы массивов — `map`, `filter`, `forEach`, `push`, и т.д.

Поэтому `arr.__proto__` и `Array.prototype` — это один и тот же объект в памяти.
Далее, в второй проверке :
-  `Array.prototype` — массив, это тоже объект.
- А все объекты в JS наследуются от `Object.prototype`.

Отсюда массивы **наследуют** не только свои методы (`map`, `filter`), но и общие методы объектов — например `toString`, `hasOwnProperty`, `valueOf`.

Получается, цепочка прототипов следующая:
```js
arr → Array.prototype → Object.prototype → null
```
`null` в конце означает, что цепочка закончилась (у `Object.prototype` нет родителя).

---

✅ **Правильный способ получить прототип:** `Object.getPrototypeOf(obj)`

---
### <font color="#c00000">ПРИМЕЧАНИЕ</font>
<p><strong>Не изменяйте прототип встроенных объектов</strong> (<code>Array.prototype</code>, <code>Object.prototype</code>), это может вызвать непредсказуемое поведение.</p>
<p>Прототип — это <strong>объект</strong>, нельзя присваивать <code>null</code> или примитив без обёртки.</p>
<p>Методы, добавленные через <code>prototype</code>, разделяются между всеми объектами-конструкторами, экономя память.</p>

---

### 🛠️ Методы для работы с прототипами

#### 1. **`Object.getPrototypeOf(obj)`**
Получить прототип объекта.

```js
const obj = {};
console.log(Object.getPrototypeOf(obj) === Object.prototype); // true
```

#### 2. **`Object.setPrototypeOf(obj, proto)`**
Установить прототип объекта (медленно, лучше использовать `Object.create`).

```js
const parent = { name: 'Parent' };
const child = {};
Object.setPrototypeOf(child, parent);
console.log(child.name); // 'Parent'
```

#### 3. **`Object.create(proto)`**
Создать объект с указанным прототипом.

```js
const proto = { greet: () => 'Hello' };
const obj = Object.create(proto);
console.log(obj.greet()); // 'Hello'
```

#### 4. **`obj.hasOwnProperty(prop)`**
Проверить, есть ли свойство у самого объекта (не в прототипе).

```js
const obj = Object.create({ inherited: 'from proto' });
obj.own = 'own property';

console.log(obj.hasOwnProperty('own'));      // true
console.log(obj.hasOwnProperty('inherited')); // false
```

#### 5. **`prop in obj`**
Проверить наличие свойства (включая прототип).

```js
const obj = Object.create({ inherited: 'value' });
obj.own = 'value';

console.log('own' in obj);       // true
console.log('inherited' in obj); // true
console.log('missing' in obj);   // false
```

#### 6. **`obj instanceof Constructor`**
Проверить, был ли объект создан через указанный конструктор.

```js
function User() {}
const user = new User();

console.log(user instanceof User);        // true
console.log(user instanceof Object);     // true (через цепочку)
```

#### 7. **`Object.getOwnPropertyNames(obj)`**
Получить все собственные свойства объекта (включая неперечисляемые).

```js
const obj = { a: 1, b: 2 };
console.log(Object.getOwnPropertyNames(obj)); // ['a', 'b']
```

#### 8. **`Object.keys(obj)`**
Получить перечисляемые собственные свойства объекта.

```js
const obj = { a: 1, b: 2 };
console.log(Object.keys(obj)); // ['a', 'b']
```

---

### 🔨 Как работает `new Constructor()`

Когда вызываешь функцию с оператором `new`, происходит следующее:

1. **Создаётся новый пустой объект** `{}`
2. **Функция-конструктор вызывается**, а `this` указывает на этот новый объект
3. **Объект начинает ссылаться на `prototype` конструктора** через `__proto__`
4. **Возвращается объект** (если конструктор не возвращает явно свой объект)

---

### ⚠️ Стрелочные функции и конструкторы

**Стрелочная функция не может быть конструктором** и не имеет своего `this`.

```js
// ❌ Ошибка — стрелочная функция не может быть конструктором
const User = (name) => {
  this.name = name;  // this не будет указывать на новый объект
};

const user = new User('Иван'); // TypeError: User is not a constructor
```

**Почему:**
- Стрелочные функции не имеют своего `this` — они берут его из внешнего контекста
- У них нет свойства `prototype`
- Оператор `new` не может работать со стрелочными функциями

---

### 💡 Пример 1: Базовое использование

```js
function User(name) {
  this.name = name;  // this указывает на новый объект
}

// Добавляем метод в prototype
User.prototype.sayHi = function() {
  return `Привет, я ${this.name}`;
};

const user = new User('Иван');
console.log(user.name);        // 'Иван'
console.log(user.sayHi());     // 'Привет, я Иван'
```

**Что происходит:**
- `new User('Иван')` создаёт объект `{}`
- `this.name = name` добавляет свойство в объект
- `user.__proto__` ссылается на `User.prototype`
- `user.sayHi()` работает, потому что метод находится в прототипе

---

### 💡 Пример 2: Цепочка прототипов

```js
function Animal(name) {
  this.name = name;
}

Animal.prototype.eat = function() {
  return `${this.name} ест`;
};

function Dog(name, breed) {
  Animal.call(this, name);  // Вызываем родительский конструктор
  this.breed = breed;
}

// Наследование: Dog.prototype наследует от Animal.prototype
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;

Dog.prototype.bark = function() {
  return `${this.name} гавкает`;
};

const dog = new Dog('Шарик', 'овчарка');
console.log(dog.eat());   // 'Шарик ест' (из Animal.prototype)
console.log(dog.bark());  // 'Шарик гавкает' (из Dog.prototype)
```

**Цепочка прототипов:**
```
dog → Dog.prototype → Animal.prototype → Object.prototype → null
```

---

### 🎯 Итог:

1. `new` создаёт новый объект и связывает его с `prototype` конструктора
2. `this` в конструкторе указывает на этот новый объект
3. Свойства и методы из `prototype` доступны через цепочку прототипов
4. Без `new` конструктор работает как обычная функция (теряется связь с прототипом)
