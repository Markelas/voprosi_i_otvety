📘 `undefined`  
- Присваивается **<u>автоматически</u>**, если переменная **объявлена, но не инициализирована**.  
- Возвращается, если **свойство объекта** или **элемент массива** не существует.  
- Означает: **значение отсутствует / не определено**.  
- Это **системное значение**, задаваемое движком JS.  

🪫 `null`  
- Присваивается <u>явно</u> разработчиком, чтобы указать: **значение пусто**.  
- Используется для **сброса** или **намеренного отсутствия данных**.  
- Это значение типа **object** (ошибка в языке, оставлена ради совместимости).  
  
---  
  ⚖️ Сравнение `null` и `undefined`  
  
```js  
null == undefined;  // true  
null === undefined; // false  
```  
  
📌 `==` приводит типы, поэтому `true`.    
📌 `===` сравнивает без приведения, поэтому `false`.  

🧠 Дополнительно  
- `typeof null` → `"object"` (историческая ошибка 🕰️)  - `typeof undefined` → `"undefined"`  