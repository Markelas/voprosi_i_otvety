## 🌐 Разница между DOM и BOM

---

### 📄 **DOM** (Document Object Model)

**Объектная модель HTML-документа.**

Браузер превращает HTML в дерево узлов (объектов), с которыми можно работать через JavaScript.

```js
document.body.style.background = 'red';
```

- ✅ Представляет **структуру страницы**
- ✅ Позволяет **изменять, добавлять, удалять** элементы
- ✅ Это часть стандарта **W3C** (не завязана на конкретный браузер)

---

### 🪟 **BOM** (Browser Object Model)

**Объектная модель всего браузера**, а не только страницы.

Даёт доступ к функциям и окружению браузера.

```js
window.location.href;
alert();
localStorage.setItem('key', 'value');
```

- ✅ Управляет **окном, историей, навигацией, localStorage, таймерами** и т.д.
- ✅ Реализуется браузерами, **не является частью стандарта W3C** (а частью Web API)
- ✅ Центральный объект — **`window`**
