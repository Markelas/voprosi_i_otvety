**Рекурсия** — это когда функция вызывает саму себя.

⚠️ Главное — чтобы у неё было **базовое условие выхода**, иначе вызовы будут бесконечны.

Каждый вызов функции помещается в **стек вызовов** (call stack).
JS приостанавливает выполнение текущей функции, пока не завершится та, что вызвана внутри.

---
### 📊 Порядок выполнения:

**То, что до рекурсивного вызова** — выполняется **"вниз"**.

**То, что после рекурсивного вызова** — выполняется **"вверх"**.

```js
function f(n) {
  console.log('↓', n);   // выполняется при спуске вниз // 3 2 1
  if (n > 1) f(n - 1);   // рекурсивный вызов
  console.log('↑', n);   // выполняется при возврате вверх // 1 2 3
}

f(3);
// ↓ 3
// ↓ 2
// ↓ 1
// ↑ 1
// ↑ 2
// ↑ 3
```

---

### 💡 Итого:

**Рекурсия накапливает вызовы в стеке, а затем выполняет код в обратном порядке возвратов.**

---

### 📝 Пример: Фибоначчи

```js
function fibonacci(n) {
    if (n === 0) return 0;
    if (n === 1) return 1;
    return fibonacci(n - 2) + fibonacci(n - 1);
}

console.log(fibonacci(6));  // 8
console.log(fibonacci(10)); // 55
```

⚠️ **Внимание:** такая реализация очень неэффективна для больших чисел (O(2ⁿ)), лучше использовать мемоизацию или итеративный подход.
