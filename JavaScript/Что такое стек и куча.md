## 📚 Что такое стек и куча и зачем они нужны

В программе есть разные типы данных и разные способы их хранения:

---

### 📦 Стек (Stack)

**Стек** — упорядоченная область памяти.

#### Используется для:

- **Вызовов функций** (call stack)
- **Хранения примитивных значений** (`number`, `string`, `boolean`, `null`, `undefined`, `symbol`, `bigint`)
- **Хранения ссылок на объекты**, которые лежат в куче

#### Механика работы:

При вызове функции создаётся **активационный кадр** (execution context):
- Запоминает параметры
- Локальные переменные
- Ссылка на внешний контекст

Этот кадр (фрейм) кладётся в стек. Когда функция завершается → кадр убирается со стека, память очищается.

---

### 🗂️ Куча (Heap)

**Куча** — неструктурированная область памяти, куда можно положить данные любого размера.

#### Используется для:

- **Хранения объектов, массивов, функций**
- **Хранения сложных структур**, которые не влезут в стек
- **Данных, которые живут дольше**, чем одна функция

#### Механика работы:

Когда создаётся объект или массив, в куче выделяется память под него. В стеке хранится **ссылка** на это место. Если на объект больше нет ссылок → сборщик мусора (GC) освобождает память.

---

### 🧠 Почему нужны обе структуры?

- ❌ Если бы всё хранили только в стеке → стек переполнился бы при хранении больших структур (например, объектов с вложенностями).

- ❌ Если бы всё хранили только в куче → управление было бы медленным, а вызовы функций выполнялись бы хаотично.

✅ Поэтому используется **комбинация двух структур**.

---

### 💻 Как это связано с устройством

Физически и стек, и куча лежат в оперативной памяти (RAM). Но управление ими делает уже не железо, а программа — среда выполнения (V8, SpiderMonkey, JavaScriptCore и т.п.).

Операционная система выделяет процессу кусок RAM, а движок делит её на стек/кучу и управляет ими.
![[Screenshot at Nov 01 15-01-16.png]]