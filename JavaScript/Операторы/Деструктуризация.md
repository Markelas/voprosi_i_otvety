## 🎯 Деструктуризация

**Деструктуризация** — это способ извлечь значения из массива или объекта и присвоить их переменным коротко и наглядно.

---

### 📝 Основные примеры:

#### Извлечение из массива:
```js
const [a, b] = arr;
```

#### Обмен значений:
```js
[a, b] = [b, a]; // меняем местами
```

---

### 🔧 Дополнительные возможности:

#### Переименование ключей:
```js
const { name: userName } = user; // userName = 'Mark'
```

#### Значения по умолчанию:
```js
const { city = 'Unknown' } = user; // city = 'Unknown'
```

#### Пропускать значения и доставать во вложенных:
```js
const matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];
const [, [, element]] = matrix; // Элемент '5'
```

---

### 💡 Комбинирование:

```js
const { name: userName, age = 18, city } = user;
```
