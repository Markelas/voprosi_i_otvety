## 🔄 Разница `for…of` и `for…in`

---

### 🔹 `for...in`

Перебирает **ключи** (имена свойств) объекта.

- ✅ Работает с объектами и массивами
- ⚠️ Может проходить по **наследуемым свойствам**
- ⚠️ Порядок ключей не гарантируется

---

### 🔹 `for...of`

Перебирает **значения** итерируемых объектов (массивы, строки, Map, Set и т.д.).

- ✅ Порядок итерации определяется самим итератором (например, у массива — по индексам)
- ❌ Не работает с обычными объектами (без итератора)

---

### 📝 Пример для массива:

```js
const arr = ['a', 'b', 'c'];

for (const i in arr) console.log(i);   // 0, 1, 2  — ключи
for (const v of arr) console.log(v);   // a, b, c  — значения
```

---

### 📝 Пример для объекта:

```js
const obj = { a: 1, b: 2 };

for (const key in obj) {
  console.log(key);          // a, b  — ключи
}

for (const val of Object.values(obj)) {
  console.log(val);          // 1, 2 — значения
}
```

### 🧩 **Итерируемость**

**Что это:**  
Если объект можно пройти с помощью цикла `for...of`, оператора `...` (spread), или функций вроде `Array.from()`, значит он **итерируемый**.

**Как это работает:**  
У объекта должен быть специальный метод `Symbol.iterator`, который возвращает итератор (объект с методом `next()`).

**Примеры итерируемых объектов:**

- массив `[]`
    
- строка `"text"`
    
- коллекции `Map`, `Set`
    
- аргументы функции (`arguments`)
    

```js
const arr = [10, 20, 30];
for (const value of arr) {
  console.log(value); // 10, 20, 30
}
```

**Ключевая идея:**  
👉 итерируемость — это про **значения** внутри структуры.

---

### ⚙️ **Перебираемость**

**Что это:**  
Если объект можно обойти с помощью `for...in` или `Object.keys()`, значит он **перебираемый**.

**Как это работает:**  
Перебираются **свойства** объекта (его ключи), причём только те, что:

- перечисляемые (`enumerable: true`),
    
- принадлежат самому объекту (если не использовать `for...in`, который захватывает и прототип).
    

**Пример:**

```js
const obj = { a: 1, b: 2 };
for (const key in obj) {
  console.log(key); // a, b
}
```

**Ключевая идея:**  
👉 перебираемость — это про **ключи (свойства)** объекта.

---

### 🔍 Разница коротко:

|Свойство|Итерируемость|Перебираемость|
|---|---|---|
|Что обходится|Значения|Ключи|
|Пример цикла|`for...of`|`for...in`|
|Что нужно|`Symbol.iterator`|`enumerable: true`|
|Пример объектов|`Array`, `Set`, `Map`, `String`|Обычные объекты `{}`|

---

💡 Итог:

- **Итерируемость** — механизм, чтобы пройтись по значениям.
    
- **Перебираемость** — свойство объектов, чтобы пройтись по их ключам.