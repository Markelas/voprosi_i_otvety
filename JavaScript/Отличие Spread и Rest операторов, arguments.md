## 🔄 Отличие `...Spread` и `...Rest` операторов, `arguments`

---

### 🔸 **Spread** (`...`)

Распаковывает элементы массива или объекта, используется при:
- Передаче аргументов
- Копировании
- Объединении

```js
const newArr = [...arr, 4]; // [1,2,3,4]
const newObj = {...obj, key: 'value'};
```

---

### 🔸 **Rest** (`...`)

Собирает оставшиеся элементы в массив или объект, используется в:
- Параметрах функций
- Деструктуризации

```js
const [a, ...rest] = [1, 2, 3]; // rest = [2,3]

function sum(...nums) { 
  return nums.reduce((a, b) => a + b); 
}
```

---

### 🔸 **`arguments`**

Псевдомассив всех переданных аргументов внутри функции.

- ✅ Доступен только в **обычных функциях**, не в стрелочных
- ❌ Не имеет методов массива (`map`, `forEach` и т.д.)

```js
function show() {
  console.log(arguments); // [1, 2, 3]
}

show(1, 2, 3);
```

💡 **Совет:** в современном JS лучше использовать `Rest` параметры вместо `arguments`:

```js
function show(...args) {
  console.log(args); // настоящий массив, можно использовать методы
}
```
