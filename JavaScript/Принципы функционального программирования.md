## 🎯 Принципы функционального программирования

**Функциональное программирование (ФП)** — это парадигма, в которой функции являются основными строительными блоками.

---

### 1️⃣ Функции первого класса

Функции могут быть:
- ✅ Переданы как аргументы
- ✅ Возвращены из других функций
- ✅ Присвоены переменным

```js
// Функция как аргумент
function applyFunction(func, value) {
  return func(value);
}

const double = x => x * 2;
console.log(applyFunction(double, 5)); // 10

// Функция как возвращаемое значение
function createMultiplier(multiplier) {
  return function(x) {
    return x * multiplier;
  };
}

const triple = createMultiplier(3);
console.log(triple(4)); // 12

// Функция как значение переменной
const greet = function(name) {
  return `Привет, ${name}!`;
};
```

---

### 2️⃣ Чистые функции (Pure Functions)

**Чистые функции:**
- ✅ Не имеют побочных эффектов
- ✅ Зависят только от входных данных
- ✅ Всегда возвращают одинаковый результат для одинаковых входных данных
- ✅ Не изменяют внешнее состояние

```js
// ✅ Чистая функция
function add(a, b) {
  return a + b;
}

// ✅ Чистая функция
function multiply(x, y) {
  return x * y;
}

// ❌ НЕ чистая функция (побочный эффект)
let counter = 0;
function increment() {
  counter++; // изменяет внешнюю переменную
  return counter;
}

// ❌ НЕ чистая функция (зависит от внешнего состояния)
function getCurrentTime() {
  return new Date(); // разный результат при каждом вызове
}
```

**Преимущества чистых функций:**
- Легко тестировать
- Предсказуемы
- Можно кэшировать (мемоизация)
- Легко распараллеливать

---

### 3️⃣ Лямбда функции (стрелочные функции)

Краткий синтаксис для написания функций.

```js
// Обычная функция
function sum(a, b) {
  return a + b;
}

// Стрелочная функция
const sum = (a, b) => a + b;

// С одним параметром
const double = x => x * 2;

// Без параметров
const greet = () => 'Привет!';

// С телом функции
const multiply = (a, b) => {
  const result = a * b;
  return result;
};
```

---

### 4️⃣ Иммутабельность (Immutability)

Объекты и массивы **не изменяются** после создания. Для изменения создаются **новые** объекты.

```js
// ❌ Мутация (изменение оригинала)
const person = { name: 'John', age: 30 };
person.age = 31; // изменяет оригинальный объект

// ✅ Иммутабельность (создание нового объекта)
const person = { name: 'John', age: 30 };
const updatedPerson = { ...person, age: 31 }; // новый объект

// ✅ Иммутабельность для массивов
const numbers = [1, 2, 3];
const newNumbers = [...numbers, 4]; // новый массив
// или
const doubled = numbers.map(n => n * 2); // новый массив

// ❌ Мутация массива
numbers.push(4); // изменяет оригинальный массив
```

**Методы для иммутабельной работы:**
- `map()` — создаёт новый массив
- `filter()` — создаёт новый массив
- `reduce()` — создаёт новое значение
- `slice()` — создаёт новый массив
- `Object.assign()` / spread `{...obj}` — создаёт новый объект

---

### 5️⃣ Функции высшего порядка (Higher-Order Functions)

Функции, которые принимают другие функции как аргументы или возвращают функции.

```js
// Функция, принимающая функцию как аргумент
function map(array, transform) {
  const result = [];
  for (let item of array) {
    result.push(transform(item));
  }
  return result;
}

const numbers = [1, 2, 3];
const doubled = map(numbers, x => x * 2); // [2, 4, 6]

// Встроенные функции высшего порядка
const numbers = [1, 2, 3, 4, 5];

// map — преобразует каждый элемент
const doubled = numbers.map(x => x * 2); // [2, 4, 6, 8, 10]

// filter — фильтрует элементы
const evens = numbers.filter(x => x % 2 === 0); // [2, 4]

// reduce — сводит массив к одному значению
const sum = numbers.reduce((acc, x) => acc + x, 0); // 15

// forEach — выполняет действие для каждого элемента
numbers.forEach(x => console.log(x));
```

---

### 6️⃣ Композиция функций

Объединение простых функций в более сложные.

```js
// Простые функции
const add = (a, b) => a + b;
const multiply = (a, b) => a * b;
const square = x => x * x;

// Композиция вручную
const addThenSquare = (a, b) => {
  const sum = add(a, b);
  return square(sum);
};

console.log(addThenSquare(3, 2)); // (3 + 2)² = 25

// Композиция через цепочку
const numbers = [1, 2, 3, 4, 5];
const result = numbers
  .filter(x => x % 2 === 0)  // [2, 4]
  .map(x => x * 2)            // [4, 8]
  .reduce((acc, x) => acc + x, 0); // 12
```

---

### 🎯 Основные концепции ФП

| Концепция | Описание | Пример |
|-----------|----------|--------|
| **Чистые функции** | Нет побочных эффектов | `const add = (a, b) => a + b` |
| **Иммутабельность** | Не изменяем данные | `const new = [...old, item]` |
| **Функции первого класса** | Функции как значения | `const fn = () => {}` |
| **Функции высшего порядка** | Функции, работающие с функциями | `array.map(fn)` |
| **Композиция** | Объединение функций | `f(g(x))` |

---

### 💡 Преимущества ФП

- ✅ **Предсказуемость** — чистые функции легче понять
- ✅ **Тестируемость** — легко тестировать изолированные функции
- ✅ **Отладка** — проще найти ошибки
- ✅ **Переиспользование** — функции можно комбинировать
- ✅ **Параллелизм** — чистые функции безопасны для параллельного выполнения

---

### ⚠️ Важно помнить

- ФП не заменяет ООП — это разные подходы
- В JavaScript можно комбинировать парадигмы
- Не все задачи лучше решать функционально
- Иммутабельность может требовать больше памяти

---

### 💡 Итог:

- **Функции первого класса** — функции как значения
- **Чистые функции** — без побочных эффектов
- **Иммутабельность** — не изменяем данные, создаём новые
- **Функции высшего порядка** — работают с другими функциями
- **Композиция** — объединение простых функций в сложные





