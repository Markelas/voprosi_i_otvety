## 🔌 Варианты отмены обработчиков событий

| Способ | Когда использовать | Как удалить |
|--------|-------------------|-------------|
| **`addEventListener`/`removeEventListener`** | Классический, гибкий | `removeEventListener` |
| **`AbortController`** | Современный, безопасный, массовая отмена | `controller.abort()` |
| **DOM-свойства** (`onclick = ...`) | Простые одноразовые обработчики | `onclick = null` |
| **React-события** (`onClick={}`) | В JSX, автоматически управляется | React сам удаляет |

---

### 📝 Примеры:

#### `addEventListener`/`removeEventListener`

```js
function handleClick() {
  console.log('Клик!');
}

element.addEventListener('click', handleClick);
element.removeEventListener('click', handleClick);
```

#### `AbortController`

```js
const controller = new AbortController();
const { signal } = controller;

element.addEventListener('click', handleClick, { signal });
controller.abort(); // удаляет все слушатели с этим signal
```

#### DOM-свойства

```js
element.onclick = handleClick;
element.onclick = null; // удаление
```
