
### 🧊 `Object.freeze()`

**Полная неизменяемость.**

- ❌ Нельзя изменять значения свойств
- ❌ Нельзя добавлять новые свойства
- ❌ Нельзя удалять свойства
- ❌ Нельзя изменять дескрипторы

```js
const obj = { name: 'John' };
Object.freeze(obj);

obj.name = 'Jane'; // не сработает
obj.age = 25;      // не сработает
delete obj.name;   // не сработает
```

---

### 🔐 `Object.seal()`

**Фиксированная структура, но можно менять значения.**

- ✅ Можно изменять значения существующих свойств
- ❌ Нельзя добавлять новые свойства
- ❌ Нельзя удалять свойства
- ❌ Нельзя изменять дескрипторы (кроме `writable`)

```js
const obj = { name: 'John' };
Object.seal(obj);

obj.name = 'Jane'; // ✅ работает
obj.age = 25;      // ❌ не сработает
delete obj.name;   // ❌ не сработает
```

---

### 🚫 `Object.preventExtensions()`

**Просто запрет добавления новых свойств.**

- ✅ Можно изменять значения существующих свойств
- ✅ Можно удалять свойства
- ❌ Нельзя добавлять новые свойства
- ✅ Можно ограниченно менять дескрипторы

```js
const obj = { name: 'John' };
Object.preventExtensions(obj);

obj.name = 'Jane'; // ✅ работает
delete obj.name;   // ✅ работает
obj.age = 25;      // ❌ не сработает
```

---

### 📚 Что такое дескриптор?

**Дескриптор** — это внутренний объект, который описывает поведение каждого свойства в объекте: можно ли его изменять, удалять, перечислять и т.д.

Каждое свойство в JavaScript хранится не просто как «ключ → значение», а как объект с дополнительными флагами.

```js
const user = { name: 'John' };

console.log(Object.getOwnPropertyDescriptor(user, 'name'));
// { 
//   value: 'John', 
//   writable: true,      // можно ли изменять значение
//   enumerable: true,    // видно ли в цикле
//   configurable: true  // удалить свойство или изменить его дескриптор
// }
```

Бывают ещё **аксессорные дескрипторы** — если свойство не хранит значение, а использует `get/set`:

```js
{
  get: [Function: get age],
  set: [Function: set age],
  enumerable: true,
  configurable: true
}
```


#### Глубокая заморозка

```js
const deepFreeze = (obj) => {
    Object.freeze(obj);
    
    for (let key in obj) {
        let value = obj[key];
        if (
            value !== null &&
            (typeof value === 'object' || typeof value === 'function') &&
            !Object.isFrozen(value)
        ) {
            deepFreeze(value);
        }
    };
    return obj;
}
```