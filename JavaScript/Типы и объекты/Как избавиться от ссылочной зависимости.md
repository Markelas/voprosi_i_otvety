В JS массивы и объекты передаются **по ссылке**, поэтому изменение копии может затронуть оригинал.

Чтобы избавиться от ссылочной зависимости, можно сделать **поверхностную копию** или **глубокую копию**, в зависимости от структуры данных и того, нужны ли вложенные объекты независимыми.

---
🔸 Поверхностное копирование - создаёт новый объект/массив, но вложенные объекты/массивы остаются ссылками.

Способы:

1. **Через spread оператор:**
```js
const copy = {...obj};
const arrCopy = [...arr];
```

2. **`Object.assign({}, obj)`**
```js
const copy = Object.assign({}, obj);
```

3. **`slice()`** — для массивов
```js
const arrCopy = arr.slice();
```

---
🔹 Глубокое копирование - создаёт **полностью независимую копию**, включая вложенные объекты и массивы.

Способы:

1. **`JSON.parse(JSON.stringify(obj))`** - ❌ Не работает с функциями, датами, `Map`, `Set` и т.д.

2. **`structuredClone(obj)`** (современный вариант) - ✅ Копирует все, кроме функций

3. **`cloneDeep(obj)`** из библиотеки `lodash` - ✅ Универсальное решение

4. Написать самому функцию (рекурсивное копирование)

```js
function deepCopy(obj) {
  // Если это примитив или функция, возвращаем как есть
  if (obj === null || typeof obj !== 'object') {
    return obj;
  }
  
  // Если это массив
  if (Array.isArray(obj)) {
    return obj.map(item => deepCopy(item));
  }
  
  // Если это объект
  const copy = {};
  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      copy[key] = deepCopy(obj[key]);
    }
  }

  return copy;
}
```