## 🚪 Portals в React

**Portals** (Порталы) в React — это механизм, позволяющий рендерить дочерние компоненты в DOM-узлы, которые находятся вне иерархии родительского компонента. Это может быть полезно, когда нужно создать модальные окна, тултипы, контекстные меню и другие UI-элементы, которые должны быть независимы от обычной иерархии компонентов.

---

## 📝 Создание портала

Портал создается с помощью функции `ReactDOM.createPortal()`. Эта функция принимает два аргумента:

1. **Дочерний элемент**, который вы хотите отобразить
2. **Целевой узел**, в который нужно рендерить дочерний элемент

### Пример создания портала:

```jsx
import React from 'react';
import ReactDOM from 'react-dom';

const Modal = ({ isOpen, onClose }) => {
  if (!isOpen) return null;
  
  return ReactDOM.createPortal(
    <div className="modal">
      <h2>Модальное окно</h2>
      <button onClick={onClose}>Закрыть</button>
    </div>,
    document.getElementById('modal-root') // Целевой узел в HTML
  );
};

const App = () => {
  const [isModalOpen, setModalOpen] = React.useState(false);
  
  return (
    <div>
      <h1>Главная страница</h1>
      <button onClick={() => setModalOpen(true)}>
        Открыть модальное окно
      </button>
      <Modal 
        isOpen={isModalOpen} 
        onClose={() => setModalOpen(false)} 
      />
    </div>
  );
};

export default App;
```

**В HTML должен быть узел для портала:**

```html
<div id="root"></div>
<div id="modal-root"></div> <!-- Узел для портала -->
```

В этом примере мы создаем простое модальное окно с использованием портала. Дочерний элемент (модальное окно) рендерится в узел с `id="modal-root"`, который может быть расположен в другом месте в DOM-дереве.

---

## 🔑 Ключевые особенности порталов

### 1. **События всплывают через портал**

**Важно:** События в портале всплывают к родителю в **React-дереве**, а не в DOM-дереве. Это означает, что обработчики событий в родительском компоненте будут работать, даже если портал рендерится в другом месте DOM.

```jsx
function App() {
  const handleClick = () => {
    console.log('Клик обработан в App');
  };

  return (
    <div onClick={handleClick}>
      <Modal />
    </div>
  );
}

function Modal() {
  return ReactDOM.createPortal(
    <div className="modal">
      <button>Кликни меня</button>
      {/* Клик на кнопку вызовет handleClick в App */}
    </div>,
    document.getElementById('modal-root')
  );
}
```

### 2. **Иерархия в React-дереве сохраняется**

Портал не меняет иерархию компонентов в React-дереве. Компонент остается дочерним для родителя, даже если рендерится в другом месте DOM.

### 3. **Контекст работает нормально**

Портал имеет доступ к контексту родительского компонента, так как иерархия в React-дереве сохраняется.

```jsx
const ThemeContext = React.createContext();

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Modal /> {/* Имеет доступ к ThemeContext */}
    </ThemeContext.Provider>
  );
}
```

---

## 💡 Почему использовать порталы

1. **Изоляция стилей:** Порталы помогают избежать проблем со стилями, когда модальные окна или тултипы могут быть перекрыты другими элементами из-за иерархии CSS (например, `z-index`, `overflow: hidden`).

2. **Упрощение управления контекстом:** Порталы позволяют обрабатывать события и состояние в родительском компоненте, не затрагивая обычную иерархию компонентов.

3. **Семантика и доступность:** Порталы помогают создавать более семантически правильные и доступные интерфейсы, особенно для модальных окон и диалогов.

4. **Избежание проблем с позиционированием:** Элементы могут быть рендерены вне родительского контейнера, что решает проблемы с `overflow: hidden` или сложным позиционированием.

---

## 🎯 Кейсы использования порталов

1. **Модальные окна:** Используйте порталы для создания модальных окон, чтобы они могли перекрывать другие элементы интерфейса.

2. **Тултипы и подсказки:** Порталы помогают отображать подсказки и тултипы, которые могут требовать отрисовки вне обычной иерархии.

3. **Контекстные меню:** При создании контекстных меню порталы могут помочь избежать проблем с позиционированием, так как меню может быть рендерено вне родительского компонента.

4. **Уведомления и тосты:** Всплывающие уведомления часто рендерятся через порталы для правильного позиционирования.

5. **Dropdown-меню:** Выпадающие меню могут использовать порталы, чтобы избежать обрезания при `overflow: hidden` у родителя.

---

## ✅ Лучшие практики

### 1. **Соблюдайте семантику**

Убедитесь, что вы используете порталы для элементов, которые логически должны быть визуально независимыми от иерархии, как, например, модальные окна.

### 2. **Стилизация**

Убедитесь, что стили для порталов применяются правильно, так как они могут быть расположены вне текущей иерархии. Рассмотрите возможность использования CSS-свойств, таких как `position: absolute` или `fixed`.

```css
.modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
}
```

### 3. **Управление состоянием**

Используйте родительский компонент для управления состоянием портала (например, открытие и закрытие модального окна).

### 4. **Обработка кликов вне**

При создании модальных окон или других интерфейсов, которые требуют взаимодействия с пользователем, добавьте обработку кликов вне компонента, чтобы закрывать его.

**Пример с обработкой кликов вне:**

```jsx
const Modal = ({ isOpen, onClose, children }) => {
  const modalRef = React.useRef();

  React.useEffect(() => {
    const handleClickOutside = (event) => {
      if (modalRef.current && !modalRef.current.contains(event.target)) {
        onClose();
      }
    };

    if (isOpen) {
      document.addEventListener('mousedown', handleClickOutside);
    }

    return () => {
      document.removeEventListener('mousedown', handleClickOutside);
    };
  }, [isOpen, onClose]);

  if (!isOpen) return null;

  return ReactDOM.createPortal(
    <div className="modal-overlay">
      <div ref={modalRef} className="modal-content">
        {children}
        <button onClick={onClose}>Закрыть</button>
      </div>
    </div>,
    document.getElementById('modal-root')
  );
};
```

### 5. **Обработка Escape**

Добавьте обработку клавиши Escape для закрытия модальных окон:

```jsx
React.useEffect(() => {
  const handleEscape = (event) => {
    if (event.key === 'Escape') {
      onClose();
    }
  };

  if (isOpen) {
    document.addEventListener('keydown', handleEscape);
  }

  return () => {
    document.removeEventListener('keydown', handleEscape);
  };
}, [isOpen, onClose]);
```

### 6. **Блокировка скролла**

При открытии модального окна блокируйте скролл фонового контента:

```jsx
React.useEffect(() => {
  if (isOpen) {
    document.body.style.overflow = 'hidden';
  } else {
    document.body.style.overflow = 'unset';
  }

  return () => {
    document.body.style.overflow = 'unset';
  };
}, [isOpen]);
```

---

## 🎯 Ключевые моменты для собеседования

**Что нужно знать:**

1. **Портал рендерит компонент в другой узел DOM**, но сохраняет иерархию в React-дереве

2. **События всплывают к родителю в React-дереве**, а не в DOM-дереве — это важное отличие

3. **Контекст работает нормально**, так как иерархия компонентов сохраняется

4. **Используется для модальных окон, тултипов, контекстных меню** — элементов, которые должны быть визуально независимы

5. **Синтаксис:** `ReactDOM.createPortal(children, container)`

6. **Важно:** Портал не меняет логику компонента, только место рендеринга в DOM

---

## 💡 Полный пример модального окна

```jsx
import React, { useEffect, useRef } from 'react';
import ReactDOM from 'react-dom';

const Modal = ({ isOpen, onClose, title, children }) => {
  const modalRef = useRef();

  // Обработка Escape
  useEffect(() => {
    const handleEscape = (e) => {
      if (e.key === 'Escape') onClose();
    };

    if (isOpen) {
      document.addEventListener('keydown', handleEscape);
      document.body.style.overflow = 'hidden';
    }

    return () => {
      document.removeEventListener('keydown', handleEscape);
      document.body.style.overflow = 'unset';
    };
  }, [isOpen, onClose]);

  // Обработка кликов вне
  useEffect(() => {
    const handleClickOutside = (e) => {
      if (modalRef.current && !modalRef.current.contains(e.target)) {
        onClose();
      }
    };

    if (isOpen) {
      document.addEventListener('mousedown', handleClickOutside);
    }

    return () => {
      document.removeEventListener('mousedown', handleClickOutside);
    };
  }, [isOpen, onClose]);

  if (!isOpen) return null;

  return ReactDOM.createPortal(
    <div className="modal-overlay">
      <div ref={modalRef} className="modal-content">
        <div className="modal-header">
          <h2>{title}</h2>
          <button onClick={onClose}>×</button>
        </div>
        <div className="modal-body">{children}</div>
      </div>
    </div>,
    document.getElementById('modal-root')
  );
};

export default Modal;
```

---

## 📝 Заключение

Порталы в React — это мощный инструмент для управления рендерингом дочерних компонентов вне иерархии родительского компонента. Они предлагают гибкость в управлении стилями и логикой, что делает их полезными для создания модальных окон, тултипов и других UI-элементов. Соблюдая лучшие практики, вы сможете эффективно использовать порталы в своих проектах, улучшая пользовательский интерфейс и его взаимодействие.

**Главное преимущество:** Портал позволяет рендерить компонент в другом месте DOM, сохраняя при этом логическую связь с родителем в React-дереве, что обеспечивает правильную работу событий и контекста.


