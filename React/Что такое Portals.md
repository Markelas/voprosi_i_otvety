## 🚪 Что такое Portals

**React Portal** — это способ рендерить дочерний элемент компонента в DOM-узел, который находится **вне иерархии родительского компонента**.

Иными словами, визуально элемент находится в другом месте DOM, но React сохраняет связь с родителем для событий и состояния.

---

### 🔧 Синтаксис:

```js
ReactDOM.createPortal(child, container)
```

- **`child`** — React-элемент или компонент, который нужно рендерить
- **`container`** — DOM-узел, куда нужно вставить элемент

---

### 💡 Пример:

```js
function Modal({ children }) {
  return ReactDOM.createPortal(
    <div className="modal">
      {children}
    </div>,
    document.body
  );
}
```

---

### 🎯 Где используется:

Это полезно для:
- ✅ **Модальных окон**
- ✅ **Всплывающих подсказок**
- ✅ **Dropdown-меню**, которые должны быть визуально поверх остального контента, но управляться React

---

### ⚠️ Важно:

- События всё ещё всплывают по React-дереву, а не по DOM-дереву
- Portal не меняет логику компонента, только место рендеринга
