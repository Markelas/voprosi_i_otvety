## 🚀 Что такое lazy loading и `React.lazy`

---

### 🔸 Lazy loading (ленивая загрузка)

**Lazy loading** — это отложенная загрузка компонентов или модулей, когда они нужны, а не при первом рендере всего приложения.

**Преимущества:**
- ✅ Уменьшает время первоначальной загрузки (initial bundle size)
- ✅ Пользователь получает контент быстрее
- ✅ Экономия трафика

---

### 🔹 `React.lazy`

**`React.lazy`** позволяет динамически импортировать компоненты:

```js
const LazyComponent = React.lazy(() => import('./HeavyComponent'));
```

---

### 📦 Использование с `Suspense`:

```js
import { Suspense } from 'react';

const LazyComponent = React.lazy(() => import('./HeavyComponent'));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <LazyComponent />
    </Suspense>
  );
}
```

---

### 🎯 Когда использовать:

- ✅ Большие компоненты, которые не нужны сразу
- ✅ Роутинг (загрузка страниц по требованию)
- ✅ Модальные окна, попапы
- ✅ Условно отображаемые секции

---

### ⚠️ Важно:

- `React.lazy` работает только с default экспортами
- Обязательно оборачивай в `<Suspense>` с fallback
- Можно использовать несколько `Suspense` границ для разной загрузки
