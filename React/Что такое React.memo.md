## 🛡️ Что такое `React.memo`

**`React.memo`** — это HOC (Higher-Order Component), который предотвращает повторный рендер функционального компонента, если пропсы не изменились.

---

### 🔍 Как работает:

React сравнивает текущие и предыдущие `props` (по умолчанию — **поверхностно**) и пропускает ререндер, если они остались прежними.

Полезно для:
- ✅ Тяжёлых компонентов
- ✅ Когда родитель часто ререндерится

---

### 💡 Пример:

```js
const MyComponent = React.memo(function MyComponent(props) {
  return <div>{props.value}</div>;
});
```

---

### ⚙️ Кастомное сравнение:

Можно передать вторым аргументом функцию `areEqual(prevProps, nextProps)`, чтобы настроить сравнение пропсов:

```js
const Child = React.memo(
  ({ count }) => <div>{count}</div>,
  (prevProps, nextProps) => prevProps.count === nextProps.count
);
```

---

### ⚠️ Когда НЕ использовать:

- Если компонент всегда получает новые пропсы (объекты, функции без мемоизации)
- Для очень простых компонентов (накладные расходы могут быть больше выгоды)

---

### 💡 Рекомендация:

Используй `React.memo` вместе с `useCallback` и `useMemo` для пропсов, чтобы избежать ложных ререндеров.
