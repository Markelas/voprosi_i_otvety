## ⚙️ Разница между функциональными и классовыми компонентами

---

### 1. 📝 Синтаксис

**Функциональные** — это обычные функции, которые возвращают JSX.

```js
function MyComponent({ name }) {
  return <div>{name}</div>;
}
```

**Классовые** — это ES6-классы, наследующие `React.Component`.

```js
class MyComponent extends React.Component {
  render() {
    return <div>{this.props.name}</div>;
  }
}
```

---

### 2. 🔄 Управление состоянием

**Классы** используют `this.setState()`:

```js
class MyComponent extends React.Component {
  state = { count: 0 };
  
  increment = () => {
    this.setState({ count: this.state.count + 1 });
  };
}
```

**Функциональные** используют хуки:

```js
function MyComponent() {
  const [count, setCount] = useState(0);
  
  const increment = () => setCount(count + 1);
}
```

---

### 3. 🎯 Методы жизненного цикла

**В классах есть методы:** `componentDidMount`, `componentDidUpdate`, `componentWillUnmount` и др.

**В функциональных** вместо этого используют **`useEffect`** (можно объединять логику нескольких жизненных циклов):

```js
useEffect(() => {
  // componentDidMount + componentDidUpdate
  return () => {
    // componentWillUnmount
  };
}, [dependencies]);
```

---

### 📊 Сравнение:

| Параметр | Функциональные | Классовые |
|----------|----------------|-----------|
| **Синтаксис** | Проще, меньше кода | Более verbose |
| **Хуки** | ✅ Можно использовать | ❌ Нельзя |
| **Состояние** | `useState` | `this.setState` |
| **Lifecycle** | `useEffect` | Методы класса |
| **Рекомендация** | ✅ Современный подход | 🟡 Legacy код |

---

### 💡 Итог:

**В современном React рекомендуется использовать функциональные компоненты с хуками** — они проще, короче и легче читаются.
