## 🔧 React под капотом

React делает работу с интерфейсом интуитивной и простой, но под капотом скрывается множество процессов. В этом разделе мы разберем, **что на самом деле происходит с компонентами** и как React превращает их в конечный интерфейс.


**<font color="#e36c09">КОРОТКО</font>**
3 стадии трансформации:
1. JSX код
2. JS объект (компилятор создает объект из JSX)
3. Создает виртуальное представление в памяти, при измениях React сравнивает новое и старое представление и обновляет только измененные части реального DOM

---

### 1️⃣ Компоненты как функции

**Определение:** React-компоненты — это обычные JavaScript-функции, которые принимают объект `props` и возвращают JSX-разметку, описывающую элементы интерфейса.

**Что происходит:**

Компонент возвращает не HTML, а JavaScript-объект, описывающий структуру элемента.

**Пример:**

```jsx
function Button(props) {
  return <button>{props.label}</button>;
}
```

**Под капотом это преобразуется в:**

```js
function Button(props) {
  return {
    type: 'button',
    props: {
      children: [props.label]
    }
  };
}
```

Этот объект становится частью **дерева компонентов** (виртуального DOM).

---

### 2️⃣ Преобразование JSX в JavaScript

**Определение:** JSX — это синтаксический сахар, который компилятор (Babel) трансформирует в вызовы функций JavaScript.

**Современный React (17+):**

Используется автоматический импорт из `react/jsx-runtime`.

```jsx
<Button label="Click me" />
```

**Преобразуется в:**

```js
import { jsx as _jsx } from 'react/jsx-runtime';

_jsx('button', { children: 'Click me' });
```

**Старый способ (до React 17):**

```js
React.createElement('button', { label: 'Click me' }, 'Click me');
```

**Результат — JavaScript-объект (React Element):**

```js
{
  type: 'button',
  props: { children: 'Click me' },
  key: null,
  ref: null
}
```

**Важно:** JSX не является HTML и не выполняется напрямую в браузере. Это промежуточное представление, которое компилируется в JavaScript.

---

### 3️⃣ Виртуальное представление DOM

**Определение:** Виртуальный DOM — это легковесная JavaScript-структура в памяти, которая представляет иерархию элементов интерфейса. Это не копия реального DOM, а абстракция над ним.

**Структура виртуального элемента:**

```js
{
  type: 'div',
  props: {
    className: 'container',
    children: [
      {
        type: 'button',
        props: { children: 'Click me' }
      }
    ]
  }
}
```

**Зачем нужен виртуальный DOM:**

1. **Оптимизация обновлений** — React сравнивает новое представление с предыдущим (diffing)
2. **Минимизация операций с DOM** — обновляются только измененные части
3. **Кроссплатформенность** — одно представление может рендериться в разные среды (браузер, мобильные приложения)

**Процесс:**

React сравнивает новое виртуальное представление с предыдущим и обновляет только изменившиеся части реального DOM через процесс **Reconciliation**.

---

### 4️⃣ Создание элементов: функция createElement

**Определение:** React использует функцию `createElement` (или `jsx` в новых версиях) для создания объектов, описывающих элементы.

**Упрощенная реализация:**

```js
function createElement(type, props = {}, ...children) {
  return {
    type,
    props: {
      ...props,
      children: children.map(child =>
        typeof child === 'object' 
          ? child 
          : { type: 'TEXT', props: { value: child } }
      )
    }
  };
}
```

**Использование:**

```js
const element = createElement(
  'div',
  { className: 'container' },
  createElement('h1', null, 'Заголовок'),
  createElement('button', { onClick: handleClick }, 'Нажми')
);
```

**Результат:**

```js
{
  type: 'div',
  props: {
    className: 'container',
    children: [
      {
        type: 'h1',
        props: { children: [{ type: 'TEXT', props: { value: 'Заголовок' } }] }
      },
      {
        type: 'button',
        props: { 
          onClick: handleClick,
          children: [{ type: 'TEXT', props: { value: 'Нажми' } }]
        }
      }
    ]
  }
}
```

---

### 5️⃣ Свойства компонентов (Props)

**Определение:** Props — это объект, содержащий входные данные компонента. Передаются как аргументы функции и являются неизменяемыми (immutable).

**Пример:**

```jsx
function Card(props) {
  return (
    <div className="card">
      <h2>{props.title}</h2>
      <p>{props.content}</p>
    </div>
  );
}
```

**Объект props:**

```js
{
  title: "React под капотом",
  content: "Компоненты — это функции, возвращающие описание элементов"
}
```

**Основные свойства элементов:**

- **`children`** — дочерние элементы, вложенные в компонент. Может быть строкой, массивом элементов или одним элементом
- **`key`** — уникальный идентификатор для оптимизации рендеринга списков. Помогает React определить, какие элементы изменились
- **`ref`** — ссылка на реальный DOM-элемент или экземпляр компонента для прямого доступа

**Важно:** Props доступны только для чтения. Компонент не должен изменять свои props.

---

### 6️⃣ Создание реального DOM

**Определение:** React преобразует виртуальное представление в реальные DOM-элементы через библиотеку `ReactDOM`. Процесс зависит от типа элемента.

**Упрощенная логика рендеринга:**

```js
function render(element, container) {
  // Создание DOM-узла
  const dom = element.type === 'TEXT'
    ? document.createTextNode(element.props.value)
    : document.createElement(element.type);

  // Применение свойств (кроме children)
  Object.keys(element.props)
    .filter(key => key !== 'children')
    .forEach(name => {
      if (name.startsWith('on')) {
        // Обработчики событий (onClick, onChange и т.д.)
        const eventType = name.toLowerCase().substring(2);
        dom.addEventListener(eventType, element.props[name]);
      } else {
        // Обычные свойства (className, id и т.д.)
        dom[name] = element.props[name];
      }
    });

  // Рекурсивный рендеринг дочерних элементов
  if (element.props.children) {
    element.props.children.forEach(child => {
      render(child, dom);
    });
  }

  container.appendChild(dom);
}
```

**Процесс рендеринга:**

1. **Если `type` — строка** (например, `"div"`, `"button"`), создается DOM-элемент через `document.createElement(type)`
2. **Если `type` — функция-компонент**, React вызывает эту функцию и рекурсивно рендерит возвращенные элементы
3. **Применяются все свойства** — атрибуты (className, id) и обработчики событий (onClick, onChange)
4. **Рекурсивно рендерятся дочерние элементы** — для каждого child вызывается `render()`

**Важно:** React использует более сложный алгоритм с Fiber архитектурой для оптимизации рендеринга и поддержки приоритетов обновлений.

---

### 7️⃣ Процесс работы React (полный цикл)

**Определение:** От JSX-кода до отображения на экране React проходит через несколько этапов трансформации и оптимизации.

**JSX-код компонента:**

```jsx
<div className="container">
  <button>Click Me</button>
</div>
```

**Этапы работы React:**

#### 1. **Трансформация JSX**
Компилятор (Babel) преобразует JSX в вызовы функций из `react/jsx-runtime` или `React.createElement`.

**Результат:** JavaScript-код с вызовами функций создания элементов.

#### 2. **Создание виртуального представления**
Формируется структура JavaScript-объектов (React Elements), описывающая элементы.

**Результат:** Дерево объектов в памяти, представляющее структуру интерфейса.

#### 3. **Reconciliation (Согласование)**
React сравнивает новое представление с предыдущим, используя алгоритм сравнения (diffing).

**Процесс:**
- Сравниваются типы элементов
- Определяются изменения в props
- Вычисляется минимальный набор операций для обновления DOM

**Результат:** Список изменений, которые нужно применить к реальному DOM.

#### 4. **Обновление DOM (Commit Phase)**
Изменяются только те части реального DOM, которые действительно изменились.

**Оптимизации:**
- Батчинг обновлений
- Минимизация перерисовок
- Использование ключей (keys) для эффективного обновления списков

**Результат:** Обновленный реальный DOM.

#### 5. **Отображение (Paint)**
Браузер перерисовывает измененные участки интерфейса.

**Результат:** Пользователь видит обновленный интерфейс.

---

### 🎯 Ключевые моменты для собеседования

**Что нужно знать:**

1. **Компоненты — это функции**, которые принимают props и возвращают JSX (который является синтаксическим сахаром)

2. **JSX компилируется в JavaScript-объекты** (React Elements), описывающие структуру элементов

3. **Виртуальный DOM** — это легковесное представление интерфейса в памяти, не копия реального DOM

4. **Reconciliation** — процесс сравнения нового и старого виртуального DOM для определения минимальных изменений

5. **React обновляет только измененные части** реального DOM, что оптимизирует производительность

6. **Props неизменяемы** — компонент не должен изменять свои props

7. **React использует Fiber архитектуру** для инкрементального рендеринга и поддержки приоритетов обновлений

---

### 💡 Дополнительная информация

**React Fiber:**

Начиная с React 16, React использует Fiber архитектуру, которая позволяет:
- Прерывать и возобновлять работу
- Приоритизировать обновления
- Разбивать работу на части (time slicing)
- Использовать приоритеты для разных типов обновлений

**Отличие от других библиотек:**

React использует декларативный подход — вы описываете, как должен выглядеть интерфейс, а React сам решает, как эффективно обновить DOM.

**Производительность:**

- Виртуальный DOM позволяет минимизировать операции с реальным DOM
- Diffing алгоритм оптимизирует сравнение
- Батчинг обновлений уменьшает количество перерисовок
- Keys помогают эффективно обновлять списки
