## 🌳 Virtual DOM в React

**Виртуальный DOM (VDOM)** — это легковесная копия реального DOM, представленная в виде JavaScript-объектов.

React использует его для **оптимизации обновлений интерфейса**.

---

### 🔄 Как это работает:

1. При изменении состояния (`state`) React создаёт **новый виртуальный DOM** (обновленное дерево компонентов).

2. Затем происходит **сравнение (diffing)** нового VDOM с предыдущим.

3. React вычисляет **минимальный набор изменений (diff)** и применяет их к реальному DOM (этот процесс называется **"commit phase"**).

---

### 🎯 Зачем это нужно?

- ✅ **Прямые манипуляции с DOM медленные**, а VDOM позволяет минимизировать количество операций
- ✅ React **группирует изменения** и обновляет DOM пакетно (batching), что улучшает производительность

**Аналогия:** Если с одного места на другое переносить яблоки по одному — будет долго. Легче взять пакет, собрать все и принести.

---

### 🔍 React Reconciliation

**React Reconciliation** — это процесс сравнения старого и нового Virtual DOM, который позволяет React минимизировать изменения в реальном DOM для повышения производительности.

**Принципы:**
- Обновление реального DOM дорого по ресурсам
- React сначала создаёт виртуальное представление (Virtual DOM), а потом эффективно обновляет только те части реального DOM, которые действительно изменились
- React использует эвристики, чтобы сделать сравнение максимально быстрым (например, ключи `key` для списков)

---

### 📊 Логика сравнения:

**Если компонент одного типа** → React обновит только его пропсы.

**Если типы отличаются** (`<div />` на `<span />`) → React удалит старый элемент и создаст новый.

**Если ключи (`key`) не совпадают** → React пересоздаёт элемент.

**Если тип и ключ совпали** → React обновляет только изменившиеся пропсы и вложенные узлы.

---

### ⚠️ Важно понимать:

**Virtual DOM сравнивает тип элемента, ключи и пропсы.**

- Если тип разный (`<div />` на `<span />`) → замена целиком
- Если ключи не совпадают → пересоздаёт элемент
- Если тип и ключ совпали → обновляет только изменившиеся пропсы и вложенные узлы

**React не сравнивает весь DOM глубоко рекурсивно** на уровне HTML-структуры — сравнение идёт по дереву элементов, а не по строкам HTML.
