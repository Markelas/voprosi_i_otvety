## 📦 Что такое `useState`

**`useState`** хранит состояние в функциональном компоненте.

Для изменения состояния используется функция, которую он возвращает.

---

### 💡 Базовое использование:

```js
const [count, setCount] = useState(0);
```

---

### 🔄 Функциональное обновление:

Если новое значение **зависит от старого**, нужно передавать колбэк с `prev`, чтобы избежать проблем с устаревшими значениями.

```js
// ✅ Правильно — функциональное обновление
setUser(prev => ({ ...prev, name: "Bob" }));

// Это актуальное значение состояния на момент обновления
```

**Почему это важно?** Если обновление зависит от предыдущего значения, при быстрых множественных вызовах может использоваться устаревшее значение.

---

### 🎯 Ленивая инициализация:

Инициализация может быть **ленивой**, если передать функцию:

```js
// Тяжёлые вычисления выполнятся только один раз при монтировании
const [data, setData] = useState(() => {
  return expensiveCalculation();
});
```

---

### 📊 Структура:

| Часть | Описание |
|-------|----------|
| **`state`** | Текущее значение состояния |
| **`setState`** | Функция для обновления |
| **Инициализатор** | Начальное значение или функция |

---

### ⚠️ Важно помнить:

- Не вызывай `setState` напрямую в теле компонента (вызовет бесконечный цикл)
- Обновления состояния асинхронные
- Для объектов/массивов используй функциональное обновление или создавай новый объект
