## 🔄 Методы жизненного цикла в React

В React-компонентах есть **методы жизненного цикла**, которые позволяют выполнять различные действия на каждом этапе жизни компонента: при **монтировании**, **обновлении** и **размонтировании**. Классовые и функциональные компоненты различаются тем, как они обрабатывают эти этапы.

---

## 📋 Этапы жизненного цикла компонента

![[Pasted image 20251129162905.png]]

1. <font color="#76923c">**Инициализация (Initialization)**</font> - Этап, когда компонент только создаётся и настраивается: Классовые: `constructor` или инициализация `state`, Функциональные: `useState` с начальным значением


2. <font color="#0070c0">**Монтирование (Mounting)**</font> - Компонент впервые добавляется в DOM


3. <font color="#d83931">**Обновление (Updating)**</font>- Компонент обновляется из-за изменений в `props` или `state`

4. <font color="#7030a0">**Размонтирование (Unmounting)**</font> - Компонент удаляется из DOM

5. <font color="#974806">**Обработка ошибок (Error Handling)**</font> - В компоненте возникает ошибка

---

## 🏗️ Методы жизненного цикла в классовых компонентах

### 📝 Список методов:

#### **Монтирование:**
- **`constructor(props)`** — инициализация состояния, биндинг методов
- **`static getDerivedStateFromProps(props, state)`** — синхронизация состояния с пропсами (вызывается перед каждым рендером)
- **`render()`** — возвращает JSX
- **`componentDidMount()`** — вызывается сразу после монтирования компонента в DOM

#### **Обновление:**
- **`static getDerivedStateFromProps(props, state)`** — вызывается перед каждым рендером
- **`shouldComponentUpdate(nextProps, nextState)`** — используется для оптимизации рендеринга (возвращает `true`/`false`)
- **`render()`** — перерисовка компонента
- **`getSnapshotBeforeUpdate(prevProps, prevState)`** — вызывается перед обновлением DOM, чтобы сохранить снимок (например, положение прокрутки)
- **`componentDidUpdate(prevProps, prevState, snapshot)`** — вызывается сразу после обновления компонента

#### **Размонтирование:**
- **`componentWillUnmount()`** — вызывается перед тем, как компонент будет удалён из DOM

#### **Обработка ошибок:**
- **`static getDerivedStateFromError(error)`** — обновляет состояние при ошибке
- **`componentDidCatch(error, errorInfo)`** — логирует информацию об ошибке

---

## 💻 Пример: Классовый компонент

```js
import React, { Component } from 'react';

class LifecycleComponent extends Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
    console.log('constructor: Инициализация состояния');
  }

  // Вызывается сразу после монтирования компонента
  componentDidMount() {
    console.log('componentDidMount: Компонент смонтирован');
    document.title = `Count: ${this.state.count}`;
    // Здесь обычно: запросы к API, подписки, работа с DOM
  }

  // Вызывается каждый раз, когда компонент получает новые пропсы или состояние
  componentDidUpdate(prevProps, prevState) {
    console.log('componentDidUpdate: Компонент обновился');
    if (prevState.count !== this.state.count) {
      document.title = `Count: ${this.state.count}`;
    }
  }

  // Вызывается перед тем, как компонент будет удалён
  componentWillUnmount() {
    console.log('componentWillUnmount: Компонент будет удалён');
    // Очистка таймеров, подписок или событий
  }

  handleClick = () => {
    this.setState({ count: this.state.count + 1 });
  };

  render() {
    console.log('render: Отрисовка компонента');
    return (
      <div>
        <h1>Счётчик: {this.state.count}</h1>
        <button onClick={this.handleClick}>Увеличить</button>
      </div>
    );
  }
}

export default LifecycleComponent;
```

### 🔍 Разбор примера:

1. **`constructor`** — инициализирует состояние `count: 0`
2. **`componentDidMount`** — вызывается после первого рендеринга, устанавливает заголовок страницы
3. **`componentDidUpdate`** — вызывается при каждом обновлении состояния, проверяет изменение `count` и обновляет заголовок
4. **`componentWillUnmount`** — вызывается перед удалением компонента, используется для очистки таймеров, подписок и др.

---

## ⚛️ Методы жизненного цикла в функциональных компонентах

В функциональных компонентах нет встроенных методов жизненного цикла. Вместо этого используются **хуки**:

- **`useEffect`** — основной хук для выполнения побочных эффектов и управления жизненным циклом
- **`useLayoutEffect`** — аналог `componentDidMount` и `componentDidUpdate`, но вызывается **синхронно** перед отрисовкой на экране
- **`useState`** — для управления состоянием (аналог `this.state`)
- **`useRef`** — для сохранения значений между рендерами

---

## 💻 Пример: Функциональный компонент

```js
import React, { useState, useEffect } from 'react';

function FunctionalComponent() {
  const [count, setCount] = useState(0);

  // Эквивалент componentDidMount и componentDidUpdate
  useEffect(() => {
    console.log('useEffect: Компонент обновился или смонтировался');
    document.title = `Count: ${count}`;

    // Возвращаем функцию очистки — аналог componentWillUnmount
    return () => {
      console.log('Очистка перед размонтированием или обновлением');
    };
  }, [count]); // Этот эффект срабатывает при изменении count

  return (
    <div>
      <h1>Счётчик: {count}</h1>
      <button onClick={() => setCount(count + 1)}>Увеличить</button>
    </div>
  );
}

export default FunctionalComponent;
```

### 🔍 Разбор примера:

**`useEffect`**:
- Вызывается при каждом изменении состояния `count` или при монтировании компонента
- Устанавливает заголовок страницы аналогично `componentDidMount` и `componentDidUpdate`
- Возвращает функцию очистки — аналог `componentWillUnmount`, которая вызывается перед размонтированием или при изменении `count`

**Обработка изменений**:
- Второй аргумент `useEffect` — массив зависимостей `[count]` — определяет, когда эффект должен запускаться
- Если передать пустой массив `[]`, эффект сработает только при монтировании компонента
- Если не передать массив вообще, эффект будет выполняться после каждого рендера

---

## 📊 Сравнение методов жизненного цикла

| Классовый метод | Функциональный хук | Описание |
|----------------|-------------------|----------|
| `constructor` | `useState(initialValue)` | Инициализация состояния |
| `componentDidMount` | `useEffect(() => {}, [])` | Выполняется после первого рендера компонента |
| `componentDidUpdate` | `useEffect(() => {}, [dep])` | Выполняется после каждого обновления компонента (при изменении `dep`) |
| `componentWillUnmount` | `useEffect(() => { return () => {} }, [])` | Выполняется перед удалением компонента из DOM |
| `shouldComponentUpdate` | `React.memo` | Позволяет оптимизировать рендеринг компонентов |
| `getDerivedStateFromProps` | `useState` + `useEffect` | Синхронизация состояния с пропсами |
| `getSnapshotBeforeUpdate` | `useLayoutEffect` | Выполняется перед тем, как изменения будут отрисованы (синхронно) |
| `componentDidCatch` | `ErrorBoundary` (классовый компонент) | Обработка ошибок в дочерних компонентах |

---

## 🎯 Варианты использования `useEffect`

### 1. **Только при монтировании** (аналог `componentDidMount`):

```js
useEffect(() => {
  // Выполнится только один раз при монтировании
  fetchData();
  const subscription = subscribe();
  
  return () => {
    // Очистка при размонтировании
    subscription.unsubscribe();
  };
}, []); // Пустой массив зависимостей
```

### 2. **При изменении зависимостей** (аналог `componentDidUpdate`):

```js
useEffect(() => {
  // Выполнится при изменении userId
  fetchUserData(userId);
}, [userId]); // Зависимость от userId
```

### 3. **При каждом рендере** (не рекомендуется):

```js
useEffect(() => {
  // Выполнится после каждого рендера
  console.log('Компонент обновился');
}); // Без массива зависимостей
```

### 4. **Очистка** (аналог `componentWillUnmount`):

```js
useEffect(() => {
  const timer = setInterval(() => {
    console.log('Тик');
  }, 1000);
  
  return () => {
    // Очистка таймера при размонтировании или изменении зависимостей
    clearInterval(timer);
  };
}, []);
```

---

## 🔧 Дополнительные методы классовых компонентов

### **`shouldComponentUpdate(nextProps, nextState)`**

Используется для оптимизации — предотвращает ненужные рендеры:

```js
shouldComponentUpdate(nextProps, nextState) {
  // Возвращаем true, если нужно обновить компонент
  // Возвращаем false, если обновление не требуется
  return this.state.count !== nextState.count;
}
```

**В функциональных компонентах:** `React.memo` с функцией сравнения:

```js
const MyComponent = React.memo(({ count }) => {
  return <div>{count}</div>;
}, (prevProps, nextProps) => {
  return prevProps.count === nextProps.count;
});
```

### **`getDerivedStateFromProps(props, state)`**

Синхронизирует состояние с пропсами (статический метод):

```js
static getDerivedStateFromProps(props, state) {
  if (props.count !== state.count) {
    return { count: props.count };
  }
  return null; // Не обновлять состояние
}
```

**В функциональных компонентах:** `useState` + `useEffect`:

```js
const [count, setCount] = useState(props.count);

useEffect(() => {
  setCount(props.count);
}, [props.count]);
```

### **`getSnapshotBeforeUpdate(prevProps, prevState)`**

Сохраняет информацию о DOM перед обновлением (например, положение прокрутки):

```js
getSnapshotBeforeUpdate(prevProps, prevState) {
  if (prevProps.list.length < this.props.list.length) {
    const list = this.listRef.current;
    return list.scrollHeight - list.scrollTop;
  }
  return null;
}

componentDidUpdate(prevProps, prevState, snapshot) {
  if (snapshot !== null) {
    const list = this.listRef.current;
    list.scrollTop = list.scrollHeight - snapshot;
  }
}
```

**В функциональных компонентах:** `useLayoutEffect`:

```js
useLayoutEffect(() => {
  // Выполняется синхронно перед отрисовкой
  const list = listRef.current;
  list.scrollTop = list.scrollHeight - snapshot;
}, [snapshot]);
```

---

## ⚠️ Важные моменты для собеседования

### <span style="background:rgba(74, 82, 199, 0.2)">**1. Порядок выполнения методов:**</span>

**При монтировании:**
1. `constructor`
2. `getDerivedStateFromProps`
3. `render`
4. `componentDidMount`

**При обновлении:**
1. `getDerivedStateFromProps`
2. `shouldComponentUpdate` (если вернёт `true`)
3. `render`
4. `getSnapshotBeforeUpdate`
5. `componentDidUpdate`

**При размонтировании:**
1. `componentWillUnmount`

### <span style="background:rgba(74, 82, 199, 0.2)">**2. Разница между `useEffect` и `useLayoutEffect`:**</span>

- **`useEffect`** — выполняется **асинхронно** после отрисовки DOM
- **`useLayoutEffect`** — выполняется **синхронно** перед отрисовкой DOM (может блокировать браузер)

### <span style="background:rgba(74, 82, 199, 0.2)">**3. Очистка в `useEffect`:**</span>

Функция очистки вызывается:
- Перед размонтированием компонента
- Перед повторным выполнением эффекта (если изменились зависимости)

### <span style="background:rgba(74, 82, 199, 0.2)">**4. Устаревшие методы (deprecated):**</span>

Следующие методы **устарели** и не должны использоваться:
- `componentWillMount`
- `componentWillReceiveProps`
- `componentWillUpdate`

### <span style="background:rgba(74, 82, 199, 0.2)">**5. Обработка ошибок:**</span>

В классовых компонентах:
- `componentDidCatch` — для логирования ошибок
- `getDerivedStateFromError` — для обновления состояния при ошибке

В функциональных компонентах:
- Обработка ошибок всё ещё требует классового компонента (Error Boundary)

---

## 💡 Лучшие практики

### **Минимизация логики в хуках:**
- Старайтесь минимизировать сложную логику в хуках, чтобы код был проще для понимания

### **Очистка в `useEffect`:**
- Не забывайте о функции очистки в `useEffect`. Это особенно важно для предотвращения утечек памяти (таймеры, подписки, слушатели событий)

### **Использование нескольких хуков:**
- Разделяйте логику на несколько хуков `useEffect` для управления различными эффектами, чтобы не смешивать действия в одном месте

### **Оптимизация с `React.memo`:**
- Для компонентов, которые часто обновляются, используйте `React.memo` и `useCallback` для предотвращения избыточных ререндеров

---

## 🎓 Итог

**Классовые компоненты:**
- Имеют встроенные методы жизненного цикла
- Более verbose синтаксис
- Используются в legacy коде

**Функциональные компоненты:**
- Используют хуки для управления жизненным циклом
- Более гибкий и лаконичный подход
- **Рекомендуемый современный подход в React**

Сравнение классовых и функциональных компонентов позволяет увидеть, что функциональные компоненты с хуками — более гибкий и лаконичный подход к управлению состоянием и жизненным циклом в React.





