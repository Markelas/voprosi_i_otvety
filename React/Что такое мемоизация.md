## 🧠 Что такое мемоизация и зачем она нужна в React

**Мемоизация** — это сохранение результата функции для предотвращения повторных вычислений с теми же аргументами.

---

### 🎯 Инструменты мемоизации в React:

#### 1. **`useMemo`**
Используется для **кэширования вычислений**:

```js
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(a, b);
}, [a, b]);
```

#### 2. **`useCallback`**
Используется для **кэширования функций**:

```js
const memoizedFn = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

#### 3. **`React.memo`**
Используется для **мемоизации компонента**:

```js
const MemoizedComponent = React.memo(MyComponent);
```

---

### ⚠️ Осторожно:

- Если зависимости указаны неправильно — можно получить **устаревшие данные**
- Мемоизация сама **потребляет ресурсы**
- При неправильных зависимостях или частых пересозданиях объектов / функций **эффекта нет**

---

### ✅ Когда применять:

- ✅ Рендер дорогой
- ✅ Функции / значения передаются в мемоизированные дочерние компоненты
- ✅ Тяжёлые вычисления (фильтрация, сортировка больших массивов)

---

### ❌ Когда НЕ применять:

- ❌ Повсеместно (мемоизация сама потребляет ресурсы)
- ❌ Для простых вычислений
- ❌ Если зависимости меняются слишком часто

---

### 💡 Правило:

**Применяй только там, где рендер дорогой или функции / значения передаются в мемоизированные дочерние компоненты.**
