## ⚡ Способы оптимизации рендеров в React

---

## 🔄 Из-за чего происходит рендер в React?

React компонент перерендеривается (re-render) в следующих случаях:

### 1. **Изменение State компонента**

Когда вызывается функция обновления состояния (`setState`, `useState`, `useReducer`), компонент перерендеривается.

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  // Каждый вызов setCount вызывает ререндер
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

### 2. **Изменение Props**

Если родительский компонент передает новые props (даже если значение не изменилось, но ссылка изменилась), дочерний компонент перерендерится.

```jsx
function Parent() {
  const [value, setValue] = useState(0);
  
  // ❌ Плохо — новый объект при каждом рендере
  return <Child config={{ theme: 'dark' }} />;
  
  // ✅ Хорошо — стабильная ссылка
  const config = useMemo(() => ({ theme: 'dark' }), []);
  return <Child config={config} />;
}
```

### 3. **Изменение Context**

Если компонент использует `useContext` и значение контекста изменилось, все потребители этого контекста перерендерятся.

```jsx
const ThemeContext = createContext();

function App() {
  const [theme, setTheme] = useState('light');
  
  // Все компоненты, использующие ThemeContext, перерендерятся при изменении theme
  return (
    <ThemeContext.Provider value={theme}>
      <ThemedComponent />
    </ThemeContext.Provider>
  );
}
```

### 4. **Рендер родительского компонента**

По умолчанию, если родительский компонент перерендерился, все его дочерние компоненты также перерендерятся, даже если их props не изменились.

```jsx
function Parent() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>Count: {count}</button>
      {/* Child перерендерится даже если его props не изменились */}
      <Child name="John" />
    </div>
  );
}
```

### 5. **Принудительный рендер**

Вызов `forceUpdate()` в классовых компонентах или использование паттернов для принудительного обновления.

---

## 🎯 Когда React решает перерендерить компонент?

React использует **поверхностное сравнение (shallow comparison)** для определения, нужно ли перерендерить компонент:

1. **Для props:** React сравнивает ссылки на объекты/массивы, а не их содержимое
2. **Для state:** Аналогично — сравниваются ссылки
3. **Для Context:** Сравнивается значение контекста

```jsx
// ❌ Эти объекты считаются разными (разные ссылки)
const obj1 = { name: 'John' };
const obj2 = { name: 'John' };
console.log(obj1 === obj2); // false

// ✅ Одинаковые ссылки
const obj3 = { name: 'John' };
const obj4 = obj3;
console.log(obj3 === obj4); // true
```

---

## ⚡ Способы оптимизации рендеров

### 🎯 Основные методы:

#### 1. **`React.memo`**

Предотвращает повторный рендер функционального компонента, если `props` не изменились (поверхностное сравнение).

```jsx
// Без React.memo — перерендерится при каждом рендере родителя
function ExpensiveComponent({ data }) {
  // Тяжелые вычисления
  return <div>{data}</div>;
}

// С React.memo — перерендерится только если data изменилась
const MemoizedComponent = React.memo(function ExpensiveComponent({ data }) {
  return <div>{data}</div>;
});

// Кастомное сравнение
const MemoizedComponent = React.memo(
  ExpensiveComponent,
  (prevProps, nextProps) => prevProps.data.id === nextProps.data.id
);
```

**Когда использовать:**
- ✅ Компонент часто перерендеривается из-за родителя
- ✅ Компонент выполняет тяжелые вычисления
- ✅ Props редко меняются

**Когда НЕ использовать:**
- ❌ Props всегда новые (объекты/функции без мемоизации)
- ❌ Очень простые компоненты (накладные расходы больше выгоды)

---

#### 2. **`useMemo`**

Мемоизация вычисленных значений. Пересчитывает значение только при изменении зависимостей.

```jsx
function ExpensiveComponent({ items, filter }) {
  // ❌ Плохо — пересчитывается при каждом рендере
  const filteredItems = items.filter(item => item.category === filter);
  
  // ✅ Хорошо — пересчитывается только при изменении items или filter
  const filteredItems = useMemo(() => {
    return items.filter(item => item.category === filter);
  }, [items, filter]);
  
  return <div>{filteredItems.map(...)}</div>;
}
```

**Когда использовать:**
- ✅ Тяжелые вычисления (фильтрация, сортировка больших массивов)
- ✅ Создание объектов/массивов для props мемоизированных компонентов
- ✅ Дорогие математические операции

**Когда НЕ использовать:**
- ❌ Простые вычисления (накладные расходы больше выгоды)
- ❌ Примитивные значения

---

#### 3. **`useCallback`**

Мемоизация функций, чтобы они не пересоздавались при каждом рендере.

```jsx
function Parent() {
  const [count, setCount] = useState(0);
  const [name, setName] = useState('');
  
  // ❌ Плохо — новая функция при каждом рендере
  const handleClick = () => {
    console.log('clicked');
  };
  
  // ✅ Хорошо — функция пересоздается только при изменении зависимостей
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []); // Пустой массив = функция создается один раз
  
  // С зависимостями
  const handleSubmit = useCallback((data) => {
    console.log(name, data);
  }, [name]); // Пересоздается при изменении name
  
  return (
    <div>
      <button onClick={() => setCount(count + 1)}>Count: {count}</button>
      <MemoizedChild onClick={handleClick} />
    </div>
  );
}

const MemoizedChild = React.memo(({ onClick }) => {
  return <button onClick={onClick}>Click me</button>;
});
```

**Когда использовать:**
- ✅ Функция передается в компонент, обернутый в `React.memo`
- ✅ Функция используется как зависимость в `useEffect`, `useMemo`
- ✅ Функция передается в кастомный хук

**Когда НЕ использовать:**
- ❌ Функция не передается в дочерние компоненты
- ❌ Функция не используется как зависимость

---

#### 4. **Разделение кода (Code Splitting)**

Динамический импорт компонентов для уменьшения начального размера бандла.

```jsx
// Обычный импорт — компонент загружается сразу
import HeavyComponent from './HeavyComponent';

// Динамический импорт — компонент загружается по требованию
const HeavyComponent = React.lazy(() => import('./HeavyComponent'));

function App() {
  return (
    <Suspense fallback={<div>Загрузка...</div>}>
      <HeavyComponent />
    </Suspense>
  );
}
```

**Преимущества:**
- ✅ Уменьшение начального размера бандла
- ✅ Ускорение первой загрузки страницы
- ✅ Загрузка компонентов по требованию

---

#### 5. **Ленивая загрузка компонентов (`React.lazy`)**

Компонент загружается только когда он действительно нужен.

```jsx
import { Suspense, lazy } from 'react';

const LazyComponent = lazy(() => import('./LazyComponent'));
const AnotherLazy = lazy(() => import('./AnotherLazy'));

function App() {
  const [show, setShow] = useState(false);
  
  return (
    <div>
      <button onClick={() => setShow(true)}>Показать компонент</button>
      {show && (
        <Suspense fallback={<div>Загрузка...</div>}>
          <LazyComponent />
        </Suspense>
      )}
    </div>
  );
}
```

**Важно:**
- Всегда оборачивай `lazy` компоненты в `<Suspense>`
- `fallback` отображается во время загрузки
- Можно использовать для маршрутов (React Router)

---

#### 6. **Оптимизация списков с помощью `key`**

Всегда используй уникальные стабильные `key`, не индексы.

```jsx
// ❌ Плохо — индекс может измениться при изменении порядка
{items.map((item, index) => (
  <Item key={index} data={item} />
))}

// ✅ Хорошо — стабильный уникальный идентификатор
{items.map(item => (
  <Item key={item.id} data={item} />
))}

// ✅ Если нет id, можно использовать комбинацию
{items.map(item => (
  <Item key={`${item.name}-${item.category}`} data={item} />
))}
```

**Почему это важно:**
- React использует `key` для определения, какой элемент изменился
- Неправильный `key` может привести к лишним ререндерам и потере состояния

---

#### 7. **Избегать анонимных функций и объектов в JSX**

Если это критично для производительности:

```jsx
function Parent() {
  const [count, setCount] = useState(0);
  
  // ❌ Плохо — создаётся новая функция при каждом рендере
  return <Child onClick={() => handleClick()} />;
  
  // ❌ Плохо — создаётся новый объект при каждом рендере
  return <Child style={{ color: 'red' }} />;
  
  // ✅ Хорошо — мемоизированная функция
  const handleClick = useCallback(() => {
    // логика
  }, []);
  return <Child onClick={handleClick} />;
  
  // ✅ Хорошо — стабильная ссылка на объект
  const style = useMemo(() => ({ color: 'red' }), []);
  return <Child style={style} />;
  
  // ✅ Или вынести за пределы компонента (если не зависит от props/state)
  const staticStyle = { color: 'red' };
  return <Child style={staticStyle} />;
}
```

---

#### 8. **Виртуализация списков**

Для больших списков используй виртуализацию (например, `react-window`, `react-virtualized`).

```jsx
import { FixedSizeList } from 'react-window';

function VirtualizedList({ items }) {
  return (
    <FixedSizeList
      height={600}
      itemCount={items.length}
      itemSize={50}
      width="100%"
    >
      {({ index, style }) => (
        <div style={style}>
          {items[index].name}
        </div>
      )}
    </FixedSizeList>
  );
}
```

**Когда использовать:**
- ✅ Списки с большим количеством элементов (1000+)
- ✅ Элементы имеют фиксированную или вычисляемую высоту

---

#### 9. **Оптимизация Context**

Мемоизируй значение контекста, чтобы избежать лишних ререндеров.

```jsx
// ❌ Плохо — новое значение при каждом рендере
function AppProvider({ children }) {
  const [state, setState] = useState({ count: 0, name: '' });
  
  return (
    <AppContext.Provider value={{ state, setState }}>
      {children}
    </AppContext.Provider>
  );
}

// ✅ Хорошо — мемоизированное значение
function AppProvider({ children }) {
  const [state, setState] = useState({ count: 0, name: '' });
  
  const value = useMemo(
    () => ({ state, setState }),
    [state]
  );
  
  return (
    <AppContext.Provider value={value}>
      {children}
    </AppContext.Provider>
  );
}

// ✅ Еще лучше — разделить контексты
const CountContext = createContext();
const NameContext = createContext();
```

---

#### 10. **Избегать лишних обновлений State**

Не обновляй state, если значение не изменилось.

```jsx
function Component() {
  const [count, setCount] = useState(0);
  
  // ❌ Плохо — вызовет ререндер даже если count уже 5
  const handleClick = () => {
    setCount(5);
  };
  
  // ✅ Хорошо — проверить перед обновлением
  const handleClick = () => {
    setCount(prev => {
      if (prev === 5) return prev; // Не обновляем, если уже 5
      return 5;
    });
  };
  
  // ✅ Или функциональное обновление
  const increment = () => {
    setCount(prev => prev + 1);
  };
}
```

---

## ⚠️ Важные моменты и подводные камни

### 1. **Чрезмерная мемоизация может замедлить приложение**

Если пересчёт `useMemo` или `useCallback` дороже повторного рендера, мемоизация только замедлит приложение.

```jsx
// ❌ Плохо — мемоизация простого вычисления
const sum = useMemo(() => a + b, [a, b]); // Накладные расходы больше выгоды

// ✅ Хорошо — простое вычисление без мемоизации
const sum = a + b;
```

### 2. **Мемоизация не предотвращает ререндер, если изменились зависимости**

```jsx
const memoizedValue = useMemo(() => expensiveCalc(a, b), [a, b]);
// Если a или b изменились, значение пересчитается и компонент перерендерится
```

### 3. **React.memo с объектами/функциями в props**

`React.memo` использует поверхностное сравнение, поэтому новые объекты/функции всегда считаются разными.

```jsx
// ❌ React.memo не поможет — новый объект при каждом рендере
function Parent() {
  return <MemoizedChild config={{ theme: 'dark' }} />;
}

// ✅ Нужно мемоизировать объект
function Parent() {
  const config = useMemo(() => ({ theme: 'dark' }), []);
  return <MemoizedChild config={config} />;
}
```

### 4. **Пустой массив зависимостей в useCallback/useMemo**

Пустой массив `[]` означает, что значение/функция создается один раз и никогда не обновляется.

```jsx
// ⚠️ Осторожно — функция не обновляется при изменении count
const handleClick = useCallback(() => {
  console.log(count); // Всегда будет начальное значение count
}, []); // Пустой массив

// ✅ Правильно — добавить count в зависимости
const handleClick = useCallback(() => {
  console.log(count);
}, [count]);
```

### 5. **useMemo не гарантирует, что функция не выполнится**

`useMemo` может выполнить функцию для проверки, нужно ли пересчитывать значение. Не используй `useMemo` для побочных эффектов.

```jsx
// ❌ Плохо — побочный эффект в useMemo
const data = useMemo(() => {
  fetch('/api/data'); // Может выполниться несколько раз
  return someValue;
}, [deps]);

// ✅ Хорошо — используй useEffect для побочных эффектов
useEffect(() => {
  fetch('/api/data');
}, [deps]);
```

### 6. **Оптимизация должна быть измерена**

Всегда измеряй производительность до и после оптимизации. Используй React DevTools Profiler.

---

## 🎯 Когда использовать оптимизацию

Используй оптимизацию только там, где это действительно нужно:

- ✅ **Тяжёлые вычисления** — фильтрация, сортировка больших массивов
- ✅ **Частые ререндеры родителя** — когда родитель часто обновляется, а дочерний компонент не должен
- ✅ **Компоненты, которые передаются в `React.memo`** — нужно мемоизировать props
- ✅ **Большие списки** — виртуализация для списков с 1000+ элементов
- ✅ **Context с часто меняющимися данными** — разделение контекстов или мемоизация

**Не оптимизируй:**
- ❌ Простые компоненты без тяжелых вычислений
- ❌ Компоненты, которые редко рендерятся
- ❌ Преждевременная оптимизация без измерения

---

## 💡 Важная информация для собеседования

### Вопрос: "Что вызывает ререндер компонента в React?"

**Ответ:**
1. Изменение state компонента (через `useState`, `useReducer`, `setState`)
2. Изменение props (даже если значение то же, но ссылка другая)
3. Изменение значения Context (если компонент использует `useContext`)
4. Рендер родительского компонента (по умолчанию все дети перерендерятся)
5. Принудительный ререндер (`forceUpdate` в классовых компонентах)

---

### Вопрос: "В чем разница между `useMemo` и `useCallback`?"

**Ответ:**
- **`useMemo`** — мемоизирует **результат вычисления** (значение)
- **`useCallback`** — мемоизирует **саму функцию**

```jsx
// useMemo возвращает значение
const value = useMemo(() => expensiveCalc(a, b), [a, b]);

// useCallback возвращает функцию
const callback = useCallback(() => doSomething(a, b), [a, b]);
```

**Когда использовать:**
- `useMemo` — для тяжелых вычислений или создания объектов/массивов для props
- `useCallback` — для функций, передаваемых в мемоизированные компоненты или используемых как зависимости

---

### Вопрос: "Почему `React.memo` не работает с объектами в props?"

**Ответ:**
`React.memo` использует **поверхностное сравнение** (shallow comparison) — сравнивает ссылки, а не содержимое. Новый объект при каждом рендере всегда считается другим, даже если содержимое одинаковое.

```jsx
// ❌ Не работает — новый объект каждый раз
<MemoizedChild config={{ theme: 'dark' }} />

// ✅ Работает — мемоизированный объект
const config = useMemo(() => ({ theme: 'dark' }), []);
<MemoizedChild config={config} />
```

---

### Вопрос: "Как оптимизировать компонент, который часто перерендеривается?"

**Ответ:**
1. Обернуть в `React.memo` (если props не меняются)
2. Мемоизировать props (объекты/функции через `useMemo`/`useCallback`)
3. Разделить компонент на более мелкие части
4. Использовать `useMemo` для тяжелых вычислений
5. Оптимизировать Context (мемоизация или разделение)
6. Проверить, действительно ли нужны все ререндеры

---

### Вопрос: "Что такое поверхностное сравнение (shallow comparison)?"

**Ответ:**
Поверхностное сравнение проверяет, равны ли примитивные значения или ссылки на объекты, но не сравнивает содержимое объектов/массивов.

```jsx
// Поверхностное сравнение
const obj1 = { a: 1 };
const obj2 = { a: 1 };
console.log(obj1 === obj2); // false (разные ссылки)

const obj3 = obj1;
console.log(obj1 === obj3); // true (одинаковые ссылки)
```

React использует поверхностное сравнение для:
- Сравнения props в `React.memo`
- Сравнения зависимостей в хуках
- Определения, нужно ли обновлять компонент

---

### Вопрос: "Когда НЕ нужно использовать мемоизацию?"

**Ответ:**
- Простые вычисления (сложение, конкатенация строк)
- Компоненты, которые редко рендерятся
- Когда накладные расходы мемоизации больше выгоды
- Преждевременная оптимизация без измерения производительности

**Правило:** Сначала измерь производительность, потом оптимизируй.

---

### Вопрос: "Как работает оптимизация списков с `key`?"

**Ответ:**
React использует `key` для идентификации элементов в списке. При изменении списка React сравнивает элементы по `key` и определяет, какие элементы добавились, удалились или изменились.

**Проблемы с индексом как key:**
```jsx
// ❌ Плохо — при удалении первого элемента все индексы сдвигаются
{items.map((item, index) => <Item key={index} />)}
```

**Правильно:**
```jsx
// ✅ Хорошо — стабильный уникальный идентификатор
{items.map(item => <Item key={item.id} />)}
```

---

### Вопрос: "Как оптимизировать Context, чтобы избежать лишних ререндеров?"

**Ответ:**
1. **Мемоизировать значение контекста:**
```jsx
const value = useMemo(() => ({ state, setState }), [state]);
```

2. **Разделить контексты** (разные контексты для разных данных):
```jsx
const CountContext = createContext();
const NameContext = createContext();
```

3. **Использовать селекторы** (как в Redux) — компонент подписывается только на нужные данные

4. **Не использовать Context для часто меняющихся данных** — лучше использовать state management библиотеки (Redux, Zustand)

---

## 🎯 Итоговая памятка

**Причины ререндеров:**
1. Изменение state
2. Изменение props (даже если ссылка другая)
3. Изменение Context
4. Рендер родителя
5. Принудительный ререндер

**Способы оптимизации:**
1. `React.memo` — мемоизация компонентов
2. `useMemo` — мемоизация значений
3. `useCallback` — мемоизация функций
4. Code splitting — разделение кода
5. Виртуализация — для больших списков
6. Оптимизация Context — мемоизация или разделение
7. Правильные `key` в списках
8. Избегать новых объектов/функций в JSX

**Важно помнить:**
- ✅ Измеряй производительность до и после оптимизации
- ✅ Не оптимизируй преждевременно
- ✅ Мемоизация имеет накладные расходы
- ✅ React.memo использует поверхностное сравнение
- ✅ Context вызывает ререндер всех потребителей при изменении
