## 🎯 События в React

**События в React** работают через систему **SyntheticEvent** — обертку над нативными событиями браузера, которая обеспечивает единообразное поведение во всех браузерах.

---

### 🎯 Определение

**SyntheticEvent** — это кроссбраузерная обертка над нативными событиями DOM. React нормализует события так, чтобы они имели одинаковые свойства во всех браузерах.

**Основные характеристики:**

- ✅ **Единообразный API** — одинаковое поведение во всех браузерах
- ✅ **Делегирование событий** — React использует event delegation на корневом элементе
- ✅ **Автоматическая очистка** — React автоматически удаляет обработчики при размонтировании
- ✅ **Нормализация** — все события имеют одинаковую структуру независимо от браузера

---

### 📊 Основные типы событий

| Категория | События | Описание |
|-----------|---------|----------|
| **Клики** | `onClick`, `onDoubleClick` | Одиночный и двойной клик |
| **Клавиши** | `onKeyDown`, `onKeyUp`, `onKeyPress` | Нажатие и отпускание клавиш |
| **Мышь** | `onMouseOver`, `onMouseOut`, `onMouseMove`, `onMouseDown`, `onMouseUp` | Действия с мышью |
| **Фокусировка** | `onFocus`, `onBlur` | Получение и потеря фокуса |
| **Формы** | `onChange`, `onInput`, `onSubmit` | Изменение и отправка формы |
| **Перетаскивание** | `onDrag`, `onDragStart`, `onDragEnd`, `onDrop` | Drag and Drop |
| **Скролл** | `onScroll` | Прокрутка элемента |
| **Выделение** | `onSelect` | Выделение текста |

**Важно:** В React все обработчики событий пишутся в **camelCase** с префиксом `on` (например, `onClick`, а не `onclick`).

---

### 🔧 Базовое использование

#### 1. **Обработчик события в функциональном компоненте**

```jsx
function Button() {
  const handleClick = () => {
    console.log('Кнопка нажата!');
  };

  return <button onClick={handleClick}>Нажми меня</button>;
}
```

#### 2. **Инлайн обработчик**

```jsx
function Button() {
  return (
    <button onClick={() => console.log('Кнопка нажата!')}>
      Нажми меня
    </button>
  );
}
```

#### 3. **Передача параметров в обработчик**

```jsx
function Button() {
  const handleClick = (id, name) => {
    console.log(`ID: ${id}, Name: ${name}`);
  };

  // Способ 1: Инлайн функция
  return (
    <button onClick={() => handleClick(1, 'John')}>
      Нажми
    </button>
  );

  // Способ 2: bind
  return (
    <button onClick={handleClick.bind(null, 1, 'John')}>
      Нажми
    </button>
  );
}
```

---

### 🎨 SyntheticEvent

**SyntheticEvent** — это обертка над нативным событием, которая предоставляет единообразный API.

**Основные свойства:**

| Свойство | Описание |
|----------|----------|
| **`event.target`** | Элемент, на котором произошло событие |
| **`event.currentTarget`** | Элемент, на котором установлен обработчик |
| **`event.type`** | Тип события (`'click'`, `'keydown'` и т.д.) |
| **`event.preventDefault()`** | Отменяет стандартное поведение браузера |
| **`event.stopPropagation()`** | Останавливает всплытие события |
| **`event.nativeEvent`** | Доступ к нативному событию браузера |

**Пример:**

```jsx
function Button() {
  const handleClick = (e) => {
    console.log(e.target);           // Элемент, на который кликнули
    console.log(e.currentTarget);    // Элемент с обработчиком
    console.log(e.type);             // 'click'
    e.preventDefault();               // Отменить дефолтное поведение
    e.stopPropagation();             // Остановить всплытие
  };

  return <button onClick={handleClick}>Нажми</button>;
}
```

**Важно:** В React 17+ события больше не пулятся (event pooling удален), но `SyntheticEvent` все еще очищается после обработчика для оптимизации памяти.

---

### 🔄 Отличия от нативных событий DOM

| Аспект | Нативные события | React события |
|--------|------------------|---------------|
| **Именование** | `onclick` (lowercase) | `onClick` (camelCase) |
| **Обработчик** | Строка или функция | Только функция |
| **Делегирование** | Вручную | Автоматически |
| **Очистка** | Вручную через `removeEventListener` | Автоматически при размонтировании |
| **API** | Разный в разных браузерах | Единообразный через SyntheticEvent |
| **Event pooling** | Нет | Удален в React 17+ |

**Пример различий:**

```jsx
// ❌ НЕПРАВИЛЬНО — строка как обработчик
<button onclick="handleClick()">Click</button>

// ✅ ПРАВИЛЬНО — функция как обработчик
<button onClick={handleClick}>Click</button>

// ❌ НЕПРАВИЛЬНО — lowercase
<button onclick={handleClick}>Click</button>

// ✅ ПРАВИЛЬНО — camelCase
<button onClick={handleClick}>Click</button>
```

---

### 🛑 Предотвращение дефолтного поведения

**`preventDefault()`** — отменяет стандартное поведение браузера (например, переход по ссылке, отправка формы).

```jsx
function Link() {
  const handleClick = (e) => {
    e.preventDefault();  // Отменить переход по ссылке
    console.log('Ссылка кликнута, но переход отменен');
  };

  return (
    <a href="https://example.com" onClick={handleClick}>
      Кликни меня
    </a>
  );
}

function Form() {
  const handleSubmit = (e) => {
    e.preventDefault();  // Отменить отправку формы
    console.log('Форма не отправлена');
  };

  return (
    <form onSubmit={handleSubmit}>
      <input type="text" />
      <button type="submit">Отправить</button>
    </form>
  );
}
```

---

### 🛑 Остановка всплытия события

**`stopPropagation()`** — останавливает всплытие события к родительским элементам.

```jsx
function Parent() {
  const handleParentClick = () => {
    console.log('Клик на родителе');
  };

  return (
    <div onClick={handleParentClick}>
      <Child />
    </div>
  );
}

function Child() {
  const handleChildClick = (e) => {
    e.stopPropagation();  // Остановить всплытие
    console.log('Клик на ребенке');
    // handleParentClick не вызовется
  };

  return <button onClick={handleChildClick}>Кликни</button>;
}
```

**Разница между методами:**

```jsx
function Button() {
  const handleClick = (e) => {
    e.preventDefault();        // Отменить дефолтное поведение
    e.stopPropagation();       // Остановить всплытие
    e.stopImmediatePropagation(); // Остановить все обработчики на этом элементе
  };

  return <button onClick={handleClick}>Click</button>;
}
```

---

### 🎯 Делегирование событий в React

React использует **event delegation** — все события обрабатываются на корневом элементе приложения, а не на каждом элементе отдельно.

**Как это работает:**

1. React добавляет один обработчик на корневой элемент
2. При событии React определяет, какой компонент должен его обработать
3. React вызывает соответствующий обработчик компонента

**Преимущества:**

- ✅ Меньше обработчиков в памяти
- ✅ Автоматическая очистка при размонтировании
- ✅ Лучшая производительность

**Пример:**

```jsx
// React автоматически делегирует события
function App() {
  return (
    <div>
      <Button />  {/* onClick обрабатывается через делегирование */}
      <Link />    {/* onClick обрабатывается через делегирование */}
    </div>
  );
}
```

---

### 📝 Работа с формами

#### 1. **Контролируемые компоненты**

```jsx
function Form() {
  const [value, setValue] = useState('');

  const handleChange = (e) => {
    setValue(e.target.value);  // Обновляем state при каждом изменении
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Отправлено:', value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input 
        type="text" 
        value={value} 
        onChange={handleChange} 
      />
      <button type="submit">Отправить</button>
    </form>
  );
}
```

#### 2. **Неконтролируемые компоненты**

```jsx
function Form() {
  const inputRef = useRef(null);

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Значение:', inputRef.current.value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input type="text" ref={inputRef} />
      <button type="submit">Отправить</button>
    </form>
  );
}
```

#### 3. **Обработка разных типов полей**

```jsx
function Form() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    age: 0,
    agree: false
  });

  const handleChange = (e) => {
    const { name, value, type, checked } = e.target;
    
    setFormData(prev => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value
    }));
  };

  return (
    <form>
      <input 
        name="name" 
        value={formData.name} 
        onChange={handleChange} 
      />
      <input 
        name="email" 
        type="email" 
        value={formData.email} 
        onChange={handleChange} 
      />
      <input 
        name="age" 
        type="number" 
        value={formData.age} 
        onChange={handleChange} 
      />
      <input 
        name="agree" 
        type="checkbox" 
        checked={formData.agree} 
        onChange={handleChange} 
      />
    </form>
  );
}
```

---

### ⌨️ События клавиатуры

#### 1. **Обработка нажатий клавиш**

```jsx
function Input() {
  const handleKeyDown = (e) => {
    console.log('Клавиша:', e.key);
    console.log('Код:', e.keyCode);  // Устаревшее, лучше использовать e.key
    console.log('Код:', e.code);     // Современный способ
    
    if (e.key === 'Enter') {
      console.log('Нажата Enter');
    }
    
    if (e.ctrlKey && e.key === 's') {
      e.preventDefault();  // Отменить Ctrl+S (сохранение страницы)
      console.log('Сохранение...');
    }
  };

  return <input onKeyDown={handleKeyDown} />;
}
```

#### 2. **Модификаторы клавиш**

```jsx
function Button() {
  const handleClick = (e) => {
    if (e.shiftKey) {
      console.log('Клик с Shift');
    }
    if (e.ctrlKey) {
      console.log('Клик с Ctrl');
    }
    if (e.altKey) {
      console.log('Клик с Alt');
    }
    if (e.metaKey) {  // Cmd на Mac, Win на Windows
      console.log('Клик с Meta');
    }
  };

  return <button onClick={handleClick}>Кликни</button>;
}
```

---

### 🖱️ События мыши

```jsx
function Box() {
  const handleMouseEnter = () => {
    console.log('Мышь вошла в элемент');
  };

  const handleMouseLeave = () => {
    console.log('Мышь покинула элемент');
  };

  const handleMouseMove = (e) => {
    console.log(`Координаты: ${e.clientX}, ${e.clientY}`);
  };

  const handleMouseDown = (e) => {
    console.log('Кнопка мыши нажата:', e.button);
    // 0 - левая, 1 - средняя, 2 - правая
  };

  return (
    <div
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
      onMouseMove={handleMouseMove}
      onMouseDown={handleMouseDown}
    >
      Наведи мышь
    </div>
  );
}
```

---

### 🎯 Оптимизация обработчиков событий

#### 1. **Мемоизация с useCallback**

```jsx
// ❌ Проблема — новая функция при каждом рендере
function Button({ onClick }) {
  return <button onClick={() => onClick(1)}>Click</button>;
}

// ✅ ПРАВИЛЬНО — мемоизация обработчика
function Button({ onClick }) {
  const handleClick = useCallback(() => {
    onClick(1);
  }, [onClick]);

  return <button onClick={handleClick}>Click</button>;
}

// Или в родительском компоненте
function Parent() {
  const handleClick = useCallback((id) => {
    console.log('Clicked:', id);
  }, []);

  return <Button onClick={handleClick} />;
}
```

#### 2. **Передача обработчика через props**

```jsx
// Родительский компонент
function App() {
  const handleClick = (id) => {
    console.log('Clicked:', id);
  };

  return <Button onClick={handleClick} />;
}

// Дочерний компонент
function Button({ onClick }) {
  return <button onClick={() => onClick(1)}>Click</button>;
}
```

---

### 🔧 Классовые компоненты и привязка this

В классовых компонентах нужно привязывать `this` к методам.

```jsx
class Button extends React.Component {
  // Способ 1: bind в конструкторе
  constructor(props) {
    super(props);
    this.handleClick = this.handleClick.bind(this);
  }

  handleClick() {
    console.log('Clicked');
  }

  render() {
    return <button onClick={this.handleClick}>Click</button>;
  }
}

// Способ 2: Стрелочная функция в классе
class Button extends React.Component {
  handleClick = () => {
    console.log('Clicked');
  }

  render() {
    return <button onClick={this.handleClick}>Click</button>;
  }
}

// Способ 3: Инлайн стрелочная функция
class Button extends React.Component {
  handleClick() {
    console.log('Clicked');
  }

  render() {
    return <button onClick={() => this.handleClick()}>Click</button>;
  }
}
```

**Рекомендация:** Использовать функциональные компоненты с хуками вместо классовых.

---

### ⚠️ Важные моменты для собеседования

#### 1. **Event Pooling (удален в React 17+)**

В React 16 и ранее события "пулились" — объект события переиспользовался после обработчика. В React 17+ это удалено.

```jsx
// React 16 (устаревшее поведение)
function Button() {
  const handleClick = (e) => {
    // e нельзя использовать асинхронно
    setTimeout(() => {
      console.log(e.target);  // ❌ Может быть null
    }, 100);
  };
}

// React 17+ (текущее поведение)
function Button() {
  const handleClick = (e) => {
    // e можно использовать асинхронно
    setTimeout(() => {
      console.log(e.target);  // ✅ Работает
    }, 100);
  };
}
```

#### 2. **Разница между target и currentTarget**

```jsx
function Parent() {
  const handleClick = (e) => {
    console.log('target:', e.target);           // Элемент, на который кликнули
    console.log('currentTarget:', e.currentTarget); // Элемент с обработчиком
  };

  return (
    <div onClick={handleClick}>
      <button>Кликни</button>
      {/* target = button, currentTarget = div */}
    </div>
  );
}
```

#### 3. **Доступ к нативному событию**

```jsx
function Button() {
  const handleClick = (e) => {
    const nativeEvent = e.nativeEvent;  // Нативное событие браузера
    console.log(nativeEvent);
  };

  return <button onClick={handleClick}>Click</button>;
}
```

#### 4. **Список всех свойств SyntheticEvent**

```jsx
function Button() {
  const handleClick = (e) => {
    // Основные свойства
    e.target
    e.currentTarget
    e.type
    e.timeStamp
    e.bubbles
    e.cancelable
    
    // Методы
    e.preventDefault()
    e.stopPropagation()
    e.stopImmediatePropagation()
    e.isDefaultPrevented()
    e.isPropagationStopped()
    
    // Нативное событие
    e.nativeEvent
    
    // Persist (в React 16, в 17+ не нужен)
    // e.persist()  // Сохранить событие для асинхронного использования
  };

  return <button onClick={handleClick}>Click</button>;
}
```

---

### 🎯 Итоговая памятка

**Что нужно помнить о событиях в React:**

1. ✅ Все обработчики событий пишутся в **camelCase** с префиксом `on`
2. ✅ Обработчики — это **функции**, а не строки
3. ✅ React использует **SyntheticEvent** для единообразного API
4. ✅ React использует **event delegation** на корневом элементе
5. ✅ События автоматически очищаются при размонтировании компонента
6. ✅ Используй `e.preventDefault()` для отмены дефолтного поведения
7. ✅ Используй `e.stopPropagation()` для остановки всплытия
8. ✅ Мемоизируй обработчики с `useCallback` для оптимизации
9. ✅ В React 17+ события можно использовать асинхронно (event pooling удален)
10. ✅ `e.target` — элемент, на который кликнули, `e.currentTarget` — элемент с обработчиком

---

### 💡 Примеры для собеседования

**Вопрос: "Как работают события в React?"**

**Ответ:**
- React использует систему SyntheticEvent — обертку над нативными событиями
- Все события обрабатываются через event delegation на корневом элементе
- Обработчики событий пишутся в camelCase (onClick, onChange)
- React автоматически очищает обработчики при размонтировании компонента
- SyntheticEvent обеспечивает единообразное поведение во всех браузерах

**Вопрос: "В чем разница между e.target и e.currentTarget?"**

**Ответ:**
- `e.target` — это элемент, на котором произошло событие (на который кликнули)
- `e.currentTarget` — это элемент, на котором установлен обработчик события
- При всплытии события `target` остается тем же, а `currentTarget` меняется

**Вопрос: "Как передать параметры в обработчик события?"**

**Ответ:**
- Через инлайн стрелочную функцию: `onClick={() => handleClick(id)}`
- Через bind: `onClick={handleClick.bind(null, id)}`
- Через замыкание: создать функцию внутри компонента, которая принимает параметры

**Вопрос: "Что такое event delegation в React?"**

**Ответ:**
- React добавляет один обработчик на корневой элемент приложения
- При событии React определяет, какой компонент должен его обработать
- Это оптимизирует память и производительность
- Обработчики автоматически очищаются при размонтировании компонентов


