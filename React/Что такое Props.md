## 📦 Что такое Props

**Props (Properties)** — это объект, содержащий данные, которые передаются от родительского компонента к дочернему. Props являются **односторонним потоком данных** и **неизменяемыми (immutable)**.

---

### 🎯 Определение

**Props** — это механизм передачи данных от родительского компонента к дочернему. Props доступны только для чтения и не должны изменяться внутри компонента, который их получает.

**Основные характеристики:**

- ✅ **Односторонний поток** — данные передаются только сверху вниз (от родителя к ребенку)
- ✅ **Неизменяемые (immutable)** — компонент не должен изменять свои props
- ✅ **Любые типы данных** — можно передавать примитивы, объекты, массивы, функции, компоненты
- ✅ **Объект по умолчанию** — если props не переданы, они равны пустому объекту `{}`

---

### 📊 Типы данных, которые можно передавать

| Тип данных | Пример | Описание |
|------------|--------|----------|
| **Примитивы** | `string`, `number`, `boolean` | Простые значения |
| **Объекты** | `{}`, `{ name: 'John' }` | Любые объекты JavaScript |
| **Массивы** | `[]`, `[1, 2, 3]` | Массивы любых элементов |
| **Функции** | `() => {}`, `handleClick` | Обработчики событий, колбэки |
| **React элементы** | `<div>`, компоненты | JSX элементы и компоненты |
| **`undefined`** | `undefined` | Неопределенное значение |
| **`null`** | `null` | Null значение |

---

### 🔑 Ключевые особенности Props

#### 1. **Неизменяемость (Immutability)**

Props должны быть **read-only**. Компонент не должен изменять props, которые он получает.

```jsx
// ❌ НЕПРАВИЛЬНО — изменение props
function User({ name }) {
  name = name.toUpperCase(); // Ошибка! Props нельзя изменять
  return <div>{name}</div>;
}

// ✅ ПРАВИЛЬНО — использование props без изменения
function User({ name }) {
  const upperName = name.toUpperCase(); // Создаем новую переменную
  return <div>{upperName}</div>;
}
```

**Почему это важно?** React полагается на неизменяемость для оптимизации рендеринга и определения изменений.

---

#### 2. **Односторонний поток данных (Unidirectional Data Flow)**

Данные передаются только от родителя к ребенку. Для передачи данных обратно используются функции-колбэки.

```jsx
// Родительский компонент
function App() {
  const [count, setCount] = useState(0);

  return (
    <Counter 
      count={count}           // Передаем данные вниз
      onIncrement={() => setCount(count + 1)}  // Передаем функцию для обновления
    />
  );
}

// Дочерний компонент
function Counter({ count, onIncrement }) {
  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={onIncrement}>Increment</button>
    </div>
  );
}
```

---

#### 3. **Специальный prop `children`**

`children` — это специальный prop, который содержит содержимое между открывающим и закрывающим тегами компонента.

```jsx
// Использование children
function Card({ children }) {
  return <div className="card">{children}</div>;
}

// Передача children
<Card>
  <h2>Заголовок</h2>
  <p>Текст</p>
</Card>

// children может быть:
// - строкой
<Card>Hello</Card>

// - одним элементом
<Card><h1>Title</h1></Card>

// - массивом элементов
<Card>
  <p>First</p>
  <p>Second</p>
</Card>

// - функцией (render props паттерн)
<Card>
  {(data) => <div>{data}</div>}
</Card>
```

**Доступ к children:**

```jsx
function Card({ children }) {
  // children может быть undefined, строкой, элементом или массивом
  return (
    <div>
      {children}
      {Array.isArray(children) ? children.length : 1} элементов
    </div>
  );
}
```

---

#### 4. **Деструктуризация Props**

Рекомендуется использовать деструктуризацию для извлечения нужных props.

```jsx
// ✅ ПРАВИЛЬНО — деструктуризация
function User({ name, age, email }) {
  return <div>{name} - {age}</div>;
}

// ❌ Менее удобно — доступ через props
function User(props) {
  return <div>{props.name} - {props.age}</div>;
}

// Деструктуризация с значениями по умолчанию
function User({ name = 'Anonymous', age = 0 }) {
  return <div>{name} - {age}</div>;
}
```

---

#### 5. **Spread оператор для передачи props**

Можно передавать все props сразу с помощью spread оператора.

```jsx
function User({ name, age, ...rest }) {
  return (
    <div>
      <p>{name} - {age}</p>
      <UserDetails {...rest} />  {/* Передаем остальные props */}
    </div>
  );
}

// Или передача всех props в дочерний компонент
function App() {
  const userProps = { name: 'John', age: 25, email: 'john@example.com' };
  return <User {...userProps} />;
}
```

---

#### 6. **Условная передача props**

Можно передавать props условно, используя spread оператор.

```jsx
function Button({ primary, ...props }) {
  return (
    <button 
      className={primary ? 'btn-primary' : 'btn'}
      {...props}  // Передаем все остальные props
    />
  );
}

// Использование
<Button primary onClick={handleClick}>Click</Button>
```

---

### 🛠️ Продвинутые техники работы с Props

#### 1. **Default Props (Значения по умолчанию)**

**Способ 1: Деструктуризация с значениями по умолчанию**

```jsx
function User({ name = 'Anonymous', age = 0 }) {
  return <div>{name} - {age}</div>;
}
```

**Способ 2: Свойство `defaultProps` (устаревший способ для функциональных компонентов)**

```jsx
function User({ name, age }) {
  return <div>{name} - {age}</div>;
}

User.defaultProps = {
  name: 'Anonymous',
  age: 0
};
```

**Способ 3: Использование `||` или `??`**

```jsx
function User({ name, age }) {
  const displayName = name || 'Anonymous';
  const displayAge = age ?? 0;  // ?? проверяет только null/undefined
  return <div>{displayName} - {displayAge}</div>;
}
```

---

#### 2. **PropTypes (Проверка типов)**

PropTypes позволяет проверять типы props во время разработки (только в development режиме).

```jsx
import PropTypes from 'prop-types';

function User({ name, age, email }) {
  return (
    <div>
      <p>{name}</p>
      <p>{age}</p>
      <p>{email}</p>
    </div>
  );
}

User.propTypes = {
  name: PropTypes.string.isRequired,
  age: PropTypes.number,
  email: PropTypes.string.isRequired,
  hobbies: PropTypes.arrayOf(PropTypes.string),
  onSave: PropTypes.func
};

User.defaultProps = {
  age: 0
};
```

**Основные типы PropTypes:**

- `PropTypes.string`
- `PropTypes.number`
- `PropTypes.bool`
- `PropTypes.array`
- `PropTypes.object`
- `PropTypes.func`
- `PropTypes.element`
- `PropTypes.node` (любой рендеримый элемент)
- `PropTypes.oneOf(['value1', 'value2'])` (одно из значений)
- `PropTypes.shape({ name: PropTypes.string })` (форма объекта)

**Примечание:** В TypeScript PropTypes не нужны, так как типизация происходит через TypeScript.

---

#### 3. **TypeScript с Props**

В TypeScript типы props определяются через интерфейсы или типы.

```tsx
// Интерфейс для props
interface UserProps {
  name: string;
  age?: number;  // Опциональный
  email: string;
  onSave?: (data: string) => void;
}

function User({ name, age = 0, email, onSave }: UserProps) {
  return (
    <div>
      <p>{name} - {age}</p>
      <p>{email}</p>
      {onSave && <button onClick={() => onSave('data')}>Save</button>}
    </div>
  );
}
```

---

#### 4. **Render Props паттерн**

Render Props — это паттерн, когда компонент принимает функцию как prop и вызывает её для рендеринга.

```jsx
function MouseTracker({ render }) {
  const [position, setPosition] = useState({ x: 0, y: 0 });

  useEffect(() => {
    const handleMouseMove = (e) => {
      setPosition({ x: e.clientX, y: e.clientY });
    };
    window.addEventListener('mousemove', handleMouseMove);
    return () => window.removeEventListener('mousemove', handleMouseMove);
  }, []);

  return render(position);
}

// Использование
<MouseTracker 
  render={({ x, y }) => (
    <div>
      Mouse position: {x}, {y}
    </div>
  )}
/>
```

---

### ⚠️ Важные моменты для собеседования

#### 1. **Props vs State**

| Props | State |
|-------|-------|
| Передаются от родителя | Управляются внутри компонента |
| Неизменяемые | Изменяемые через setState |
| Доступны только для чтения | Можно изменять |
| Передаются сверху вниз | Локальное состояние компонента |

**Когда использовать Props:**
- Данные передаются от родителя
- Компонент должен быть переиспользуемым
- Нужно передать обработчики событий

**Когда использовать State:**
- Данные меняются внутри компонента
- Нужно хранить промежуточные значения
- Компонент должен управлять своим состоянием

---

#### 2. **Проблема с объектами и массивами в props**

Если передавать объекты или массивы напрямую, React может не заметить изменения, если ссылка не изменилась.

```jsx
// ❌ Проблема — объект создается заново при каждом рендере
function App() {
  return <User config={{ theme: 'dark' }} />;  // Новый объект каждый раз
}

// ✅ ПРАВИЛЬНО — вынести за пределы компонента или использовать useMemo
const config = { theme: 'dark' };

function App() {
  return <User config={config} />;
}

// Или
function App() {
  const config = useMemo(() => ({ theme: 'dark' }), []);
  return <User config={config} />;
}
```

---

#### 3. **Передача функций как props**

Функции можно передавать как обычные props. Важно помнить о мемоизации, чтобы избежать лишних рендеров.

```jsx
// ❌ Проблема — новая функция при каждом рендере
function App() {
  return <Button onClick={() => console.log('click')} />;
}

// ✅ ПРАВИЛЬНО — использовать useCallback
function App() {
  const handleClick = useCallback(() => {
    console.log('click');
  }, []);
  
  return <Button onClick={handleClick} />;
}
```

---

#### 4. **Проверка существования props**

```jsx
function User({ name, age }) {
  // Проверка через условный рендеринг
  return (
    <div>
      {name && <p>Name: {name}</p>}
      {age !== undefined && <p>Age: {age}</p>}
    </div>
  );
}

// Или с деструктуризацией и значениями по умолчанию
function User({ name = '', age = 0 }) {
  return (
    <div>
      {name && <p>Name: {name}</p>}
      {age > 0 && <p>Age: {age}</p>}
    </div>
  );
}
```

---

### 🎯 Итоговая памятка

**Что нужно помнить о Props:**

1. ✅ Props — это объект с данными, передаваемыми от родителя к ребенку
2. ✅ Props неизменяемы — компонент не должен их изменять
3. ✅ Можно передавать любые типы данных (примитивы, объекты, массивы, функции, элементы)
4. ✅ `children` — специальный prop для содержимого компонента
5. ✅ Используй деструктуризацию для удобства работы с props
6. ✅ Односторонний поток данных — данные идут только сверху вниз
7. ✅ Для передачи данных обратно используй функции-колбэки
8. ✅ Используй значения по умолчанию для опциональных props
9. ✅ В TypeScript типизируй props через интерфейсы
10. ✅ Мемоизируй объекты и функции, передаваемые как props, чтобы избежать лишних рендеров

---

### 💡 Примеры для собеседования

**Вопрос: "Что такое props и чем они отличаются от state?"**

**Ответ:**
- Props — это данные, передаваемые от родительского компонента к дочернему
- Props неизменяемы и доступны только для чтения
- State — это локальное состояние компонента, которое можно изменять
- Props используются для передачи данных сверху вниз, state — для управления внутренним состоянием

**Вопрос: "Можно ли изменять props внутри компонента?"**

**Ответ:**
- Нет, props должны быть неизменяемыми
- Изменение props нарушает принципы React и может привести к непредсказуемому поведению
- Если нужно изменить данные, они должны быть в state родительского компонента

**Вопрос: "Как передать данные обратно от дочернего компонента к родительскому?"**

**Ответ:**
- Через функции-колбэки, передаваемые как props
- Родитель передает функцию, дочерний компонент вызывает её с данными
- Это реализует односторонний поток данных в React


