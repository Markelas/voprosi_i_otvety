## 🎁 Что такое HOC (Higher-Order Component)

**HOC (Higher-Order Component)** — это функция, которая принимает компонент и возвращает новый компонент с дополнительной логикой.

Это паттерн переиспользования кода в React — он позволяет оборачивать базовые компоненты, расширяя их функциональность без изменения исходного кода.

---

### 💡 Пример:

```js
const withExtraLogic = (WrappedComponent) => {
  return function EnhancedComponent(props) {
    // Дополнительная логика
    const extraData = fetchExtraData();
    
    return <WrappedComponent {...props} extraData={extraData} />;
  };
};

// Использование
const EnhancedButton = withExtraLogic(Button);
```

---

### 🎯 Где используется:

- ✅ Добавление общей логики (логирование, авторизация)
- ✅ Подписка на данные (контекст, Redux)
- ✅ Переиспользование поведения между компонентами

---

### ⚠️ Важно:

- Передавай все пропсы обёрнутому компоненту (`{...props}`)
- Не мутируй оригинальный компонент
- Используй `displayName` для отладки

---

### 🔄 Альтернатива в современном React:

С появлением хуков, многие HOC можно заменить на **кастомные хуки**, которые проще и понятнее:

```js
// Вместо HOC
const useExtraData = () => {
  return fetchExtraData();
};

function Component() {
  const extraData = useExtraData();
  // ...
}
```
