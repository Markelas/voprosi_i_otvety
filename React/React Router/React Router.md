## 🧭 React Router

**React Router** — это библиотека для маршрутизации в React-приложениях, которая позволяет создавать одностраничные приложения (SPA) с навигацией между различными компонентами без перезагрузки страницы.

---

### 🎯 Зачем нужен React Router?

<font color="#31859b">React Router необходим для:</font>

- **<font color="#31859b">Навигация без перезагрузки страницы</font>**: Позволяет переключаться между "страницами" в SPA без полной перезагрузки браузера, что делает приложение быстрее и отзывчивее.

- **<font color="#31859b">Управление URL</font>**: Синхронизирует состояние приложения с URL в адресной строке, что позволяет пользователям использовать кнопки "Назад"/"Вперед" браузера, сохранять закладки и делиться ссылками.

- **<font color="#31859b">Условный рендеринг компонентов</font>**: Показывает разные компоненты в зависимости от текущего URL, что является основой для создания многостраничных интерфейсов в одностраничных приложениях.

- **<font color="#31859b">Защита маршрутов</font>**: Возможность ограничивать доступ к определенным страницам (например, только для авторизованных пользователей).

- **<font color="#31859b">Передача параметров через URL</font>**: Позволяет передавать данные через URL (query-параметры, path-параметры), что удобно для глубоких ссылок и SEO.

- **<font color="#31859b">query-параметры</font>**: позволяют пользователям сохранять, обновлять страницу и делиться ссылками с установленными фильтрами и сортировкой. Можно использовать кнопки браузера "назад" и "вперед", для перемещения между состояниями фильтров. Также можно передать состояние, например, открыто ли модальное окно. 

- **<font color="#31859b"> Изменение квери параметров приводит к повторному рендерингу </font>** соответствующего компонента. Когда квери параметры в URL изменяются, React Router воспринимает это как изменение состояния маршрута, что вызывает повторное рендеринг компонента, связанного с этим маршрутом.

---

### 🔧 Что происходит под капотом

React Router использует механизм управления историей браузера, чтобы отслеживать изменения URL и управлять отображением компонентов. Вот как это работает под капотом:

1. **<font color="#d99694">Изменение URL</font>**: Когда пользователь нажимает на ссылку, React Router использует `history.pushState` (или `history.replaceState`), чтобы изменить URL без перезагрузки страницы. Это часть **History API** браузера.

2. **<font color="#d99694">Отображение компонентов</font>**: При изменении URL React Router анализирует текущий путь и выбирает соответствующий компонент для отображения. Это делается с помощью метода `match` для сопоставления URL с определенными маршрутами, определенными в конфигурации `<Routes>`.

3. **<font color="#d99694">Поддержка истории</font>**: React Router управляет историей браузера, позволяя пользователям использовать кнопки "Назад" и "Вперед" для навигации между страницами, сохраняя состояние приложения. Внутри используется объект `history`, который отслеживает стек навигации.

4. **<font color="#d99694">Слушатели событий</font>**: React Router подписывается на события `popstate` (для кнопок назад/вперед) и перехватывает клики по ссылкам через `<Link>`, предотвращая стандартное поведение браузера.

5. **<font color="#d99694">Контекст</font>**: React Router использует React Context API для передачи информации о текущем маршруте, истории и локации во все дочерние компоненты через провайдер `<Router>`.

---

### 📦 Основные методы и компоненты

#### **Компоненты:**

| Компонент              | Назначение                                                                                                                                                                                                                              |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`<BrowserRouter>`**  | Управляет историей браузера и синхронизацией URL с приложением. Оборачиваем вокруг всей части приложения.<br>Использует HTML5 History API (`pushState`, `replaceState`) для управления URL. URL выглядит как обычный путь: `/users/123` |
| **`<HashRouter>`**     | Использует хэш в URL (`#`) для маршрутизации. URL выглядит как: `/app#/users/123`. Полезен для статических хостингов без настройки сервера                                                                                              |
| **`<Routes>`**         | Контейнер для маршрутов (заменил `<Switch>` в v6). Выбирает первый подходящий маршрут                                                                                                                                                   |
| **`<Route>`**          | Определяет маршрут и компонент, который должен отображаться при совпадении пути                                                                                                                                                         |
| **`<Link>`**           | Компонент для навигации. Рендерит `<a>` тег, но предотвращает перезагрузку страницы                                                                                                                                                     |
| **`<NavLink>`**        | Расширенная версия `<Link>` с возможностью добавления активных классов/стилей для текущего маршрута                                                                                                                                     |
| **`<Navigate>`**       | Программный редирект. При рендере автоматически перенаправляет на указанный маршрут                                                                                                                                                     |
| **`<Outlet>`**         | Место для рендеринга вложенных маршрутов (для вложенной маршрутизации)                                                                                                                                                                  |
| **`<MemoryRouter>`**   | Для управления маршрутизацией где нет доступа к истории браузера, например, в тестах или React Native приложениях                                                                                                                       |
| **`<RouterProvider>`** | Интегрирует маршрутизатор, созданный с помощью createBrowserRouter, в приложении                                                                                                                                                        |

#### **Хуки:**

| Хук                      | Назначение                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------ |
| **`useNavigate()`**      | Возвращает функцию для программной навигации. Заменил `useHistory()` в v6                  |
| **`useParams()`**        | Возвращает объект с параметрами из URL (например, `:id` из `/users/:id`)                   |
| **`useLocation()`**      | Возвращает объект `location` с информацией о текущем URL (pathname, search, hash, state)   |
| **`useSearchParams()`**  | Возвращает `[searchParams, setSearchParams]` для работы с query-параметрами (`?key=value`) |
| **`useMatch()`**         | Проверяет, соответствует ли текущий URL указанному пути                                    |
| **`useResolvedPath()`**  | Разрешает относительный путь в абсолютный путь                                             |
| **`useOutletContext()`** | Получает контекст, переданный из родительского маршрута через `<Outlet context={...}>`     |

---
#### Определение маршрутов
<ul>
<li><p><strong>Маршрут (Route)</strong>: Связывает определённый путь URL с React-компонентом, который должен быть отображён при совпадении пути.</p></li>
<li><p><strong>Компонент-раскладка (Layout Component)</strong>: Компонент, который содержит общие элементы интерфейса (например, навигацию) и место для отображения вложенных маршрутов с помощью компонента <code>&lt;Outlet&gt;</code>.</p></li>
<li><p><strong>Вложенные маршруты (Nested Routes)</strong>: Маршруты, которые находятся внутри других маршрутов, создавая иерархическую структуру.</p></li>
</ul>

Определение маршрутов с использованием <code>RouterProvider</code> и <code>createBrowserRouter</code><p>Этот подход использует новый API, представленный в React Router версии 6.4 и выше. Он обеспечивает централизованную конфигурацию маршрутов и поддерживает продвинутые возможности, такие как ленивые загрузки и асинхронные загрузки данных.</p>


---

### Разница между `RouterProvider` и `BrowserRouter`

Оба подхода используют одинаковую навигационную логику под капотом. Разница именно в API для разработчика и возможностях работы с данными. React Router рекомендует использовать `RouterProvider` для новых приложений, так как это будущее библиотеки.

- **`BrowserRouter`** — классический компонент для обертки приложения, используется с `<Routes>` и `<Route>`.
    
- **`RouterProvider`** — новый компонент для работы с **роутером, созданным через `createBrowserRouter`**, который позволяет использовать мощные фичи для загрузки данных (`loader`, `action`, `errorElement`).

Когда что использовать?

**Используйте BrowserRouter, если:**

- У вас небольшое приложение без сложной логики загрузки данных
    
- Вы переходите с React Router v5 и хотите минимальных изменений
    
- Предпочитаете декларативный JSX-синтаксис
    
- Не нужны встроенные `loader`/`action`
    

**Используйте RouterProvider, если:**

- Нужна тесная интеграция загрузки данных с маршрутизацией
    
- Хотите использовать новые Data API React Router
    
- Строите сложное приложение с формами, валидацией, ошибками
    
- Нужны современные возможности React Router

---

### 💡 Примеры использования

#### **Базовый пример:**

```jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <nav>
        <Link to="/">Home</Link>
        <Link to="/about">About</Link>
        <Link to="/users">Users</Link>
      </nav>
      
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/users" element={<Users />} />
        <Route path="/users/:id" element={<UserDetail />} />
        <Route path="*" element={<NotFound />} />
      </Routes>
    </BrowserRouter>
  );
}
```

#### **Использование хуков:**

```jsx
import { useParams, useNavigate, useLocation, useSearchParams } from 'react-router-dom';

function UserDetail() {
  const { id } = useParams(); // Получить параметр из URL
  const navigate = useNavigate(); // Функция для навигации
  const location = useLocation(); // Информация о текущем URL
  const [searchParams, setSearchParams] = useSearchParams(); // Query-параметры
  
  const userId = searchParams.get('tab'); // Получить ?tab=value
  
  return (
    <div>
      <h1>User {id}</h1>
      <button onClick={() => navigate('/users')}>Back</button>
      <button onClick={() => setSearchParams({ tab: 'profile' })}>
        Show Profile
      </button>
    </div>
  );
}
```

#### **Защищенные маршруты:**

```jsx
function ProtectedRoute({ children }) {
  const isAuthenticated = useAuth(); // Ваша логика проверки
  
  if (!isAuthenticated) {
    return <Navigate to="/login" />;
  }
  
  return children;
}

// Использование:
<Route 
  path="/dashboard" 
  element={
    <ProtectedRoute>
      <Dashboard />
    </ProtectedRoute>
  } 
/>
```

#### **Вложенные маршруты:**

```jsx
<Routes>
  <Route path="/dashboard" element={<Dashboard />}>
    <Route path="profile" element={<Profile />} />
    <Route path="settings" element={<Settings />} />
    <Route index element={<DashboardHome />} /> {/* index route */}
  </Route>
</Routes>

// Компонент Dashboard:
function Dashboard() {
  return (
    <div>
      <Sidebar />
      <Outlet /> {/* Здесь рендерятся Profile, Settings или DashboardHome */}
    </div>
  );
}
```

---

### 🎯 Краткий ответ (суть)

**React Router** — это библиотека для маршрутизации в React-приложениях, которая:

- Позволяет создавать навигацию между компонентами без перезагрузки страницы
- Синхронизирует URL с состоянием приложения
- Использует History API браузера (`pushState`, `popstate`) для управления URL
- Предоставляет компоненты (`<Routes>`, `<Route>`, `<Link>`) и хуки (`useNavigate`, `useParams`, `useLocation`) для работы с маршрутизацией
- В v6 использует `<Routes>` вместо `<Switch>`, `element` вместо `component`, и `useNavigate()` вместо `useHistory()`

**Основная идея**: React Router отслеживает изменения URL и условно рендерит нужные компоненты, создавая иллюзию многостраничного приложения в одностраничном.


---

### 📚 Полезные практики

- **Используйте `<NavLink>`** для активных ссылок в навигации
- **Защищайте приватные маршруты** через компоненты-обертки
- **Используйте lazy loading** для больших приложений
- **Обрабатывайте 404** через wildcard маршрут
- **Используйте `replace` в `Navigate`** когда не нужна история (например, после логина)
- **Передавайте состояние через `state`** для временных данных между маршрутами





