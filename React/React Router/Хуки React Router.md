## 🎣 Хуки React Router

Хуки React Router предоставляют программный доступ к информации о маршрутизации и навигации.

---

## 📍 `useParams()`

### 🎯 Что делает?

**`useParams()`** возвращает объект с параметрами из URL, которые были определены в маршруте через `:paramName`.

### 💡 Зачем используется?

- Получение динамических параметров (ID, slug) из URL
- Использование параметров для запросов к API

### 📝 Пример:

```jsx
// Маршрут: <Route path="/users/:id" element={<UserDetail />} />
// URL: /users/123

function UserDetail() {
  const { id } = useParams(); // { id: '123' }
  return <div>User ID: {id}</div>;
}
```

### ⚠️ Важно:

- **Всегда строки**: Параметры всегда возвращаются как строки
- **Нужно преобразовывать**: `const userId = Number(id)` для чисел

---

## 🧭 `useNavigate()`

### 🎯 Что делает?

**`useNavigate()`** возвращает функцию для программной навигации между маршрутами. Заменил `useHistory()` в React Router v6.

### 💡 Зачем используется?

- Программная навигация из обработчиков событий
- Условные редиректы (после логина, отправки формы)
- Передача данных через `state` (не видно в URL)

### 📝 Примеры:

```jsx
function LoginForm() {
  const navigate = useNavigate();
  
  const handleSubmit = () => {
    // Логика логина...
    navigate('/dashboard');
  };
  
  return <form onSubmit={handleSubmit}>...</form>;
}

// С передачей состояния:
navigate('/products/123', {
  state: { from: 'list', data: {...} }
});

// С replace (заменяет запись в истории):
navigate('/dashboard', { replace: true });

// Навигация по истории:
navigate(-1); // Назад
navigate(1);  // Вперед
```

### ⚠️ Важно:

- **Не вызывать в рендере**: Вызов в теле компонента вызовет бесконечный цикл
- **Использовать в обработчиках или useEffect**

---

## 📍 `useLocation()`

### 🎯 Что делает?

**`useLocation()`** возвращает объект `location` с информацией о текущем URL: `pathname`, `search`, `hash` и `state`.

### 💡 Зачем используется?

- Доступ к переданному состоянию через `navigate(..., { state: {...} })`
- Отслеживание изменений маршрута (для аналитики)
- Получение полной информации о URL

### 📝 Пример:

```jsx
function Component() {
  const location = useLocation();
  
  // location = {
  //   pathname: '/users/123',
  //   search: '?tab=profile',
  //   hash: '#section-1',
  //   state: { from: 'dashboard' }
  // }
  
  // Доступ к переданному состоянию:
  const { from } = location.state || {};
  
  // Отслеживание изменений:
  useEffect(() => {
    analytics.track('page_view', { path: location.pathname });
  }, [location]);
  
  return <div>Current: {location.pathname}</div>;
}
```

### ⚠️ Важно:

- **state теряется при перезагрузке**: Не сохраняется после перезагрузки страницы
- **Всегда проверять**: `location.state?.someData` (может быть `undefined`)

---

## 🔍 `useSearchParams()`

### 🎯 Что делает?

**`useSearchParams()`** возвращает `[searchParams, setSearchParams]` для работы с query-параметрами (`?key=value`).

### 💡 Зачем используется?

- Работа с фильтрами, сортировкой, пагинацией через URL
- Синхронизация состояния с URL (можно делиться ссылками)
- Чтение и изменение query-параметров

### 📝 Пример:

```jsx
// URL: /products?category=electronics&sort=price

function Products() {
  const [searchParams, setSearchParams] = useSearchParams();
  
  const category = searchParams.get('category'); // 'electronics'
  const sort = searchParams.get('sort'); // 'price'
  
  const handleChange = (newCategory) => {
    // ✅ Сохраняем существующие параметры
    setSearchParams(prev => {
      prev.set('category', newCategory);
      return prev;
    });
  };
  
  return (
    <select onChange={(e) => handleChange(e.target.value)}>
      <option value="electronics">Electronics</option>
      <option value="clothing">Clothing</option>
    </select>
  );
}
```

### ⚠️ Важно:

- **Сохранять другие параметры**: При обновлении использовать функциональную форму
- **Все значения — строки**: Нужно преобразовывать: `Number(searchParams.get('page'))`

---

## 🎯 Сравнительная таблица

| Хук | Возвращает | Когда использовать |
|-----|------------|-------------------|
| **`useParams()`** | Объект с параметрами пути (`:id`) | Получение ID или slug из URL |
| **`useNavigate()`** | Функция навигации | Программная навигация, условные редиректы |
| **`useLocation()`** | Объект location (pathname, search, hash, state) | Доступ к state, отслеживание изменений |
| **`useSearchParams()`** | `[searchParams, setSearchParams]` | Работа с query-параметрами (фильтры, сортировка) |

---

## 🎯 Краткие ответы для собеседования

### **`useParams()`**
"Возвращает объект с параметрами из URL (`:paramName`). Используется для получения динамических значений (ID, slug). Всегда возвращает строки."

### **`useNavigate()`**
"Возвращает функцию для программной навигации. Позволяет переходить на маршруты из кода, передавать состояние через `state`, управлять историей через `replace`. Заменил `useHistory()` в v6."

### **`useLocation()`**
"Возвращает объект с информацией о текущем маршруте: `pathname`, `search`, `hash` и `state`. Используется для доступа к переданному состоянию и отслеживания изменений."

### **`useSearchParams()`**
"Возвращает `[searchParams, setSearchParams]` для работы с query-параметрами. Позволяет читать и изменять параметры в URL (фильтры, сортировка, пагинация)."

---

## ⚠️ Частые ошибки

1. **Не проверяют параметры**: `const id = Number(useParams().id)` — может быть `undefined`
2. **Вызов navigate в рендере**: Вызовет бесконечный цикл (нужно в обработчике или useEffect)
3. **Потеря параметров в searchParams**: `setSearchParams({ category: 'x' })` — теряются другие параметры
4. **Не проверяют location.state**: `location.state.data` — может быть `undefined` (нужно `location.state?.data`)

