## 🔄 Как передать `ref` из родительского в дочерний компонент

Чтобы передать `ref` в дочерний компонент, нужно использовать **`React.forwardRef`**.

Это позволяет дочернему компоненту принять `ref` вторым параметром и привязать его к нужному DOM-элементу или другому компоненту.

---

### 📝 Родительский компонент:

```js
const Parent = () => {
  const inputRef = useRef(null);
  
  return <MyInput ref={inputRef} />;
};
```

---

### 📝 Дочерний компонент:

```js
const MyInput = forwardRef((props, ref) => {
  return <input {...props} ref={ref} />;
});
```

---

### 💡 Важно:

- `forwardRef` принимает функцию с двумя параметрами: `props` и `ref`
- `ref` передаётся вторым параметром (не через `props`)
- Привязывай `ref` к нужному DOM-элементу

---

### 🎯 Пример с кастомной логикой:

```js
const MyInput = forwardRef((props, ref) => {
  const handleFocus = () => {
    ref.current.focus();
  };
  
  return (
    <div>
      <input {...props} ref={ref} />
      <button onClick={handleFocus}>Focus</button>
    </div>
  );
});
```
