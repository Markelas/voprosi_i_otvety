## 🔄 `useEffect` и аналогия методов классового компонента

---

### 📊 Таблица соответствий:

| Хук | Аналог метода классового компонента | Назначение |
|-----|------------------------------------|------------|
| `useState` | `this.state` | Состояние компонента |
| `useEffect(() => { }, [])` | `componentDidMount` | Выполняется один раз после монтирования |
| `useEffect(() => { })` без зависимостей | `componentDidMount + componentDidUpdate` | Выполняется после каждого рендера |
| `useEffect(() => { return () => { } }, [])` | `componentWillUnmount` | Очистка таймеров, подписок |
| `useLayoutEffect` | `getSnapshotBeforeUpdate + componentDidUpdate` | Синхронные эффекты до отрисовки DOM |

---

### 🎯 Этапы жизненного цикла классового компонента

#### 1. **Mounting** (монтирование)
Компонент создаётся и добавляется в DOM, методы:

- **`constructor()`** – инициализация `state`, биндинг методов
- **`static getDerivedStateFromProps()`** – синхронизация `state` с `props`
- **`render()`** – рендер JSX
- **`componentDidMount()`** – запросы к API, подписки, работа с DOM

#### 2. **Updating** (обновление)
Происходит при изменении `props` или `state`, методы:

- **`static getDerivedStateFromProps()`**
- **`shouldComponentUpdate()`** – оптимизация рендера
- **`render()`**
- **`getSnapshotBeforeUpdate(prevProps, prevState)`** – получить состояние DOM до обновления
- **`componentDidUpdate(prevProps, prevState, snapshot)`** – обновление после рендера, работа с сетью / DOM

#### 3. **Unmounting** (размонтирование)

- **`componentWillUnmount()`** – отписки, очистка таймеров

---

### 💡 Примеры в функциональных компонентах:

```js
// componentDidMount
useEffect(() => {
  fetchData();
}, []);

// componentDidUpdate
useEffect(() => {
  updateData();
}, [dependency]);

// componentWillUnmount
useEffect(() => {
  const timer = setInterval(() => {}, 1000);
  return () => clearInterval(timer);
}, []);
```
