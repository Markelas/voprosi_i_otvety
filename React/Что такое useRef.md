## 🔗 Что такое `useRef`

**`useRef`** нужен для хранения **мутабельного значения**, которое сохраняется между рендерами и **не вызывает повторного рендера** при изменении.

---

### 🎯 Основные применения:

1. **Доступ к DOM-элементам**
2. **Хранение предыдущих значений**
3. **Работа с таймерами**

---

### 💡 Особенность:

**В отличие от `useState`, он не триггерит обновление компонента.**

```js
const inputRef = useRef(null);

<input ref={inputRef} />
```

---

### 📝 Что такое `current`?

**`current`** — это единственное свойство объекта, который возвращает `useRef`.

- Если `useRef` используется для **данных** — `current` содержит твои данные
- Если `useRef` используется для **DOM-элемента** — React сам присвоит `current` реальный DOM-элемент

```js
const timerRef = useRef(null);

// Сохраняем данные
timerRef.current = setTimeout(() => {}, 1000);

// Получаем DOM
const inputRef = useRef(null);
inputRef.current.focus(); // работаем с DOM
```

---

### 🔄 Отличие от `useState`:

| Параметр | `useState` | `useRef` |
|----------|------------|----------|
| **Триггерит ререндер** | ✅ Да | ❌ Нет |
| **Доступ к DOM** | ❌ Нет | ✅ Да |
| **Сохранение между рендерами** | ✅ Да | ✅ Да |
| **Изменение мутабельно** | ❌ Нет (иммутабельно) | ✅ Да |
