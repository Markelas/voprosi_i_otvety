## 🎮 Разница между контролируемыми и неконтролируемыми компонентами

---

### 🔸 Контролируемые компоненты

**Хранят данные в состоянии (`state`)** и обновляются через `onChange`, давая **полный контроль над вводом**.

```js
const [value, setValue] = useState('');

<input 
  value={value} 
  onChange={(e) => setValue(e.target.value)} 
/>
```

**Особенности:**
- ✅ React полностью контролирует значение
- ✅ Можно валидировать и обрабатывать на лету
- ✅ Легко сбросить значение
- ✅ Удобны для сложных форм с валидацией

---

### 🔹 Неконтролируемые компоненты

**Работают как обычные HTML-элементы** — данные хранятся в DOM, а React получает их через `ref` при необходимости (например, при отправке формы).

```js
const inputRef = useRef(null);

const handleSubmit = () => {
  console.log(inputRef.current.value);
};

<input ref={inputRef} defaultValue="Initial" />
```

**Особенности:**
- ✅ Меньше кода (нет состояния)
- ✅ Меньше ререндеров
- ✅ Подходят для простых случаев
- ✅ Полезны для интеграции с нативным кодом

---

### 📊 Сравнение:

| Параметр | Контролируемые | Неконтролируемые |
|----------|----------------|------------------|
| **Хранение данных** | В `state` | В DOM |
| **Получение значения** | Из `state` | Через `ref` |
| **Валидация** | ✅ В реальном времени | ❌ Только при отправке |
| **Сброс значения** | Легко (`setValue('')`) | Нужно через `ref` |
| **Использование** | Сложные формы | Простые формы |

---

### 💡 Рекомендация:

**Используй контролируемые компоненты** для большинства случаев — они дают больше контроля и лучше интегрируются с React.
