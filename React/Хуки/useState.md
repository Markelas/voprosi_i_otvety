## 📦 Что такое `useState`

**`useState`** хранит состояние в функциональном компоненте.

Для изменения состояния используется функция, которую он возвращает.

---

### 💡 Базовое использование:

```js
const [count, setCount] = useState(0);
```

---

### 🔄 Функциональное обновление:

Если новое значение **зависит от старого**, нужно передавать колбэк с `prev`, чтобы избежать проблем с устаревшими значениями.

```js
// ✅ Правильно — функциональное обновление
setUser(prev => ({ ...prev, name: "Bob" }));

// Это актуальное значение состояния на момент обновления
```

**Почему это важно?** Если обновление зависит от предыдущего значения, при быстрых множественных вызовах может использоваться устаревшее значение.

---

### 🎯 Ленивая инициализация:

Инициализация может быть **ленивой**, если передать функцию:

```js
// Тяжёлые вычисления выполнятся только один раз при монтировании
const [data, setData] = useState(() => {
  return expensiveCalculation();
});
```

---

### 📊 Структура:

| Часть | Описание |
|-------|----------|
| **`state`** | Текущее значение состояния |
| **`setState`** | Функция для обновления |
| **Инициализатор** | Начальное значение или функция |

---

### 🔧 Как работает `useState` под капотом

**<font color="#d99694">Ключевая концепция:</font>** Состояние хранится **не в самом компоненте**, а в специальном объекте React — **Fiber Node** (узле Fiber), который связан с компонентом.

#### **1. Где хранится состояние?**

```jsx
function MyComponent() {
  const [count, setCount] = useState(0); // Где хранится count?
  return <div>{count}</div>;
}
```

**Ответ:** Состояние хранится в **Fiber Node** компонента, а не в самом JSX или функции компонента.

**Fiber Node** — это внутренний объект React, который содержит:
- Ссылку на компонент
- Props компонента
- **Состояние (state) и хуки**
- Ссылки на дочерние и соседние узлы
- Информацию для рендеринга

```js
// Упрощенная структура Fiber Node
const fiberNode = {
  type: MyComponent,        // Ссылка на функцию компонента
  props: { ... },          // Props компонента
  memoizedState: {         // Здесь хранится состояние!
    count: 0,              // Значение из useState
    // ... другие хуки
  },
  // ... другие поля
};
```

#### **2. Как React связывает вызовы хуков с компонентом?**

React использует **связанный список (linked list) хуков** внутри Fiber Node:

```js
// Упрощенная структура
fiberNode.memoizedState = {
  // Первый хук (useState)
  {
    memoizedState: 0,        // Текущее значение count
    next: {                  // Ссылка на следующий хук
      // Второй хук (например, другой useState)
      memoizedState: null,
      next: { ... }          // Следующий хук
    }
  }
};
```

**Как это работает:**

1. **При первом рендере** компонента React создает Fiber Node и инициализирует список хуков
2. **При каждом вызове `useState`** React:
   - Определяет текущий компонент (через внутренний контекст)
   - Находит соответствующий Fiber Node
   - Читает/записывает состояние из связанного списка хуков
   - Переходит к следующему хуку в списке

```js
// Упрощенная реализация useState (концептуально)
let currentFiberNode = null; // React отслеживает текущий компонент
let hookIndex = 0;            // Индекс текущего хука

function useState(initialValue) {
  const fiber = currentFiberNode;
  const hooks = fiber.memoizedState;
  
  // Получаем текущий хук по индексу
  let hook = hooks[hookIndex];
  
  if (!hook) {
    // Первый рендер - создаем новый хук
    hook = {
      memoizedState: typeof initialValue === 'function' 
        ? initialValue() 
        : initialValue,
      next: null
    };
    hooks[hookIndex] = hook;
  }
  
  hookIndex++; // Переходим к следующему хуку
  
  const setState = (newValue) => {
    hook.memoizedState = typeof newValue === 'function'
      ? newValue(hook.memoizedState)
      : newValue;
    // Планирует обновление компонента
    scheduleUpdate(fiber);
  };
  
  return [hook.memoizedState, setState];
}
```

#### **3. Почему важен порядок вызова хуков?**

**Правило:** Хуки должны вызываться **в одном и том же порядке** при каждом рендере.

**Почему?** React использует **индекс** для доступа к хукам в списке:

```jsx
// ✅ Правильно - порядок всегда одинаковый
function Component() {
  const [name, setName] = useState('');      // Индекс 0
  const [age, setAge] = useState(0);        // Индекс 1
  const [email, setEmail] = useState('');    // Индекс 2
  return <div>...</div>;
}

// ❌ Неправильно - порядок меняется
function Component({ condition }) {
  if (condition) {
    const [name, setName] = useState('');   // Индекс 0 (только если condition = true)
  }
  const [age, setAge] = useState(0);         // Индекс 0 или 1? Проблема!
  return <div>...</div>;
}
```

**Что произойдет при нарушении порядка:**

```jsx
// Первый рендер: condition = true
// useState('') -> индекс 0 -> хранится в hooks[0]
// useState(0)  -> индекс 1 -> хранится в hooks[1]

// Второй рендер: condition = false
// useState(0)  -> индекс 0 -> читает из hooks[0] (ожидает строку, получает число!)
// Ошибка или неправильное поведение!
```

#### **4. Как React находит правильный Fiber Node?**

React использует **внутренний контекст выполнения**:

```js
// React поддерживает стек компонентов во время рендеринга
let currentlyRenderingFiber = null;

function renderComponent(Component, props) {
  // Создаем или находим Fiber Node
  const fiber = getOrCreateFiber(Component);
  
  // Устанавливаем как текущий
  currentlyRenderingFiber = fiber;
  hookIndex = 0; // Сбрасываем индекс хуков
  
  // Вызываем компонент
  const result = Component(props);
  
  // Очищаем контекст
  currentlyRenderingFiber = null;
  
  return result;
}
```

#### **5. Что происходит при обновлении состояния?**

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  const handleClick = () => {
    setCount(count + 1); // Что происходит здесь?
  };
  
  return <button onClick={handleClick}>{count}</button>;
}
```

**Процесс обновления:**

1. **`setCount` вызывается** → React находит Fiber Node компонента
2. **Обновляется значение** в `fiber.memoizedState` (в соответствующем хуке)
3. **Планируется обновление** компонента (добавляется в очередь обновлений)
4. **React перерисовывает** компонент с новым значением
5. **При повторном рендере** `useState` читает новое значение из Fiber Node

```js
// Упрощенный процесс
setCount(5) 
  → fiber.memoizedState[hookIndex].memoizedState = 5
  → scheduleUpdate(fiber)
  → renderComponent(Counter, props)
  → useState(0) // Читает 5 из fiber.memoizedState, игнорируя initialValue
  → return [5, setCount]
```

**Важно:** При повторном рендере `initialValue` в `useState(initialValue)` **игнорируется** — используется значение из Fiber Node.

---

### 🎯 Краткое объяснение для собеседования

**Вопрос:** "Где хранится состояние в `useState`?"

**Ответ:**
"Состояние хранится не в самом компоненте, а в **Fiber Node** — внутреннем объекте React, связанном с компонентом. React использует связанный список хуков внутри Fiber Node, где каждый хук хранит своё состояние. При вызове `useState` React находит текущий компонент через внутренний контекст, обращается к соответствующему Fiber Node и читает/записывает значение из списка хуков по индексу. Поэтому важно, чтобы хуки вызывались в одном порядке — React использует индекс для доступа к правильному состоянию."

---

### ⚠️ Важно помнить:

- Не вызывай `setState` напрямую в теле компонента (вызовет бесконечный цикл)
- Обновления состояния асинхронные
- Для объектов/массивов используй функциональное обновление или создавай новый объект

Управление состоянием с объектом в useState

```jsx
import React, { useState } from 'react';
function UserForm() {
  const [user, setUser] = useState({ name: '', email: '' });
  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setUser((prev) => ({ ...prev, [name]: value }));
  };
  return (
    <div>
      <input
        type="text"
        name="name"
        placeholder="Имя"
        value={user.name}
        onChange={handleInputChange}
      />
      <input
        type="email"
        name="email"
        placeholder="Email"
        value={user.email}
        onChange={handleInputChange}
      />
      <p>Имя: {user.name}</p>
      <p>Email: {user.email}</p>
    </div>
  );
}
export default UserForm;
    
```