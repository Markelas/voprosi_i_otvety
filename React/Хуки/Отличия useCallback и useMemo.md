## 🎯 Отличия `useCallback` и `useMemo`

---

### 🔹 `useCallback`

**Возвращает мемоизированную версию функции**, которая не будет пересоздаваться, если зависимости не изменились.

Используется для оптимизации производительности, чтобы не создавать новую функцию при каждом рендере, особенно если функцию передают в дочерние компоненты.

```js
const memoizedCallback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

**Когда использовать:**
- Передаёте функцию в дочерний компонент, который оптимизирован через `React.memo`
- Хотите избежать лишних эффектов в `useEffect`, где функция — зависимость

---

### 🔹 `useMemo`

**Возвращает мемоизированное значение**, вычисляемое функцией.

Используется для **дорогих вычислений**, чтобы результат не пересчитывался при каждом рендере, если зависимости не изменились.

```js
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(a, b);
}, [a, b]);
```

**Когда использовать:**
- Тяжёлые вычисления (фильтрация, сортировка больших массивов)
- Создание объектов/массивов, которые передаются в мемоизированные компоненты

---

### 📊 РАЗНИЦА

| Хук | Возвращает | Назначение |
|-----|------------|------------|
| **`useCallback(fn, deps)`** | Мемоизированную **функцию** | Избежать лишних ререндеров (часто вместе с `React.memo`) |
| **`useMemo(calc, deps)`** | **Результат вычислений** | Кэшировать результат дорогих вычислений |

---

### 🧠 Что такое мемоизация?

**Мемоизация** — это оптимизация: мы кэшируем результат функции. Если входные данные не изменились, мы не выполняем заново вычисления, а используем уже готовый результат.

---

### ⚠️ Важно:

Не злоупотребляй мемоизацией — она сама потребляет ресурсы. Используй только там, где это действительно нужно для производительности.
