## 🎣 Все хуки React

Обзор всех хуков React, разделённых по категориям и назначению.

### 📦 Основные хуки (State & Effects)

<font color="#4a52c7">Эти хуки используются в большинстве React-компонентов для управления состоянием и побочными эффектами.</font>

**<font color="#4a52c7">**useState**</font> - <font color="#7030a0">Управление локальным состоянием компонента</font>**

Позволяет добавлять состояние в функциональные компоненты. Возвращает текущее значение состояния и функцию для его обновления.

```js
const [count, setCount] = useState(0);
```

**Когда использовать:**
- Хранение локального состояния компонента
- Управление формой, счётчиками, флагами
- Любые данные, которые должны вызывать ререндер при изменении

---

**<font color="#4a52c7">**useEffect**</font> - <font color="#7030a0">Побочные эффекты: запросы, подписки, DOM</font>**

Позволяет выполнять побочные эффекты в функциональных компонентах. Выполняется после рендера компонента.

```js
useEffect(() => {
  // Побочный эффект
  return () => {
    // Очистка
  };
}, [dependencies]);
```

**Когда использовать:**
- Запросы к API
- Подписки на события
- Работа с DOM
- Таймеры и интервалы
- Интеграция со сторонними библиотеками

---

**<font color="#4a52c7">**useContext**</font> - <font color="#7030a0">Доступ к контексту без пробрасывания props</font>**

Позволяет подписаться на контекст React без вложенности компонентов. Избегает "prop drilling" — передачи props через множество уровней.

```js
const value = useContext(MyContext);
```

**Когда использовать:**
- Передача данных через несколько уровней компонентов
- Глобальное состояние (тема, язык, пользователь)
- Избежание prop drilling

---

<font color="#4a52c7">**`useReducer`**</font> - <font color="#7030a0">Управление сложным состоянием через reducer</font>

Альтернатива `useState` для управления сложным состоянием. Принимает reducer-функцию и начальное состояние, возвращает текущее состояние и функцию dispatch.

```js
const [state, dispatch] = useReducer(reducer, initialState);
```

**Когда использовать:**
- Сложное состояние с множественной логикой обновления
- Состояние с несколькими подзначениями
- Логика обновления состояния в одном месте
- Альтернатива `useState` для сложных случаев

---

<font color="#4a52c7">**`useRef`**</font> - <font color="#7030a0">Доступ к DOM и хранение мутабельных значений</font>

Возвращает мутабельный объект с свойством `current`. Изменение `ref.current` не вызывает ререндер компонента.

```js
const inputRef = useRef(null);
```

**Когда использовать:**
- Прямой доступ к DOM-элементам (фокус, прокрутка, измерения)
- Хранение таймеров, интервалов
- Сохранение предыдущих значений
- Интеграция со сторонними библиотеками
- Значения, которые не должны вызывать ререндер

---

### ⚡ Хуки для оптимизации

<font color="#4a52c7">Эти хуки помогают оптимизировать производительность компонентов, предотвращая ненужные ререндеры и вычисления.</font>

<font color="#4a52c7">**`useMemo`**</font> - <font color="#7030a0">Мемоизация вычислений</font>

Возвращает мемоизированное значение. Вычисление выполняется только при изменении зависимостей.

```js
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(a, b);
}, [a, b]);
```

**Когда использовать:**
- Тяжёлые вычисления (фильтрация, сортировка больших массивов)
- Создание объектов/массивов для мемоизированных компонентов
- Оптимизация производительности при дорогих операциях

---


**<font color="#4a52c7">**useCallback**</font> - <font color="#7030a0">Мемоизация функций</font>**

Возвращает мемоизированную версию функции. Функция не пересоздаётся, если зависимости не изменились.

```js
const memoizedCallback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

**Когда использовать:**
- Передача функций в мемоизированные дочерние компоненты (`React.memo`)
- Функции как зависимости в `useEffect`
- Предотвращение лишних ререндеров дочерних компонентов

---

**<font color="#4a52c7">**React.memo**</font> - <font color="#7030a0">Мемоизация компонента</font>**


Высокоуровневый компонент для мемоизации. Компонент не ререндерится, если props не изменились.

```js
const MemoizedComponent = React.memo(MyComponent);
```

**Когда использовать:**
- Компоненты, которые часто ререндерятся с теми же props
- Дорогие компоненты для рендеринга
- Оптимизация производительности приложения

---

### 🔧 Дополнительные хуки

<font color="#4a52c7">Специализированные хуки для конкретных задач и продвинутых сценариев.</font>

**<font color="#4a52c7">**useLayoutEffect**</font> - <font color="#7030a0">Синхронные эффекты до отрисовки DOM</font>**

Аналог `useEffect`, но выполняется синхронно после всех мутаций DOM, но до того, как браузер отрисует изменения.

```js
useLayoutEffect(() => {
  // Синхронный эффект
}, [dependencies]);
```

**Когда использовать:**
- Измерения DOM перед отрисовкой
- Анимации, требующие синхронного выполнения
- Предотвращение визуального "мигания"
- Работа с прокруткой

---

**<font color="#4a52c7">**useImperativeHandle**</font> - <font color="#7030a0">Кастомизация ref, передаваемого родительскому компоненту</font>**

Позволяет настроить значение, которое будет доступно через `ref` в родительском компоненте.

```js
useImperativeHandle(ref, () => ({
  focus: () => {
    inputRef.current.focus();
  }
}));
```

**Когда использовать:**
- Ограничение API, доступного через ref
- Создание кастомных методов для родительского компонента
- Использование вместе с `forwardRef`

---

<font color="#4a52c7">**`useDebugValue`**</font> - <font color="#7030a0">Отображение метки для кастомных хуков в React DevTools</font>

Позволяет отображать кастомную метку для кастомных хуков в инструментах разработчика React.

```js
useDebugValue(isOnline ? 'Online' : 'Offline');
```

**Когда использовать:**
- Отладка кастомных хуков
- Улучшение опыта разработки в React DevTools
- Документирование состояния хука

### 📚 Кастомные хуки

Кастомные хуки — это функции, которые начинаются с "use" и могут вызывать другие хуки. Они позволяют переиспользовать логику состояния между компонентами.

**Правила кастомных хуков:**
- Имя должно начинаться с "use"
- Могут вызывать другие хуки
- Позволяют переиспользовать логику состояния

**Пример:**

```js
function useCounter(initialValue = 0) {
  const [count, setCount] = useState(initialValue);
  
  const increment = () => setCount(c => c + 1);
  const decrement = () => setCount(c => c - 1);
  const reset = () => setCount(initialValue);
  
  return { count, increment, decrement, reset };
}
```

---

### 📊 Сравнительная таблица хуков

| Хук | Категория | Триггерит ререндер | Назначение |
|-----|-----------|-------------------|------------|
| `useState` | Основной | ✅ Да | Управление состоянием |
| `useEffect` | Основной | ❌ Нет | Побочные эффекты |
| `useContext` | Основной | ✅ Да (при изменении контекста) | Доступ к контексту |
| `useReducer` | Основной | ✅ Да | Сложное состояние |
| `useRef` | Основной | ❌ Нет | DOM-доступ, мутабельные значения |
| `useMemo` | Оптимизация | ❌ Нет | Мемоизация вычислений |
| `useCallback` | Оптимизация | ❌ Нет | Мемоизация функций |
| `useLayoutEffect` | Дополнительный | ❌ Нет | Синхронные эффекты |
| `useImperativeHandle` | Дополнительный | ❌ Нет | Кастомизация ref |
| `useDebugValue` | Дополнительный | ❌ Нет | Отладка в DevTools |

---

### 🎯 Когда какой хук использовать?

#### **Для состояния:**
- **`useState`** — простое локальное состояние
- **`useReducer`** — сложное состояние с множественной логикой
- **`useContext`** — глобальное состояние, доступное многим компонентам

#### **Для эффектов:**
- **`useEffect`** — большинство побочных эффектов (асинхронно)
- **`useLayoutEffect`** — синхронные эффекты, требующие измерения DOM

#### **Для оптимизации:**
- **`useMemo`** — дорогие вычисления
- **`useCallback`** — функции, передаваемые в мемоизированные компоненты
- **`React.memo`** — мемоизация компонентов

#### **Для DOM:**
- **`useRef`** — прямой доступ к DOM-элементам

---

### ⚠️ Важные правила хуков

1. **Хуки можно вызывать только на верхнем уровне** — не внутри условий, циклов или вложенных функций
2. **Хуки можно вызывать только в функциональных компонентах** или кастомных хуках
3. **Порядок вызова хуков должен быть одинаковым** при каждом рендере
4. **Имена кастомных хуков должны начинаться с "use"**

---

### 🔗 Связанные темы

- [[useState]] — подробнее про управление состоянием
- [[useRef]] — подробнее про доступ к DOM
- [[useEffect и useLayoutEffect]] — подробнее про эффекты
- [[Отличия useCallback и useMemo]] — подробнее про оптимизацию
- [[Что такое мемоизация]] — подробнее про мемоизацию

