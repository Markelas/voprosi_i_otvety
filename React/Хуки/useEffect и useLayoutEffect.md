## ⚡ Разница между `useEffect` и `useLayoutEffect`

`useEffect` и `useLayoutEffect` — это хуки, которые позволяют выполнять побочные эффекты в функциональных компонентах. Оба они предназначены для выполнения кода после рендеринга компонента, но их поведение различается по времени выполнения и влиянию на рендеринг.

---

### 🔸 `useEffect`

Выполняется **после того, как браузер закончил обновлять DOM**.

Это означает, что все изменения в DOM уже произошли, и браузер уже отрисовал интерфейс.

**Используется для:**
- Выполнения побочных эффектов, которые не блокируют отрисовку
- Сетевых запросов
- Подписок на события
- Манипуляций с DOM (не критичные к рендерингу)
- Регистрации таймеров

**Не блокирует рендеринг.** Это означает, что пользователь может видеть обновленный интерфейс перед выполнением побочного эффекта.

**Зависимости:**
- Если нет массива с зависимостями — эффект будет выполняться после каждого рендера
- Если пустой `[]` — то один раз при монтировании компонента (такое нужно, например, для `fetch`)

---

### 🔹 `useLayoutEffect`

Выполняется **синхронно после всех мутировавших эффектов DOM** (пока чисто ДОМ дерево, до их изменений), но **перед тем, как браузер обновит экран**.

Это означает, что изменения, сделанные в `useLayoutEffect`, будут видны пользователю до того, как браузер нарисует обновленный интерфейс.

**Используется для:**
- Измерения размеров DOM-элементов
- Выполнения синхронных изменений в DOM, которые должны произойти перед отрисовкой
- Операций, которые блокируют отрисовку (анимации, позиционирование)

**Блокирует отрисовку** до тех пор, пока эффект не будет выполнен. Это может привести к задержке в отображении интерфейса, если эффект выполняется долго.

---

### 📊 Основные различия

| Параметр | `useEffect` | `useLayoutEffect` |
|----------|-------------|------------------|
| **Время выполнения** | Асинхронно после обновления экрана | Синхронно перед обновлением экрана |
| **Блокирует отрисовку** | ❌ Нет | ✅ Да |
| **Использование** | Побочные эффекты, которые не влияют на компоновку и отрисовку | Эффекты, которые должны быть выполнены перед отрисовкой (измерение, изменения DOM) |

---

### 💡 Рекомендация

**Используй `useEffect` по умолчанию**, и переходи на `useLayoutEffect` только если нужно измерить DOM или изменить его до отрисовки.

Запрос на сервер при изменении пропсов

```jsx
function DataFetcher({ url }) {
  const [data, setData] = useState(null);
  useEffect(() => {
    const fetchData = async () => {
      const response = await fetch(url);
      const result = await response.json();
      setData(result);
    };
    fetchData();
  }, [url]); // Повторный вызов эффекта при изменении URL
  if (!data) return <p>Загрузка...</p>;
  return <div>{JSON.stringify(data)}</div>;
}
```