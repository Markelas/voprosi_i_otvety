## 🔗 Что такое `useRef`

**Рефы (сокращение от "references")** — это особый инструмент React, который позволяет получить доступ к DOM-элементам или хранить любой другой изменяемый объект. В отличие от состояния (`state`), изменения рефов **не вызывают повторного рендера компонента**, что делает их идеальными для определённых случаев, таких как взаимодействие с DOM напрямую или сохранение данных, которые должны изменяться вне цикла рендеринга.

---

## 🎯 Для чего нужны `ref`?

1. **Доступ к DOM-элементам** — прямое взаимодействие с элементами (фокус, прокрутка, измерения)
2. **Хранение mutable объектов** между рендерами — таймеры, счётчики, ссылки на элементы
3. **Интеграция со сторонними библиотеками** — работа с canvas, библиотеками анимаций или сторонними компонентами
4. **Хранение состояния, которое не влияет на UI** — значения, которые не должны вызывать ререндер

---

## 📝 Что такое `current`?

**`current`** — это единственное свойство объекта, который возвращает `useRef`.

- Если `useRef` используется для **данных** — `current` содержит твои данные
- Если `useRef` используется для **DOM-элемента** — React сам присвоит `current` реальный DOM-элемент

```js
const timerRef = useRef(null);

// Сохраняем данные
timerRef.current = setTimeout(() => {}, 1000);

// Получаем DOM
const inputRef = useRef(null);
inputRef.current.focus(); // работаем с DOM
```

---

## 🔄 Отличие от `useState`:

| Параметр | `useState` | `useRef` |
|----------|------------|----------|
| **Триггерит ререндер** | ✅ Да | ❌ Нет |
| **Доступ к DOM** | ❌ Нет | ✅ Да |
| **Сохранение между рендерами** | ✅ Да | ✅ Да |
| **Изменение мутабельно** | ❌ Нет (иммутабельно) | ✅ Да |
| **Когда использовать** | Данные, влияющие на UI | DOM-доступ, значения без ререндера |

---

## 💻 Создание рефа

Рефы создаются с помощью хука `useRef()`:

```js
import React, { useRef } from 'react';

const myRef = useRef(null); // Создаём реф с начальным значением null
```

---

## 🎯 Основные применения с примерами

### 1. **Программный фокус и выделение текста**

Рефы часто используются для управления фокусом, что полезно для создания полей ввода, которые автоматически получают фокус при загрузке страницы.

```js
import React, { useEffect, useRef } from 'react';

function AutoFocusInput() {
  const inputRef = useRef(null);

  useEffect(() => {
    // Автоматический фокус на поле ввода при загрузке компонента
    inputRef.current.focus();
  }, []);

  return <input ref={inputRef} type="text" placeholder="Автоматический фокус" />;
}

export default AutoFocusInput;
```

**Пример с кнопкой для фокуса:**

```js
function InputFocusExample() {
  const inputRef = useRef(null);

  const handleFocus = () => {
    inputRef.current.focus(); // Доступ к DOM-элементу
  };

  const handleSelect = () => {
    inputRef.current.select(); // Выделение всего текста
  };

  return (
    <div>
      <input ref={inputRef} type="text" placeholder="Введите текст здесь..." />
      <button onClick={handleFocus}>Фокус на поле ввода</button>
      <button onClick={handleSelect}>Выделить текст</button>
    </div>
  );
}
```

---

### 2. **Измерение размеров или положения DOM-элемента**

Иногда нужно измерить размеры или позицию элемента на странице.

```js
import React, { useRef, useEffect, useState } from 'react';

function MeasureExample() {
  const divRef = useRef(null);
  const [rect, setRect] = useState({ width: 0, height: 0, x: 0, y: 0 });

  useEffect(() => {
    // Измеряем размеры и позицию блока
    const measurements = divRef.current.getBoundingClientRect();
    setRect({
      width: measurements.width,
      height: measurements.height,
      x: measurements.x,
      y: measurements.y
    });
  }, []);

  return (
    <div>
      <div 
        ref={divRef} 
        style={{ width: '200px', height: '100px', background: 'lightblue' }}
      >
        Измеряем этот блок
      </div>
      <p>Ширина: {rect.width}px, Высота: {rect.height}px</p>
      <p>Позиция: x={rect.x}px, y={rect.y}px</p>
    </div>
  );
}
```

**Пример с отслеживанием изменений размера:**

```js
function ResizeObserverExample() {
  const divRef = useRef(null);
  const [dimensions, setDimensions] = useState({ width: 0, height: 0 });

  useEffect(() => {
    const resizeObserver = new ResizeObserver(entries => {
      for (let entry of entries) {
        setDimensions({
          width: entry.contentRect.width,
          height: entry.contentRect.height
        });
      }
    });

    if (divRef.current) {
      resizeObserver.observe(divRef.current);
    }

    return () => {
      resizeObserver.disconnect();
    };
  }, []);

  return (
    <div>
      <div ref={divRef} style={{ padding: '20px', border: '1px solid' }}>
        Размеры: {dimensions.width} × {dimensions.height}
      </div>
    </div>
  );
}
```

---

### 3. **Сохранение изменяемых значений между рендерами (таймеры, интервалы)**

Рефы могут использоваться для хранения **изменяемых данных**, которые не должны приводить к перерисовке компонента.

```js
import React, { useRef, useState } from 'react';

function TimerComponent() {
  const [count, setCount] = useState(0);
  const intervalRef = useRef(null); // Используем реф для хранения ID таймера

  const startTimer = () => {
    // Сохраняем ID таймера в реф
    intervalRef.current = setInterval(() => {
      setCount((prev) => prev + 1);
    }, 1000);
  };

  const stopTimer = () => {
    // Очищаем таймер, используя сохранённый ID
    clearInterval(intervalRef.current);
  };

  const resetTimer = () => {
    stopTimer();
    setCount(0);
  };

  return (
    <div>
      <p>Секунды: {count}</p>
      <button onClick={startTimer}>Старт</button>
      <button onClick={stopTimer}>Стоп</button>
      <button onClick={resetTimer}>Сброс</button>
    </div>
  );
}
```

**Объяснение:**
- Мы используем реф для хранения ID интервала
- Изменение значения `intervalRef` не вызывает рендер, поскольку React следит только за изменением состояния (`count`)
- Это позволяет управлять таймером без лишних ререндеров

---

### 4. **Хранение предыдущего значения**

Иногда нужно хранить предыдущее значение состояния или пропсов, чтобы сравнить, как изменилось значение между рендерами.

```js
import React, { useEffect, useRef, useState } from 'react';

function PreviousValueExample({ value }) {
  const prevValue = useRef(value);

  useEffect(() => {
    // Обновляем предыдущее значение на каждом рендере
    prevValue.current = value;
  }, [value]);

  return (
    <div>
      <p>Текущее значение: {value}</p>
      <p>Предыдущее значение: {prevValue.current}</p>
    </div>
  );
}
```

**Пример с отслеживанием изменений:**

```js
function ChangeTracker({ count }) {
  const prevCountRef = useRef();
  const [hasChanged, setHasChanged] = useState(false);

  useEffect(() => {
    if (prevCountRef.current !== undefined && prevCountRef.current !== count) {
      setHasChanged(true);
      console.log(`Значение изменилось с ${prevCountRef.current} на ${count}`);
    }
    prevCountRef.current = count;
  }, [count]);

  return (
    <div>
      <p>Счётчик: {count}</p>
      {hasChanged && <p>Значение изменилось!</p>}
    </div>
  );
}
```

---

### 5. **Интеграция со сторонними библиотеками**

Рефы полезны для работы с библиотеками, которые требуют прямого доступа к DOM-элементу (например, canvas, D3.js, анимации).

```js
import React, { useEffect, useRef } from 'react';

function CanvasExample() {
  const canvasRef = useRef(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    
    // Рисуем на canvas
    ctx.fillStyle = 'blue';
    ctx.fillRect(10, 10, 100, 100);
  }, []);

  return <canvas ref={canvasRef} width={200} height={200} />;
}
```

**Пример с библиотекой анимаций:**

```js
import { useEffect, useRef } from 'react';
import { gsap } from 'gsap';

function AnimatedBox() {
  const boxRef = useRef(null);

  useEffect(() => {
    // Используем GSAP для анимации через ref
    gsap.to(boxRef.current, {
      x: 100,
      rotation: 360,
      duration: 2
    });
  }, []);

  return (
    <div ref={boxRef} style={{ width: '50px', height: '50px', background: 'red' }}>
      Box
    </div>
  );
}
```

---

### 6. **Прокрутка к элементу**

Рефы можно использовать для программной прокрутки к определённому элементу.

```js
function ScrollToElement() {
  const targetRef = useRef(null);

  const scrollToTarget = () => {
    targetRef.current.scrollIntoView({ behavior: 'smooth', block: 'center' });
  };

  return (
    <div>
      <button onClick={scrollToTarget}>Прокрутить к элементу</button>
      <div style={{ height: '100vh' }}>Прокрутите вниз</div>
      <div ref={targetRef} style={{ height: '200px', background: 'yellow' }}>
        Целевой элемент
      </div>
    </div>
  );
}
```

---

### 7. **Хранение значений без ререндера**

Когда нужно хранить значение, которое не должно вызывать ререндер при изменении.

```js
function RenderCounter() {
  const renderCount = useRef(0);
  const [trigger, setTrigger] = useState(0);

  renderCount.current += 1; // Не вызывает ререндер!

  return (
    <div>
      <p>Компонент отрендерился {renderCount.current} раз</p>
      <p>Триггер: {trigger}</p>
      <button onClick={() => setTrigger(prev => prev + 1)}>
        Обновить (вызовет ререндер)
      </button>
    </div>
  );
}
```

---

## 🔄 Forwarding refs (передача рефов)

Иногда требуется передать реф вниз по иерархии компонентов. Для этого используется `forwardRef`.

```js
import React, { useRef, forwardRef } from 'react';

// Дочерний компонент с forwardRef
const CustomInput = forwardRef((props, ref) => (
  <input ref={ref} type="text" {...props} placeholder="Переданный реф" />
));

// Родительский компонент
function ForwardRefExample() {
  const inputRef = useRef(null);

  const handleClick = () => {
    inputRef.current.focus(); // Фокус на input внутри CustomInput
  };

  return (
    <div>
      <CustomInput ref={inputRef} />
      <button onClick={handleClick}>Фокус на поле ввода</button>
    </div>
  );
}
```

**Объяснение:**
- `forwardRef` позволяет передать реф компоненту `CustomInput`
- Теперь `inputRef` в `ForwardRefExample` ссылается на внутренний `<input>` компонента `CustomInput`
- Реф передаётся вторым параметром (не через `props`)

**Пример с кастомной логикой:**

```js
const MyInput = forwardRef((props, ref) => {
  const handleFocus = () => {
    ref.current.focus();
  };
  
  return (
    <div>
      <input {...props} ref={ref} />
      <button onClick={handleFocus}>Focus</button>
    </div>
  );
});
```

---

## ⚠️ Когда использовать рефы?

Рефы следует использовать только в тех случаях, когда нужно:

✅ **Программное управление фокусом или выделением текста**  
✅ **Измерение размеров или позиции элементов**  
✅ **Сохранение значений, которые не должны сбрасываться при повторном рендере**  
✅ **Интеграция со сторонними библиотеками**  
✅ **Работа с таймерами и интервалами**  
✅ **Прокрутка к элементам**

❌ **НЕ используйте рефы для:**
- Хранения данных, которые должны вызывать ререндер (используйте `useState`)
- Управления состоянием компонента (используйте `useState`)
- Передачи данных между компонентами (используйте `props`)

---

## 🎓 Важные моменты для собеседования

### <span >**1. Изменение `ref.current` не вызывает ререндер:**</span>

```js
const countRef = useRef(0);

// Это НЕ вызовет ререндер
countRef.current = 10;
```

### <span >**2. `ref.current` может быть `null`:**</span>

Всегда проверяйте наличие элемента перед использованием:

```js
useEffect(() => {
  if (inputRef.current) {
    inputRef.current.focus();
  }
}, []);
```

### <span>**3. Рефы сохраняются между рендерами:**</span>

Значение `ref.current` не сбрасывается при ререндере компонента.

### <span >**4. Рефы не должны использоваться для условного рендеринга:**</span>

```js
// ❌ Неправильно
if (myRef.current) {
  return <div>...</div>;
}

// ✅ Правильно - используйте state
const [isVisible, setIsVisible] = useState(false);
```

### <span >**5. Рефы и `useEffect`:**</span>

Рефы часто используются в `useEffect`, так как они доступны после монтирования компонента:

```js
useEffect(() => {
  // Реф доступен здесь
  inputRef.current?.focus();
}, []);
```

---

## 💡 Итоги

- **Рефы полезны** для прямого взаимодействия с DOM, хранения изменяемых данных между рендерами и интеграции со сторонними библиотеками
- **Используйте их** для программного управления фокусом, измерений или хранения значений, не требующих перерисовки
- **Будьте осторожны:** чрезмерное использование рефов может усложнить логику и нарушить концепцию "однонаправленного потока данных" в React
- **Рефы — это мощный инструмент**, но их нужно применять только в случаях, когда стандартные механизмы React не подходят

---

## 🔗 Связанные темы

- [[Как передать ref из родительского в дочерний компонент]] — использование `forwardRef`
- [[Контролируемые и неконтролируемые компоненты]] — использование рефов в неконтролируемых компонентах
