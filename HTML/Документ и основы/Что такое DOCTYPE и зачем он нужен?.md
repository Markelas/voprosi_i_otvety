DOCTYPE — это специальная строка в начале HTML-документа, которая говорит браузеру, что это HTML5. Благодаря этому браузер рендерит страницу в стандартах, а не в старом режиме совместимости. Если DOCTYPE не указан, могут быть проблемы с отображением.
