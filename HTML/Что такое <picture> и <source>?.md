`<picture>` — контейнер для нескольких изображений с разными форматами или размерами.
`<source>` — задаёт конкретный вариант изображения (по размеру, формату или медиа-запросу), ОН ВЛОЖЕН В `<picture>`. Также, туда же добавляют `<img>` как запасной вариант.