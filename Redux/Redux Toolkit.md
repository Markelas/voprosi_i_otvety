## 🛠️ Redux Toolkit (RTK)

**Redux Toolkit** — это официальный набор инструментов для эффективной разработки приложений на Redux. Он упрощает написание Redux-кода и решает типичные проблемы обычного Redux.

---

### 🎯 Что такое Redux Toolkit?

**Redux Toolkit (RTK)** — это официальная библиотека от команды Redux, которая предоставляет утилиты для упрощения работы с Redux. Она включает в себя лучшие практики и решает проблемы "boilerplate" кода.

**Аналогия:** Если обычный Redux — это набор инструментов для строительства, то Redux Toolkit — это готовый набор с инструкциями и оптимизированными инструментами.

---

### 🔄 Разница между обычным Redux и Redux Toolkit:

#### **1. Создание Store**

**Обычный Redux:**
- Нужно вручную настраивать `createStore`
- Нужно подключать middleware вручную (Thunk, DevTools)
- Больше кода для базовой настройки

**Redux Toolkit:**
- `configureStore` включает Thunk и DevTools по умолчанию
- Автоматическая настройка middleware
- Меньше кода, проще настройка

**Сравнение:**
```js
// Обычный Redux
const store = createStore(reducer, applyMiddleware(thunk));

// Redux Toolkit
const store = configureStore({ reducer });
```

---

#### **2. Создание Reducers и Actions**

**Обычный Redux:**
- Нужно вручную создавать Action Types (константы)
- Нужно создавать Action Creators отдельно
- Нужно писать большой switch-case в Reducer
- Много повторяющегося кода

**Redux Toolkit:**
- `createSlice` автоматически создаёт Actions и Reducers
- Нет необходимости в Action Types
- Меньше кода, всё в одном месте

**Сравнение:**

**Обычный Redux:**
```js
// Action Types
const ADD_TODO = 'ADD_TODO';
const REMOVE_TODO = 'REMOVE_TODO';

// Action Creators
const addTodo = (text) => ({ type: ADD_TODO, payload: text });
const removeTodo = (id) => ({ type: REMOVE_TODO, payload: id });

// Reducer
function todosReducer(state = [], action) {
  switch (action.type) {
    case ADD_TODO:
      return [...state, action.payload];
    case REMOVE_TODO:
      return state.filter(todo => todo.id !== action.payload);
    default:
      return state;
  }
}
```

**Redux Toolkit:**
```js
const todosSlice = createSlice({
  name: 'todos',
  initialState: [],
  reducers: {
    addTodo: (state, action) => {
      state.push(action.payload);
    },
    removeTodo: (state, action) => {
      return state.filter(todo => todo.id !== action.payload);
    }
  }
});
```

---

#### **3. Иммутабельность**

**Обычный Redux:**
- Нужно вручную создавать новые объекты
- Много spread-операторов для глубоких обновлений
- Легко забыть и мутировать состояние

**Redux Toolkit:**
- Использует Immer под капотом
- Можно писать код "как будто" мутируя
- Автоматически создаёт иммутабельные обновления

**Пример:**
```js
// В Redux Toolkit можно писать так (Immer делает это безопасным):
reducers: {
  updateUser: (state, action) => {
    state.user.name = action.payload; // Кажется мутацией, но это безопасно!
  }
}
```

---

#### **4. Асинхронные операции**

**Обычный Redux:**
- Нужно вручную создавать три Actions (START, SUCCESS, ERROR)
- Много boilerplate кода
- Сложная обработка ошибок

**Redux Toolkit:**
- `createAsyncThunk` автоматически создаёт три состояния
- Меньше кода
- Упрощённая обработка ошибок

---

### ✅ Преимущества Redux Toolkit:

#### **1. Меньше boilerplate кода**

Redux Toolkit значительно сокращает количество кода, который нужно писать. Вместо создания Action Types, Action Creators и больших switch-case, всё делается в одном месте через `createSlice`.

**Экономия:** Примерно в 2-3 раза меньше кода для типичных операций.

#### **2. Встроенные лучшие практики**

Redux Toolkit включает лучшие практики по умолчанию:
- Redux Thunk для асинхронных операций
- Redux DevTools для отладки
- Immer для работы с иммутабельностью
- Правильная структура кода

#### **3. Проще для новичков**

Меньше концепций для изучения, проще синтаксис, меньше мест для ошибок. Новичкам легче начать работать с Redux Toolkit.

#### **4. Автоматическая иммутабельность**

Благодаря Immer можно писать код с "мутациями", не беспокоясь о создании новых объектов. Это делает код проще и понятнее.

#### **5. Упрощённая работа с асинхронностью**

`createAsyncThunk` автоматически обрабатывает три состояния (pending, fulfilled, rejected), что значительно упрощает работу с API.

#### **6. Официальная поддержка**

Redux Toolkit — это официальная библиотека от команды Redux. Это означает долгосрочную поддержку, обновления и интеграцию с экосистемой Redux.

#### **7. Меньше ошибок**

Меньше кода = меньше мест для ошибок. Автоматическая генерация Actions уменьшает риск опечаток в типах действий.

---

### 🔧 Основные функции Redux Toolkit:

#### **1. configureStore**

Создаёт Store с предустановленными настройками (Thunk, DevTools, проверка на мутации).

**Что делает:**
- Автоматически подключает Redux Thunk
- Включает Redux DevTools
- Добавляет middleware для проверки мутаций
- Упрощает настройку

**Основные параметры:**

| Параметр | Тип | Описание | Обязательный |
|----------|-----|----------|--------------|
| `reducer` | Function/Object | Корневой reducer или объект с reducers | ✅ Да |
| `middleware` | Array/Function | Массив middleware или функция для настройки | ❌ Нет |
| `devTools` | Boolean/Object | Настройки DevTools (по умолчанию `true`) | ❌ Нет |
| `preloadedState` | Object | Начальное состояние | ❌ Нет |
| `enhancers` | Array | Дополнительные enhancers | ❌ Нет |

---

#### **2. createSlice**

Создаёт Reducer и Actions автоматически из объекта с reducers. Это основная функция Redux Toolkit для создания логики состояния.

**Что делает:**
- Генерирует Action Types автоматически (формат: `sliceName/reducerName`)
- Создаёт Action Creators для каждого reducer
- Объединяет всё в один Reducer
- Использует Immer для иммутабельности (можно писать с "мутациями")
- Возвращает объект с `actions`, `reducer`, `name`, `getInitialState`

**Параметры createSlice:**

| Параметр | Тип | Описание | Обязательный |
|----------|-----|----------|--------------|
| `name` | String | Имя slice (используется в Action Types) | ✅ Да |
| `initialState` | Any | Начальное состояние (может быть функцией) | ✅ Да |
| `reducers` | Object | Объект с reducer-функциями | ✅ Да |
| `extraReducers` | Object/Function | Reducers для внешних Actions (например, из `createAsyncThunk`) | ❌ Нет |

**Что возвращает createSlice:**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `actions` | Object | Объект с Action Creators для каждого reducer |
| `reducer` | Function | Reducer функция для использования в Store |
| `name` | String | Имя slice |
| `getInitialState` | Function | Функция для получения начального состояния |

**Пример структуры:**
```js
const todosSlice = createSlice({
  name: 'todos',
  initialState: [],
  reducers: {
    addTodo: (state, action) => { /* ... */ },
    removeTodo: (state, action) => { /* ... */ }
  }
});

// todosSlice.actions.addTodo - Action Creator
// todosSlice.reducer - Reducer функция
```

**Поля в объекте `reducers`:**
- Каждый ключ становится именем Action Type: `todos/addTodo`
- Каждая функция получает `state` и `action`
- Можно мутировать `state` (Immer создаст новый объект)
- `action.payload` содержит данные, переданные в Action Creator

**Поля в `extraReducers`:**
- Используется для обработки Actions из других источников
- Может быть объектом с `builder` или функцией `(builder) => {}`
- Часто используется с `createAsyncThunk` для обработки `pending`, `fulfilled`, `rejected`

---

#### **3. createAsyncThunk**

Создаёт асинхронный Thunk с автоматической обработкой трёх состояний.

**Что делает:**
- Автоматически создаёт три Action Types: `pending`, `fulfilled`, `rejected`
- Упрощает обработку ошибок
- Уменьшает boilerplate код
- Возвращает функцию, которую можно dispatch

**Параметры createAsyncThunk:**

| Параметр | Тип | Описание | Обязательный |
|----------|-----|----------|--------------|
| `typePrefix` | String | Префикс для Action Types (например, `'user/fetchUser'`) | ✅ Да |
| `payloadCreator` | Function | Асинхронная функция, возвращающая данные | ✅ Да |
| `options` | Object | Дополнительные опции (condition, dispatchConditionRejection) | ❌ Нет |

**Что возвращает createAsyncThunk:**

| Свойство | Тип | Описание |
|----------|-----|----------|
| `pending` | String | Action Type для состояния загрузки |
| `fulfilled` | String | Action Type для успешного выполнения |
| `rejected` | String | Action Type для ошибки |
| `typePrefix` | String | Префикс, переданный в функцию |
| Функция | Function | Thunk-функция для dispatch |

**Автоматически создаваемые Action Types:**
- `typePrefix + '/pending'` — начало загрузки
- `typePrefix + '/fulfilled'` — успешное выполнение
- `typePrefix + '/rejected'` — ошибка

**payloadCreator получает:**
- Первый аргумент — данные, переданные при dispatch
- Второй аргумент — объект с `dispatch`, `getState`, `rejectWithValue`, `fulfillWithValue`

---

#### **4. createAction**

Создаёт Action Creator с автоматической генерацией типа.

**Параметры:**

| Параметр | Тип | Описание | Обязательный |
|----------|-----|----------|--------------|
| `type` | String | Тип Action | ✅ Да |
| `prepareAction` | Function | Функция для подготовки payload (опционально) | ❌ Нет |

**Что возвращает:**
- Функцию-Action Creator, которая принимает payload и возвращает Action объект
- Свойство `type` с типом Action
- Свойство `match` для проверки типа Action

**Использование:**
- Обычно не нужен, так как `createSlice` создаёт Actions автоматически
- Полезен для создания отдельных Actions вне slice

---

#### **5. createReducer**

Создаёт Reducer с поддержкой Immer (можно писать с "мутациями").

**Параметры:**

| Параметр | Тип | Описание | Обязательный |
|----------|-----|----------|--------------|
| `initialState` | Any | Начальное состояние | ✅ Да |
| `builderCallback` | Function | Функция с `builder` для добавления case-ов | ✅ Да |

**Что делает:**
- Использует Immer для безопасных "мутаций"
- Позволяет использовать `builder.addCase()`, `builder.addMatcher()`, `builder.addDefaultCase()`
- Альтернатива большому switch-case

**Когда использовать:**
- Когда нужен reducer без создания slice
- Для сложной логики с множеством условий
- Для совместимости с существующим кодом

---

### 📊 Сравнительная таблица:

| Аспект | Обычный Redux | Redux Toolkit |
|--------|---------------|--------------|
| **Настройка Store** | Вручную, много кода | `configureStore`, автоматически |
| **Создание Actions** | Вручную, Action Types | Автоматически в `createSlice` |
| **Reducers** | Большой switch-case | Объект с функциями |
| **Иммутабельность** | Вручную, spread-операторы | Автоматически через Immer |
| **Асинхронность** | Вручную, три Actions | `createAsyncThunk`, автоматически |
| **Boilerplate код** | Много | Минимум |
| **DevTools** | Нужно настраивать | Включены по умолчанию |
| **Thunk** | Нужно подключать | Включён по умолчанию |

---

### 🎯 Когда использовать Redux Toolkit?

**Используйте Redux Toolkit, если:**
- ✅ Начинаете новый проект
- ✅ Хотите меньше кода и больше продуктивности
- ✅ Нужны лучшие практики из коробки
- ✅ Работаете в команде (проще для новичков)

**Можно использовать обычный Redux, если:**
- ⚠️ У вас уже есть большой проект на обычном Redux
- ⚠️ Нужен полный контроль над каждым аспектом
- ⚠️ Используете специфичные middleware, не поддерживаемые RTK

**Рекомендация:** Для новых проектов всегда используйте Redux Toolkit. Это официальный стандарт и будущее Redux.

---

### 💡 Краткий ответ для собеседования:

**Redux Toolkit** — это официальный набор инструментов для упрощения работы с Redux. Основные преимущества: меньше boilerplate кода (в 2-3 раза), встроенные лучшие практики (Thunk, DevTools, Immer), автоматическое создание Actions и Reducers через `createSlice`, упрощённая работа с асинхронностью через `createAsyncThunk`, и возможность писать код с "мутациями" благодаря Immer. Redux Toolkit — это рекомендуемый способ работы с Redux для новых проектов.

---

### 🔍 Типичные вопросы на собеседовании:

**Q: Что такое Redux Toolkit и зачем он нужен?**
A: Redux Toolkit — это официальная библиотека от команды Redux, которая упрощает работу с Redux. Она решает проблему большого количества boilerplate кода, включает лучшие практики по умолчанию (Thunk, DevTools, Immer) и делает Redux проще в использовании.

**Q: В чём основная разница между обычным Redux и Redux Toolkit?**
A: Основные отличия: Redux Toolkit автоматически создаёт Actions и Reducers через `createSlice`, использует Immer для упрощения работы с иммутабельностью, включает Thunk и DevTools по умолчанию, и значительно сокращает количество кода (в 2-3 раза меньше).

**Q: Можно ли использовать Redux Toolkit с существующим проектом на обычном Redux?**
A: Да, можно постепенно мигрировать. Redux Toolkit совместим с обычным Redux. Можно использовать `configureStore` вместо `createStore` и постепенно переводить reducers на `createSlice`.

**Q: Что такое `createSlice` и чем он лучше обычного Reducer?**
A: `createSlice` автоматически создаёт Action Types, Action Creators и Reducer из одного объекта. Это уменьшает код в 2-3 раза, исключает опечатки в типах действий и использует Immer для безопасной работы с иммутабельностью.

**Q: Как Redux Toolkit работает с иммутабельностью?**
A: Redux Toolkit использует Immer под капотом. Это позволяет писать код с "мутациями" (например, `state.todos.push(...)`), но Immer автоматически создаёт новый иммутабельный объект. Это делает код проще и безопаснее.

**Q: Что такое `createAsyncThunk`?**
A: `createAsyncThunk` — это функция для создания асинхронных Actions. Она автоматически создаёт три Actions: `pending` (начало), `fulfilled` (успех), `rejected` (ошибка). Это упрощает работу с асинхронными операциями и уменьшает boilerplate код.

**Q: Нужно ли устанавливать Redux Thunk отдельно при использовании Redux Toolkit?**
A: Нет, Redux Thunk уже включён в Redux Toolkit по умолчанию. При использовании `configureStore` Thunk подключается автоматически.

**Q: Можно ли использовать Redux Toolkit без Immer?**
A: Технически можно, но не рекомендуется. Immer — это ключевая особенность Redux Toolkit, которая делает код проще. Если нужен полный контроль, можно использовать обычный Redux.

**Q: Как мигрировать с обычного Redux на Redux Toolkit?**
A: Постепенно: заменить `createStore` на `configureStore`, перевести reducers на `createSlice`, заменить ручные Thunk на `createAsyncThunk`. Можно делать это по частям, так как они совместимы.

**Q: В чём преимущества `configureStore` перед `createStore`?**
A: `configureStore` автоматически подключает Redux Thunk, включает Redux DevTools, добавляет middleware для проверки мутаций, упрощает настройку и включает лучшие практики по умолчанию.

**Q: Можно ли использовать middleware с Redux Toolkit?**
A: Да, можно передать middleware в `configureStore` через параметр `middleware`. Redux Toolkit также позволяет настроить существующие middleware или добавить новые.

**Q: Что такое RTK Query и как он связан с Redux Toolkit?**
A: RTK Query — это дополнительный инструмент в Redux Toolkit для работы с API. Он автоматически генерирует Actions и Reducers для запросов к серверу, кэширует данные и упрощает работу с асинхронными запросами.

**Q: Нужно ли знать обычный Redux перед изучением Redux Toolkit?**
A: Желательно понимать основные концепции Redux (Store, Actions, Reducers), но Redux Toolkit можно изучать параллельно или даже начать с него, так как он проще и включает лучшие практики.

**Q: Какие недостатки у Redux Toolkit?**
A: Основные недостатки: больший размер бандла (из-за Immer и других зависимостей), меньше контроля над деталями по сравнению с обычным Redux, и необходимость изучения новых API (`createSlice`, `createAsyncThunk`). Но преимущества обычно перевешивают недостатки.

**Q: Можно ли использовать Redux Toolkit с TypeScript?**
A: Да, Redux Toolkit отлично работает с TypeScript и имеет встроенную поддержку типов. `createSlice` и `createAsyncThunk` автоматически выводят типы для Actions и State.
