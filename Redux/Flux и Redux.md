## 🔄 Flux и Redux

**Flux** — это архитектурный паттерн для управления состоянием приложения, разработанный Facebook. **Redux** — это реализация идей Flux с некоторыми улучшениями.

---

### 🎯 Что такое Flux?

**Flux** — это однонаправленный поток данных (unidirectional data flow), который решает проблему сложного управления состоянием в больших приложениях.

---

### 🏗️ Архитектура Flux:

```
Action → Dispatcher → Store → View
  ↑                              ↓
  └──────────────────────────────┘
```

![[0208b8e50e8d40eeb8d4db589b7c5869.png]]

#### **1. Action** (Действие)
Объект, который описывает, что произошло в приложении.

```js
{ type: 'ADD_TODO', payload: 'Купить молоко' }
```

#### **2. Dispatcher** (Диспетчер)
Центральный хаб, который получает все действия и распределяет их по хранилищам.

#### **3. Store** (Хранилище)
Хранит состояние приложения и логику его обновления.

#### **4. View** (Представление)
Компоненты, которые отображают данные из Store и отправляют Actions.

---

### 🔗 Связь Flux и Redux:

**Redux** — это упрощённая и улучшенная версия Flux:

| Flux | Redux |
|------|-------|
| Множественные Stores | **Один Store** (единое хранилище) |
| Dispatcher (отдельный объект) | **Встроен в Store** |
| Store содержит логику обновления | **Reducer** (чистая функция) |
| Сложная структура | **Проще и предсказуемее** |

---

### ✅ Основные отличия Redux от Flux:

1. **Один Store вместо множества**
   - В Flux может быть несколько Stores
   - В Redux — один централизованный Store

2. **Reducer вместо логики в Store**
   - В Flux Store сам управляет обновлениями
   - В Redux логика вынесена в чистые функции-редьюсеры

3. **Нет Dispatcher**
   - В Flux есть отдельный Dispatcher
   - В Redux dispatch встроен в Store

4. **Иммутабельность**
   - Redux требует неизменяемости состояния
   - Flux не требует строго

---

### 🎯 Зачем нужен Flux/Redux?

**Проблемы без Flux/Redux:**
- ❌ Состояние разбросано по компонентам
- ❌ Сложно отследить, где и как меняется состояние
- ❌ Трудно отлаживать и тестировать

**Преимущества Flux/Redux:**
- ✅ **Единый источник истины** (Single Source of Truth)
- ✅ **Предсказуемость** — все изменения через Actions
- ✅ **Отладка** — легко отследить поток данных
- ✅ **Тестируемость** — чистые функции легко тестировать

---

### 💡 Краткий ответ для собеседования:

**Flux** — это архитектурный паттерн с однонаправленным потоком данных (Action → Dispatcher → Store → View). **Redux** — это упрощённая реализация Flux с одним Store, редьюсерами вместо логики в Store и встроенным dispatch. Redux решает проблему управления состоянием в больших приложениях, обеспечивая предсказуемость и простоту отладки.

---

### 🔍 Каверзные вопросы:

**Q: Почему Redux лучше Flux?**
A: Redux упрощает архитектуру: один Store вместо множества, чистые функции-редьюсеры вместо логики в Store, встроенный dispatch. Это делает код проще, предсказуемее и легче тестируемым.

**Q: Обязательно ли использовать Redux?**
A: Нет. Redux нужен для сложных приложений с большим количеством глобального состояния. Для простых приложений достаточно Context API или локального состояния компонентов.

**Q: В чём главное отличие Flux от MVC?**
A: В MVC данные могут течь в обе стороны (двунаправленный поток), что усложняет отладку. Flux использует однонаправленный поток данных, что делает изменения предсказуемыми.
