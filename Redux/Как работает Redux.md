## ⚙️ Как работает Redux

Redux управляет состоянием приложения через строгий однонаправленный поток данных.

---

### 🔄 Принцип работы:

```
Component → dispatch(action) → Reducer → New State → Component (re-render)
```

**Простыми словами:**
1. Компонент отправляет **Action** (описание того, что нужно сделать)
2. **Dispatch** передаёт Action в Store
3. **Reducer** обрабатывает Action и возвращает новое состояние
4. **Store** обновляется
5. Компоненты получают обновлённое состояние

---

### 🏦 Аналогия с банком:

![[Screenshot at Jan 15 19-04-47.png]]

**Store (Хранилище)** — это сейф с деньгами (состояние приложения)

**Dispatch (Диспетчер)** — это работник банка, который:
- Принимает заявки (Actions) от клиентов (компонентов)
- Проверяет их правильность
- Передаёт в хранилище для обработки

**Reducer** — это правила банка, которые определяют:
- Как изменить количество денег в сейфе
- Какие операции разрешены

**Action** — это заявка клиента (например, "снять 100 рублей")

```js
// Клиент (компонент) хочет снять деньги
dispatch({ type: 'WITHDRAW', amount: 100 });

// Работник банка (dispatch) передаёт заявку в хранилище
// Правила банка (reducer) обрабатывают заявку
// Сейф (store) обновляется
```

---

### 📦 Action (Действие)

**Action** — это простой объект, который описывает, что произошло.

```js
// Структура Action
{
  type: 'ADD_TODO',        // Тип действия (обязательно)
  payload: 'Купить молоко' // Данные (опционально)
}

// Примеры
{ type: 'INCREMENT' }
{ type: 'SET_USER', payload: { name: 'John', age: 30 } }
{ type: 'DELETE_TODO', payload: 5 }
```

**Правила:**
- ✅ Должен иметь поле `type` (строка)
- ✅ Может иметь дополнительные поля (`payload`, `id`, и т.д.)
- ✅ Не должен содержать логику

---

### 🚀 Dispatch (Отправка)

**Dispatch** — это метод Store, который отправляет Action в Reducer. Он является основным способом взаимодействия с store и позволяет обновлять состояние приложения. Диспатч работает синхронно. Это означает, что после вызова dispatch новое состояние будет доступно сразу, и вы можете использовать его в компонентах.

Когда вы вызываете dispatch с определённым action, Redux передает этот action всем редюсерам в приложении. Каждый редюсер проверяет, обрабатывает ли он это действие, и если да, то возвращает новое состояние.

```js
// Создание Store
import { createStore } from 'redux';
const store = createStore(reducer);

// Отправка Action
store.dispatch({ type: 'ADD_TODO', payload: 'Купить молоко' });
```

**Что происходит при dispatch:**
1. Action передаётся в Reducer
2. Reducer вычисляет новое состояние
3. Store обновляется
4. Все подписчики уведомляются об изменении

---

### 🔧 Reducer (Редьюсер)

**Reducer** — это чистая функция, которая принимает текущее состояние и Action, и возвращает новое состояние.
Когда действие отправляется (dispatch), Redux передает текущее состояние и это действие всем редюсерам. Каждый редюсер обрабатывает действие и возвращает новое состояние. Если редюсер не обрабатывает данное действие, он должен просто возвращать текущее состояние.

```js
function todosReducer(state = [], action) {
  switch (action.type) {
    case 'ADD_TODO':
      return [...state, action.payload]; // Новое состояние
    case 'REMOVE_TODO':
      return state.filter(todo => todo !== action.payload);
    default:
      return state; // Возвращаем старое состояние, если Action не распознан
  }
}
```

**Правила Reducer:**
- ✅ Должен быть чистой функцией
- ✅ Не должен мутировать состояние
- ✅ Должен возвращать состояние по умолчанию для неизвестных Actions
- ✅ Должен иметь начальное состояние (default parameter)

---

### 🏪 Store (Хранилище)

**Store** — это объект, который:
- Хранит состояние приложения
- Предоставляет методы `dispatch`, `getState`, `subscribe`

```js
import { createStore } from 'redux';

const store = createStore(reducer);

// Получить текущее состояние
const state = store.getState();

// Отправить Action
store.dispatch({ type: 'ADD_TODO', payload: 'Купить молоко' });

// Подписаться на изменения
const unsubscribe = store.subscribe(() => {
  console.log('State changed:', store.getState());
});

// Отписаться
unsubscribe();
```

---

### 🔗 Полный цикл работы:

```js
// 1. Создаём Reducer
function counterReducer(state = 0, action) {
  switch (action.type) {
    case 'INCREMENT':
      return state + 1;
    case 'DECREMENT':
      return state - 1;
    default:
      return state;
  }
}

// 2. Создаём Store
const store = createStore(counterReducer);

// 3. Компонент отправляет Action
store.dispatch({ type: 'INCREMENT' }); // state = 1
store.dispatch({ type: 'INCREMENT' }); // state = 2
store.dispatch({ type: 'DECREMENT' });  // state = 1

// 4. Получаем состояние
console.log(store.getState()); // 1
```

---

### 💡 Краткий ответ для собеседования:

Redux работает через однонаправленный поток: Component отправляет Action через dispatch → Reducer обрабатывает Action и возвращает новое состояние → Store обновляется → Компоненты получают обновление. Dispatch — это "работник банка", который принимает заявки (Actions) и передаёт их в хранилище (Store) для обработки по правилам (Reducer).

---

### 🔍 Типичные вопросы на собеседования:

**Q: Что происходит внутри при вызове `dispatch(action)`?**
A: Dispatch передаёт Action в Reducer. Reducer вычисляет новое состояние на основе текущего состояния и Action. Store обновляется, и все подписчики (компоненты) уведомляются об изменении.

**Q: Можно ли вызвать dispatch несколько раз подряд?**
A: Да, каждый вызов dispatch обрабатывается синхронно. Redux гарантирует, что состояние обновится последовательно для каждого Action.

**Q: Что вернёт `dispatch(action)`?**
A: `dispatch` возвращает сам Action, который был передан. Это позволяет создавать цепочки действий или логировать их.

**Q: В чём разница между `dispatch` и `action`?**
A: `action` — это объект, описывающий что произошло (`{ type: 'ADD_TODO', payload: '...' }`). `dispatch` — это метод Store, который отправляет Action в Reducer для обработки.

**Q: Что произойдёт, если отправить Action, который не обрабатывается в Reducer?**
A: Reducer вернёт текущее состояние без изменений (через `default` case). Это безопасно и не вызывает ошибок.

**Q: Можно ли отправить Action без `type`?**
A: Технически можно, но это плохая практика. Redux ожидает, что Action будет иметь `type`. Без `type` Reducer не сможет правильно обработать Action.

**Q: Как работает `getState()`?**
A: `getState()` возвращает текущее состояние Store. Это синхронный метод, который просто возвращает ссылку на состояние. Изменения состояния не происходят при вызове `getState()`.

**Q: Что такое `subscribe` и зачем он нужен?**
A: `subscribe(callback)` позволяет подписаться на изменения Store. Callback вызывается каждый раз, когда Store обновляется. Метод возвращает функцию для отписки. В React-Redux это используется внутри `useSelector` и `connect`.

**Q: Можно ли вызвать `dispatch` внутри Reducer?**
A: Нет, это вызовет ошибку. Reducer должен быть чистой функцией без побочных эффектов. Для асинхронных операций нужно использовать middleware (например, Redux Thunk).

**Q: Что произойдёт, если Reducer вернёт `undefined`?**
A: Это вызовет ошибку. Reducer всегда должен возвращать состояние, даже если оно не изменилось. Для начального состояния используется default parameter: `state = []`.

**Q: Как Redux определяет, что состояние изменилось?**
A: Redux сравнивает ссылки на объекты. Если Reducer возвращает новый объект (не мутирует старый), Redux считает, что состояние изменилось. Если вернуть тот же объект, Redux не уведомит подписчиков.

**Q: Можно ли иметь несколько Reducers?**
A: Да, с помощью `combineReducers` можно объединить несколько Reducers в один. Каждый Reducer управляет своей частью состояния.

**Q: Что такое Action Creator и зачем он нужен?**
A: Action Creator — это функция, которая создаёт Action. Вместо `dispatch({ type: 'ADD_TODO', payload: text })` можно использовать `dispatch(addTodo(text))`, где `addTodo` — это Action Creator. Это упрощает создание Actions и позволяет добавлять логику.

**Q: Синхронный или асинхронный dispatch?**
A: `dispatch` синхронный по умолчанию. Для асинхронных операций (API-запросы) нужно использовать middleware, например Redux Thunk, который позволяет dispatch функций вместо объектов.

**Q: Что произойдёт, если в Reducer сделать `return state` вместо нового объекта?**
A: Если вернуть тот же объект (та же ссылка), Redux не уведомит подписчиков об изменении, так как ссылка не изменилась. Компоненты не перерендерятся, даже если внутри объекта что-то изменилось (если была мутация).
