## 🔌 RTK Query

**RTK Query** — это мощный инструмент для работы с API запросами, встроенный в Redux Toolkit. Он автоматизирует кэширование, синхронизацию данных и управление состоянием загрузки.

---

### 🎯 Зачем нужен RTK Query?

**RTK Query** решает проблемы, которые возникают при ручной работе с API в Redux Toolkit:

- ❌ Много boilerplate кода для каждого запроса
- ❌ Нужно вручную управлять состоянием загрузки и ошибок
- ❌ Нет автоматического кэширования
- ❌ Дублируются запросы к одному эндпоинту
- ❌ Сложно синхронизировать данные между компонентами

**RTK Query** автоматизирует всё это и значительно уменьшает количество кода.

---

### 💾 Что такое кэширование и зачем оно нужно?

**Кэширование** — это сохранение результатов запросов в памяти приложения для их повторного использования без повторных обращений к серверу.

#### 🎯 Зачем нужно кэширование:

1. **Уменьшение нагрузки на сервер**
   - Один и тот же запрос не отправляется многократно
   - Экономия ресурсов сервера и сети

2. **Улучшение производительности**
   - Мгновенный отклик при повторном запросе тех же данных
   - Нет ожидания ответа от сервера

3. **Лучший UX**
   - Данные отображаются сразу из кэша
   - Нет "мигания" загрузки при навигации

4. **Синхронизация между компонентами**
   - Несколько компонентов используют одни и те же кэшированные данные
   - Изменения в одном месте автоматически видны везде

5. **Работа офлайн**
   - Можно показывать последние загруженные данные даже без интернета

#### 🔄 Как работает кэширование в RTK Query:

```ts
// Первый запрос — идёт на сервер
const { data } = useGetUsersQuery(); // ⏳ Загрузка...

// Второй запрос (тот же) — берётся из кэша
const { data } = useGetUsersQuery(); // ✅ Мгновенно из кэша

// После mutation — кэш инвалидируется и обновляется
createUser({ name: 'New' }); // 🔄 Кэш обновлён
```

**Ключевые моменты:**
- Кэш хранится в Redux store
- Каждый уникальный запрос (URL + параметры) имеет свой ключ в кэше
- Кэш автоматически инвалидируется при обновлении данных через mutations
- Неиспользуемые данные удаляются через `keepUnusedDataFor`

---

### 📊 Сравнение RTK Query vs Ручная реализация:

| Функция | RTK Query | Ручная реализация | Пояснение |
|---------|-----------|-------------------|-----------|
| **Кэширование** | ✅ Автоматически | ❌ Вручную | Сохранение результатов запросов для переиспользования |
| **Инвалидация кэша** | ✅ Автоматически | ❌ Вручную | Автоматическая пометка кэша как устаревшего при обновлении данных |
| **Дублирующие запросы** | ✅ Предотвращает | ❌ Нужно реализовывать | Если несколько компонентов запрашивают одно — отправляется один запрос |
| **Загрузка/ошибка** | ✅ Автоматически | ❌ Вручную | Состояния `isLoading`, `isError` управляются автоматически |
| **Оптимистичные обновления** | ✅ Поддержка | ❌ Сложная реализация | Обновление UI до получения ответа от сервера |
| **Prefetching** | ✅ Встроенный | ❌ Вручную | Предзагрузка данных до того, как они понадобятся |
| **Отмена запросов** | ✅ Автоматически | ❌ Вручную | Отмена запросов при размонтировании компонента |
| **Retry логика** | ✅ Настраиваемая | ❌ Вручную | Автоматические повторные попытки при ошибках |
| **Повторная отправка при фокусе** | ✅ Настраивается | ❌ Вручную | Обновление данных при возврате фокуса на вкладку |
| **Сериализация ключей** | ✅ Автоматически | ❌ Вручную | Автоматическое создание уникальных ключей для кэша |

---

### 🔑 Основные параметры и функции RTK Query:

| Параметр/Функция | Что делает | Пример |
|------------------|------------|--------|
| **`createApi`** | Главная функция для создания API. Создаёт reducer, middleware и автоматически генерирует хуки для React. Объединяет все эндпоинты в один API slice. | `createApi({ reducerPath: 'api', ... })` |
| **`fetchBaseQuery`** | Базовый query функция на основе fetch API. Настраивает базовый URL, заголовки, обработку ошибок для всех запросов. Можно заменить на axios или другой HTTP клиент. | `fetchBaseQuery({ baseUrl: '/api' })` |
| **`builder.query`** | Создаёт эндпоинт для чтения данных (GET запросы). Автоматически кэширует результаты, управляет состоянием загрузки. Генерирует хук `useQueryNameQuery`. | `getUsers: builder.query<User[], void>({ query: () => 'users' })` |
| **`builder.mutation`** | Создаёт эндпоинт для изменения данных (POST/PUT/DELETE). Не кэшируется, но может инвалидировать кэш других запросов. Генерирует хук `useMutationNameMutation`. | `createUser: builder.mutation<User, Partial<User>>({ ... })` |
| **`tagTypes`** | Определяет типы тегов для системы кэширования. Теги используются для связи запросов и автоматической инвалидации кэша. Позволяет группировать связанные данные. | `tagTypes: ['User', 'Post']` |
| **`providesTags`** | Указывает, какие теги "предоставляет" query. Помечает кэшированные данные тегами, чтобы другие запросы могли на них ссылаться. Может быть массивом или функцией для динамических тегов. | `providesTags: ['User']` или `providesTags: (result) => [{ type: 'User', id: result.id }]` |
| **`invalidatesTags`** | Указывает, какие теги инвалидировать при выполнении mutation. Автоматически помечает связанные кэши как устаревшие, заставляя их перезагружаться. Может быть массивом или функцией. | `invalidatesTags: ['User']` или `invalidatesTags: (result, error, arg) => [{ type: 'User', id: arg.id }]` |
| **`keepUnusedDataFor`** | Время в секундах, сколько хранить неиспользуемые данные в кэше. После этого времени данные удаляются, если на них нет активных подписок. По умолчанию 60 секунд. | `keepUnusedDataFor: 60` |
| **`refetchOnMountOrArgChange`** | Перезапрашивает данные при монтировании компонента или при изменении аргументов запроса. Полезно для получения актуальных данных. Может быть `true`, `false` или число (секунды). | `refetchOnMountOrArgChange: true` |
| **`refetchOnFocus`** | Автоматически перезапрашивает данные при возврате фокуса на вкладку браузера. Полезно для синхронизации данных после того, как пользователь вернулся к приложению. | `refetchOnFocus: true` |
| **`refetchOnReconnect`** | Перезапрашивает данные при восстановлении интернет-соединения. Автоматически обновляет данные после потери и восстановления связи. | `refetchOnReconnect: true` |
| **`skip`** | Условно пропускает выполнение запроса. Если `true`, запрос не выполняется. Полезно для запросов, которые зависят от наличия данных (например, ID). | `const { data } = useGetUserQuery(id, { skip: !id })` |
| **`pollingInterval`** | Интервал в миллисекундах для автоматических периодических запросов. Запрос будет повторяться через указанный интервал. Полезно для real-time данных. Установите `0` для остановки. | `pollingInterval: 5000` |
| **`transformResponse`** | Преобразует ответ сервера перед сохранением в кэш. Позволяет нормализовать данные, извлечь нужную часть ответа, преобразовать структуру. Применяется только к успешным ответам. | `transformResponse: (response) => response.data` |
| **`onQueryStarted`** | Callback функция, вызываемая при начале запроса. Используется для оптимистичных обновлений, side effects, логирования. Имеет доступ к `dispatch`, `queryFulfilled`, `getState`. Может обновлять кэш до получения ответа. | `onQueryStarted: async (arg, { dispatch, queryFulfilled }) => { ... }` |

---

### 🪝 Хуки RTK Query:

RTK Query автоматически генерирует хуки для каждого эндпоинта. Название хука формируется из названия эндпоинта: `getUsers` → `useGetUsersQuery`, `createUser` → `useCreateUserMutation`.

#### 🔹 Query хуки (`useQueryNameQuery`):

Хуки для чтения данных (GET запросы). Выполняются автоматически при монтировании компонента.

**Параметры хука:**
- Первый аргумент — параметры запроса (если эндпоинт их принимает)
- Второй аргумент — опции запроса (объект с настройками)

**Возвращаемые значения:**
- **`data`** — данные из ответа сервера или кэша. `undefined` пока запрос не выполнен.
- **`isLoading`** — `true` при первой загрузке (когда данных ещё нет в кэше).
- **`isFetching`** — `true` во время любого запроса (включая refetch).
- **`isError`** — `true` если запрос завершился с ошибкой.
- **`error`** — объект ошибки, если запрос провалился.
- **`isSuccess`** — `true` если запрос успешно завершён.
- **`refetch`** — функция для ручного перезапроса данных.
- **`status`** — строка: `'pending'`, `'fulfilled'`, `'rejected'`.
- **`currentData`** — текущие данные из кэша (могут быть устаревшими).
- **`isUninitialized`** — `true` если запрос ещё не был инициирован.

**Опции запроса (второй параметр):**
- **`skip`** — пропустить запрос (условный запрос).
- **`pollingInterval`** — интервал автоматических запросов в миллисекундах.
- **`refetchOnMountOrArgChange`** — перезапрос при монтировании или изменении аргументов.
- **`refetchOnFocus`** — перезапрос при возврате фокуса на вкладку.
- **`refetchOnReconnect`** — перезапрос при восстановлении соединения.

#### 🔹 Mutation хуки (`useMutationNameMutation`):

Хуки для изменения данных (POST/PUT/DELETE запросы). Выполняются вручную при вызове функции.

**Возвращаемые значения:**
- **`[trigger, result]`** — массив из функции-триггера и объекта результата.

**Функция-триггер:**
- Принимает аргументы для mutation (данные для отправки).
- Возвращает Promise с результатом запроса.
- Можно использовать с `await` или `.then()`.

**Объект результата:**
- **`data`** — данные из ответа сервера после успешного выполнения.
- **`isLoading`** / **`isUninitialized`** — состояние запроса.
- **`isError`** — `true` если запрос провалился.
- **`error`** — объект ошибки.
- **`isSuccess`** — `true` если запрос успешен.
- **`reset`** — функция для сброса состояния mutation.

**Особенности:**
- Mutation не кэшируется, но может инвалидировать кэш других запросов через `invalidatesTags`.
- Состояние mutation сбрасывается при размонтировании компонента (если не настроено иначе).

#### 🔹 Lazy Query хуки (`useLazyQueryNameQuery`):

Ленивые запросы, которые не выполняются автоматически. Полезны для запросов по действию пользователя (клик, submit формы).

**Возвращаемые значения:**
- **`[trigger, result]`** — аналогично mutation, но для query.

**Функция-триггер:**
- Принимает параметры запроса и опции.
- Выполняет запрос при вызове.
- Возвращает Promise с результатом.

**Объект результата:**
- Те же поля, что и у обычного query хука (`data`, `isLoading`, `error` и т.д.).

**Отличия от обычного query:**
- Не выполняется автоматически при монтировании.
- Выполняется только при явном вызове функции-триггера.
- Полезен для условных запросов или запросов по действию пользователя.

#### 🔹 Prefetch хуки (`usePrefetch`):

Хук для предзагрузки данных до того, как они понадобятся. Полезен для улучшения UX (например, при наведении на ссылку).

**Возвращаемые значения:**
- Функция для предзагрузки данных.

**Использование:**
- Принимает параметры запроса и опции.
- Начинает загрузку данных в фоне.
- Данные сохраняются в кэш и будут доступны мгновенно при реальном запросе.

**Особенности:**
- Не вызывает ре-рендер компонента.
- Работает в фоне.
- Улучшает воспринимаемую производительность приложения.

---

### 🔧 Основные возможности:

#### 1. **Автоматическое кэширование**
RTK Query автоматически кэширует результаты запросов и переиспользует их при повторных запросах.

#### 2. **Предотвращение дублирующих запросов**
Если несколько компонентов запрашивают одни и те же данные одновременно, RTK Query отправляет только один запрос.

#### 3. **Автоматическое управление состоянием**
Состояния `isLoading`, `isError`, `data` управляются автоматически для каждого запроса.

#### 4. **Инвалидация кэша**
При обновлении данных можно автоматически инвалидировать связанные кэшированные запросы.

#### 5. **Оптимистичные обновления**
Можно обновлять UI до получения ответа от сервера для лучшего UX.

#### 6. **Prefetching**
Предзагрузка данных до того, как они понадобятся (например, при наведении на ссылку).

---

### 💻 Пример использования:

#### 1. Создание API:

```ts
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';

export const api = createApi({
  reducerPath: 'api',
  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),
  endpoints: (builder) => ({
    getUsers: builder.query<User[], void>({
      query: () => 'users',
    }),
    createUser: builder.mutation<User, Partial<User>>({
      query: (newUser) => ({
        url: 'users',
        method: 'POST',
        body: newUser,
      }),
    }),
  }),
});

export const { useGetUsersQuery, useCreateUserMutation } = api;
```

#### 2. Использование в компоненте:

```ts
function UsersList() {
  const { data, isLoading, error } = useGetUsersQuery();
  const [createUser] = useCreateUserMutation();

  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error!</div>;

  return (
    <div>
      {data?.map(user => <div key={user.id}>{user.name}</div>)}
      <button onClick={() => createUser({ name: 'New User' })}>
        Add User
      </button>
    </div>
  );
}
```

**Что происходит автоматически:**
- ✅ Кэширование данных
- ✅ Управление `isLoading`, `error`, `data`
- ✅ Предотвращение дублирующих запросов
- ✅ Отмена запросов при размонтировании

---

### 🎯 Когда использовать RTK Query vs обычный RTK:

#### ✅ **Используйте RTK Query, когда:**

- Много API запросов в приложении
- Нужно кэширование данных
- Автоматическая синхронизация между компонентами
- Сложные зависимости между данными
- Оптимистичные обновления важны для UX
- Хотите уменьшить boilerplate

#### 🤔 **Используйте обычный Redux Toolkit, когда:**

- Мало API запросов (1-2 простых)
- Серверные данные простые, не требуют кэширования
- Нужен полный контроль над логикой запросов
- Работаете с WebSockets, real-time данными
- Сложная бизнес-логика, не связанная с API
- Миграция старого проекта (постепенная)

---

### 💡 Краткий ответ для собеседования:

**RTK Query** — это инструмент Redux Toolkit для работы с API. Автоматизирует кэширование, управление состоянием загрузки, предотвращает дублирующие запросы и синхронизирует данные между компонентами. Уменьшает boilerplate код и упрощает работу с серверными данными. Используется когда в приложении много API запросов и нужен кэш. Для простых случаев или когда нужен полный контроль, лучше использовать обычный RTK.

---
