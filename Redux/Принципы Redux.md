## 📐 Принципы Redux

Redux основан на **трёх фундаментальных принципах**, которые обеспечивают предсказуемость и простоту управления состоянием.

---

### 🎯 Принцип 1: Единый источник истины (Single Source of Truth)

**Вся информация о состоянии приложения хранится в одном объекте состояния внутри одного Store.**

```js
// ❌ Плохо — состояние разбросано
const userState = { name: 'John' };
const todosState = ['todo1', 'todo2'];
const settingsState = { theme: 'dark' };

// ✅ Хорошо — всё в одном Store
const store = {
  user: { name: 'John' },
  todos: ['todo1', 'todo2'],
  settings: { theme: 'dark' }
};
```

**Преимущества:**
- ✅ Легко отлаживать — видно всё состояние в одном месте
- ✅ Проще работать с DevTools
- ✅ Нет дублирования данных

---

### 🔒 Принцип 2: Состояние только для чтения (State is Read-Only)

**Единственный способ изменить состояние — это отправить Action (объект, описывающий, что произошло).**

```js
// ❌ Плохо — прямое изменение
store.state.todos.push('new todo'); // Запрещено!

// ✅ Хорошо — через Action
dispatch({ type: 'ADD_TODO', payload: 'new todo' });
```

**Почему это важно:**
- ✅ Предсказуемость — все изменения явные и отслеживаемые
- ✅ Отладка — можно логировать все действия
- ✅ Тестирование — легко воспроизвести сценарии

---

### ⚡ Принцип 3: Изменения через чистые функции (Pure Functions)

**Чтобы указать, как состояние обновляется действиями, вы пишете чистые функции-редьюсеры.**

**Чистая функция:**
- ✅ Возвращает одинаковый результат для одинаковых входных данных
- ✅ Не имеет побочных эффектов (не изменяет внешние переменные)
- ✅ Не зависит от внешнего состояния

```js
// ✅ Чистая функция-редьюсер
function todosReducer(state = [], action) {
  switch (action.type) {
    case 'ADD_TODO':
      return [...state, action.payload]; // Новый массив, не мутируем старый
    case 'REMOVE_TODO':
      return state.filter(todo => todo !== action.payload);
    default:
      return state; // Всегда возвращаем состояние
  }
}

// ❌ НЕ чистая функция
function badReducer(state = [], action) {
  state.push(action.payload); // Мутация!
  return state;
}
```

**Почему важна чистота:**
- ✅ Предсказуемость — одинаковый результат для одинаковых входов
- ✅ Тестируемость — легко тестировать без моков
- ✅ Отладка — можно "перемотать" состояние назад

---

### 🔄 Однонаправленный поток данных (Unidirectional Data Flow)

**Данные в Redux следуют строгому однонаправленному потоку:**

```
Component → dispatch(action) → Reducer → New State → Component (re-render)
```

**Пример:**
```js
// 1. Компонент отправляет Action
dispatch({ type: 'INCREMENT', payload: 1 });

// 2. Reducer обрабатывает Action
function counterReducer(state = 0, action) {
  if (action.type === 'INCREMENT') {
    return state + action.payload; // Новое состояние
  }
  return state;
}

// 3. Store обновляется
// 4. Компоненты, подписанные на изменения, получают обновление
```

---

### 🛡️ Иммутабельность (Immutability)

**Состояние в Redux неизменяемое (immutable). Вместо изменения существующего состояния, создаётся новое.**

```js
// ❌ Плохо — мутация
const newState = state;
newState.todos.push('new todo'); // Изменяем старое состояние

// ✅ Хорошо — создаём новое
const newState = {
  ...state,
  todos: [...state.todos, 'new todo']
};
```

**Почему это важно:**
- ✅ Отслеживание изменений — можно сравнить старое и новое состояние
- ✅ Оптимизация React — легко определить, что изменилось
- ✅ Time-travel debugging — можно вернуться к любому состоянию

---

### 💡 Краткий ответ для собеседования:

**Redux основан на трёх принципах:**
1. **Единый источник истины** — всё состояние в одном Store
2. **Состояние только для чтения** — изменения только через Actions
3. **Чистые функции** — редьюсеры не имеют побочных эффектов

Дополнительно: **иммутабельность** (создаём новое состояние, не изменяем старое) и **однонаправленный поток данных** (Component → Action → Reducer → State → Component).

---

### 🔍 Типичные вопросы на собеседовании:

**Q: Почему нельзя напрямую изменять состояние в Redux?**
A: Прямое изменение нарушает предсказуемость и отслеживаемость изменений. Все изменения должны проходить через Actions и Reducers, чтобы можно было логировать, отлаживать и воспроизводить изменения.

**Q: Что такое чистая функция и почему редьюсеры должны быть чистыми?**
A: Чистая функция возвращает одинаковый результат для одинаковых входных данных и не имеет побочных эффектов. Редьюсеры должны быть чистыми для предсказуемости, тестируемости и возможности "перемотки" состояния назад.

**Q: Что произойдёт, если мутировать состояние в редьюсере?**
A: Redux может не заметить изменения (так как ссылка на объект не изменилась), компоненты не обновятся, DevTools не покажут изменения, и нарушится предсказуемость приложения.

**Q: Почему Redux использует иммутабельность?**
A: Иммутабельность позволяет легко отслеживать изменения (сравнивая ссылки), оптимизировать рендеринг в React, реализовать time-travel debugging и обеспечивает предсказуемость.

**Q: Можно ли использовать несколько Store в Redux?**
A: Технически можно, но это нарушает принцип "единого источника истины". Redux рекомендует один Store, но можно разделить логику через `combineReducers` для разных частей состояния.

**Q: Что такое однонаправленный поток данных?**
A: Это поток, где данные движутся в одном направлении: Component → dispatch(action) → Reducer → New State → Component. Это делает поток данных предсказуемым и легко отслеживаемым.

**Q: В чём разница между Action и Action Creator?**
A: Action — это объект `{ type: 'ADD_TODO', payload: '...' }`. Action Creator — это функция, которая создаёт Action: `const addTodo = (text) => ({ type: 'ADD_TODO', payload: text })`. Action Creator упрощает создание Actions и позволяет добавлять логику.

**Q: Можно ли вызывать API в редьюсере?**
A: Нет, редьюсеры должны быть чистыми функциями. API-вызовы нужно делать в Action Creators (с помощью middleware, например, Redux Thunk) или в компонентах, а затем отправлять результат через Action.

**Q: Что такое middleware в Redux?**
A: Middleware — это промежуточный слой между dispatch и reducer. Он позволяет перехватывать Actions, выполнять асинхронные операции (Redux Thunk), логировать (redux-logger) и т.д.

**Q: Зачем нужен `combineReducers`?**
A: `combineReducers` позволяет разделить один большой редьюсер на несколько маленьких, каждый из которых управляет своей частью состояния. Это улучшает организацию кода и читаемость.
