## 🗄️ Redux

**Redux** — это библиотека для управления состоянием приложения.

---

### 🎯 Основная идея:

**Всё состояние приложения хранится в одном месте (`store`).**

---

### 🏗️ Основные концепции:

#### 1. **Store** (хранилище)
Объект, где хранится всё глобальное состояние.

Создаётся через `createStore` (или `configureStore` в Redux Toolkit):

```js
import { createStore } from 'redux';
const store = createStore(reducer);
```

#### 2. **Action** (действие)
Обычный объект `{ type: 'SOME_ACTION', payload: ... }`.

Он описывает, **что произошло**, но не как менять состояние.

```js
{ type: 'ADD_TODO', payload: 'Купить молоко' }
```

#### 3. **Reducer** (редьюсер)
Чистая функция `(state, action) => newState`, которая по действию возвращает новое состояние.

```js
function todosReducer(state = [], action) {
  switch (action.type) {
    case 'ADD_TODO':
      return [...state, action.payload];
    default:
      return state;
  }
}
```

#### 4. **Dispatch**
Метод `store.dispatch(action)`, который «отправляет» действие в хранилище.

После вызова `dispatch` Redux передаёт это действие в редьюсеры, чтобы они вернули новое состояние.

#### 5. **Selector**
Функция, которая достаёт нужный кусок состояния из хранилища (например, `useSelector` в React).

```js
const todos = useSelector(state => state.todos);
```

---

### 🔄 Как это работает:

1. Компонент вызывает `dispatch(action)`
2. Redux передаёт action в reducer
3. Reducer возвращает новое состояние
4. Store обновляется
5. Компоненты, подписанные на изменения, получают обновление

---

### 💡 Итог:

Redux обеспечивает **предсказуемое управление состоянием** через единое хранилище и чистые функции-редьюсеры.
