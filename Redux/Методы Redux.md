## 🛠️ Методы Redux

Основные методы и функции для работы с Redux.

---

### 📊 Таблица методов:

| Метод | Библиотека | Назначение | Пример использования |
|-------|-----------|------------|---------------------|
| **`createStore`** | `redux` | Создаёт Store для хранения состояния | `const store = createStore(reducer)` |
| **`configureStore`** | `@reduxjs/toolkit` | Создаёт Store с настройками (рекомендуется) | `const store = configureStore({ reducer })` |
| **`combineReducers`** | `redux` | Объединяет несколько Reducers в один | `const rootReducer = combineReducers({ todos, user })` |
| **`dispatch`** | `redux` | Отправляет Action в Store | `store.dispatch({ type: 'ADD_TODO', payload: '...' })` |
| **`getState`** | `redux` | Возвращает текущее состояние Store | `const state = store.getState()` |
| **`subscribe`** | `redux` | Подписывается на изменения Store | `store.subscribe(() => console.log(store.getState()))` |
| **`connect`** | `react-redux` | Подключает компонент к Store (HOC) | `export default connect(mapStateToProps, mapDispatchToProps)(Component)` |
| **`useSelector`** | `react-redux` | Хук для получения данных из Store | `const todos = useSelector(state => state.todos)` |
| **`useDispatch`** | `react-redux` | Хук для получения функции dispatch | `const dispatch = useDispatch()` |
| **`Provider`** | `react-redux` | Компонент-обёртка для предоставления Store | `<Provider store={store}><App /></Provider>` |

---

### 🔧 Подробнее о методах:

#### **1. createStore / configureStore**

**Создание Store:**

```js
// Старый способ (redux)
import { createStore } from 'redux';
const store = createStore(reducer);

// Новый способ (Redux Toolkit) - рекомендуется
import { configureStore } from '@reduxjs/toolkit';
const store = configureStore({
  reducer: rootReducer,
  middleware: (getDefaultMiddleware) => getDefaultMiddleware(),
});
```

**Разница:**
- `configureStore` включает Redux DevTools по умолчанию
- `configureStore` включает Redux Thunk по умолчанию
- `configureStore` упрощает настройку middleware

---

#### **2. combineReducers**

**Объединение нескольких Reducers:**

```js
import { combineReducers } from 'redux';

function todosReducer(state = [], action) {
  // логика для todos
}

function userReducer(state = null, action) {
  // логика для user
}

const rootReducer = combineReducers({
  todos: todosReducer,
  user: userReducer,
});

// Результирующее состояние:
// {
//   todos: [...],
//   user: {...}
// }
```

**Зачем:**
- Разделение логики на модули
- Упрощение поддержки кода
- Каждый Reducer управляет своей частью состояния

---

#### **3. connect (HOC)**

**Подключение компонента к Store (старый способ):**

```js
import { connect } from 'react-redux';

function TodoList({ todos, addTodo }) {
  return (
    <div>
      {todos.map(todo => <div key={todo.id}>{todo.text}</div>)}
      <button onClick={() => addTodo('New todo')}>Add</button>
    </div>
  );
}

// mapStateToProps - какие данные из Store нужны
const mapStateToProps = (state) => ({
  todos: state.todos,
});

// mapDispatchToProps - какие Actions нужно отправлять
const mapDispatchToProps = (dispatch) => ({
  addTodo: (text) => dispatch({ type: 'ADD_TODO', payload: text }),
});

export default connect(mapStateToProps, mapDispatchToProps)(TodoList);
```

**Когда использовать:**
- В классовых компонентах
- В функциональных компонентах (если не используете хуки)

---

#### **4. useSelector**

**Хук для получения данных из Store:**

```js
import { useSelector } from 'react-redux';

function TodoList() {
  // Получаем todos из Store
  const todos = useSelector(state => state.todos);
  
  // Можно использовать селектор с мемоизацией
  const completedTodos = useSelector(state => 
    state.todos.filter(todo => todo.completed)
  );
  
  return (
    <div>
      {todos.map(todo => <div key={todo.id}>{todo.text}</div>)}
    </div>
  );
}
```

**Особенности:**
- Компонент перерендерится только при изменении выбранных данных
- Можно использовать несколько `useSelector` в одном компоненте
- Рекомендуется использовать вместо `connect` в функциональных компонентах

---

#### **5. useDispatch**

**Хук для получения функции dispatch:**

```js
import { useDispatch } from 'react-redux';

function AddTodo() {
  const dispatch = useDispatch();
  
  const handleAdd = () => {
    dispatch({ type: 'ADD_TODO', payload: 'New todo' });
  };
  
  return <button onClick={handleAdd}>Add Todo</button>;
}
```

**Особенности:**
- Возвращает стабильную ссылку на `dispatch`
- Можно использовать в любом функциональном компоненте
- Рекомендуется использовать вместо `connect` для отправки Actions

---

#### **6. Provider**

**Компонент-обёртка для предоставления Store:**

```js
import { Provider } from 'react-redux';
import { createStore } from 'redux';

const store = createStore(reducer);

function App() {
  return (
    <Provider store={store}>
      <TodoList />
      <AddTodo />
    </Provider>
  );
}
```

**Зачем:**
- Предоставляет Store всем дочерним компонентам
- Обычно оборачивает корневой компонент приложения
- Без Provider хуки `useSelector` и `useDispatch` не работают

---

#### **7. getState**

**Получение текущего состояния:**

```js
const store = createStore(reducer);

// Получить текущее состояние
const currentState = store.getState();
console.log(currentState); // { todos: [...], user: {...} }
```

**Особенности:**
- Синхронный метод
- Возвращает ссылку на текущее состояние
- Не вызывает обновление компонентов

---

#### **8. subscribe**

**Подписка на изменения Store:**

```js
const store = createStore(reducer);

// Подписаться на изменения
const unsubscribe = store.subscribe(() => {
  console.log('State changed:', store.getState());
});

// Отписаться
unsubscribe();
```

**Особенности:**
- Callback вызывается после каждого обновления Store
- Возвращает функцию для отписки
- В React-Redux используется внутри `useSelector` и `connect`

---

### 💡 Краткий ответ для собеседования:

**Основные методы Redux:**
- **`createStore`/`configureStore`** — создание Store
- **`combineReducers`** — объединение Reducers
- **`dispatch`** — отправка Actions
- **`getState`** — получение состояния
- **`connect`** — HOC для подключения компонентов (старый способ)
- **`useSelector`** — хук для получения данных из Store
- **`useDispatch`** — хук для получения dispatch
- **`Provider`** — обёртка для предоставления Store

---

### 🔍 Типичные вопросы на собеседовании:

**Q: В чём разница между `connect` и `useSelector`/`useDispatch`?**
A: `connect` — это HOC (Higher Order Component) для классовых компонентов. `useSelector` и `useDispatch` — это хуки для функциональных компонентов. Хуки проще в использовании и рекомендуются для новых проектов.

**Q: Когда использовать `createStore`, а когда `configureStore`?**
A: `configureStore` из Redux Toolkit рекомендуется, так как включает DevTools и Thunk по умолчанию, упрощает настройку. `createStore` — старый способ, используется реже.

**Q: Зачем нужен `combineReducers`?**
A: Для разделения логики на модули. Каждый Reducer управляет своей частью состояния, что упрощает поддержку и тестирование кода.

**Q: Что произойдёт, если не обернуть приложение в `Provider`?**
A: Хуки `useSelector` и `useDispatch` выбросят ошибку, так как не смогут найти Store в контексте React.

**Q: Можно ли использовать несколько `useSelector` в одном компоненте?**
A: Да, можно использовать сколько угодно. Каждый `useSelector` подписывается на свою часть состояния и перерендерит компонент только при изменении выбранных данных.

**Q: Что такое `mapStateToProps` и `mapDispatchToProps`?**
A: Это функции для `connect`. `mapStateToProps` определяет, какие данные из Store передать в компонент. `mapDispatchToProps` определяет, какие функции для отправки Actions передать в компонент.

**Q: Можно ли использовать `getState` в компоненте?**
A: Технически можно, но не рекомендуется. Лучше использовать `useSelector`, который автоматически подписывается на изменения и обновляет компонент.

**Q: Что возвращает `subscribe`?**
A: Функцию для отписки от изменений Store. Это позволяет очистить подписку и избежать утечек памяти.

**Q: В чём разница между `dispatch` и `useDispatch`?**
A: `dispatch` — это метод Store. `useDispatch` — это хук, который возвращает функцию `dispatch` для использования в компонентах. Оба делают одно и то же, но `useDispatch` удобнее в React-компонентах.

**Q: Можно ли использовать `connect` и хуки вместе?**
A: Да, можно, но обычно выбирают один подход для всего проекта. Хуки (`useSelector`, `useDispatch`) проще и рекомендуются для новых проектов.
