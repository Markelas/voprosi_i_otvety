## 🔍 Elasticsearch и Kibana на фронтенде

**Elasticsearch** — это поисковая и аналитическая система для хранения, поиска и анализа больших объёмов данных в реальном времени.

**Kibana** — это веб-интерфейс для визуализации и анализа данных из Elasticsearch. Позволяет создавать дашборды, графики и выполнять поиск по данным.

На фронтенде **Elasticsearch + Kibana** используются для централизованного логирования ошибок, событий пользователей и мониторинга производительности клиентского приложения.

---

### 🎯 Зачем нужны Elasticsearch и Kibana на фронтенде?

#### Проблемы без логирования:

- ❌ Ошибки происходят у пользователей, но разработчики о них не знают
- ❌ Невозможно воспроизвести баги, которые происходят только в продакшене
- ❌ Нет понимания, как пользователи взаимодействуют с приложением
- ❌ Сложно отследить проблемы производительности
- ❌ Нет аналитики по использованию функций приложения

#### Решение с Elasticsearch + Kibana:

- ✅ **Мониторинг ошибок** — все ошибки JavaScript собираются и анализируются
- ✅ **Отслеживание событий** — действия пользователей логируются для аналитики
- ✅ **Производительность** — метрики загрузки, времени ответа API
- ✅ **Централизованное хранение** — все логи в одном месте
- ✅ **Быстрый поиск** — мгновенный поиск по миллионам записей
- ✅ **Визуализация** — дашборды для анализа данных

---

### 📊 Что такое Elasticsearch и Kibana?

**Elasticsearch** — это NoSQL база данных и поисковый движок, который хранит данные в виде JSON документов. Используется для хранения логов, ошибок и событий.

**Kibana** — это веб-интерфейс для визуализации данных из Elasticsearch. Позволяет создавать дашборды, графики и выполнять поиск по логам.

#### Основные концепции Elasticsearch:

- **Index (Индекс)** — коллекция документов (например, индекс для логов ошибок)
- **Document (Документ)** — единица данных в JSON формате (один лог)
- **Field (Поле)** — отдельное свойство документа (например, `error.message`, `user.id`)

#### Основные возможности Kibana:

- **Discover** — поиск и просмотр логов в реальном времени
- **Visualize** — создание графиков и диаграмм (например, график ошибок по времени)
- **Dashboard** — объединение визуализаций в единый дашборд
- **Dev Tools** — консоль для выполнения запросов к Elasticsearch

---

### 📝 Логирование на фронтенде

Логирование на фронтенде — это процесс записи информации о событиях, ошибках и действиях пользователей в клиентском приложении для последующего анализа и отладки.

#### 🎯 Зачем нужно логирование на фронтенде:

1. **Отладка продакшена** — ошибки происходят у пользователей, но их нужно найти и исправить
2. **Мониторинг в реальном времени** — отслеживание работы приложения у всех пользователей
3. **Аналитика поведения** — понимание, как пользователи используют приложение
4. **Производительность** — отслеживание медленных запросов, проблем с рендерингом
5. **Бизнес-метрики** — конверсии, клики, покупки, регистрации
6. **Безопасность** — обнаружение подозрительной активности, атак

#### 📋 Уровни логирования:

- **DEBUG** — детальная информация для отладки (только в development)
- **INFO** — общая информация о работе приложения (загрузка страницы, навигация)
- **WARN** — предупреждения о потенциальных проблемах (устаревший API, медленный запрос)
- **ERROR** — ошибки, которые не останавливают работу приложения (ошибка запроса, валидация)
- **FATAL** — критические ошибки, останавливающие приложение (необработанные исключения)

#### 🔧 Что логировать на фронтенде:

**Ошибки JavaScript:**
- Необработанные исключения (`window.onerror`, `unhandledrejection`)
- Ошибки React (Error Boundaries)
- Ошибки валидации форм
- Ошибки парсинга JSON
- Stack traces с информацией о файле и строке

**Ошибки API:**
- HTTP ошибки (4xx, 5xx)
- Таймауты запросов
- Ошибки сети (нет интернета)
- Ошибки валидации с сервера

**События пользователей:**
- Клики по кнопкам, ссылкам
- Навигация между страницами
- Заполнение форм (начало, отправка, отмена)
- Покупки, регистрации, авторизации
- Использование функций приложения

**Производительность:**
- Время загрузки страницы
- Время ответа API запросов
- Время рендеринга компонентов
- Медленные запросы (> 1 секунды)
- Проблемы с памятью

**Контекст (обязательно добавлять к каждому логу):**
- User ID (если пользователь авторизован)
- Session ID
- URL страницы
- User Agent, браузер, ОС
- Временная метка
- Версия приложения
- Размер экрана, устройство
- Язык интерфейса

---

### 🔄 Как работает логирование на фронтенде с Elasticsearch + Kibana:

#### Процесс логирования:

1. **Сбор логов на фронтенде** — приложение перехватывает ошибки и события
2. **Отправка на сервер** — логи отправляются через HTTP API на backend
3. **Обработка на backend** — сервер получает логи и отправляет их в Elasticsearch
4. **Хранение в Elasticsearch** — логи индексируются и хранятся
5. **Визуализация в Kibana** — разработчики просматривают логи через Kibana
6. **Анализ и мониторинг** — поиск, фильтрация, создание дашбордов, алерты

#### Как отправлять логи с фронтенда:

**Прямая отправка (не рекомендуется):**
- Фронтенд отправляет логи напрямую в Elasticsearch через HTTP API
- Проблема: нужно открывать Elasticsearch для внешнего доступа (небезопасно)

**Через backend API (рекомендуется):**
- Фронтенд отправляет логи на свой backend API (`/api/logs`)
- Backend валидирует и отправляет логи в Elasticsearch
- Безопаснее и позволяет добавить фильтрацию/обработку

**Через готовые сервисы:**
- **Sentry** — специализированный сервис для мониторинга ошибок
- **LogRocket** — запись сессий пользователей и ошибок
- **Datadog** — мониторинг и логирование
- Эти сервисы сами интегрируются с Elasticsearch или имеют свои дашборды

#### Инструменты для логирования на фронтенде:

**Библиотеки для отправки логов:**
- **Winston** — популярная библиотека для логирования (можно настроить отправку в Elasticsearch)
- **Pino** — быстрая библиотека для логирования
- **Bunyan** — структурированное логирование

**Сервисы мониторинга ошибок:**
- **Sentry** — автоматический сбор ошибок, stack traces, контекст
- **LogRocket** — запись сессий пользователей, воспроизведение ошибок
- **Rollbar** — мониторинг ошибок в реальном времени

**Интеграция в React:**
- Error Boundaries для перехвата ошибок React
- Глобальные обработчики `window.onerror` и `unhandledrejection`
- Middleware для Redux (логирование actions)
- Interceptors для axios/fetch (логирование запросов)

#### Преимущества Elasticsearch + Kibana для фронтенда:

- **Централизованное хранение** — все логи от всех пользователей в одном месте
- **Быстрый поиск** — мгновенный поиск по миллионам записей (например, найти все ошибки конкретного пользователя)
- **Визуализация** — графики ошибок по времени, популярные ошибки, метрики производительности
- **Масштабируемость** — обработка больших объёмов данных от тысяч пользователей
- **Retention политики** — автоматическое удаление старых логов (например, хранить 30 дней)
- **Алерты** — настройка уведомлений при критических событиях (например, резкий рост ошибок)

---

### 🛠️ Как интегрировать логирование в фронтенд приложение:

#### 1. Настройка глобальных обработчиков ошибок:

Настраивается глобальный обработчик `window.onerror` для перехвата всех необработанных ошибок JavaScript. Обработчик получает сообщение об ошибке, источник (файл), номер строки и колонки, а также объект ошибки со stack trace. Все эти данные отправляются в систему логирования.

Также настраивается обработчик `unhandledrejection` для перехвата необработанных отклонённых Promise. Это важно, так как ошибки в асинхронном коде (async/await, Promise) не перехватываются через `window.onerror`.

#### 2. Error Boundary в React:

Создаётся класс-компонент Error Boundary, который оборачивает части приложения. Метод `componentDidCatch` перехватывает все ошибки, возникающие в дочерних компонентах во время рендеринга, в методах жизненного цикла или конструкторах. В этом методе логируется сообщение об ошибке, stack trace и информация о компоненте, где произошла ошибка.

#### 3. Логирование API запросов:

Настраиваются interceptors для HTTP клиента (например, axios). Interceptor для успешных ответов логирует тип события (API_SUCCESS), URL, метод запроса и время выполнения. Interceptor для ошибок логирует тип (API_ERROR), URL, метод, статус код ошибки и сообщение. Это позволяет отслеживать все API запросы и их производительность.

#### 4. Функция отправки логов:

Создаётся функция, которая принимает данные об ошибке и дополняет их контекстной информацией: временная метка, информация о пользователе, ID сессии, текущий URL, User Agent, окружение (development/production) и версия приложения. Затем отправляет эти данные на backend API через POST запрос. Если отправка не удалась (нет сети), можно сохранить логи в localStorage для последующей отправки.

#### 5. Логирование событий пользователей:

Создаётся функция для логирования действий пользователей. Функция принимает тип события и дополнительные данные, дополняет их временной меткой, информацией о пользователе и текущим URL, затем отправляет на сервер. Вызывается в обработчиках событий (клики, отправка форм, навигация) для отслеживания поведения пользователей.

---

### 💡 Краткий ответ для собеседования:

**Elasticsearch** — это поисковая система для хранения и анализа больших объёмов данных. На фронтенде используется для хранения логов ошибок и событий пользователей. Хранит данные в виде JSON документов в индексах.

**Kibana** — это веб-интерфейс для визуализации данных из Elasticsearch. Позволяет создавать дашборды с графиками ошибок, анализировать поведение пользователей и искать конкретные проблемы.

**Зачем на фронтенде:**
- Мониторинг ошибок JavaScript в продакшене
- Отслеживание действий пользователей для аналитики
- Мониторинг производительности (время загрузки, API запросы)
- Централизованное хранение всех логов от всех пользователей

**Что логировать:**
- Ошибки: необработанные исключения, ошибки React (Error Boundaries), ошибки API
- События: клики, навигация, бизнес-события (покупки, регистрации)
- Производительность: время загрузки, время ответа API
- Контекст: User ID, URL, User Agent, временные метки

**Как работает:**
Фронтенд перехватывает ошибки (через `window.onerror`, Error Boundaries) и события, отправляет их на backend API, который сохраняет в Elasticsearch. Разработчики просматривают логи через Kibana, где можно искать, фильтровать и визуализировать данные.

**Инструменты:**
Можно использовать готовые сервисы (Sentry, LogRocket) или настроить свою интеграцию через библиотеки логирования (Winston, Pino) с отправкой на backend API.

---
