## 🔄 Замена подстроки в строке

### Задача

Напишите функцию, которая принимает строку, подстроку для поиска и подстроку для замены.

Функция должна возвращать новую строку, где все вхождения искомой подстроки заменены на подстроку для замены.

---

### Примеры:

```js
replaceSubstring("hello world", "world", "there")  // "hello there"
replaceSubstring("abc abc abc", "abc", "123")      // "123 123 123"
```

---

### Решение:

```js
function replaceSubstring(str, search, replace) {
  let result = '';
  let i = 0;

  while (i < str.length) {
    if (str.slice(i, i + search.length) === search) {
      // Берем строчку, вырезаем и проверяем, совпадает ли текст с искомым
      result += replace;  // Добавляем новое слово
      i += search.length; // Перепрыгиваем это слово в str, так как мы заменили его новым
    } else {
      result += str[i];
      i++;
    }
  }

  return result;
}
```

---

### Объяснение:

1. Проходим по строке символ за символом
2. Проверяем, совпадает ли текущая подстрока с искомой (`str.slice()`)
3. Если да — добавляем замену и перепрыгиваем на длину искомой подстроки
4. Если нет — добавляем текущий символ
