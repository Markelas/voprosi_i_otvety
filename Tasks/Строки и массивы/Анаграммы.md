## 🔤 Анаграммы

### Задача

Напиши функцию `isAnagram(str1, str2)`, которая проверяет, являются ли две строки анаграммами.

**Анаграммы** — это строки, в которых одинаковое количество всех символов, но порядок неважен.

---

### Примеры:

```js
isAnagram('listen', 'silent')  // true
isAnagram('Hello', 'ol lHe')   // true
isAnagram('Test', 'ttew')      // false
```

---

### Решение:

```js
function isAnagram(strA, strB) {
  const clean = (str) => 
    str.toLowerCase()
      .split(' ')         // разбиваем по пробелам
      .join('')           // убираем все пробелы
      .split('')          // разбиваем по символам
      .sort()             // сортируем
      .join('');          // собираем обратно в строку

  return clean(strA) === clean(strB);
}
```

---

### Объяснение:

1. Приводим обе строки к нижнему регистру
2. Убираем пробелы
3. Сортируем символы
4. Сравниваем результат

Если отсортированные строки совпадают → анаграммы.
