## 🔄 Одинаковые массивы

### Задача

Напишите функцию `isSimilarNumbers(arr1, arr2)`, которая принимает два массива чисел и возвращает `true`, если массивы содержат одинаковые элементы в любом порядке, и `false` в противном случае.

---

### Примеры:

```js
isSimilarNumbers([1, 2, 2], [2, 1, 2])  // true, совпадают по количеству
isSimilarNumbers([1, 2, 3], [1, 2, 2])  // false, количество элементов 2 различается
isSimilarNumbers([], [])                 // true, два пустых массива считаются одинаковыми
```

---

### Решение:

```js
const isSimilarNumbers = (arr1, arr2) => {
  if (arr1.length !== arr2.length) return false;

  const count = {}; // number: count

  for (const num of arr1) {
    count[num] = (count[num] || 0) + 1;
  }

  for (const num of arr2) {
    if (!count[num]) return false;
    count[num]--;
  }

  return true;
};
```

---

### Объяснение:

1. Проверяем одинаковую длину
2. Создаём объект `count` для подсчёта каждого числа
3. В первом цикле увеличиваем счётчики для `arr1`
4. Во втором цикле уменьшаем счётчики для `arr2`
5. Если число отсутствует или счётчик стал отрицательным → `false`
6. Если все прошло успешно → `true`
