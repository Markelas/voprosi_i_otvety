## 🔎 Поиск отсутствующих элементов

### Задача 1: Один отсутствующий элемент

Напиши функцию `findMissing`, которая принимает два массива:
- `fullList` — массив всех элементов
- `partialList` — тот же массив, но с одним отсутствующим элементом

**Верни отсутствующий элемент.**

---

### Примеры:

```js
findMissing(['a', 'b', 'c'], ['b', 'a'])         // → 'c'
findMissing(['hello', 'world'], ['hello'])       // → 'world'
findMissing(['x'], [])                           // → 'x'
```

---

### Решение:

```js
function findMissing(fullList, partialList) {
  return fullList.find(el => !partialList.includes(el));
}
```

**`Array.find()`** возвращает первый элемент, удовлетворяющий условию. В нашем случае — это и есть отсутствующий элемент.

---

### Задача 2: Несколько отсутствующих элементов

Напиши функцию `findMissingElements`, которая принимает два массива строк:
- `fullList` — полный список (без повторов)
- `partialList` — тот же список, но возможно с несколькими отсутствующими элементами

**Верни массив всех отсутствующих элементов, в любом порядке.**

---

### Примеры:

```js
findMissingElements(['a', 'b', 'c', 'd'], ['a', 'b'])       // → ['c', 'd']
findMissingElements(['dog', 'cat', 'mouse'], ['mouse'])    // → ['dog', 'cat']
findMissingElements(['x', 'y'], ['x', 'y'])                // → []
```

---

### Решение:

```js
function findMissingElements(fullList, partialList) {
  const settedList = new Set(partialList);
  return fullList.filter(item => !settedList.has(item));
}
```

---

### Объяснение:

- **`new Set(partialList)`** — создаём структуру данных, где каждый элемент хранится как ключ
- Используем **`filter`** для поиска элементов, которых нет в `Set`
- **Сложность:** `O(n+m)` вместо `O(n*m)` с `includes()`
