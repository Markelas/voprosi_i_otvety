## 🔄 Повёрнутый отсортированный массив

### Задача 1: Найти min/max элемент

Найти минимальный / максимальный элемент в повёрнутом отсортированном массиве.

---

### Пример:

```js
findMin([4,5,6,7,0,1,2]) // → 0
```

---

### Решение для min:

```js
function findMin(nums) {
  let left = 0, right = nums.length - 1;

  while (left < right) {
    let mid = Math.floor((left + right) / 2);

    if (nums[mid] > nums[right]) {
      left = mid + 1; // минимум справа
    } else {
      right = mid;    // минимум слева или mid
    }
  }

  return nums[left];
}
```

---

### Решение для max:

```js
function findMax(nums) {
  let left = 0, right = nums.length - 1;

  while (left < right) {
    let mid = Math.floor((left + right) / 2);

    if (nums[mid] > nums[right]) {
      left = mid; // максимум либо mid, либо справа
    } else {
      right = mid - 1; // максимум слева от mid
    }

    if (left === right - 1) {
      // когда осталось 2 элемента
      return Math.max(nums[left], nums[right]);
    }
  }

  return nums[left];
}
```

---

### Задача 2: Поиск в повёрнутом массиве

Найти элемент в повёрнутом отсортированном массиве.

---

### Решение:

```js
function search(nums, target) {
  let left = 0;
  let right = nums.length - 1;

  while (left <= right) {
    let mid = Math.floor((left + right) / 2);

    if (nums[mid] === target) return mid;

    // Левая половина отсортирована
    if (nums[left] <= nums[mid]) {
      if (nums[left] <= target && target < nums[mid]) {
        right = mid - 1;
      } else {
        left = mid + 1;
      }
    }
    // Правая половина отсортирована
    else {
      if (nums[mid] < target && target <= nums[right]) {
        left = mid + 1;
      } else {
        right = mid - 1;
      }
    }
  }

  return -1;
}
```
