## 🔢 Количество повторений элемента

### Задача

Найти **количество повторений элемента** в отсортированном массиве.

---

### Примеры:

```js
countOccurrences([1, 1, 2, 2, 2, 2, 3, 4, 4, 4, 5], 2)  // → 4
countOccurrences([1, 1, 2, 2, 2, 2, 3, 4, 4, 4, 5], 4)  // → 3
countOccurrences([1, 1, 2, 2, 2, 2, 3, 4, 4, 4, 5], 6)  // → 0
```

---

### Решение:

```js
function countOccurrences(arr, target) {
    // Находим первую позицию элемента
    function findFirst(arr, target) {
        let left = 0;
        let right = arr.length - 1;
        let result = -1;

        while (left <= right) {
            let mid = Math.floor((left + right) / 2);

            if (arr[mid] === target) {
                result = mid;
                right = mid - 1;  // Ищем левее
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return result;
    }

    // Находим последнюю позицию элемента
    function findLast(arr, target) {
        let left = 0;
        let right = arr.length - 1;
        let result = -1;

        while (left <= right) {
            let mid = Math.floor((left + right) / 2);

            if (arr[mid] === target) {
                result = mid;
                left = mid + 1;   // Ищем правее
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return result;
    }

    const first = findFirst(arr, target);
    
    // Если элемент не найден
    if (first === -1) {
        return 0;
    }

    const last = findLast(arr, target);
    
    // Количество = последний индекс - первый индекс + 1
    return last - first + 1;
}
```

---

### Объяснение:

1. **Находим первую позицию:** Используем бинарный поиск, при совпадении ищем левее
2. **Находим последнюю позицию:** Используем бинарный поиск, при совпадении ищем правее
3. **Вычисляем количество:** `последний_индекс - первый_индекс + 1`
4. **Обработка отсутствия:** Если первая позиция не найдена, возвращаем 0

---

### Оптимизированное решение (одна функция):

```js
function countOccurrences(arr, target) {
    function findBound(arr, target, isFirst) {
        let left = 0;
        let right = arr.length - 1;
        let result = -1;

        while (left <= right) {
            let mid = Math.floor((left + right) / 2);

            if (arr[mid] === target) {
                result = mid;
                if (isFirst) {
                    right = mid - 1;  // Ищем первое вхождение
                } else {
                    left = mid + 1;   // Ищем последнее вхождение
                }
            } else if (arr[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return result;
    }

    const first = findBound(arr, target, true);
    
    if (first === -1) {
        return 0;
    }

    const last = findBound(arr, target, false);
    
    return last - first + 1;
}
```

---

### Особенности:

- **Сложность:** `O(log n)` — два бинарных поиска
- **Использует два бинарных поиска:** для первой и последней позиции
- **Формула:** `количество = последний_индекс - первый_индекс + 1`
- Если элемент не найден, возвращаем 0

