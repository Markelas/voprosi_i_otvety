## 🔍 Базовый бинарный поиск

### Задача

Напишите функцию бинарного поиска. Дан отсортированный массив `[1, 2, 4, 5, 7, 9]`, нужно найти число `4`.

---

### Решение:

```js
function binarySearch(array, target) {
  let left = 0;              // Индексы
  let right = array.length - 1; // Индексы

  while (left <= right) {
    const mid = Math.floor((left + right) / 2);

    if (array[mid] === target) {
      return mid;
    } else if (array[mid] < target) {
      left = mid + 1;
    } else {
      right = mid - 1;
    }
  }

  return -1;
}
```

---

### Объяснение:

1. Создаём переменные для индексов начала и конца
2. Цикл работает пока `left <= right`
3. Вычисляем середину: `(left + right) / 2`
4. Если элемент найден → возвращаем индекс
5. Если элемент меньше target → ищем справа (`left = mid + 1`)
6. Если элемент больше target → ищем слева (`right = mid - 1`)

---

### Особенности:

- **Сложность:** `O(log n)` — на каждом шаге отбрасываем половину массива
- **Работает только с отсортированным массивом**
- Не имеет смысла применять `sort` и затем бинарный поиск — используй, если массив уже отсортирован
