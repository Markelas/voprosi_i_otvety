## 🔍 Find Ceiling (Наименьшее число ≥ target)

### Задача

Найти **наименьшее число в массиве, которое больше или равно target**. Если такого нет, вернуть -1.

---

### Примеры:

```js
findCeiling([1, 5, 10, 15, 20], 7)  // → 10
findCeiling([1, 5, 10, 15, 20], 20) // → 20
findCeiling([1, 5, 10, 15, 20], 25) // → -1
```

---

### Решение:

```js
function findCeiling(arr, target) {
    // Обработка граничного случая
    if (target > arr[arr.length - 1]) {
        return -1;  // Нет элемента >= target
    }

    let left = 0;
    let right = arr.length - 1;
    let result = -1;

    while (left <= right) {
        let mid = Math.floor((left + right) / 2);

        if (arr[mid] >= target) {
            result = mid;        // Запоминаем кандидата
            right = mid - 1;     // Ищем левее, может быть меньший
        } else {
            left = mid + 1;      // Ищем справа
        }
    }

    return result;
}
```

---

### Объяснение:

1. **Граничный случай:** Если target больше последнего элемента, возвращаем -1
2. **Бинарный поиск:** Ищем элемент ≥ target
3. **Сохранение кандидата:** Когда находим подходящий элемент, запоминаем его и продолжаем искать левее
4. **Результат:** Возвращаем индекс наименьшего элемента ≥ target

---

### Альтернативное решение (без предварительной проверки):

```js
function findCeiling(arr, target) {
    let left = 0;
    let right = arr.length - 1;
    let result = -1;

    while (left <= right) {
        let mid = Math.floor((left + right) / 2);

        if (arr[mid] >= target) {
            result = mid;
            right = mid - 1;
        } else {
            left = mid + 1;
        }
    }

    return result;  // Вернёт -1, если не найдено
}
```

---

### Особенности:

- **Сложность:** `O(log n)`
- **Похоже на Lower Bound**, но с дополнительной проверкой на отсутствие результата
- Если элемент найден, возвращаем его индекс
- Если элемента нет, но есть больший — возвращаем индекс большего
- Если все элементы меньше target — возвращаем -1

