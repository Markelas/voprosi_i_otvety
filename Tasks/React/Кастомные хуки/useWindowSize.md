## 📐 useWindowSize

### Что делает:

**useWindowSize** — хук для отслеживания размеров окна браузера. Возвращает текущую ширину и высоту окна, обновляя их при изменении размера.

---

### Реализация:

```js
import { useState, useEffect } from 'react';

function useWindowSize() {
  const [windowSize, setWindowSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight,
  });

  useEffect(() => {
    function handleResize() {
      setWindowSize({
        width: window.innerWidth,
        height: window.innerHeight,
      });
    }

    window.addEventListener('resize', handleResize);

    // Очистка при размонтировании
    return () => {
      window.removeEventListener('resize', handleResize);
    };
  }, []);

  return windowSize;
}
```

---

### Пример использования:

```jsx
function ResponsiveComponent() {
  const { width, height } = useWindowSize();

  return (
    <div>
      <p>Ширина окна: {width}px</p>
      <p>Высота окна: {height}px</p>
      {width < 768 && <p>Мобильная версия</p>}
      {width >= 768 && <p>Десктопная версия</p>}
    </div>
  );
}
```

---

### Объяснение:

1. **Инициализация состояния:**
   - При первом рендере получаем текущие размеры окна через `window.innerWidth` и `window.innerHeight`
   - Сохраняем их в состоянии как объект с `width` и `height`

2. **Подписка на событие resize:**
   - В `useEffect` добавляем обработчик события `resize` на объект `window`
   - При изменении размера окна вызывается `handleResize`, который обновляет состояние

3. **Очистка подписки:**
   - Функция очистки удаляет обработчик события при размонтировании компонента
   - Это предотвращает утечки памяти

4. **Пустой массив зависимостей:**
   - `[]` означает, что эффект выполняется только при монтировании и размонтировании
   - Обработчик создаётся один раз и остаётся активным всё время жизни компонента

---

### Важные моменты:

- **Производительность:** событие `resize` может срабатывать очень часто, поэтому можно добавить debounce для оптимизации
- **SSR:** при серверном рендеринге `window` недоступен, нужно добавить проверку
- **Использование:** полезен для адаптивных компонентов, модальных окон, графиков

---

### Улучшенная версия (с debounce):

```js
import { useState, useEffect } from 'react';

function useWindowSize() {
  const [windowSize, setWindowSize] = useState({
    width: typeof window !== 'undefined' ? window.innerWidth : 0,
    height: typeof window !== 'undefined' ? window.innerHeight : 0,
  });

  useEffect(() => {
    if (typeof window === 'undefined') return;

    let timeoutId;
    
    function handleResize() {
      clearTimeout(timeoutId);
      timeoutId = setTimeout(() => {
        setWindowSize({
          width: window.innerWidth,
          height: window.innerHeight,
        });
      }, 150);
    }

    window.addEventListener('resize', handleResize);
    return () => {
      window.removeEventListener('resize', handleResize);
      clearTimeout(timeoutId);
    };
  }, []);

  return windowSize;
}
```

**Улучшения:**
- Проверка на существование `window` (для SSR)
- Debounce на 150мс для оптимизации частых обновлений
- Очистка таймера при размонтировании

