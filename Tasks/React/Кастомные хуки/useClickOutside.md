## 🖱️ useClickOutside

### Что делает:

**useClickOutside** — хук, который вызывает callback при клике вне указанного элемента. Часто используется для закрытия выпадающего меню, модалки или попапа при клике снаружи.

---

### Реализация:

```js
import { useEffect, useRef } from 'react';

function useClickOutside(callback) {
  const ref = useRef(null);

  useEffect(() => {
    function handleClick(e) {
      if (ref.current && !ref.current.contains(e.target)) {
        callback();
      }
    }

    document.addEventListener('mousedown', handleClick);
    return () => document.removeEventListener('mousedown', handleClick);
  }, [callback]);

  return ref;
}
```

---

### Пример использования:

```jsx
function Dropdown() {
  const [isOpen, setOpen] = useState(false);
  const ref = useClickOutside(() => setOpen(false));

  return (
    <div ref={ref} style={{ position: 'relative' }}>
      <button onClick={() => setOpen(!isOpen)}>Меню</button>
      {isOpen && (
        <ul className="dropdown">
          <li>Пункт 1</li>
          <li>Пункт 2</li>
        </ul>
      )}
    </div>
  );
}
```

---

### Объяснение:

1. **ref:** возвращаемый ref нужно повесить на контейнер, «внутри» которого клик считается внутренним.
2. **contains:** проверка `ref.current.contains(e.target)` — клик был по элементу внутри контейнера или по самому контейнеру; иначе считаем клик «снаружи».
3. **mousedown:** используется `mousedown`, чтобы сработать раньше других обработчиков (например, фокуса), но можно заменить на `click` при необходимости.
4. **Очистка:** при размонтировании удаляем слушатель с `document`.

---

### Важные моменты:

- **callback:** если передаётся анонимная функция, её лучше обернуть в `useCallback`, чтобы не вешать/снимать слушатель при каждом рендере.
- Подходит для дропдаунов, модалок, тултипов — один и тот же паттерн.
