## 🔄 useToggle

### Что делает:

**useToggle** — хук для булева переключателя (true/false). Возвращает текущее значение и функцию переключения. Удобен для модалок, дропдаунов, чекбоксов «показать ещё».

---

### Реализация:

```js
import { useState, useCallback } from 'react';

function useToggle(initialValue = false) {
  const [value, setValue] = useState(initialValue);

  const toggle = useCallback(() => {
    setValue((v) => !v);
  }, []);

  return [value, toggle];
}
```

---

### Пример использования:

```jsx
function Dropdown() {
  const [isOpen, toggleOpen] = useToggle(false);

  return (
    <div>
      <button onClick={toggleOpen}>
        {isOpen ? 'Закрыть' : 'Открыть'}
      </button>
      {isOpen && (
        <ul>
          <li>Пункт 1</li>
          <li>Пункт 2</li>
        </ul>
      )}
    </div>
  );
}
```

---

### Объяснение:

1. **initialValue:** начальное состояние (по умолчанию `false`).
2. **toggle:** инвертирует значение через функциональное обновление `setValue((v) => !v)`.
3. **useCallback:** стабильная ссылка на `toggle`, чтобы не пересоздавать функцию при каждом рендере (удобно передавать в дочерние компоненты).

---

### Важные моменты:

- Минимальный и запоминающийся хук для собеседования.
- При необходимости можно вернуть не только `toggle`, но и `setTrue` / `setFalse` для явной установки значения.
