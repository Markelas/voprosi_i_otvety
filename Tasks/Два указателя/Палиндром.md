## 🔤 Палиндром

### Задача

Проверить, является ли строка или число палиндромом (читается одинаково в обе стороны).

---

### Решение:

```js
function isPalindrome(s) {
  let left = 0, right = s.length - 1;

  while (left < right) {
    if (s[left] !== s[right]) return false;
    left++;
    right--;
  }

  return true;
}
```

---

### Объяснение:

- Два указателя двигаются навстречу друг другу
- Сравниваем символы на позициях `left` и `right`
- Если не совпадают → не палиндром
- Если дошли до середины и всё совпало → палиндром
