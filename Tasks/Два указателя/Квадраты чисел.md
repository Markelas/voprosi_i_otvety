## 🔢 Квадраты чисел в отсортированном порядке

### Задача

Вернуть массив с квадратами чисел, расставив их по порядку после преобразования.

---

### Пример:

```js
Было: [-4,-1,0,3,10]
Стало: [0,1,9,16,100]
```

---

### Решение:

```js
function sortedSquares(nums) {
  let left = 0;
  let right = nums.length - 1;
  let pos = nums.length - 1;
  let res = new Array(nums.length);

  while (left <= right) {
    if (Math.abs(nums[left]) > Math.abs(nums[right])) {
      res[pos] = nums[left] * nums[left];
      left++;
    } else {
      res[pos] = nums[right] * nums[right];
      right--;
    }
    pos--;
  }

  return res;
}
```

---

### Объяснение:

- Два указателя: слева и справа
- Заполняем результат справа налево (наибольшие квадраты)
- Сравниваем абсолютные значения: большее идёт в конец
- Двигаем указатели к центру
