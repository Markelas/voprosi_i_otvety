## 🔄 Два одинаковых числа в пределах k

### Задача

Дан массив целых чисел `nums` и целое число `k`. Нужно определить, есть ли в массиве два одинаковых числа, такие что разница их индексов не превышает `k`.

---

### Примеры:

```js
nums = [1, 2, 3, 1], k = 3   // true, потому что nums[0] и nums[3] равны (оба 1), а 3 - 0 = 3 ≤ k
nums = [1, 2, 3, 1, 2, 3], k = 2  // false, потому что одинаковые элементы есть, но все находятся дальше чем k индексов друг от друга
```

---

### Решение:

```js
function findTwoDigits(list, maxIndex) {
  const map = new Map();

  for (let i = 0; i < list.length; i++) {
    if (map.has(list[i])) {
      if (i - map.get(list[i]) <= maxIndex) {
        return true;
      }
    }
    map.set(list[i], i);
  }

  return false;
}
```

---

### Объяснение:

1. Проходимся по массиву и проверяем, есть ли число в `map`
2. Если есть — проверяем разницу индексов: `i - map.get(list[i])`
3. Если разница ≤ `maxIndex` → возвращаем `true`
4. Иначе обновляем индекс в `map` для этого числа

**Пример:** массив `[1, 2, 5, 1]`, `k = 3`:
- На индексе 3 встречаем число 1
- В `map` первое вхождение было на индексе 0
- `3 - 0 = 3 ≤ k` → `true`
