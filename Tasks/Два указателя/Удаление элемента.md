## 🗑️ Удаление элемента из массива

### Задача

Удалить из массива все элементы `val` и вернуть количество элементов без него.

---

### Пример:

```js
[0,1,2,2,3,0,4,2], val = 2
// Возвращается 5 элементов в массиве [0,1,3,0,4] (нет числа 2 в массиве)
```

---

### Решение:

```js
function removeElement(nums, val) {
  let k = 0;

  for (let i = 0; i < nums.length; i++) {
    if (nums[i] !== val) {
      nums[k] = nums[i];
      k++;
    }
  }

  return k;
}
```

---

### Объяснение:

- Используем два указателя: `i` (итератор) и `k` (позиция для записи)
- Проходим по массиву и копируем только те элементы, которые ≠ val
- `k` — количество элементов, которые остались
