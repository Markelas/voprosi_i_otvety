## 📏 Минимальный размер суммы подмассива

### Задача

Дан массив положительных целых чисел `nums` и положительное целое число `target`.

Верните минимальную длину подмассива, сумма которого больше или равна `target`. Если такого подмассива нет, верните `0`.

---

### Пример:

```js
target = 7, nums = [2,3,1,2,4,3]
// Вывод: 2
```

---

### Решение:

```js
function minSubArrayLen(target, nums) {
  let sum = 0;
  let left = 0;
  let minLen = Infinity;

  for (let right = 0; right < nums.length; right++) {
    sum += nums[right];

    while (sum >= target) {
      minLen = Math.min(minLen, right - left + 1);
      sum -= nums[left];
      left++;
    }
  }

  return minLen === Infinity ? 0 : minLen;
}
```

---

### Объяснение:

1. Используем два указателя: `left` и `right`
2. Расширяем окно (`right++`) пока сумма < target
3. Когда сумма ≥ target — сжимаем окно (`left++`), ища минимальный размер
4. Сохраняем минимальную длину
5. Возвращаем результат (или 0, если не нашли)
