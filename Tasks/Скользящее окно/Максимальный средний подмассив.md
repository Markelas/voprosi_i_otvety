## 📊 Максимальный средний подмассив

### Задача

Найдите непрерывный подмассив, длина которого равна `k`, имеющей максимальное среднее значение, и верните это значение.

---

### Пример:

```js
nums = [1,12,-5,-6,50,3], k = 4
// Вывод: 12.75000
```

---

### Решение:

```js
function findMaxAverage(nums, k) {
  let windowSum = 0;

  // Инициализируем окно
  for (let i = 0; i < k; i++) {
    windowSum += nums[i];
  }

  let maxSum = windowSum;

  // Двигаем окно
  for (let i = k; i < nums.length; i++) {
    windowSum += nums[i] - nums[i - k];
    // Двигаем окно на один элемент вправо: вычитаем левый элемент, прибавляем новый справа
    maxSum = Math.max(maxSum, windowSum);
  }

  return maxSum / k;
}
```

---

### Объяснение:

1. Инициализируем сумму первых `k` элементов
2. Двигаем окно, вычитая уходящий элемент и прибавляя новый
3. Отслеживаем максимальную сумму
4. Возвращаем среднее (сумма / k)
