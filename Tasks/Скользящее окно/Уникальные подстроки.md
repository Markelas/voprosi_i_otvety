## 🔤 Подстроки с различными символами

### Задача

Найти количество подстрок размера три с различными символами.

---

### Пример:

```js
s = "xyzzaz"
// Вывод: 1
// Имеется 4 подстроки длины 3: "xyz", "yzz", "zza" и "zaz"
// Единственная подстрока длины 3 — это "xyz"
```

---

### Решение:

```js
function countGoodSubstrings(s) {
  let count = 0;

  for (let i = 0; i <= s.length - 3; i++) {
    let substring = s.slice(i, i + 3);
    let set = new Set(substring);

    if (set.size === 3) {
      count++;
    }
  }

  return count;
}
```

---

### Объяснение:

- Проходим по строке окном размера 3
- Для каждой подстроки создаём `Set`
- Если размер `Set` равен 3 → все символы уникальны → увеличиваем счётчик
