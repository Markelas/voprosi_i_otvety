## 🔤 Самая длинная подстрока без повторяющихся символов

### Задача

Найти длину самой длинной подстроки без повторяющихся символов.

---

### Пример:

```js
s = "abcabcbb"
// Вывод: 3
// Ответ — "abc", длина — 3
```

---

### Решение:

```js
function lengthOfLongestSubstring(s) {
  const map = new Map();
  let left = 0;
  let maxLength = 0;

  for (let right = 0; right < s.length; right++) {
    const char = s[right];

    if (map.has(char) && map.get(char) >= left) {
      // Если символ уже встречался и находится в текущем окне
      left = map.get(char) + 1;
    }

    maxLength = Math.max(maxLength, right - left + 1);
    map.set(char, right);
  }

  return maxLength;
}
```

---

### Объяснение:

1. Используем `Map` для хранения последнего индекса каждого символа
2. Два указателя: `left` и `right`
3. Если символ повторяется и находится в текущем окне → двигаем `left`
4. Обновляем максимальную длину
5. Сохраняем индекс текущего символа в `map`
