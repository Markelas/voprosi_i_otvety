## 🧊 Глубокая заморозка

### Решение:

```js
const deepFreeze = (obj) => {
  Object.freeze(obj);

  for (let key in obj) {
    let value = obj[key];

    if (
      value !== null &&
      (typeof value === 'object' || typeof value === 'function') &&
      !Object.isFrozen(value)
    ) {
      deepFreeze(value);
    }
  };

  return obj;
};
```

---

### Объяснение:

1. Замораживаем текущий объект через `Object.freeze()`
2. Проходим по всем свойствам объекта
3. Для каждого вложенного объекта рекурсивно применяем `deepFreeze()`
4. Проверяем, не заморожен ли уже объект (`Object.isFrozen()`), чтобы избежать лишних операций
