## 📋 Глубокое копирование

### Решение:

```js
function deepCopy(obj) {
  // Если это примитив или функция, возвращаем как есть
  if (obj === null || typeof obj !== 'object') {
    return obj;
  }

  // Если это массив
  if (Array.isArray(obj)) {
    return obj.map(item => deepCopy(item));
  }

  // Если это объект
  const copy = {};

  for (const key in obj) {
    if (obj.hasOwnProperty(key)) {
      copy[key] = deepCopy(obj[key]);
    }
  }

  return copy;
}
```

---

### Объяснение:

1. Проверяем, является ли значение примитивом или null
2. Если массив — рекурсивно копируем каждый элемент
3. Если объект — создаём новый объект и рекурсивно копируем все свойства
4. Используем `hasOwnProperty()` для проверки собственных свойств
