## 📖 Памятка по операторам JavaScript

---

### 🔹 Унарные операторы

Все унарные (`+`, `-`, `!`) пытаются привести значение к числу или логическому:

#### Унарный плюс (`+`):

```js
+true      → 1
+false     → 0
+'42'      → 42
+[]        → 0
+{}        → NaN   // объект не число
+''        → 0     // В число превращается
+null      → 0     // С Number() также
+undefined → NaN   // С Number() также
```

#### Унарный минус (`-`):

```js
-'42'      → -42
-true      → -1
-[]        → -0
-{}        → NaN
```

#### Логическое отрицание (`!`):

```js
!0        → true   // falsy → true
!''       → true
!42       → false  // truthy → false
![]       → false
!null     → true
```

---

### ➕ Арифметические операторы

⚡ Все арифметические (`-`, `*`, `/`, `%`, `**`) всегда приводят оба операнда к числу.

⚡ Только `+` — особый: может быть строковой конкатенацией.

#### Сложение (`+`):

```js
'1' + 2        → "12"      // строка → конкатенация
1 + '2'        → "12"
1 + true       → 2         // число
'a' + true     → "atrue"   // строка
[] + false     → "false"   // строка
[1] + 1        → "11"      // строка
[] + []        → ""        // строка
[1, 2] + [3, 4] → "1,23,4" // строка
```

#### Остальные арифметические:

```js
'6' - '2'      → 4         // числа
'6' * '2'      → 12
'6' / 2        → 3
'2' ** '3'     → 8
'6' % '4'      → 2
```

---

### 🔍 Сравнения

⚡ При `<`, `>`, `<=`, `>=` операнды приводятся к числу (если не обе строки).

⚡ При `==` действует нестрогое сравнение со спец. правилами.

⚡ При `===` — без приведения типов.

#### Нестрогое сравнение (`==`):

```js
'2' < 10                → true   // '2' → 2
'2' < '10'              → false  // строки сравниваются лексикографически
null >= 0               → true   // Преобразование null к 0
undefined == null       → true   // В нестрогом null == undefined, и больше ничему
'2' == 2                → true
true == '1'             → true
[] == ''                → true
[] == 0                 → true
[1] == 1                → true
[1, 2] == '1,2'         → true
{} == "[object Object]" → true
NaN == NaN              → false
```

#### Строгое сравнение (`===`):

```js
'2' === 2           → false
true === 1          → false
null === undefined  → false
null === 0          → false
+"Infinity" === Infinity → true
undefined === void 0    → true
```

---

### 🧠 Логические операторы

⚡ `||`, `&&`, `??` не приводят к числу — они возвращают один из операндов как есть.

⚡ Приведение к boolean происходит только для выбора ветки, но само значение не меняется.

#### `||` (ИЛИ):

⚡ Возвращает первый truthy. Оба falsy → возвращает последнее

```js
null || 'hello'      → "hello"
0 || false || 42     → 42
```

#### `&&` (И):

⚡ Возвращает первый falsy или последний truthy

```js
'hello' && 42        → 42
0 && 'hi'            → 0
[] && 'value'        → 'value'
```

#### `??` (Nullish coalescing):

⚡ Возвращает первый операнд, который не является `null` или `undefined`

```js
null ?? 'default'        → "default"
undefined ?? 'default'   → "default"
0 ?? 42                  → 0   // только null/undefined считаются "нет значением"
null ?? undefined        → undefined
```

---

### 🔄 Логические с присваиванием

#### `||=` (логическое ИЛИ с присваиванием):

```js
config ||= { theme: 'dark', debug: false };
// Если config falsy, то присвоится объект
```

#### `&&=` (логическое И с присваиванием):

```js
let isAdmin = true;
isAdmin &&= 'SuperAdmin';
// Присвоится SuperAdmin
```

#### `??=` (nullish coalescing с присваиванием):

```js
let settings = null;
settings ??= { theme: 'dark' };
// { theme: 'dark' } (только если settings === null или undefined)
```

---

### 📊 Постфиксные (`a++`, `a--`)

Сначала возвращают старое значение, потом изменяют переменную.

```js
let a = 1;
console.log(a++); // 1 (вернули старое значение)
console.log(a);   // 2 (увеличилось после вызова)

let b = 5;
console.log(b--); // 5 (вернули старое значение)
console.log(b);   // 4 (уменьшилось после вызова)
```

---

### 📈 Префиксные (`++a`, `--a`)

Сначала изменяют переменную, потом возвращают новое значение.

```js
let x = 1;
console.log(++x); // 2 (увеличили, потом вернули)
console.log(x);   // 2

let y = 5;
console.log(--y); // 4 (уменьшили, потом вернули)
console.log(y);   // 4
```

---

### 📋 Приоритет операторов (главное помнить)

1. `()` — скобки
2. `!x`, `+x`, `-x`, `typeof ...` (унарные)
3. `**` — возведение в степень
4. `*`, `/`, `%` — умножение, деление, остаток
5. `+`, `-` — сложение, вычитание
6. `<`, `>`, `<=`, `>=` — сравнения
7. `==`, `!=`, `===`, `!==` — равенство
8. `&&` — логическое И
9. `||` — логическое ИЛИ
10. `??` — nullish coalescing
11. `? :` — тернарный оператор
12. `=`, `+=`, `-=`, ... — присваивание

---

### ⚠️ Подводные камни

#### Массивы и объекты:

```js
[] == ![]        → true   // [] → '' → 0, ![] → false → 0
{} + []          → 0      // {} → пустой блок, [] → 0
[] + {}          → "[object Object]"
[] + []          → ""     // строки
{} + {}          → NaN    // объекты → числа
{} - {}          → NaN
```

#### null и undefined:

```js
null == 0        → false
null >= 0        → true   // null → 0 в сравнении
null > 0         → false
undefined == 0   → false
undefined >= 0   → false

null + 1         → 1      // число, так как null посчитался как 0
undefined + 1    → NaN    // NaN
```

#### Логические значения:

```js
true + true      → 2
true == 1        → true
false == 0       → true
```

#### Смешанные операции:

```js
'5' - true       → 4      // → числа
'5' + true       → "5true" // + → строка
'5' * []         → 0
'5' * {}         → NaN

!!'hello'        → true
!![]             → true
!!{}             → true
!!0              → false
!!null           → false
```

#### Сложные примеры:

```js
x++ - --x + ++x - x--   // Будет 5 – 5 + 6 – 6 = 0

Boolean(new Boolean(false))  // Так как есть new, создаётся объект, а объект это true
```

---

### 💡 Рекомендации:

✅ **Всегда используй `===`** для сравнений  
✅ **Используй `??`** вместо `||` когда нужно проверить только `null`/`undefined`  
✅ **Помни про конкатенацию строк** при использовании `+`  
✅ **Осторожно с `==`** — может давать неожиданные результаты
