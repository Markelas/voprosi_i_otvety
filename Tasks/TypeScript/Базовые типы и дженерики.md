## 📋 Базовые типы и дженерики

---

### **Задача 1: Типизация функции getProperty**

**Условие:**
Создайте функцию `getProperty`, которая принимает объект и ключ, и возвращает значение по этому ключу. Функция должна быть типобезопасной.

**Начальный код:**
```ts
const X = { a: 1, b: 2, c: 3 };

function getProperty(obj: any, key: any): any {
  return obj[key];
}

getProperty(X, 'a');
getProperty(X, 'm'); // Должна быть ошибка
```

**Решение:**
```ts
const X = { a: 1, b: 2, c: 3 };

function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}

getProperty(X, 'a'); // ✅ OK, возвращает number
getProperty(X, 'm'); // ❌ Ошибка: 'm' не существует в типе
```

**Объяснение:**
- `T` — тип объекта
- `K extends keyof T` — ключ должен быть одним из ключей объекта `T`
- `T[K]` — возвращаемый тип — значение по ключу `K` в объекте `T`

---

### **Задача 2: Базовые типы и интерфейсы**

**Условие:**
Добавьте в тип `User` новое поле `email?: string` (опциональное) и модифицируйте функцию `greet`, чтобы она возвращала `Hello, Alice! Your email is alice@example.com`, если email есть.

**Начальный код:**
```ts
function greet(user: any): any {
  return `Hello, ${user.name}!`;
}

const newUser = { name: "Alice", age: 25 };
console.log(greet(newUser));
```

**Решение:**
```ts
interface User {
  name: string;
  age: number;
  email?: string; // Опциональное поле
}

function greet(user: User): string {
  const greeting = `Hello, ${user.name}!`;
  return user.email 
    ? `${greeting} Your email is ${user.email}`
    : greeting;
}

const newUser: User = { name: "Alice", age: 25, email: "alice@example.com" };
console.log(greet(newUser)); // "Hello, Alice! Your email is alice@example.com"
```

---

### **Задача 3: Типизация функций (перегрузка)**

**Условие:**
Напишите перегруженную функцию `add`, которая может складывать два числа или конкатенировать две строки.

**Начальный код:**
```ts
function add(a: number, b: number): number {
  return a + b;
}
add(2, 3);      // ✅
add('2', '3');  // ❌ Ошибка
```

**Решение:**
```ts
// Перегрузки
function add(a: number, b: number): number;
function add(a: string, b: string): string;
// Реализация
function add(a: number | string, b: number | string): number | string {
  if (typeof a === 'number' && typeof b === 'number') {
    return a + b;
  }
  return String(a) + String(b);
}

add(2, 3);      // ✅ number: 5
add('2', '3');  // ✅ string: "23"
```

---

### **Задача 4: Дженерики**

**Условие:**
Перепишите функцию `getFirstElement` с использованием дженериков, чтобы TypeScript корректно определял тип возвращаемого значения.

**Начальный код:**
```ts
function getFirstElement(arr: any[]): any {
  return arr[0];
}

const first = getFirstElement([{ name: 'rr' }, { name: 'dd' }]);
// first имеет тип any
```

**Решение:**
```ts
function getFirstElement<T>(arr: T[]): T | undefined {
  return arr[0];
}

const first = getFirstElement([{ name: 'rr' }, { name: 'dd' }]);
// first: { name: string } | undefined ✅

const second = getFirstElement([{ age: 1 }, { age: 4 }]);
// second: { age: number } | undefined ✅
```

---

### **Задача 5: Преобразование массива в union тип**

**Условие:**
Преобразуйте массив строк в union тип.

**Начальный код:**
```ts
const fruits = ["banana", "orange", "apple"] as const;

type FRUITS = any;
```

**Решение:**
```ts
const fruits = ["banana", "orange", "apple"] as const;

type FRUITS = typeof fruits[number];
// Результат: "banana" | "orange" | "apple"

const fruit: FRUITS = "banana"; // ✅
const fruit2: FRUITS = "grape"; // ❌ Ошибка
```

**Объяснение:**
- `as const` делает массив readonly и значения литеральными типами
- `typeof fruits[number]` извлекает union тип всех элементов массива

---

### **Задача 6: Преобразование типа со скобками**

**Условие:**
Преобразуйте тип `Status` в тип, где каждое значение обернуто в квадратные скобки.

**Начальный код:**
```ts
type Status = 'new' | 'waiting_load' | 'in_work' | 'done' | 'cert_ready';

type StatusWithBrackets = // TODO

const val: StatusWithBrackets = '[waiting_load]';
```

**Решение:**
```ts
type Status = 'new' | 'waiting_load' | 'in_work' | 'done' | 'cert_ready';

type StatusWithBrackets = `[${Status}]`;
// Результат: "[new]" | "[waiting_load]" | "[in_work]" | "[done]" | "[cert_ready]"

const val: StatusWithBrackets = '[waiting_load]'; // ✅
```

**Объяснение:**
Template literal types позволяют создавать строковые типы на основе других типов.

```ts
interface Post {
	id: number;
	title: string;
	published: boolean;
}
interface User {
	id: number;
	name: string;
}

function updateEntity<T extends object>(
	obj: T,
	updatedObj: { [K in keyof T]?: T[K] } // Проверка, что есть ключ
	): T {
	return {...obj, ...updatedObj}
}

const user: User = { id: 1, name: 'Alice' }
const updatedUser = updateEntity(user, { name: 'Bob' });

const post: Post = { id: 1, title: "Hello", published: false };
const updatedPost = updateEntity(post, { published: true });

// ошибка, не существует age
updateEntity(user, { age: 30 })
```

Здесь проверяем, что ключ в обновленном объекте есть, но не все поля обязательны, так как мы меняем только одно или несколько. 
