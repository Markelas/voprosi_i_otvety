## 🔧 Продвинутые утилитарные типы

---

### **Задача 1: Написать нативный Exclude<T, U>**

**Условие:**
Реализуйте утилитарный тип `MyExclude`, который исключает из union типа `T` все типы, которые можно присвоить `U`.

**Начальный код:**
```ts
// TODO: Реализовать MyExclude
type Result = MyExclude<'a' | 'b' | 'c', 'a'>;
// Должно быть: 'b' | 'c'
```

**Решение:**
```ts
type MyExclude<T, U> = T extends U ? never : T;

type Result = MyExclude<'a' | 'b' | 'c', 'a'>;
// 'b' | 'c' ✅
```

**Объяснение:**
- Условный тип проверяет, можно ли `T` присвоить `U`
- Если да → `never` (исключается), иначе → `T`

---

### **Задача 2: Написать нативный Extract<T, U>**

**Условие:**
Реализуйте утилитарный тип `MyExtract`, который извлекает из union типа `T` только те типы, которые можно присвоить `U`.

**Начальный код:**
```ts
// TODO: Реализовать MyExtract
type Result = MyExtract<'a' | 'b' | 'c', 'a' | 'd'>;
// Должно быть: 'a'
```

**Решение:**
```ts
type MyExtract<T, U> = T extends U ? T : never;

type Result = MyExtract<'a' | 'b' | 'c', 'a' | 'd'>;
// 'a' ✅
```

**Объяснение:**
- Условный тип проверяет, можно ли `T` присвоить `U`
- Если да → `T`, иначе → `never` (исключается)

---

### **Задача 3: Написать нативный NonNullable<T>**

**Условие:**
Реализуйте утилитарный тип `MyNonNullable`, который исключает `null` и `undefined` из типа `T`.

**Начальный код:**
```ts
// TODO: Реализовать MyNonNullable
type Result = MyNonNullable<string | null | undefined>;
// Должно быть: string
```

**Решение:**
```ts
type MyNonNullable<T> = T extends null | undefined ? never : T;

type Result = MyNonNullable<string | null | undefined>;
// string ✅
```

**Объяснение:**
- Условный тип проверяет, является ли `T` `null` или `undefined`
- Если да → `never`, иначе → `T`

---

### **Задача 4: Написать нативный Parameters<T>**

**Условие:**
Реализуйте утилитарный тип `MyParameters`, который извлекает типы параметров функции в виде кортежа.

**Начальный код:**
```ts
// TODO: Реализовать MyParameters
type Func = (a: number, b: string) => void;
type Result = MyParameters<Func>;
// Должно быть: [number, string]
```

**Решение:**
```ts
type MyParameters<T extends (...args: any) => any> = 
  T extends (...args: infer P) => any ? P : never;

type Func = (a: number, b: string) => void;
type Result = MyParameters<Func>;
// [number, string] ✅
```

**Объяснение:**
- `T extends (...args: any) => any` — проверяем, что `T` функция
- `infer P` — извлекаем тип параметров
- Возвращаем `P` или `never`

---

### **Задача 5: Написать нативный ReturnType<T>**

**Условие:**
Реализуйте утилитарный тип `MyReturnType`, который извлекает возвращаемый тип функции.

**Начальный код:**
```ts
// TODO: Реализовать MyReturnType
type Func = () => string;
type Result = MyReturnType<Func>;
// Должно быть: string
```

**Решение:**
```ts
type MyReturnType<T extends (...args: any) => any> = 
  T extends (...args: any) => infer R ? R : any;

type Func = () => string;
type Result = MyReturnType<Func>;
// string ✅
```

**Объяснение:**
- `T extends (...args: any) => any` — проверяем, что `T` функция
- `infer R` — извлекаем тип возвращаемого значения
- Возвращаем `R` или `any`

---

### **Задача 6: Написать нативный ConstructorParameters<T>**

**Условие:**
Реализуйте утилитарный тип `MyConstructorParameters`, который извлекает типы параметров конструктора класса.

**Начальный код:**
```ts
// TODO: Реализовать MyConstructorParameters
class User {
  constructor(public name: string, public age: number) {}
}
type Result = MyConstructorParameters<typeof User>;
// Должно быть: [string, number]
```

**Решение:**
```ts
type MyConstructorParameters<T extends new (...args: any) => any> = 
  T extends new (...args: infer P) => any ? P : never;

class User {
  constructor(public name: string, public age: number) {}
}
type Result = MyConstructorParameters<typeof User>;
// [string, number] ✅
```

**Объяснение:**
- `T extends new (...args: any) => any` — проверяем, что `T` конструктор
- `infer P` — извлекаем тип параметров конструктора
- Возвращаем `P` или `never`

---

### **Задача 7: Написать нативный InstanceType<T>**

**Условие:**
Реализуйте утилитарный тип `MyInstanceType`, который извлекает тип экземпляра класса из конструктора.

**Начальный код:**
```ts
// TODO: Реализовать MyInstanceType
class User {
  name: string = '';
}
type Result = MyInstanceType<typeof User>;
// Должно быть: User
```

**Решение:**
```ts
type MyInstanceType<T extends new (...args: any) => any> = 
  T extends new (...args: any) => infer R ? R : any;

class User {
  name: string = '';
}
type Result = MyInstanceType<typeof User>;
// User ✅
```

**Объяснение:**
- `T extends new (...args: any) => any` — проверяем, что `T` конструктор
- `infer R` — извлекаем тип экземпляра
- Возвращаем `R` или `any`

---

### **Задача 8: Написать нативный ThisParameterType<T>**

**Условие:**
Реализуйте утилитарный тип `MyThisParameterType`, который извлекает тип параметра `this` функции.

**Начальный код:**
```ts
// TODO: Реализовать MyThisParameterType
function func(this: { name: string }) {
  return this.name;
}
type Result = MyThisParameterType<typeof func>;
// Должно быть: { name: string }
```

**Решение:**
```ts
type MyThisParameterType<T> = 
  T extends (this: infer U, ...args: any[]) => any ? U : unknown;

function func(this: { name: string }) {
  return this.name;
}
type Result = MyThisParameterType<typeof func>;
// { name: string } ✅
```

**Объяснение:**
- `T extends (this: infer U, ...args: any[]) => any` — проверяем функцию с `this`
- `infer U` — извлекаем тип `this`
- Возвращаем `U` или `unknown`

---

### **Задача 9: Написать нативный OmitThisParameter<T>**

**Условие:**
Реализуйте утилитарный тип `MyOmitThisParameter`, который удаляет параметр `this` из типа функции.

**Начальный код:**
```ts
// TODO: Реализовать MyOmitThisParameter
function func(this: { name: string }, age: number) {
  return this.name;
}
type Result = MyOmitThisParameter<typeof func>;
// Должно быть: (age: number) => string
```

**Решение:**
```ts
type MyOmitThisParameter<T> = 
  T extends (this: any, ...args: infer P) => infer R 
    ? (...args: P) => R 
    : T;

function func(this: { name: string }, age: number) {
  return this.name;
}
type Result = MyOmitThisParameter<typeof func>;
// (age: number) => string ✅
```

**Объяснение:**
- Извлекаем параметры (`P`) и возвращаемый тип (`R`)
- Создаем новую функцию без `this`, но с теми же параметрами и возвращаемым типом

---

### **Задача 10: Написать FirstChar<T>**

**Условие:**
Реализуйте тип `FirstChar`, который извлекает первый символ строкового типа.

**Начальный код:**
```ts
// TODO: Реализовать FirstChar
type Result = FirstChar<'Hello'>;
// Должно быть: 'H'
```

**Решение:**
```ts
type FirstChar<T extends string> = T extends `${infer F}${string}` ? F : never;

type Result = FirstChar<'Hello'>;
// 'H' ✅
```

**Объяснение:**
- Template literal type с `infer F` извлекает первый символ
- `${infer F}${string}` — первый символ и остальная строка
- Возвращаем `F` или `never`

---

### **Задача 11: Написать LastChar<T>**

**Условие:**
Реализуйте тип `LastChar`, который извлекает последний символ строкового типа.

**Начальный код:**
```ts
// TODO: Реализовать LastChar
type Result = LastChar<'Hello'>;
// Должно быть: 'o'
```

**Решение:**
```ts
type LastChar<T extends string> = T extends `${string}${infer L}` 
  ? L extends `${string}${string}` 
    ? LastChar<L> 
    : L 
  : never;

type Result = LastChar<'Hello'>;
// 'o' ✅
```

**Объяснение:**
- Рекурсивно извлекаем последний символ
- Если осталось больше одного символа → рекурсия
- Если один символ → возвращаем его

