## 🛠️ Утилитарные типы (Pick, Omit, Partial, Record)

---

### **Задача 1: Написать нативный Pick<T, K>**

**Условие:**
Реализуйте утилитарный тип `MyPick`, который выбирает указанные свойства из типа.

**Начальный код:**
```ts
interface User {
  name: string;
  age: number;
  email: string;
}

// TODO: Реализовать MyPick
type UserWithName = MyPick<User, 'name'>;
// Должно быть: { name: string }
```

**Решение:**
```ts
type MyPick<T, K extends keyof T> = {
  [key in K]: T[key];
};

interface User {
  name: string;
  age: number;
  email: string;
}

type UserWithName = MyPick<User, 'name'>;
// { name: string } ✅

type UserNameAndAge = MyPick<User, 'name' | 'age'>;
// { name: string; age: number } ✅
```

**Объяснение:**
- `K extends keyof T` — ключи должны быть из типа `T`
- `[key in K]` — итерируемся по ключам `K`
- `T[key]` — берем тип значения из исходного типа

---

### **Задача 2: Написать нативный Omit<T, K>**

**Условие:**
Реализуйте утилитарный тип `MyOmit`, который исключает указанные свойства из типа.

**Начальный код:**
```ts
interface User {
  name: string;
  age: number;
  email: string;
}

// TODO: Реализовать MyOmit
type UserWithoutEmail = MyOmit<User, 'email'>;
// Должно быть: { name: string; age: number }
```

**Решение:**
```ts
type MyOmit<T, K extends keyof T> = {
  [Key in keyof T as Key extends K ? never : Key]: T[Key];
};

interface User {
  name: string;
  age: number;
  email: string;
}

type UserWithoutEmail = MyOmit<User, 'email'>;
// { name: string; age: number } ✅
```

**Альтернативное решение через Pick:**
```ts
type MyOmit<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;
```

**Объяснение:**
- `Key extends K ? never : Key` — если ключ входит в `K`, исключаем его (never), иначе оставляем
- `as` используется для фильтрации ключей в mapped types

---

### **Задача 3: Написать нативный Partial<T>**

**Условие:**
Реализуйте утилитарный тип `MyPartial`, который делает все свойства опциональными.

**Начальный код:**
```ts
interface User {
  name: string;
  age: number;
  email: string;
}

// TODO: Реализовать MyPartial
type PartialUser = MyPartial<User>;
// Должно быть: { name?: string; age?: number; email?: string }
```

**Решение:**
```ts
type MyPartial<T> = {
  [Key in keyof T]?: T[Key];
};

interface User {
  name: string;
  age: number;
  email: string;
}

type PartialUser = MyPartial<User>;
// { name?: string; age?: number; email?: string } ✅
```

**Объяснение:**
- `?` делает свойство опциональным
- Итерируемся по всем ключам типа `T`

---

### **Задача 4: Написать нативный Record<K, T>**

**Условие:**
Реализуйте утилитарный тип `MyRecord`, который создает объект с ключами типа `K` и значениями типа `T`.

**Начальный код:**
```ts
// TODO: Реализовать MyRecord
type UserRoles = MyRecord<'admin' | 'user' | 'guest', boolean>;
// Должно быть: { admin: boolean; user: boolean; guest: boolean }
```

**Решение:**
```ts
type MyRecord<K extends keyof any, T> = {
  [Key in K]: T;
};

type UserRoles = MyRecord<'admin' | 'user' | 'guest', boolean>;
// { admin: boolean; user: boolean; guest: boolean } ✅

type StatusMap = MyRecord<'pending' | 'success' | 'error', string>;
// { pending: string; success: string; error: string } ✅
```

**Объяснение:**
- `K extends keyof any` — ключи могут быть string, number или symbol, так как any возвращает string | number | symbol и в последствии, любой тип становится string ключом.
- `[Key in K]` — создаем свойство для каждого ключа из `K`
- `T` — тип значения для всех свойств

---

### **Задача 5: Использование Pick и Omit**

**Условие:**
Создайте типы `BookPreview` (только title и author) и `BookWithoutPages` (без pages), используя `Pick` и `Omit`.

**Начальный код:**
```ts
interface Book {
  title: string;
  author: string;
  pages: number;
  isPublished: boolean;
}

// TODO: Создать типы
```

**Решение:**
```ts
interface Book {
  title: string;
  author: string;
  pages: number;
  isPublished: boolean;
}

// Используя Pick
type BookPreview = Pick<Book, 'title' | 'author'>;
// { title: string; author: string }

// Используя Omit
type BookWithoutPages = Omit<Book, 'pages'>;
// { title: string; author: string; isPublished: boolean }

// Примеры использования
const preview: BookPreview = {
  title: "TypeScript Guide",
  author: "John Doe"
};

const book: BookWithoutPages = {
  title: "TypeScript Guide",
  author: "John Doe",
  isPublished: true
};
```

---

### **Задача 6: Написать нативный Required<T>**

**Условие:**
Реализуйте утилитарный тип `MyRequired`, который делает все свойства обязательными (противоположность `Partial`).

**Начальный код:**
```ts
interface User {
  name?: string;
  age?: number;
  email?: string;
}

// TODO: Реализовать MyRequired
type RequiredUser = MyRequired<User>;
// Должно быть: { name: string; age: number; email: string }
```

**Решение:**
```ts
type MyRequired<T> = {
  [Key in keyof T]-?: T[Key];
};

interface User {
  name?: string;
  age?: number;
  email?: string;
}

type RequiredUser = MyRequired<User>;
// { name: string; age: number; email: string } ✅
```

**Объяснение:**
- `-?` удаляет опциональность (убирает `?`)
- Итерируемся по всем ключам типа `T`

---

### **Задача 7: Написать нативный Readonly<T>**

**Условие:**
Реализуйте утилитарный тип `MyReadonly`, который делает все свойства только для чтения.

**Начальный код:**
```ts
interface User {
  name: string;
  age: number;
}

// TODO: Реализовать MyReadonly
type ReadonlyUser = MyReadonly<User>;
// Должно быть: { readonly name: string; readonly age: number }
```

**Решение:**
```ts
type MyReadonly<T> = {
  readonly [Key in keyof T]: T[Key];
};

interface User {
  name: string;
  age: number;
}

type ReadonlyUser = MyReadonly<User>;
// { readonly name: string; readonly age: number } ✅
```

**Объяснение:**
- `readonly` делает каждое свойство только для чтения
- Итерируемся по всем ключам типа `T`


