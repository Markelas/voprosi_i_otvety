## 🔒 Задачи на область видимости и замыкания

---

### Задача 1: var и hoisting

```js
var foo = 1;

function bar() {
  if (!foo) {
    var foo = 10;
  }
  console.log(foo); // 10
}

bar();
```

**Объяснение:** `var` поднимается (hoisting), но инициализируется как `undefined`. `!foo` → `true`, заходим в блок, присваиваем `10`.

---

### Задача 2: function declaration hoisting

```js
var a = 1;

function b() {
  a = 10;
  return;
  function a() {}
}

b();
console.log(a); // 1
```

**Объяснение:** Функция `a()` поднимается и переопределяет локальную переменную. `a = 10` изменяет локальную функцию, а не глобальную `a`.

---

### Задача 3: var в цикле

```js
for (var i = 0; i < 5; i++) {
  setTimeout(function() {
    console.log(i); // Что выведет?
  }, 1000);
}
```

**Вывод:** `5 5 5 5 5`

**Объяснение:** `var` не имеет блочной области видимости — одна переменная на весь цикл. Все таймеры видят одно и то же значение `5` (после завершения цикла).

**Решение:** Используй `let` или замыкание:

```js
// С let
for (let i = 0; i < 5; i++) {
  setTimeout(function() {
    console.log(i); // 0 1 2 3 4
  }, 1000);
}

// С замыканием
for (var i = 0; i < 5; i++) {
  (function(j) {
    setTimeout(function() {
      console.log(j); // 0 1 2 3 4
    }, 1000);
  })(i);
}
```

---

### Задача 4: IIFE и инкапсуляция

```js
var counterModule = (function() {
  let count = 0;

  return {
    increment() {
      return ++count;
    },
    reset() {
      count = 0;
    }
  };
})();

console.log(counterModule.increment()); // 1
console.log(counterModule.increment()); // 2
console.log(counterModule.reset());
console.log(counterModule.increment()); // 1
```

**Объяснение:**

1. **IIFE** создаёт свою область видимости — переменная `count` не видна снаружи
2. **Инкапсуляция через замыкание:** методы сохраняют доступ к `count` даже после выполнения IIFE
3. Переменная `count` «живёт» между вызовами, но остаётся недоступной извне
