## 🔗 Валидные скобки

### Задача

Если задана строка `s`, содержащая только символы `'('`, `')'`, `'{'`, `'}'`, `'['` и `']'`, определите, является ли входная строка допустимой.

---

### Примеры:

```js
s = "()[]{}"  // ✅ правильно
s = "([)]"    // ❌ ложь
```

---

### Решение:

```js
function isValid(s) {
  const brackets = {
    '}': '{',
    ']': '[',
    ')': '('
  };

  const stack = [];

  for (let i = 0; i < s.length; i++) {
    const char = s[i];

    // Здесь будут закрывающие }, ], )
    if (brackets[char]) {
      // Берем из стека последнюю скобку
      const lastBracket = stack.pop();

      if (lastBracket !== brackets[char]) {
        // Если взятая скобка из стека не совпадает с закрывающей из char, значит порядок нарушен
        return false;
      }
    } else {
      // Открывающие скобки добавляем в стек
      stack.push(char);
    }
  }

  return stack.length === 0;
}
```

---

### Объяснение:

1. Создаём объект-маппинг закрывающих скобок на открывающие
2. Используем стек для хранения открывающих скобок
3. Если встречаем открывающую → добавляем в стек
4. Если встречаем закрывающую → проверяем соответствие с последней открывающей
5. Если не совпадает → невалидно
6. В конце стек должен быть пустым
