### 🧩 Задача

Напиши `infiniteCurry(fn)`, чтобы она могла принимать аргументы **по частям сколько угодно**, пока не будет пустой вызов:

```js
const add = infiniteCurry((...nums) => nums.reduce((a, b) => a + b, 0));  

console.log(add(1)(2)(3)()); // 6
console.log(add(5)(10)(15)(20)()); // 50
```

<font color="#76923c">Решение</font>
Пустой вызов `()` можно использовать как сигнал “выполнить функцию”.

```js
function infiniteCurry(fn) {
  return function next(...args) {
    if (args.length === 0) {
      return fn();
    }
    fn = fn.bind(null, ...args);
    return next;
  };
}

```

<font color="#76923c">Второй вариант, без</font> <font color="#31859b">.bind</font>

```js
function infiniteCurry(fn) {
  let collectedArgs = [];

  function next(...args) {
    if (args.length === 0) {
      return fn(...collectedArgs);
    }

    collectedArgs = [...collectedArgs, ...args];
    return next;
  }

  return next;
}

```
💡<font color="#e36c09"> Что делает:</font>

- Не знает, сколько аргументов должно быть.
- Каждый раз, когда передаёшь аргументы — **частично привязывает их** к функции через `.bind`.
- Выполняется **только после пустого вызова `()`**.

 ⚙️ <font color="#31859b">Механизм:</font>
 
- Каждый вызов `add(1)` → создаёт новую версию функции `fn`, где уже «прибиты» аргументы.
- Когда встречается вызов без аргументов `()`, выполняется `fn()` с накопленными значениями.
- За счёт `.bind` ты можешь вызывать сколько угодно раз до пустых скобок.