```js
Function.prototype.myCall = function(context, ...args) {
    // Если контекст не передан, берем глобальный объект (window в браузере)
    context = context || window;
    
    // Создаем уникальное временное свойство (чтобы не перезаписать существующее)
    const temp = Symbol();
    
    // Привязываем нашу функцию к контексту
    context[temp] = this;
    
    // Вызываем функцию с аргументами и сохраняем результат
    const fnToCall = context[uniqueKey];
    const result = fnToCall(...args);
    
    // Удаляем временное свойство (убираем за собой)
    delete context[temp];
    
    // Возвращаем результат
    return result;
};
```

1. **Проверь контекст** - если нет, возьми window
    
2. **Создай временное имя** - чтобы не сломать объект
    
3. **Прикрепи функцию** - повесь её на объект
    
4. **Вызови** - с нужными аргументами
    
5. **Удали** - чтобы не оставлять мусор