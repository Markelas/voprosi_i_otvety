## 🔄 Полифил Array.reduce

### Реализация:

```js
Array.prototype.myReduce = function(callback, initialValue) {
  let accumulator;
  let startIndex;

  // Если есть initialValue, начинаем с него, иначе с первого элемента
  if (initialValue !== undefined) {
    accumulator = initialValue;
    startIndex = 0;
  } else {
    // Если initialValue нет, берём первый элемент как начальное значение
    accumulator = this[0];
    startIndex = 1;
  }

  // Проходим по массиву, начиная с нужного индекса
  for (let i = startIndex; i < this.length; i++) {
    accumulator = callback(accumulator, this[i], i, this);
  }

  return accumulator;
};
```

---

### Пример использования:

```js
const numbers = [1, 2, 3, 4, 5];

// С начальным значением
const sum = numbers.myReduce((acc, num) => acc + num, 0); // 15

// Без начального значения
const product = numbers.myReduce((acc, num) => acc * num); // 120

// Более сложный пример
const people = [
  { name: 'Alice', age: 25 },
  { name: 'Bob', age: 30 },
  { name: 'Charlie', age: 35 }
];

const totalAge = people.myReduce((acc, person) => acc + person.age, 0); // 90
```

---

### Объяснение:

1. Если передан `initialValue`, используем его как начальное значение аккумулятора
2. Если `initialValue` не передан, используем первый элемент массива как начальное значение
3. Проходим по массиву, вызывая `callback` для каждого элемента
4. `callback` получает: аккумулятор, текущий элемент, индекс, массив
5. Возвращаем финальное значение аккумулятора

**Важно:**
- Если массив пустой и нет `initialValue`, должна быть ошибка
- Если массив пустой и есть `initialValue`, возвращаем `initialValue`
