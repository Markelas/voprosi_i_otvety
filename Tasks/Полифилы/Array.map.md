## 🔄 Полифил Array.map

### Реализация:

```js
Array.prototype.myMap = function(callback) {
  const result = [];

  for (let i = 0; i < this.length; i++) {
    result.push(callback(this[i], i, this));
    // this здесь это массив
  }

  return result;
};
```

---

### Пример использования:

```js
const arr = [1, 2, 3, 4, 5];
const mappedArr = arr.myMap((digit) => digit * 2); // [2, 4, 6, 8, 10]
```

---

### Объяснение:

1. Создаём новый массив `result`
2. Проходим по каждому элементу исходного массива
3. Вызываем `callback` для каждого элемента с параметрами: элемент, индекс, массив
4. Результат добавляем в новый массив
5. Возвращаем новый массив
