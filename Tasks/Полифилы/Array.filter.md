## 🔄 Полифил Array.filter

### Реализация:

```js
Array.prototype.myFilter = function(callback) {
  const result = [];

  for (let i = 0; i < this.length; i++) {
    // Если callback возвращает true, добавляем элемент в результат
    if (callback(this[i], i, this)) {
      result.push(this[i]);
    }
  }

  return result;
};
```

---

### Пример использования:

```js
const numbers = [1, 2, 3, 4, 5, 6];
const evens = numbers.myFilter((num) => num % 2 === 0); // [2, 4, 6]

const words = ['apple', 'banana', 'cherry', 'date'];
const longWords = words.myFilter((word) => word.length > 5); // ['banana', 'cherry']
```

---

### Объяснение:

1. Создаём новый массив `result`
2. Проходим по каждому элементу исходного массива
3. Вызываем `callback` для каждого элемента с параметрами: элемент, индекс, массив
4. Если `callback` возвращает `true` (truthy значение), добавляем элемент в результат
5. Возвращаем новый массив с отфильтрованными элементами
