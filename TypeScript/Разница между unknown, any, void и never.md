## 🔍 Разница между `unknown`, `any`, `void` и `never`

---

### 🔴 `any`

**"Выключатель" типизации.** Полностью отключает проверку типов для переменной.

Позволяет делать что угодно: вызывать методы, обращаться к свойствам — TypeScript не будет ругаться.

```ts
let value: any = "hello";
value.foo.bar(); // ✅ Не ошибка
value = 42; // ✅ Не ошибка
```

---

### 🟡 `unknown`

**Безопасная альтернатива `any`.**

Тип "неизвестно что" — переменная может хранить любое значение, но TypeScript заставит вас проверить тип перед использованием.

**Безопасность:** невозможно случайно вызвать метод или обратиться к свойству без проверки.

```ts
let value: unknown = getData();
value.foo(); // ❌ Ошибка!

// Нужно проверить тип
if (typeof value === 'string') {
  value.toUpperCase(); // ✅ Теперь можно
}
```

**Рекомендуется использовать в ситуациях:**
- Парсинг JSON
- Получение данных с сервера (пока не знаем тип)

---

### 🔵 `void`

Используют для функций, которые **выполняют действие, но не возвращают значение**.

```ts
function log(message: string): void {
  console.log(message);
  // Нет return или return без значения
}
```

---

### ⚫ `never`

Используют для функций, которые **никогда не возвращаются нормальным образом**.

Например:
- Всегда бросают исключение
- Находятся в бесконечном цикле

```ts
function throwError(): never {
  throw new Error("Ошибка!");
}

function infiniteLoop(): never {
  while (true) {}
}
```

---

### 📊 Сравнение:

| Тип | Назначение | Безопасность |
|-----|-----------|--------------|
| `any` | Отключение типизации | ❌ Небезопасно |
| `unknown` | Неизвестный тип | ✅ Безопасно (нужна проверка) |
| `void` | Отсутствие возвращаемого значения | ✅ |
| `never` | Функция никогда не возвращается | ✅ |
