## 📦 Встроенные дженерики в TypeScript

**Встроенные дженерики** — это типы и интерфейсы TypeScript, которые используют дженерики для работы с различными типами данных (Array, Promise, Map, Set и т.д.).

---

### 📊 Таблица встроенных дженериков:

| Тип/Интерфейс | Синтаксис | Описание | Пример |
|---------------|-----------|----------|--------|
| **`Array<T>`** | `Array<T>` или `T[]` | Массив элементов типа `T` | `Array<string>`, `number[]` |
| **`Promise<T>`** | `Promise<T>` | Промис, который разрешается в тип `T` | `Promise<string>`, `Promise<User>` |
| **`ReadonlyArray<T>`** | `ReadonlyArray<T>` | Неизменяемый массив | `ReadonlyArray<number>` |
| **`Map<K, V>`** | `Map<K, V>` | Коллекция ключ-значение | `Map<string, number>` |
| **`Set<T>`** | `Set<T>` | Коллекция уникальных значений | `Set<string>` |
| **`WeakMap<K, V>`** | `WeakMap<K, V>` | Слабая карта (ключи - объекты) | `WeakMap<object, string>` |
| **`WeakSet<T>`** | `WeakSet<T>` | Слабое множество (только объекты) | `WeakSet<object>` |

---

### 💡 Примеры использования:

#### **Array<T>**

```ts
// Два способа записи (эквивалентны)
const numbers: Array<number> = [1, 2, 3];
const strings: string[] = ["a", "b", "c"];

interface User {
  id: number;
  name: string;
}

const users: User[] = [
  { id: 1, name: "Alice" },
  { id: 2, name: "Bob" }
];

// Многомерные массивы
const matrix: number[][] = [[1, 2], [3, 4]];
const matrix2: Array<Array<number>> = [[1, 2], [3, 4]];
```

#### **Promise<T>**

```ts
interface User {
  id: number;
  name: string;
}

// Функция возвращает Promise
const fetchUser = (): Promise<User> => {
  return fetch('/api/user').then(res => res.json());
};

// Async функция
async function getUser(): Promise<User> {
  const response = await fetch('/api/user');
  return response.json();
}

// Promise с обработкой ошибок
async function getUserSafe(): Promise<User | null> {
  try {
    return await getUser();
  } catch (error) {
    return null;
  }
}
```

#### **ReadonlyArray<T>**

```ts
const readonlyNumbers: ReadonlyArray<number> = [1, 2, 3];
// readonlyNumbers.push(4); // ❌ Ошибка - нельзя изменять
// readonlyNumbers[0] = 10; // ❌ Ошибка

// Можно читать
const first = readonlyNumbers[0]; // ✅ number

// Можно использовать методы, которые не изменяют массив
const doubled = readonlyNumbers.map(n => n * 2); // ✅ number[]
```

#### **Map<K, V>**

```ts
// Map с типизацией
const userMap: Map<string, User> = new Map();
userMap.set("1", { id: 1, name: "Alice" });
userMap.set("2", { id: 2, name: "Bob" });

const user = userMap.get("1"); // User | undefined
const hasUser = userMap.has("1"); // boolean

// Итерация
for (const [id, user] of userMap) {
  console.log(id, user.name);
}

// Map с разными типами ключей
const numberMap: Map<number, string> = new Map();
numberMap.set(1, "one");
```

#### **Set<T>**

```ts
const uniqueIds: Set<number> = new Set([1, 2, 3, 1]); // [1, 2, 3]
const hasId = uniqueIds.has(1); // boolean

uniqueIds.add(4); // ✅
uniqueIds.delete(1); // ✅

// Итерация
for (const id of uniqueIds) {
  console.log(id);
}
```

#### **WeakMap<K, V>**

```ts
// WeakMap - ключи только объекты
const cache: WeakMap<object, string> = new WeakMap();
const obj = { id: 1 };
cache.set(obj, "cached value");

const value = cache.get(obj); // string | undefined

// Нельзя использовать примитивы как ключи
// cache.set("key", "value"); // ❌ Ошибка
```

#### **WeakSet<T>**

```ts
// WeakSet - только объекты
const processed: WeakSet<object> = new WeakSet();
const obj = { id: 1 };

processed.add(obj);
const isProcessed = processed.has(obj); // boolean

// Нельзя использовать примитивы
// processed.add("string"); // ❌ Ошибка
```

---

### 🔄 Разница между Array и ReadonlyArray:

```ts
const arr: number[] = [1, 2, 3];
arr.push(4); // ✅ Можно изменять
arr[0] = 10; // ✅ Можно изменять

const readonlyArr: ReadonlyArray<number> = [1, 2, 3];
// readonlyArr.push(4); // ❌ Ошибка - нельзя изменять
// readonlyArr[0] = 10; // ❌ Ошибка
```

**Когда использовать ReadonlyArray:**
- Когда массив не должен изменяться
- Для защиты данных от случайных изменений
- В функциях, которые не должны мутировать массив

---

### 🔄 Разница между Map и Record:

```ts
// Map - динамическая коллекция (можно добавлять/удалять)
const map: Map<string, number> = new Map();
map.set("a", 1);
map.set("b", 2);
map.delete("a"); // ✅ Можно удалять

// Record - статический объект (ключи известны заранее)
type Status = "pending" | "success";
const record: Record<Status, string> = {
  pending: "Processing",
  success: "Done"
};
// record["error"] = "Failed"; // ❌ Ошибка - ключа нет в типе
```

**Когда использовать:**
- **Map** — когда ключи динамические, нужно добавлять/удалять
- **Record** — когда ключи известны заранее, нужна типобезопасность

---

### 🔄 Разница между Map и WeakMap:

```ts
// Map - хранит ссылки на ключи (не позволяет сборщику мусора удалить объект)
const map = new Map<object, string>();
let obj = { id: 1 };
map.set(obj, "value");
obj = null; // Объект все еще в памяти (Map держит ссылку)

// WeakMap - не хранит ссылки (позволяет сборщику мусора удалить объект)
const weakMap = new WeakMap<object, string>();
let obj2 = { id: 1 };
weakMap.set(obj2, "value");
obj2 = null; // Объект может быть удален сборщиком мусора
```

**Когда использовать:**
- **Map** — когда нужны примитивные ключи или нужно итерироваться
- **WeakMap** — когда ключи объекты и не нужна итерация

---

### 🎯 Краткий ответ для собеседования

**Встроенные дженерики TypeScript:**
- **Array<T>** / **T[]** — массив элементов типа T
- **Promise<T>** — промис, разрешающийся в тип T
- **ReadonlyArray<T>** — неизменяемый массив
- **Map<K, V>** — динамическая коллекция ключ-значение
- **Set<T>** — коллекция уникальных значений
- **WeakMap/WeakSet** — слабые коллекции (только объекты)

---

### ❓ Каверзные вопросы и ответы

#### **1. В чем разница между `Array<T>` и `T[]`?**

**Ответ:**
Никакой разницы — это два способа записи одного и того же типа:

```ts
const arr1: Array<number> = [1, 2, 3];
const arr2: number[] = [1, 2, 3];
// arr1 и arr2 имеют одинаковый тип
```

**Рекомендация:** Используйте `T[]` для простоты, `Array<T>` для читаемости в сложных типах.

---

#### **2. Можно ли использовать `Promise<void>`?**

**Ответ:**
Да, `Promise<void>` используется для промисов, которые ничего не возвращают:

```ts
async function saveData(): Promise<void> {
  await fetch('/api/save', { method: 'POST' });
  // Нет return
}
```

---

#### **3. Как типизировать Promise, который может вернуть ошибку?**

**Ответ:**
Используйте union тип или Result паттерн:

```ts
// Union тип
async function fetchUser(): Promise<User | null> {
  try {
    return await getUser();
  } catch {
    return null;
  }
}

// Или с типом ошибки
async function fetchUser(): Promise<User | Error> {
  try {
    return await getUser();
  } catch (error) {
    return error instanceof Error ? error : new Error("Unknown error");
  }
}
```

---

#### **4. В чем разница между `Set` и `Array`?**

**Ответ:**
- **Array** — упорядоченная коллекция, может содержать дубликаты
- **Set** — коллекция уникальных значений, автоматически удаляет дубликаты

```ts
const arr = [1, 2, 2, 3]; // [1, 2, 2, 3]
const set = new Set([1, 2, 2, 3]); // Set {1, 2, 3}
```

---

#### **5. Можно ли использовать `Map` с объектами как ключами?**

**Ответ:**
Да, но объекты сравниваются по ссылке, а не по значению:

```ts
const map = new Map<object, string>();
const obj1 = { id: 1 };
const obj2 = { id: 1 };

map.set(obj1, "value1");
map.set(obj2, "value2"); // Разные ключи (разные объекты)

console.log(map.get(obj1)); // "value1"
console.log(map.get(obj2)); // "value2"
console.log(map.get({ id: 1 })); // undefined (новый объект)
```

---

#### **6. Зачем нужен `WeakMap`, если есть `Map`?**

**Ответ:**
`WeakMap` позволяет сборщику мусора удалять объекты-ключи, когда на них нет других ссылок:

```ts
// Map - держит ссылку на объект
const map = new Map();
let obj = { data: "important" };
map.set(obj, "value");
obj = null; // Объект все еще в памяти (Map держит ссылку)

// WeakMap - не держит ссылку
const weakMap = new WeakMap();
let obj2 = { data: "important" };
weakMap.set(obj2, "value");
obj2 = null; // Объект может быть удален сборщиком мусора
```

**Используйте WeakMap** для кеширования или метаданных объектов без утечек памяти.

---

#### **7. Как типизировать вложенные Promise?**

**Ответ:**
Используйте `Awaited<T>` (TypeScript 4.5+) или условный тип:

```ts
// Awaited (рекомендуется)
type User = { id: number; name: string };
async function fetchUser(): Promise<Promise<User>> {
  return Promise.resolve({ id: 1, name: "John" });
}

type UserType = Awaited<ReturnType<typeof fetchUser>>; // User

// Без Awaited
type UnwrapPromise<T> = T extends Promise<infer U> 
  ? U extends Promise<any> 
    ? UnwrapPromise<U> 
    : U 
  : T;
```

---

## 📚 Полезные практики

- **Используйте `T[]`** для простоты записи массивов
- **Используйте `ReadonlyArray`** когда массив не должен изменяться
- **Используйте `Map`** для динамических коллекций
- **Используйте `Record`** для статических объектов с известными ключами
- **Используйте `WeakMap/WeakSet`** для избежания утечек памяти
- **Типизируйте Promise** для безопасной работы с асинхронным кодом


