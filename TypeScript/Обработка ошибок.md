## ⚠️ Обработка ошибок в TypeScript

TypeScript предоставляет инструменты для типобезопасной обработки ошибок, используя типы `unknown`, `never` и type guards.

---

## 🎯 Основные подходы

### **1. Использование `unknown` для ошибок**

В `catch` блоках ошибка имеет тип `unknown` (начиная с TypeScript 4.4), а не `any`.

```ts
try {
  // какой-то код
} catch (error) {
  // error имеет тип unknown
  // ❌ error.message // Ошибка!
  
  // ✅ Нужна проверка типа
  if (error instanceof Error) {
    console.log(error.message);
  }
}
```

**Почему `unknown`, а не `any`?** `unknown` заставляет проверять тип перед использованием, что безопаснее.

### **2. Использование `never` для функций, которые всегда бросают ошибки**

```ts
function throwError(message: string): never {
  throw new Error(message);
}

function assert(condition: boolean, message: string): asserts condition {
  if (!condition) {
    throw new Error(message);
  }
}
```

---

## 🛡️ Type Guards для ошибок

### **Проверка на Error:**

```ts
function isError(error: unknown): error is Error {
  return error instanceof Error;
}

try {
  // код
} catch (error) {
  if (isError(error)) {
    console.log(error.message); // ✅ TypeScript знает тип
  } else {
    console.log('Unknown error:', error);
  }
}
```

### **Проверка на кастомную ошибку:**

```ts
class CustomError extends Error {
  code: number;
  constructor(message: string, code: number) {
    super(message);
    this.code = code;
  }
}

function isCustomError(error: unknown): error is CustomError {
  return error instanceof CustomError;
}

try {
  throw new CustomError("Error", 404);
} catch (error) {
  if (isCustomError(error)) {
    console.log(error.code); // ✅ TypeScript знает тип
  }
}
```

---

## 📦 Result паттерн (без исключений)

Альтернатива try-catch для явной обработки ошибок:

```ts
type Result<T, E = Error> = 
  | { success: true; data: T }
  | { success: false; error: E };

function divide(a: number, b: number): Result<number, string> {
  if (b === 0) {
    return { success: false, error: "Division by zero" };
  }
  return { success: true, data: a / b };
}

// Использование
const result = divide(10, 2);
if (result.success) {
  console.log(result.data); // ✅ TypeScript знает тип
} else {
  console.log(result.error); // ✅ TypeScript знает тип
}
```

---

## 🎯 Практические примеры

### **Безопасная обработка JSON:**

```ts
function parseJSON<T>(json: string): Result<T, Error> {
  try {
    const data = JSON.parse(json);
    return { success: true, data };
  } catch (error) {
    if (error instanceof Error) {
      return { success: false, error };
    }
    return { success: false, error: new Error("Unknown error") };
  }
}
```

### **Обработка асинхронных ошибок:**

```ts
async function fetchData(): Promise<Result<Data, Error>> {
  try {
    const response = await fetch('/api/data');
    if (!response.ok) {
      return { success: false, error: new Error(`HTTP ${response.status}`) };
    }
    const data = await response.json();
    return { success: true, data };
  } catch (error) {
    if (error instanceof Error) {
      return { success: false, error };
    }
    return { success: false, error: new Error("Unknown error") };
  }
}
```

---

## 🎯 Краткий ответ для собеседования

**Обработка ошибок в TypeScript:**
- В `catch` блоках ошибка имеет тип `unknown` (не `any`)
- Используйте `instanceof Error` для проверки типа ошибки
- Функции, которые всегда бросают ошибки, должны возвращать `never`
- Используйте type guards для безопасной проверки ошибок
- Result паттерн — альтернатива try-catch для явной обработки ошибок

---

## ❓ Каверзные вопросы и ответы

### **1. Почему в catch ошибка имеет тип `unknown`, а не `any`?**

**Ответ:**
`unknown` заставляет проверять тип перед использованием, что предотвращает случайные ошибки. `any` отключает проверку типов и небезопасен.

```ts
try {
  // код
} catch (error) {
  // error: unknown
  // error.message // ❌ Ошибка - нужно проверить тип
  
  if (error instanceof Error) {
    error.message; // ✅ OK
  }
}
```

---

### **2. Как правильно обработать ошибку, которая может быть не Error?**

**Ответ:**
Используйте type guard для проверки:

```ts
function handleError(error: unknown): void {
  if (error instanceof Error) {
    console.log(error.message);
  } else if (typeof error === 'string') {
    console.log(error);
  } else {
    console.log('Unknown error:', error);
  }
}
```

---

### **3. В чем разница между `throw Error()` и `throw new Error()`?**

**Ответ:**
Технически нет разницы — оба создают экземпляр Error. Но `new Error()` более явный и рекомендуется.

```ts
throw Error("message");     // Работает
throw new Error("message"); // Рекомендуется
```

---

### **4. Когда использовать `never` для обработки ошибок?**

**Ответ:**
`never` используется для функций, которые **никогда не возвращаются нормально**:

```ts
function throwError(message: string): never {
  throw new Error(message);
  // Код после throw недостижим
}

function assert(condition: boolean, message: string): asserts condition {
  if (!condition) {
    throw new Error(message); // never
  }
  // После этого TypeScript знает, что condition === true
}
```

---

### **5. Что такое Result паттерн и когда его использовать?**

**Ответ:**
Result паттерн — это явная обработка ошибок без исключений. Полезен когда:
- Нужна явная обработка ошибок
- Хотите избежать try-catch
- Нужна типобезопасность

```ts
type Result<T, E> = 
  | { success: true; data: T }
  | { success: false; error: E };

// Преимущества:
// - Явная обработка ошибок
// - TypeScript знает тип в каждой ветке
// - Нет неожиданных исключений
```

---

### **6. Как обработать ошибку из Promise без try-catch?**

**Ответ:**
Используйте `.catch()`:

```ts
fetch('/api/data')
  .then(response => response.json())
  .catch((error: unknown) => {
    if (error instanceof Error) {
      console.log(error.message);
    }
  });
```

Или оберните в функцию с Result:

```ts
async function safeFetch<T>(url: string): Promise<Result<T, Error>> {
  try {
    const response = await fetch(url);
    const data = await response.json();
    return { success: true, data };
  } catch (error) {
    return { 
      success: false, 
      error: error instanceof Error ? error : new Error("Unknown") 
    };
  }
}
```

---

### **7. Можно ли типизировать ошибку в catch?**

**Ответ:**
Нет, ошибка в catch всегда `unknown`. Но можно использовать type guard:

```ts
try {
  // код
} catch (error: unknown) {
  // Явно указываем unknown (опционально, это по умолчанию)
  if (error instanceof CustomError) {
    // TypeScript знает тип
    error.customProperty;
  }
}
```

---

### **8. Как создать кастомный тип ошибки?**

**Ответ:**
Расширьте класс Error:

```ts
class ValidationError extends Error {
  field: string;
  
  constructor(message: string, field: string) {
    super(message);
    this.field = field;
    this.name = 'ValidationError';
  }
}

// Использование
function validate(data: unknown): void {
  if (!data) {
    throw new ValidationError("Data is required", "data");
  }
}

// Обработка
try {
  validate(null);
} catch (error) {
  if (error instanceof ValidationError) {
    console.log(error.field); // ✅ TypeScript знает тип
  }
}
```

---

## 📚 Полезные практики

- **Всегда проверяйте тип ошибки** перед использованием (`instanceof Error`)
- **Используйте `unknown`** для ошибок, не `any`
- **Создавайте кастомные классы ошибок** для разных типов ошибок
- **Используйте type guards** для безопасной проверки ошибок
- **Рассмотрите Result паттерн** для явной обработки ошибок
- **Функции, которые всегда бросают ошибки**, должны возвращать `never`




