## 🔄 Разница между `type` и `interface`

**`interface`** и **`type`** — это два способа определения типов в TypeScript. Оба позволяют описывать структуру данных, но имеют разные возможности и особенности.

### 🔹 Что такое `interface`?

**`interface`** — это способ описать форму объекта или контракт класса. Используется для определения структуры данных, которую должен иметь объект.

```ts
interface User {
  name: string;
  age: number;
}
```

**Особенности:**
- Предназначен для объектов и классов
- Можно расширять через `extends`
- Поддерживает declaration merging (можно дополнять)
- Не поддерживает union типы напрямую

### 🔹 Что такое `type`?

**`type`** — это способ создать псевдоним для любого типа. Более гибкий инструмент, который может описывать не только объекты, но и примитивы, union, intersection и другие сложные типы.

```ts
type User = {
  name: string;
  age: number;
};

type Status = 'active' | 'inactive'; // Union тип
```

**Особенности:**
- Может описывать любые типы (объекты, примитивы, union, tuples)
- Расширяется через intersection (`&`)
- Поддерживает union типы
- Нельзя дополнять (нет declaration merging)

---

### 📊 Сравнение:

| Параметр | `interface` | `type` |
|----------|-------------|-------|
| **Расширение** | `extends` | `&` (intersection) |
| **Объединение типов** | ❌ Не поддерживает union (`\|`) | ✅ Поддерживает union (`\|`) |
| **Дополнение** | ✅ Можно дополнять (declaration merging) | ❌ Нельзя |
| **Применение** | Только для объектов / классов | Для любых типов (включая примитивы, union, tuples) |

---

### 🔹 Расширение:

**`interface`:**

```ts
interface User {
  name: string;
}

interface Admin extends User {
  role: string;
}
```

**`type`:**

```ts
type User = {
  name: string;
};

type Admin = User & {
  role: string;
};
```

---

### 🔹 Объединение типов:

**`type` поддерживает union:**

```ts
type Status = 'active' | 'inactive' | 'pending';
```

**`interface` не поддерживает напрямую.**

---

### 🔹 Дополнение (declaration merging):

**`interface` можно дополнять:**

```ts
interface User {
  name: string;
}

interface User {
  age: number;
}

// Теперь User имеет и name, и age
```

**`type` нельзя дополнять** — будет ошибка.

---

### 💡 Рекомендация:

- Используй **`interface`** для объектов и классов
- Используй **`type`** для union, intersection, примитивов и других сложных типов
