## 🔗 Alias, Union и Intersection

### **Alias (псевдонимы типов, ярлык типа) — это способ создать новое имя для существующего типа в TypeScript. Это как создание ярлыка или сокращения для сложных или часто используемых типов.**

```ts
// Определение Type Alias для строки
type Name = string;
// Определение Type Alias для числа
type Age = number;
// Объединение нескольких типов в один
type Person = {
	name: Name;
	age: Age;
	isActive: boolean;
};
// Использование Type Alias для создания объекта
let user: Person = {
	name: "Kirill",
	age: 25,
	isActive: true
};
```

### **Union (`|`)** — объединение типов. Позволяют переменной принимать значения разных типов. Это полезно, когда переменная может принимать одно из нескольких возможных значений.

```ts
let id: number | string;
id = 42;
id = "abc";

type Status = "pending" | "success" | "error";
```

### **Intersection (`&`)** — пересечение типов. Позволяют объединять типы, создавая новый тип, который включает в себя все свойства и методы из обоих типов. Это полезно для создания гибких объектов, которые могут содержать комбинацию различных характеристик.

```ts
interface A { a: string; }
interface B { b: number; }
type AB = A & B; // { a: string; b: number; }
```
