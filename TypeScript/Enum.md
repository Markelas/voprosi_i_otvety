## 🔢 Enum в TypeScript

**Enum** — это специальный тип данных в TypeScript, который позволяет определять набор константных значений с именованными значениями. Это простой способ моделировать множества связанных значений, представляя их в виде списка именованных констант.

---

## 🎯 Что такое Enum?

Enum помогает улучшить читаемость кода, особенно когда нужно использовать несколько связанных значений, которые должны быть легко доступными и легко воспринимаемыми.

В TypeScript существуют два вида Enum: **числовой (numeric)** и **строковый (string)**.

---
## 📊 Типы Enum

### **1. Numeric Enum (Числовой)**

В числовом Enum значения начинаются с нуля и увеличиваются на единицу. Такие Enum автоматически присваивают числовые значения перечисленным константам.

```ts
enum Direction {
  Up,      // 0
  Down,    // 1
  Left,    // 2
  Right    // 3
}

let direction: Direction = Direction.Up;
console.log(direction); // 0
```

**С явными значениями:**

```ts
enum Status {
  Pending = 1,
  Success = 2,
  Error = 3
}

const currentStatus: Status = Status.Success; // 2
```

**С начальным значением:**

```ts
enum Status {
  Pending = 10,
  Success,    // 11
  Error       // 12
}
```

### **2. String Enum (Строковый)**

В строковом Enum каждый член перечисления задается строковым значением.

```ts
enum Colors {
  Red = "red",
  Green = "green",
  Blue = "blue"
}

let color: Colors = Colors.Red;
console.log(color); // "red"
```

**Преимущества строковых enum:**
- Более осмысленные значения
- Лучше для сериализации/десериализации
- Удобнее для отладки

### **3. Const Enum**

Const enum — более эффективный вариант, который полностью удаляется при компиляции.

```ts
const enum Status {
  Active,
  Inactive
}

const status = Status.Active; // Компилируется в: const status = 0;
```

**Особенности:**
- Не создает объект в runtime
- Значения встраиваются напрямую
- Нельзя использовать `Object.keys()` или обратное отображение

---
## 💡 Зачем использовать Enum?

### **1. Упрощение кода**

Enum помогают уменьшить количество числовых констант в коде и избавиться от магических чисел.

```ts
// ❌ Плохо - магические числа
const STATUS_ACTIVE = 0;
const STATUS_INACTIVE = 1;
if (userStatus === 0) { ... }

// ✅ Хорошо - осмысленные имена
enum Status {
  Active = 0,
  Inactive = 1
}
if (userStatus === Status.Active) { ... }
```

### **2. Автодополнение и типобезопасность**

TypeScript автоматически поддерживает автодополнение для Enum, что упрощает работу с перечислениями.

```ts
enum UserRole {
  Admin,
  User,
  Guest
}

function checkAccess(role: UserRole) {
  // IDE подскажет доступные значения: Admin, User, Guest
  if (role === UserRole.Admin) {
    // ...
  }
}
```

### **3. Инкапсуляция**

Enum защищает внутренние значения от случайных изменений. Значения Enum четко определены и их нельзя изменить после объявления.

---
## Как работает под капотом?

Enum создаёт объект в рантайме с двусторонним маппингом — и по имени получаем число, и по числу имя.

1. **Числовой** (по умолчанию):
``` ts
enum Direction { Up, Down } // Up = 0, Down = 1
```

Создает **двусторонний маппинг** в JS:

```ts
Direction = { 0: "Up", 1: "Down", "Up": 0, "Down": 1 }
```

2. **Строкового** только **односторонний маппинг**, обратного нет.

```ts 
enum Status { Success = "SUCCESS" }
```

3. **Константный** (`const enum`):

```ts
const enum Size { Small, Large }
const s = Size.Small; // → компилируется в: const s = 0
```

Не стоит использовать постоянно, потому что:
1. **Увеличивает бандл** — мешает tree-shaking
2. **Небезопасен** — в числовой enum можно записать любое число
3. **Есть лучшие альтернативы**:
   - Union types: `type Status = 'pending' | 'approved'`
   - Объекты с `as const`

Используйте enum только когда нужен обратный маппинг или работаете с библиотеками, которые его требуют. В остальных случаях — union types.

Иначе работает так:
```ts
enum Direction {
  Up = 0,
  Down = 1
}

const move = Direction.Up; 
console.log(Direction[0]); // "Up" — обратное отображение работает

// Код выше будет скомпилирован в код ниже

var Direction;

(function (Direction) {
  Direction[Direction["Up"] = 0] = "Up";
  Direction[Direction["Down"] = 1] = "Down";
})(Direction || (Direction = {}));

const move = Direction.Up;

console.log(Direction[0]); // "Up"
```

---


## 🔄 Reverse Mapping (Обратное отображение)

**Только для числовых enum!** TypeScript автоматически создает обратное отображение.

```ts
enum Status {
  Pending = 0,
  Success = 1,
  Error = 2
}

console.log(Status[0]); // "Pending"
console.log(Status[1]); // "Success"
console.log(Status["Pending"]); // 0
```

**Строковые enum не имеют обратного отображения:**

```ts
enum Colors {
  Red = "red",
  Green = "green"
}

// Colors["red"] // ❌ Ошибка - нет обратного отображения
```

---

## 🎯 Практические примеры

### **Статусы пользователя:**

```ts
enum UserStatus {
  Active = 0,
  Inactive = 1,
  Banned = 2
}

function checkUserStatus(status: UserStatus): string {
  switch (status) {
    case UserStatus.Active:
      return "User is active";
    case UserStatus.Inactive:
      return "User is inactive";
    case UserStatus.Banned:
      return "User is banned";
    default:
      return "Unknown status";
  }
}
```

### **HTTP методы:**

```ts
enum HttpMethod {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE"
}

function makeRequest(method: HttpMethod, url: string) {
  // ...
}

makeRequest(HttpMethod.GET, "/api/users");
```

---

## 🎯 Краткий ответ для собеседования

**Enum** — это набор именованных констант. Бывают числовые (автоматическая нумерация с 0) и строковые (явные строковые значения). Используются для замены магических чисел, улучшения читаемости и типобезопасности. Числовые enum имеют обратное отображение, строковые — нет.

---

## ❓ Каверзные вопросы и ответы

### **1. В чем разница между числовым и строковым enum?**

**Ответ:**
- **Числовой enum**: Автоматически присваивает числовые значения (начиная с 0), имеет обратное отображение (`Status[0]` → `"Pending"`)
- **Строковый enum**: Требует явного указания строковых значений, не имеет обратного отображения, лучше для сериализации

```ts
// Числовой
enum Num { A, B } // A = 0, B = 1
Num[0] // "A" ✅

// Строковый
enum Str { A = "a", B = "b" }
Str["a"] // ❌ Ошибка
```

---

### **2. Что такое reverse mapping и когда оно работает?**

**Ответ:**
Reverse mapping (обратное отображение) — это возможность получить имя члена enum по его значению. Работает **только для числовых enum**.

```ts
enum Status {
  Pending = 0,
  Success = 1
}

Status[0] // "Pending" ✅
Status[1] // "Success" ✅

// Для строковых enum не работает
enum Colors {
  Red = "red"
}
Colors["red"] // ❌ Ошибка
```

**Почему только для числовых?** Потому что строковые значения могут конфликтовать с именами членов enum.

---

### **3. Когда использовать enum, а когда union types?**

**Ответ:**

**Используй enum когда:**
- Нужно обратное отображение (числовой enum)
- Значения используются в разных местах проекта
- Нужна группировка связанных констант

**Используй union types когда:**
- Простые строковые/числовые литералы
- Не нужна обратная совместимость с JavaScript
- Хочешь более легковесное решение

```ts
// Enum
enum Status {
  Active,
  Inactive
}

// Union type (альтернатива)
type Status = "active" | "inactive";
```

**Современный подход:** Многие предпочитают union types из-за их простоты и отсутствия дополнительного кода в runtime.

---

### **4. Что такое const enum и зачем он нужен?**

**Ответ:**
`const enum` — это enum, который полностью удаляется при компиляции. Значения встраиваются напрямую в код.

```ts
const enum Status {
  Active,
  Inactive
}

const status = Status.Active;
// Компилируется в: const status = 0;
```

**Преимущества:**
- Не создает объект в runtime (меньше размер бандла)
- Значения встраиваются напрямую

**Ограничения:**
- Нельзя использовать `Object.keys(Status)`
- Нет обратного отображения
- Нельзя использовать в выражениях, требующих объекта enum

---

### **5. Можно ли смешивать числовые и строковые значения в одном enum?**

**Ответ:**
Технически можно, но это **не рекомендуется** и может привести к неожиданному поведению.

```ts
enum Mixed {
  A = 0,        // Числовое
  B = "b",      // Строковое
  C             // ❌ Ошибка! Нельзя после строкового
}
```

**Проблема:** После строкового значения нельзя использовать автоматическую нумерацию.

---

### **6. Как enum компилируется в JavaScript?**

**Ответ:**

**Обычный числовой enum:**
```ts
enum Status {
  Pending,
  Success
}
```

**Компилируется в:**
```js
var Status;
(function (Status) {
    Status[Status["Pending"] = 0] = "Pending";
    Status[Status["Success"] = 1] = "Success";
})(Status || (Status = {}));
```

**Const enum:**
```ts
const enum Status {
  Pending,
  Success
}
```

**Компилируется в:** Ничего (полностью удаляется, значения встраиваются).

---

### **7. Можно ли использовать enum как ключи объекта?**

**Ответ:**
Да, можно использовать enum как ключи объекта.

```ts
enum Status {
  Active,
  Inactive
}

const statusMessages: Record<Status, string> = {
  [Status.Active]: "User is active",
  [Status.Inactive]: "User is inactive"
};
```

---

### **8. В чем разница между enum и объектом с константами?**

**Ответ:**

**Enum:**
```ts
enum Status {
  Active,
  Inactive
}
```

**Объект с константами:**
```ts
const Status = {
  Active: 0,
  Inactive: 1
} as const;
```

**Разница:**
- Enum создает тип и значение одновременно
- Enum имеет обратное отображение (для числовых)
- Enum компилируется в функцию (если не const)
- Объект с `as const` более легковесный, но нет обратного отображения

---

### **9. Можно ли расширять enum?**

**Ответ:**
Нет, enum нельзя расширять напрямую. Но можно использовать union types для объединения значений:

```ts
enum Status {
  Active,
  Inactive
}

enum Role {
  Admin,
  User
}

// Объединение через union
type UserStatus = Status | Role;
```

Или использовать namespace для дополнения:

```ts
enum Status {
  Active,
  Inactive
}

namespace Status {
  export function isActive(status: Status): boolean {
    return status === Status.Active;
  }
}
```

---

### **10. Когда enum может быть проблемой?**

**Ответ:**

1. **Размер бандла**: Обычные enum добавляют код в runtime
2. **Tree-shaking**: Enum сложнее удалить при tree-shaking
3. **Строковые enum**: Нет обратного отображения
4. **Неизменяемость**: Нельзя динамически добавлять значения

**Решение:** Использовать `const enum` или union types для уменьшения размера.

---

## 📚 Полезные практики

- **Используйте строковые enum** для лучшей сериализации и отладки
- **Используйте const enum** когда не нужна обратная совместимость и важен размер бандла
- **Рассмотрите union types** как альтернативу для простых случаев
- **Избегайте смешивания** числовых и строковых значений в одном enum
- **Используйте явные значения** для числовых enum, если значения важны (например, для API)




