## 🔄 Технологии для реального времени / постоянной связи

---

### 1. 🔁 Long Polling

**Обычный HTTP-запрос, сервер держит соединение открытым до появления данных.**

**Особенности:**
- После ответа клиент сразу делает новый запрос

**Плюсы:**
- ✅ Работает везде, совместимо со старым HTTP

**Минусы:**
- ❌ Лишние HTTP-заголовки
- ❌ Задержки между запросами
- ❌ Нагрузка на сервер

**Используется:** когда WebSocket недоступен или нужна совместимость (старые браузеры).

---

### 2. 🔌 WebSocket

**Постоянное двустороннее соединение** `ws://` или `wss://`.

**Плюсы:**
- ✅ Мгновенные обновления
- ✅ Экономия трафика
- ✅ Идеально для чатов, игр, торгов

**Минусы:**
- ❌ Требует серверной поддержки
- ❌ Сложнее масштабировать

**Особенность:** работает как постоянное соединение, отправка данных в обе стороны.

**Используется:** для чатов, игр и прочее.

---

### 3. 📡 Server-Sent Events (SSE)

**Клиент открывает однонаправленный поток от сервера к клиенту через HTTP** (клиент только слушает).

Сервер может отправлять события, пока соединение открыто.

**Плюсы:**
- ✅ Просто
- ✅ Нативно в браузерах
- ✅ Соединение одно, не нужно переоткрывать

**Минусы:**
- ❌ Только сервер → клиент
- ❌ Не работает в старых браузерах или без HTTP/2

**Используется:** уведомления, ленты новостей, обновления состояния. Например, chatGPT тоже, медленно печатает

---

### 📊 Сравнение:

| Технология | Направление | Сложность | Применение |
|-----------|------------|-----------|------------|
| **Long Polling** | Двустороннее | Простая | Старые браузеры |
| **WebSocket** | Двустороннее | Сложная | Чаты, игры, торговля |
| **SSE** | Сервер → Клиент | Простая | Уведомления, ленты |
