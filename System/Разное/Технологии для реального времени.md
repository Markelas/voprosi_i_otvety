## 🔄 Технологии для реального времени / постоянной связи

---

### 1. ⏱️ Short Polling

**Клиент периодически отправляет обычные HTTP-запросы** через фиксированный интервал (например, каждые 2–5 секунд), чтобы проверить, есть ли новые данные. Сервер каждый раз сразу отвечает — либо с данными, либо «пока ничего».

**Особенности:**
- Запрос → ответ → пауза → снова запрос. Соединение не держится открытым.
- Интервал задаётся на клиенте (setInterval, setTimeout).

**Плюсы:**
- ✅ Очень просто реализовать
- ✅ Работает везде (обычный HTTP)
- ✅ Не нужна особая поддержка на сервере

**Минусы:**
- ❌ Задержка до обновления = интервал опроса (данные могли появиться сразу после предыдущего запроса, но клиент узнает только при следующем)
- ❌ Много «пустых» запросов, если данные приходят редко — лишний трафик и нагрузка на сервер
- ❌ При частом опросе — высокая нагрузка; при редком — данные устаревают

**Используется:** когда нужна максимальная простота и не критична задержка (проверка статуса заказа, простые уведомления). Часто заменяют на Long Polling или WebSocket для более быстрого отклика и меньшей нагрузки.

---

### 2. 🔁 Long Polling

**Обычный HTTP-запрос, сервер держит соединение открытым до появления данных.** В отличие от Short Polling: клиент не ходит по таймеру — один запрос «висит» до ответа с данными, затем клиент сразу отправляет следующий.

**Особенности:**
- После ответа клиент сразу делает новый запрос

**Плюсы:**
- ✅ Работает везде, совместимо со старым HTTP

**Минусы:**
- ❌ Лишние HTTP-заголовки
- ❌ Задержки между запросами
- ❌ Нагрузка на сервер

**Используется:** когда WebSocket недоступен или нужна совместимость (старые браузеры).

---

### 3. 🔌 WebSocket

**Постоянное двустороннее соединение** `ws://` или `wss://`.

**Плюсы:**
- ✅ Мгновенные обновления
- ✅ Экономия трафика
- ✅ Идеально для чатов, игр, торгов

**Минусы:**
- ❌ Требует серверной поддержки
- ❌ Сложнее масштабировать

**Особенность:** работает как постоянное соединение, отправка данных в обе стороны.

**Используется:** для чатов, игр и прочее.

---

### 4. 📡 Server-Sent Events (SSE)

**Клиент открывает однонаправленный поток от сервера к клиенту через HTTP** (клиент только слушает).

Сервер может отправлять события, пока соединение открыто.

**Плюсы:**
- ✅ Просто
- ✅ Нативно в браузерах
- ✅ Соединение одно, не нужно переоткрывать

**Минусы:**
- ❌ Только сервер → клиент
- ❌ Не работает в старых браузерах или без HTTP/2

**Используется:** уведомления, ленты новостей, обновления состояния. Например, chatGPT тоже, медленно печатает

---

### 📊 Сравнение:

| Технология | Направление | Сложность | Применение |
|-----------|------------|-----------|------------|
| **Short Polling** | Клиент опрашивает сервер | Очень простая | Простые проверки, когда задержка не критична |
| **Long Polling** | Двустороннее (сервер держит запрос) | Простая | Старые браузеры, когда WebSocket недоступен |
| **WebSocket** | Двустороннее | Сложная | Чаты, игры, торговля |
| **SSE** | Сервер → Клиент | Простая | Уведомления, ленты |
