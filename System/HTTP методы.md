## 📡 Основные и дополнительные HTTP-методы

---

### 🔹 Основные методы:

| Метод | Назначение | Тело | Особенности / Пример |
|-------|-----------|------|---------------------|
| **GET** | Получить данные | ❌ | Безопасный, кэшируется, параметры в URL<br>`fetch('/users')` |
| **POST** | Создать ресурс | ✅ | Побочные эффекты, не кэшируется<br>`fetch('/users', {method:'POST', body: JSON.stringify({...})})` |
| **PUT** | Полностью обновить ресурс | ✅ | Отправляется весь объект, идемпотентный<br>`fetch('/users/1', {method:'PUT', body: JSON.stringify({...})})` |
| **PATCH** | Частично обновить ресурс | ✅ | Изменяются только указанные поля<br>`fetch('/users/1', {method:'PATCH', body: JSON.stringify({age:30})})` |
| **DELETE** | Удалить ресурс | ❌ | Может вернуть `200 OK`, либо `204 No Content`, идемпотентный<br>`fetch('/users/1', {method:'DELETE'})` |

---

### 📊 Характеристики:

- 🔹 **Безопасный** — не должен изменять данные на сервере
- 🔹 **Идемпотентный** — повторный вызов даёт тот же результат (например, дважды удалить одного пользователя — эффект тот же, ресурс исчез)

---

### ⚙️ Дополнительные методы:

| Метод | Назначение | Безопасный | Идемпотентный |
|-------|-----------|------------|---------------|
| **HEAD** | Аналог GET, но возвращает только заголовки, без тела. Используется для проверки ресурса или метаданных | ✅ | ✅ |
| **OPTIONS** | Показывает, какие методы и заголовки поддерживает сервер (особенно важен при CORS-проверке) | ✅ | ✅ |
| **CONNECT** | Устанавливает туннель (например, для HTTPS-прокси) | ❌ | ❌ |
| **TRACE** | Отладочный метод — возвращает, как сервер видит запрос (почти не используется из-за соображений безопасности) | ✅ | ✅ |

---

### 💡 Итог:

- **GET, HEAD, OPTIONS, TRACE** — безопасные и идемпотентные
- **PUT, DELETE** — небезопасные, но идемпотентные
- **POST, PATCH** — небезопасные и неидемпотентные
