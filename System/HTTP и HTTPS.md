## 🌐 HTTP и HTTPS

---

### 🔷 HTTP (HyperText Transfer Protocol)

**Протокол передачи данных между клиентом (браузером) и сервером.**

- Работает поверх **TCP/IP**
- Данные передаются **открытым текстом** → можно перехватить (sniffing), изменить (MITM-атака)
- Порт **80**

---

### 🔒 HTTPS = HTTP + TLS/SSL (шифрование)

**Использует TLS (Transport Layer Security)** (ранее SSL) - криптографический протокол, который:

- ✅ **Шифрует трафик** (симметричное шифрование)
- ✅ **Проверяет подлинность сервера** (сертификат)
- ✅ **Гарантирует целостность данных** (никто не изменил по пути)
- Порт **443**

---

### 🔐 Что шифруется:

Все данные шифруются, включая:

- **Заголовки (headers)** — путь запроса, cookies, авторизационные данные
- **Тело (body)** — содержимое запроса/ответа (JSON, HTML, файлы)

**Не шифруется** только имя домена (SNI - индикация имени сервера), IP и порт, потому что оно нужно для маршрутизации.

---

### 📊 Характеристики:

Они работают **без состояния** – сервер не помнит, что было в предыдущем запросе.

Для сохранения состояния используются cookies, сессии, токены и т.д.

---

### 📝 Сравнение:

| Параметр | HTTP | HTTPS |
|----------|------|-------|
| **Шифрование** | ❌ Нет | ✅ Да |
| **Порт** | 80 | 443 |
| **Безопасность** | ⚠️ Уязвим к перехвату | ✅ Защищён |
| **Производительность** | Быстрее (нет накладных расходов) | Чуть медленнее (шифрование) |

---

## 📦 Структура HTTP-сообщения

HTTP-сообщение (запрос или ответ) состоит из **трёх частей**:

### 1. Стартовая строка (Start Line)

**Для запроса:**

```
GET /api/users HTTP/1.1
```

Формат: `МЕТОД ПУТЬ ВЕРСИЯ_ПРОТОКОЛА`

- **Метод** — GET, POST, PUT, DELETE и т.д.
- **Путь** — `/api/users`, `/index.html`
- **Версия** — HTTP/1.1, HTTP/2

**Для ответа:**

```
HTTP/1.1 200 OK
```

Формат: `ВЕРСИЯ_ПРОТОКОЛА КОД_СТАТУСА ТЕКСТ_СТАТУСА`

- **Версия** — HTTP/1.1
- **Код** — 200, 404, 500 и т.д.
- **Текст** — OK, Not Found, Internal Server Error

---

### 2. Заголовки (Headers)

**Заголовки** — это метаданные о запросе/ответе в формате `Ключ: Значение`.

**Основные категории:**

#### Общие заголовки (для запроса и ответа):
- `Date` — дата и время сообщения
- `Connection` — управление соединением (keep-alive, close)
- `Cache-Control` — политика кэширования

#### Заголовки запроса:
- `Host` — доменное имя сервера (обязательный)
- `User-Agent` — информация о браузере/клиенте
- `Accept` — какие типы контента клиент принимает (`application/json`, `text/html`)
- `Accept-Language` — предпочитаемый язык (`en`, `ru`)
- `Accept-Encoding` — поддерживаемые кодировки (gzip, deflate)
- `Authorization` — токен/данные для авторизации (`Bearer <token>`)
- `Cookie` — cookies, отправляемые серверу
- `Referer` — откуда пришёл пользователь
- `Origin` — источник запроса (для CORS)

#### Заголовки ответа:
- `Server` — информация о сервере (nginx, Apache)
- `Set-Cookie` — установка cookies в браузере
- `Access-Control-Allow-Origin` — разрешение CORS
- `Location` — редирект на другой URL (для 3xx)
- `ETag` — идентификатор версии ресурса (для кэширования)
- `Expires` — дата истечения кэша
- `Last-Modified` — дата последнего изменения ресурса

#### Заголовки тела (Entity Headers):
- `Content-Type` — тип данных в теле (`application/json`, `text/html`, `multipart/form-data`)
- `Content-Length` — размер тела в байтах
- `Content-Encoding` — как сжато тело (gzip, br)
- `Content-Language` — язык контента

**Пример заголовков запроса:**

```
GET /api/users HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0
Accept: application/json
Authorization: Bearer abc123xyz
Cookie: sessionId=xyz789
```

**Пример заголовков ответа:**

```
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 1234
Set-Cookie: sessionId=abc; HttpOnly; Secure
Cache-Control: no-cache
```

---

### 3. Тело (Body)

**Тело сообщения** — это фактические данные, которые передаются.

**Когда есть тело:**
- **Запросы:** POST, PUT, PATCH (для отправки данных на сервер)
- **Ответы:** почти всегда (HTML, JSON, файлы), кроме 204 No Content

**Когда НЕТ тела:**
- **Запросы:** GET, DELETE, HEAD (данные в URL или заголовках)
- **Ответы:** 204 No Content, 304 Not Modified

**Типы контента в теле (Content-Type):**

| Content-Type | Описание | Пример использования |
|--------------|----------|----------------------|
| `application/json` | JSON-данные | REST API |
| `application/x-www-form-urlencoded` | Данные формы (ключ=значение) | HTML-формы |
| `multipart/form-data` | Данные формы с файлами | Загрузка файлов |
| `text/html` | HTML-страница | Ответ сервера |
| `text/plain` | Простой текст | Логи, текстовые файлы |
| `image/png`, `image/jpeg` | Изображения | Картинки |

**Пример POST-запроса с JSON:**

```
POST /api/users HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 45

{"name": "John", "email": "john@example.com"}
```

**Пример ответа с JSON:**

```
HTTP/1.1 201 Created
Content-Type: application/json
Content-Length: 67

{"id": 123, "name": "John", "email": "john@example.com"}
```

---

## 🔄 Как работает HTTP-запрос (пошагово)

1. **Клиент формирует запрос:**
   - Стартовая строка: `GET /api/users HTTP/1.1`
   - Заголовки: `Host`, `User-Agent`, `Accept` и т.д.
   - Тело (если есть): данные в JSON, форме и т.д.

2. **Запрос отправляется по TCP/IP** (порт 80 для HTTP, 443 для HTTPS)

3. **Сервер получает запрос:**
   - Парсит стартовую строку (метод, путь)
   - Читает заголовки
   - Читает тело (если есть)

4. **Сервер обрабатывает запрос:**
   - Выполняет логику (обращение к БД, бизнес-логика)
   - Формирует ответ

5. **Сервер отправляет ответ:**
   - Стартовая строка: `HTTP/1.1 200 OK`
   - Заголовки: `Content-Type`, `Set-Cookie` и т.д.
   - Тело: HTML, JSON, файлы

6. **Клиент получает ответ** и отображает данные

---

## 🔐 Как работает HTTPS (TLS Handshake)

1. **Client Hello** — клиент отправляет поддерживаемые алгоритмы шифрования
2. **Server Hello** — сервер выбирает алгоритм и отправляет свой **сертификат**
3. **Проверка сертификата** — клиент проверяет подлинность (выдан ли доверенным CA)
4. **Обмен ключами** — создаётся общий секретный ключ для симметричного шифрования
5. **Шифрованное соединение** — все дальнейшие данные шифруются этим ключом

Этот процесс называется **TLS Handshake** и занимает дополнительное время (~100-200ms), но обеспечивает безопасность.

---

## Вопросы на собеседовании

| Вопрос | Краткий ответ |
|--------|----------------|
| Из чего состоит HTTP-сообщение? | Стартовая строка, заголовки, тело |
| Что в стартовой строке запроса? | Метод, путь, версия протокола (`GET /api/users HTTP/1.1`) |
| Что в стартовой строке ответа? | Версия, код статуса, текст статуса (`HTTP/1.1 200 OK`) |
| Какие бывают заголовки? | Запроса (Host, Authorization), ответа (Set-Cookie, Location), тела (Content-Type, Content-Length) |
| Когда есть тело в запросе? | POST, PUT, PATCH — для отправки данных |
| Когда НЕТ тела в ответе? | 204 No Content, 304 Not Modified |
| Что такое Content-Type? | Заголовок, указывающий тип данных в теле (application/json, text/html) |
| В чём отличие HTTP от HTTPS? | HTTPS = HTTP + TLS/SSL (шифрование, проверка подлинности, целостность) |
| Что шифруется в HTTPS? | Всё: заголовки, тело; НЕ шифруется только доменное имя (SNI), IP, порт |
| Как работает TLS Handshake? | Client Hello → Server Hello с сертификатом → проверка → обмен ключами → шифрование |

---

## Кратко для ответа

HTTP-сообщение состоит из трёх частей: **стартовая строка** (метод/код, путь, версия), **заголовки** (метаданные: Host, Content-Type, Authorization), **тело** (данные: JSON, HTML, файлы). HTTPS добавляет TLS-шифрование: все заголовки и тело шифруются, кроме доменного имени и IP. TLS Handshake устанавливает защищённое соединение через обмен сертификатом и ключами.
