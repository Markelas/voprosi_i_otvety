## 🌐 HTTP и HTTPS

---

### 🔷 HTTP (HyperText Transfer Protocol)

**Протокол передачи данных между клиентом (браузером) и сервером.**

- Работает поверх **TCP/IP**
- Данные передаются **открытым текстом** → можно перехватить (sniffing), изменить (MITM-атака)
- Порт **80**

---

### 🔒 HTTPS = HTTP + TLS/SSL (шифрование)

**Использует TLS (Transport Layer Security)** (ранее SSL) - криптографический протокол, который:

- ✅ **Шифрует трафик** (симметричное шифрование)
- ✅ **Проверяет подлинность сервера** (сертификат)
- ✅ **Гарантирует целостность данных** (никто не изменил по пути)
- Порт **443**

---

### 🔐 Что шифруется:

Все данные шифруются, включая:

- **Заголовки (headers)** — путь запроса, cookies, авторизационные данные
- **Тело (body)** — содержимое запроса/ответа (JSON, HTML, файлы)

**Не шифруется** только имя домена (SNI - индикация имени сервера), IP и порт, потому что оно нужно для маршрутизации.

---

### 📊 Характеристики:

Они работают **без состояния** – сервер не помнит, что было в предыдущем запросе.

Для сохранения состояния используются cookies, сессии, токены и т.д.

---

### 📝 Сравнение:

| Параметр | HTTP | HTTPS |
|----------|------|-------|
| **Шифрование** | ❌ Нет | ✅ Да |
| **Порт** | 80 | 443 |
| **Безопасность** | ⚠️ Уязвим к перехвату | ✅ Защищён |
| **Производительность** | Быстрее (нет накладных расходов) | Чуть медленнее (шифрование) |
