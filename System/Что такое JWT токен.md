## 🔑 Что такое JWT токен

**JWT (JSON Web Token)** — это способ безопасно передавать информацию между клиентом и сервером в виде зашифрованной строки.

Обычно JWT используется для **аутентификации** — подтверждения, кто пользователь.

---

### 📦 Структура JWT:

JWT состоит из **трёх частей**, разделённых точками:

1. **Header** - Алгоритм шифрования (`alg`) и тип токена (`typ`)
2. **Payload** - Полезная информация (ID, email, роль и т.д.)
3. **Signature** - Подпись для проверки подлинности (хеш от первых двух частей + секрет сервера)

---

### 🔐 Важное:

- ✅ **JWT — это не зашифрованные, а подписанные данные.** Любой может прочитать payload (если декодирует base64), но изменить нельзя — подпись не сойдётся.
- ✅ **Срок действия** задаётся через `exp` (expiration). После истечения — токен недействителен.
- ✅ **Refresh-токен** используется для обновления access-токена без повторного входа.
- ⚠️ **JWT не должен хранить чувствительные данные** (пароль, e-mail и т.п.) — только идентификатор и роль.

---

### 🔄 Как работает JWT-аутентификация:

1. 🔐 Пользователь логинится (отправляет логин и пароль на сервер).
2. 🧮 Сервер проверяет данные и создаёт JWT (подписывает его секретом).
3. 📩 Сервер отправляет токен клиенту.
4. 💾 Клиент сохраняет токен (см. ниже).
5. 📨 При следующих запросах клиент передаёт токен в заголовке: `Authorization: Bearer <токен>`
6. ✅ Сервер проверяет подпись и срок действия токена — если всё ок, разрешает доступ.

---

### 💾 Где хранить JWT и Refresh Token:

| Что | Где хранить | Почему так | Риски / Комментарий |
|-----|------------|-----------|---------------------|
| **Access Token (JWT)** | 🔸 Memory (JS-переменная) или Session Storage | Безопаснее — не сохраняется постоянно, стирается при закрытии вкладки | Пропадает после обновления страницы (если в JS) |
| **Refresh Token** | 🍪 HttpOnly Cookie | Сервер сам управляет, JS не может прочитать → защита от XSS | Уязвим к CSRF, но можно добавить `SameSite=Strict` |
| **Альтернатива** | Хранить оба в cookie (разные флаги: `HttpOnly` и `Secure`) | Удобно при SSR и авторизации без JS | Нужно тщательно настроить безопасность |
| **Не рекомендуется** | ❌ Local Storage | Можно украсть через XSS | Использовать только в SPA при коротком времени жизни токена |
