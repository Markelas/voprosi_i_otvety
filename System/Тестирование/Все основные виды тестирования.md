# Все основные виды тестирования

## Краткий ответ для собеседования

Основные виды: **Unit** (одна функция/модуль в изоляции, быстро, много) → **Интеграционные** (несколько модулей или модуль + API) → **E2E** (полный сценарий в браузере, как пользователь). Во фронте добавляют **компонентные** (один UI-компонент), **snapshot** (сравнение вывода с эталоном), **визуальные** (скриншоты), **тесты доступности (a11y)**. Ещё по цели: **дымовые (smoke)**, **регрессионные**, **нагрузочные**. Пирамида: внизу много unit, выше меньше интеграционных/компонентных, наверху мало e2e — чтобы быстро ловить баги и при этом проверять реальное поведение.

---

## 📌 Зачем вообще тестирование

- Находить баги **до продакшена**, а не от пользователей.
- Не ломать уже работающее при **рефакторинге** и новых фичах.
- Делать код и контракты **предсказуемыми** — тесты как живая документация.
- Уверенно **деплоить** — зелёные тесты как «можно выкатывать».

---

## Пирамида тестов (запомнить)

```
        🔺 Мало тестов
       /  \
      / E2E \     ← полный сценарий в браузере, медленно
     /--------\
    / Интеграц. \  ← модули вместе, API, средняя скорость
   / Компонентные \
  /----------------\
 /   Много тестов   \
/     UNIT           \  ← функции/модули по одному, быстро
----------------------
```

**Смысл:** внизу много быстрых unit-тестов (основа), выше меньше интеграционных и компонентных, наверху мало медленных e2e. Так и баги ловим рано, и критичные сценарии проверяем по-настоящему.

---

## 1. 🧪 Unit-тесты (модульные)

### Что это

Проверяют **одну единицу кода в изоляции** — функцию, утилиту, хук, редьюсер. Внешние зависимости (API, DOM, другие модули) **подменяют моками**. Никакого реального браузера и сети.

### Что проверяют

- Чистые функции: форматирование даты, валидация, расчёты.
- Редьюсеры (Redux и т.п.), селекторы.
- Хуки с подменёнными зависимостями.

### Характеристики

| | Unit |
|---|------|
| **Скорость** | Очень быстро |
| **Количество** | Много |
| **Что ловят** | Ошибки в логике одной функции/модуля |
| **Что не ловят** | Ошибки «стыков», реальный UI и сеть |

**Плюсы:** быстро, много, легко понять, что упало.  
**Минусы:** не проверяют, что части системы правильно работают вместе.

**На собеседовании:** unit — тестируем одну функцию или модуль в изоляции, всё остальное мокаем; это основа пирамиды, таких тестов больше всего.

---

## 2. 🔗 Интеграционные тесты

### Что это

Проверяют **взаимодействие нескольких частей** — модулей между собой или модуля с API (реальным или подставным). Не обязательно полный UI; часто «логика + запрос» или «форма + стор».

### Что проверяют

- Форма отправляет запрос с правильными данными.
- После ответа API стор обновляется.
- Несколько сервисов/модулей вместе дают ожидаемый результат.

### Характеристики

| | Интеграционные |
|---|----------------|
| **Скорость** | Средне (медленнее unit) |
| **Количество** | Меньше, чем unit |
| **Что ловят** | Ошибки в контрактах и «стыках» модулей |
| **Что не ловят** | Реальное поведение в браузере у пользователя |

**Плюсы:** находят ошибки там, где модули соединяются.  
**Минусы:** сложнее писать и поддерживать (моки, контракты).

**На собеседовании:** интеграционные тесты проверяют, что несколько модулей или модуль и API работают вместе корректно.

---

## 3. 🌐 E2E-тесты (end-to-end)

### Что это

**Полный сценарий от действия пользователя до результата** в реальном браузере: открыть страницу, кликнуть, ввести текст, перейти по ссылке, проверить контент и ответ сервера. Максимально близко к тому, как пользуется человек.

### Что проверяют

- «Логин → профиль → изменить имя → сохранить».
- «Добавить в корзину → оформить заказ».
- Критичные пути: регистрация, оплата, ключевые фичи.

### Характеристики

| | E2E |
|---|-----|
| **Скорость** | Медленно |
| **Количество** | Мало (критичные сценарии) |
| **Что ловят** | Проблемы интеграции, сети, рендера, реального UI |
| **Что не ловят** | — (но дороги в поддержке и хрупкие) |

**Плюсы:** проверяют то, что видит пользователь, и полную цепочку.  
**Минусы:** медленные, хрупкие (селекторы, таймауты), сложнее поддерживать.

**На собеседовании:** e2e — тесты полного сценария в браузере, как пользователь; их мало, но они проверяют критичные сценарии от начала до конца.

---

## 4. 🧩 Компонентные тесты (во фронтенде)

### Что это

Проверяют **один UI-компонент**: рендер, пропсы, действия пользователя (клик, ввод), вызовы колбэков. Сеть и роутинг обычно **мокают**. DOM может быть эмулирован (jsdom) или в реальном браузере (Playwright Component, Cypress Component).

### Что проверяют

- Кнопка в состоянии loading показывает спиннер.
- Форма при невалидном поле показывает ошибку.
- Список по клику вызывает callback с выбранным элементом.

### Характеристики

| | Компонентные |
|---|--------------|
| **Скорость** | Быстрее e2e, медленнее unit |
| **Количество** | Много (ключевые компоненты) |
| **Что ловят** | Поведение компонента, контракт props/events |
| **Что не ловят** | Полный путь пользователя, реальный бэкенд |

**Плюсы:** быстрее e2e, проверяют именно компонент и его API.  
**Минусы:** не заменяют e2e; среда может отличаться от продакшена.

**На собеседовании:** компонентные тесты — проверка одного компонента: рендер, пропсы, клики, ввод; зависимости мокаем, чтобы изолировать компонент.

---

## 5. 📸 Snapshot-тесты

### Что это

Сохраняют **«снимок»** вывода (разметка компонента или структура данных). При следующих запусках текущий результат **сравнивается** с сохранённым. Падение — если что-то изменилось.

### Когда полезны

- Критичные компоненты, где случайное изменение разметки — регрессия.
- Контракты данных (структура ответа API).

### Осторожно

Много **ложных срабатываний**: легитимное изменение дизайна или текста ломает тест, snapshot приходится обновлять. Не проверяют логику и поведение — только «вывод такой же, как был».

| | Snapshot |
|---|----------|
| **Плюсы** | Быстро ловят случайные изменения разметки/структуры |
| **Минусы** | Хрупкие, не проверяют поведение, нужна дисциплина по обновлению |

**На собеседовании:** snapshot — сравнение текущего вывода с сохранённым снимком; падает при любом отличии; используют выборочно, не как основу.

---

## 6. 👁️ Визуальные тесты (Visual regression)

### Что это

Сравнение **внешнего вида** страницы или компонента с **эталонным скриншотом**. Падение — если пиксели отличаются (сдвиг, обрезание, сломанные стили).

### Когда полезны

- Дизайн-системы, UI-библиотеки.
- Ключевые экраны (лендинг, каталог).

### Осторожно

Хрупкость из-за шрифтов, разрешения, анимаций, окружения. Нужны единые настройки и дисциплина по эталонам.

| | Визуальные |
|---|------------|
| **Плюсы** | Ловят визуальные регрессии |
| **Минусы** | Чувствительны к окружению, много ложных падений при легитимных правках |

---

## 7. ♿ Тестирование доступности (a11y)

### Что это

Проверка соответствия **критериям доступности**: контраст, семантика HTML, ARIA-атрибуты, навигация с клавиатуры, фокус. Часто автоматизируют правилами (axe-core, jest-axe) внутри обычных тестов (компонентных, e2e).

### Зачем

Чтобы интерфейсом могли пользоваться люди с ограниченными возможностями (экранные читалки, только клавиатура). Плюс лучше UX и соответствие стандартам.

| | A11y |
|---|-----|
| **Плюсы** | Ловят типичные ошибки доступности на ранних этапах |
| **Минусы** | Не покрывают всё; часть проверок остаётся ручной |

**На собеседовании:** тесты доступности проверяют контраст, семантику, ARIA, клавиатуру; часто встраивают в компонентные и e2e через axe и подобные инструменты.

---

## 8. 💨 Дымовые тесты (Smoke)

### Что это

Минимальная проверка: **приложение вообще поднимается и ключевые вещи открываются**. «Сайт открылся», «главная отрисовалась», «логин доступен». Не глубокие сценарии — только «не сломалось ли всё полностью».

### Зачем

После деплоя или большой сборки быстро понять: можно ли вообще пользоваться системой или что-то критичное сломалось. Мало тестов, очень быстрые.

**На собеседовании:** smoke — быстрая проверка «система жива», ключевые страницы/действия доступны; не глубокая проверка логики.

---

## 9. 🔄 Регрессионное тестирование

### Что это

Проверка, что **ранее работавшее не сломалось** после изменений. Это не отдельный «вид» теста, а **цель**: мы запускаем уже существующие тесты (unit, интеграционные, e2e), чтобы убедиться, что новая фича или рефакторинг не сломали старые сценарии.

**На собеседовании:** регрессия — проверка, что мы не сломали то, что уже работало; для этого и нужна стабильная база тестов (unit, интеграционные, e2e).

---

## 10. ⚡ Нагрузочное тестирование

### Что это

Проверка поведения системы под **нагрузкой**: много одновременных пользователей, много запросов. Смотрят: время ответа, падение ошибок, деградация. Инструменты: k6, Artillery, JMeter и т.д.

### Где важно

Чаще для бэкенда и инфраструктуры. Фронтенд может участвовать косвенно (например, тестировать, что приложение не «подвисает» при многих операциях), но основная зона — сервер и API.

**На собеседовании:** нагрузочное тестирование — проверка работы под большой нагрузкой (много запросов/пользователей); чаще на бэкенде.

---

## Сводная таблица: что когда использовать

| Вид | Что проверяем | Скорость | Сколько | Когда говорить на собеседовании |
|-----|----------------|----------|---------|----------------------------------|
| **Unit** | Одна функция/модуль в изоляции | Быстро | Много | Основа пирамиды; логика, утилиты, редьюсеры |
| **Интеграционные** | Несколько модулей, модуль + API | Средне | Меньше | Стыки и контракты между частями |
| **E2E** | Полный сценарий в браузере | Медленно | Мало | Критичные пользовательские сценарии |
| **Компонентные** | Один UI-компонент, рендер и действия | Средне | Много | Поведение компонентов без полного e2e |
| **Snapshot** | Неизменность вывода | Быстро | Выборочно | Защита от случайных изменений разметки/структуры |
| **Визуальные** | Совпадение с эталонным скриншотом | Средне | По необходимости | Дизайн-системы, ключевые экраны |
| **A11y** | Доступность (контраст, семантика, клавиатура) | В составе других | В составе тестов | Встраиваем в компонентные/e2e |
| **Smoke** | «Система жива», ключевые пути открыты | Очень быстро | Мало | После деплоя, быстрая проверка |
| **Нагрузочные** | Поведение под нагрузкой | Отдельный процесс | По необходимости | Чаще бэкенд/инфраструктура |

---

## Вопросы на собеседовании

| Вопрос | Краткий ответ |
|--------|----------------|
| Какие виды тестирования знаешь? | Unit, интеграционные, e2e, компонентные, snapshot, визуальные, a11y, smoke, регрессия, нагрузочные. |
| В чём разница unit и e2e? | Unit — одна единица в изоляции, быстро, много. E2E — полный сценарий в браузере, медленно, мало, но реалистично. |
| Что такое пирамида тестов? | Внизу много unit, выше меньше интеграционных/компонентных, наверху мало e2e. Баланс скорости и покрытия реальных сценарий. |
| Зачем e2e, если есть unit? | Unit не проверяют полный путь пользователя и интеграцию; e2e проверяют реальное поведение в браузере. |
| Что такое компонентный тест? | Проверка одного UI-компонента: рендер, пропсы, действия; зависимости мокают. |
| Что такое snapshot-тест? | Сравнение текущего вывода с сохранённым снимком; падает при изменении. |
| Что такое smoke-тесты? | Быстрая проверка «система жива», ключевые вещи открываются после деплоя. |

---

## Кратко для ответа

**Основные виды:** Unit (одна единица кода, изоляция) → интеграционные (модули вместе, API) → e2e (полный сценарий в браузере). Во фронте: компонентные (один компонент), snapshot (снимок вывода), визуальные (скриншоты), a11y (доступность). По цели: smoke (система жива), регрессия (не сломали старое), нагрузочные (под нагрузкой). **Пирамида:** много unit, меньше интеграционных/компонентных, мало e2e — чтобы быстро находить баги и при этом проверять реальное поведение для пользователя.
