## 🛡️ CSP (Content Security Policy)

**CSP** — это политика безопасности контента, которая ограничивает, откуда сайт может загружать ресурсы.

Она защищает от **XSS-атак** и выполнения стороннего кода, разрешая только доверенные источники (белый список).

Настраивается через **HTTP-заголовок** или **meta-тег**.

---

### 💡 Пример:

```
Content-Security-Policy: script-src 'self' https://trusted.cdn.com
```

- ✅ Можно выполнять скрипты только с текущего домена и `trusted.cdn.com`
- ❌ Все остальные скрипты (например, вставленные злоумышленником через XSS) заблокируются

---

### 📝 Примеры ограничений:

- **`script-src`** — откуда можно брать JS
- **`style-src`** — откуда можно брать CSS
- **`img-src`** — откуда можно грузить картинки
- **`default-src`** — правило по умолчанию для всех типов контента

---

### 🔧 Пример полной политики:

```
Content-Security-Policy: 
  default-src 'self';
  script-src 'self' https://trusted.cdn.com;
  style-src 'self' 'unsafe-inline';
  img-src 'self' data: https:;
```

---

### ✅ Преимущества:

- Защита от XSS
- Предотвращение загрузки вредоносного кода
- Контроль источников ресурсов
