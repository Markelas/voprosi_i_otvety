## 🔗 Что такое TCP и UDP

---

### 🔷 TCP (Transmission Control Protocol)

**Коротко:** TCP — это транспортный протокол, обеспечивающий **надёжную передачу данных**.

Он делит поток байтов на небольшие сегменты (чанки), добавляет им номера, и по этим номерам сервер собирает поток обратно.

**Применяется там, где нужна точная и подтверждаемая передача данных:**
- ✅ Отправка фото
- ✅ Переписка
- ✅ Транзакции
- ✅ HTTP запросы (работает поверх TCP)

---

### 🔄 Если что-то потерялось

TCP автоматически пересылает.

---

### 📦 Проблема: данные не помещаются в один пакет

Сеть не может отправить мегабайты данных «одним куском». Каждый IP-пакет (а внутри — TCP-сегмент) имеет ограниченный размер — обычно около **1500 байт** (MTU).

Если файл 2 МБ или даже просто большой JSON, TCP:

1. Разбивает поток байтов на небольшие чанки (сегменты) ~1.5 KB
2. Каждому сегменту добавляет заголовок с номером последовательности (sequence number)
3. Отправляет их по сети один за другим
4. На стороне получателя TCP собирает их обратно в правильном порядке
5. Если какой-то сегмент (ACK) потерялся — запрашивает его повторно

Поэтому браузер может начать получать часть ответа, пока сервер ещё не отправил всё.

Например, `ReadableStream` или `response.body.getReader()` читают данные чанками прямо по мере прихода.

---

### ⚠️ Особенность TCP

TCP гарантирует доставку, но это цена — если один кусок потерян, весь поток ждёт его повторной передачи (head-of-line blocking).

---

### 🔸 UDP (User Datagram Protocol)

**UDP** — транспортный протокол, как и TCP, но **без гарантий доставки и порядка**.

Главная цель — **скорость и минимальные задержки**.

---

### 📊 Характеристики UDP:

| Характеристика | Описание |
|----------------|----------|
| **Без соединения** | Нет "рукопожатия" — данные просто отправляются |
| **Без гарантии** | Пакеты могут теряться, приходить не по порядку или дублироваться |
| **Минимальная задержка** | Быстрее TCP, т.к. нет контроля ошибок и подтверждений |
| **Передача "датаграмм"** | Каждый пакет — независимая единица данных |
| **Нет потока** | В отличие от TCP, UDP не объединяет пакеты в единый поток |

---

### 🎯 Где используется UDP:

- 🎮 **Онлайн-игры** (скорость важнее надёжности)
- 📹 **Стриминг, видео-звонки**
- 📡 **DNS-запросы**
- 💬 **VoIP** (звонки, речь в реальном времени)

---

### 💡 Сравнение:

| Параметр | TCP | UDP |
|----------|-----|-----|
| **Надёжность** | ✅ Гарантирует доставку | ❌ Не гарантирует |
| **Порядок** | ✅ Сохраняет порядок | ❌ Не сохраняет |
| **Скорость** | Медленнее | Быстрее |
| **Соединение** | Нужно установить | Не нужно |
| **Применение** | HTTP, файлы, переписка | Игры, стриминг, DNS |
