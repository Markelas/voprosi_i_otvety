## 🌐 Что происходит, когда в браузере вводят URL и нажимают Enter

---

### 1. 📝 Ввод URL и парсинг

Браузер определяет протокол (`http`, `https`, `ftp` и т.д.). Если URL неполный (например, `google.com`), браузер добавляет протокол по умолчанию (`https://`).

---

### 2. 💾 Проверка кешей

Проверяется:
- **Browser cache**
- **DNS cache**
- **OS cache**

Проверка: нет ли уже готового ответа или IP-адреса.

---

### 3. 🔍 DNS-резолвинг

Если IP неизвестен — запрос идёт к **DNS-серверу**, чтобы узнать IP по домену.

Результат кэшируется.

---

### 4. 🤝 Установление соединения

#### TCP-соединение (3-way handshake)

Процесс установления TCP соединения известен как **"трёхстороннее рукопожатие"**:

1. Клиент отправляет серверу сегмент (только служебную информацию) с флагом **SYN** (synchronize), чтобы инициировать соединение.

2. Сервер отвечает сегментом с флагами **SYN** и **ACK** (acknowledge), подтверждая получение запроса.

3. Клиент отправляет серверу сегмент с флагом **ACK**, подтверждая получение ответа.

Этот сегмент завершает процесс установления соединения, и данные могут передаваться между клиентом и сервером.

#### TLS handshake (если HTTPS)

Если HTTPS — дополнительно **TLS handshake** (обмен ключами, шифрование).

**Важно:** TCP сам по себе не шифрует и не аутентифицирует данные — всё идёт в открытом виде. TLS — шифрует и защищает эти байты, но сам не занимается доставкой. TLS обычно использует TCP как транспорт (пример — HTTPS = HTTP + TLS поверх TCP).

---

### 5. 📤 Отправка HTTP-запроса

Метод (GET), заголовки (Host, User-Agent и др.). Может быть cookie, токены.

---

### 6. 📥 Ответ сервера

Сервер возвращает HTTP-ответ (код состояния, заголовки, тело). Если есть редирект (301, 302) — браузер повторяет запрос по новому адресу.

---

### 7. 🎨 Обработка ответа

- Парсинг HTML → построение **DOM**
- Загрузка CSS → построение **CSSOM**
- Загрузка JS → выполнение JS
- Загрузка картинок, шрифтов, медиа

---

### 8. 🖼️ Отрисовка страницы

- Объединение DOM и CSSOM → **render tree**
- **Layout** (расчёт позиций)
- **Painting** (отрисовка пикселей на экране)
- Возможные последующие перерисовки при выполнении JS
