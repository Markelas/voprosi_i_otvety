## 🌐 Что происходит, когда в браузере вводят URL и нажимают Enter

**Краткий ответ для собеседования:**  
Браузер парсит URL, проверяет кеш, делает DNS-запрос для получения IP, устанавливает TCP-соединение (3-way handshake), для HTTPS — TLS handshake, отправляет HTTP-запрос, получает ответ, парсит HTML, строит DOM и CSSOM, создаёт render tree, делает layout и painting. В процессе загружаются CSS, JS, изображения.

---

### 1. 📝 Ввод URL и парсинг

**Что происходит:**
- Пользователь вводит URL: `https://example.com/page`
- Браузер **парсит URL** на части:
  - **Протокол:** `https://`
  - **Домен:** `example.com`
  - **Путь:** `/page`
  - **Query-параметры:** `?id=123` (если есть)
  - **Хэш:** `#section` (если есть)

**Автозаполнение:**
- Если URL неполный (`google.com`), браузер добавляет протокол (`https://google.com`)
- Если это поисковый запрос — перенаправляет в поисковик

**Проверка протокола:**
- `http://` → порт 80, без шифрования
- `https://` → порт 443, с TLS-шифрованием

---

### 2. 💾 Проверка кешей

Браузер проверяет несколько уровней кеша:

**1. Browser cache (кеш браузера):**
- Проверяется, есть ли закешированный HTML/CSS/JS для этой страницы
- Смотрит на заголовки `Cache-Control`, `Expires`, `ETag`
- Если ресурс свежий — используется из кеша (нет сетевого запроса)

**2. DNS cache (кеш DNS):**
- Есть ли уже IP-адрес для домена `example.com`?
- Сначала проверяется кеш браузера, потом ОС

**3. OS cache (кеш операционной системы):**
- ОС хранит DNS-записи

Если нашли IP и свежий HTML — можно сразу показать страницу. Иначе — идём дальше.

---

### 3. 🔍 DNS-резолвинг (Domain Name System)

**Задача:** преобразовать доменное имя (`example.com`) в IP-адрес (`93.184.216.34`).

**Процесс:**

1. **Проверка локального кеша:**
   - Кеш браузера → кеш ОС → файл hosts

2. **Запрос к DNS-резолверу** (обычно провайдер или 8.8.8.8 — Google DNS):
   - Браузер отправляет DNS-запрос: "Какой IP у `example.com`?"

3. **Рекурсивный поиск** (если резолвер не знает):
   - **Root DNS** → указывает на DNS-сервер зоны `.com`
   - **TLD DNS** (Top-Level Domain) → указывает на DNS-сервер `example.com`
   - **Authoritative DNS** → возвращает IP-адрес

4. **Результат кэшируется:**
   - В браузере, ОС, резолвере (с TTL — время жизни записи)

**Время:** обычно 20-120ms (если не в кеше).

**Типы DNS-записей:**
- **A-запись** — IPv4 адрес
- **AAAA-запись** — IPv6 адрес
- **CNAME** — алиас на другой домен
- **MX** — почтовый сервер

---

### 4. 🤝 Установление соединения

Теперь браузер знает IP-адрес сервера. Нужно установить соединение.

#### TCP-соединение (3-way handshake)

**TCP** (Transmission Control Protocol) — надёжный протокол передачи данных. Гарантирует доставку и порядок пакетов.

**Процесс 3-way handshake:**

1. **SYN** (Synchronize) — клиент → сервер:
   - "Привет, хочу установить соединение"
   - Клиент отправляет случайный номер последовательности (ISN)

2. **SYN-ACK** — сервер → клиент:
   - "Принято, вот мой номер последовательности"
   - Сервер подтверждает (ACK) и отправляет свой ISN

3. **ACK** (Acknowledge) — клиент → сервер:
   - "Подтверждаю, соединение установлено"

После этого можно передавать данные.

**Время:** обычно 1 RTT (Round-Trip Time) — ~20-100ms в зависимости от расстояния до сервера.

**Важно:**  
TCP сам по себе **не шифрует** данные — всё идёт в открытом виде. Для шифрования нужен TLS.

#### TLS Handshake (если HTTPS)

Если протокол `https://`, после TCP следует **TLS Handshake** для установки защищённого соединения.

**Процесс TLS Handshake:**

1. **Client Hello:**
   - Клиент отправляет поддерживаемые версии TLS и алгоритмы шифрования (cipher suites)

2. **Server Hello:**
   - Сервер выбирает версию TLS и алгоритм
   - Отправляет свой **SSL-сертификат** (с публичным ключом)

3. **Проверка сертификата:**
   - Браузер проверяет, выдан ли сертификат доверенным центром (CA — Certificate Authority)
   - Проверяет срок действия и доменное имя

4. **Обмен ключами:**
   - Создаётся общий **секретный ключ** для симметричного шифрования
   - Используется асимметричное шифрование (RSA/ECDHE) для безопасного обмена

5. **Finished:**
   - Обе стороны подтверждают, что соединение защищено
   - Все дальнейшие данные шифруются симметричным ключом

**Время:** дополнительные 1-2 RTT (~20-200ms).

**Итог:**  
HTTP: TCP handshake → готово (1 RTT)  
HTTPS: TCP handshake + TLS handshake → готово (2-3 RTT)

---

### 5. 📤 Отправка HTTP-запроса

Соединение установлено, можно отправлять данные.

**Браузер формирует HTTP-запрос:**

```
GET /page HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64)
Accept: text/html,application/xhtml+xml
Accept-Language: ru-RU,ru;q=0.9,en;q=0.8
Accept-Encoding: gzip, deflate, br
Connection: keep-alive
Cookie: sessionId=abc123
```

**Структура запроса:**
1. **Стартовая строка:** метод (GET), путь (`/page`), версия протокола (HTTP/1.1)
2. **Заголовки:** метаданные (Host, User-Agent, Cookie и т.д.)
3. **Тело:** для POST/PUT (данные формы, JSON) — для GET обычно пусто

**Ключевые заголовки:**
- `Host` — обязательный, доменное имя
- `User-Agent` — информация о браузере
- `Accept` — какие форматы принимает клиент
- `Cookie` — сохранённые cookies
- `Authorization` — токен авторизации (Bearer token)
- `Referer` — откуда пришёл пользователь

Запрос **шифруется** (если HTTPS) и отправляется на сервер.

---

### 6. 📥 Ответ сервера

**Сервер обрабатывает запрос:**
- Маршрутизация (роутинг) — определяет обработчик для `/page`
- Выполнение бизнес-логики (обращение к БД, API и т.д.)
- Формирование ответа

**Сервер отправляет HTTP-ответ:**

```
HTTP/1.1 200 OK
Date: Mon, 27 Jan 2025 12:00:00 GMT
Server: nginx/1.18.0
Content-Type: text/html; charset=UTF-8
Content-Length: 15234
Set-Cookie: sessionId=xyz789; HttpOnly; Secure
Cache-Control: max-age=3600
Connection: keep-alive

<!DOCTYPE html>
<html>
<head>...</head>
<body>...</body>
</html>
```

**Структура ответа:**
1. **Стартовая строка:** версия, код статуса (200), текст статуса (OK)
2. **Заголовки ответа:** метаданные (Content-Type, Set-Cookie)
3. **Тело:** HTML, JSON, изображения и т.д.

**Коды статусов:**
- **2xx** — успех (200 OK, 201 Created, 204 No Content)
- **3xx** — редирект (301 Moved Permanently, 302 Found, 304 Not Modified)
- **4xx** — ошибка клиента (400 Bad Request, 401 Unauthorized, 404 Not Found)
- **5xx** — ошибка сервера (500 Internal Server Error, 503 Service Unavailable)

**Редиректы:**  
Если код 301/302, браузер читает заголовок `Location` и повторяет запрос по новому URL (весь процесс заново: DNS, TCP, запрос).

---

### 7. 🎨 Обработка ответа браузером

Браузер получил HTML. Начинается парсинг и построение страницы.

#### 1. **Парсинг HTML → построение DOM (Document Object Model)**

- Браузер читает HTML построчно
- Создаёт **DOM-дерево** — древовидная структура элементов страницы
- Каждый тег (`<div>`, `<p>`) становится узлом в дереве

**Блокирующие ресурсы:**
- Если встречается `<script>` без `async`/`defer` — парсинг **останавливается** до загрузки и выполнения скрипта
- `<link rel="stylesheet">` — CSS блокирует рендеринг (но не парсинг)

#### 2. **Загрузка и парсинг CSS → построение CSSOM (CSS Object Model)**

- Браузер загружает CSS-файлы (`<link>`, `<style>`)
- Парсит стили и строит **CSSOM-дерево** — структура всех стилей
- CSS — **render-blocking** (блокирует отрисовку, пока не загружен)

#### 3. **Загрузка и выполнение JavaScript**

- Браузер загружает и выполняет JS-скрипты
- JS может изменять DOM и CSSOM (например, `document.createElement()`)
- **Блокирующие скрипты** (`<script src="...">`) останавливают парсинг
- **Асинхронные** (`async`) — загружаются параллельно, выполняются сразу после загрузки
- **Отложенные** (`defer`) — выполняются после полного парсинга HTML

#### 4. **Загрузка других ресурсов**

Параллельно загружаются:
- Изображения (`<img>`)
- Шрифты (`@font-face`)
- Видео, аудио
- Иконки, фавиконки

Эти ресурсы **не блокируют** парсинг HTML.

---

### 8. 🖼️ Отрисовка страницы (Rendering)

После построения DOM и CSSOM начинается отрисовка.

#### 1. **Построение Render Tree (дерева рендеринга)**

- Браузер объединяет DOM и CSSOM
- **Render Tree** — это DOM-дерево + применённые стили
- Исключаются невидимые элементы (`display: none`, `<script>`, `<meta>`)

#### 2. **Layout (Reflow) — вычисление позиций и размеров**

- Браузер рассчитывает **точные координаты** и **размеры** каждого элемента
- Учитывается viewport (размер окна), box model, позиционирование
- **Дорогая операция** — при изменении размеров пересчитываются все зависимые элементы

#### 3. **Painting (Repaint) — отрисовка пикселей**

- Браузер рисует каждый элемент на экране: фон, границы, текст, тени
- Создаются **слои** (layers) для элементов с `position`, `z-index`, `transform`
- Каждый слой рисуется отдельно

#### 4. **Compositing — сборка слоёв**

- Браузер собирает все слои в финальную картинку
- GPU ускоряет этот процесс (особенно для `transform`, `opacity`)
- Пользователь видит страницу

**Последующие обновления:**
- Если JS изменяет DOM/стили — запускается **reflow** (пересчёт layout) и **repaint** (перерисовка)
- Тяжёлые операции (изменение `width`, `height`) вызывают полный reflow
- Лёгкие (`color`, `background`) — только repaint

---

## Таймлайн (примерное время)

| Этап | Время |
|------|-------|
| Парсинг URL | < 1ms |
| Проверка кеша | 1-10ms |
| DNS-запрос | 20-120ms (или 0, если в кеше) |
| TCP handshake | 20-100ms (1 RTT) |
| TLS handshake (HTTPS) | 40-200ms (1-2 RTT) |
| HTTP-запрос/ответ | 20-100ms (1 RTT) |
| Парсинг HTML | 50-200ms |
| Загрузка CSS/JS | 100-500ms |
| Layout + Painting | 50-200ms |
| **Общее время (First Contentful Paint)** | **~300-1500ms** |

**Метрики:**
- **FCP** (First Contentful Paint) — первый контент на экране
- **LCP** (Largest Contentful Paint) — основной контент загружен
- **TTI** (Time to Interactive) — страница интерактивна

---

## Вопросы на собеседовании

| Вопрос | Краткий ответ |
|--------|----------------|
| Что происходит при вводе URL? | Парсинг URL → проверка кеша → DNS → TCP handshake (+ TLS для HTTPS) → HTTP-запрос → ответ → парсинг HTML → DOM + CSSOM → render tree → layout → painting |
| Что такое DNS? | Система преобразования доменного имени в IP-адрес |
| Что такое TCP 3-way handshake? | SYN → SYN-ACK → ACK — установка соединения |
| Зачем TLS handshake? | Для шифрования HTTPS: проверка сертификата, обмен ключами |
| Что такое DOM? | Document Object Model — древовидная структура HTML-элементов |
| Что такое CSSOM? | CSS Object Model — структура всех стилей страницы |
| Что такое Render Tree? | Объединение DOM и CSSOM — дерево элементов с применёнными стилями |
| Что такое Layout (Reflow)? | Вычисление позиций и размеров элементов на странице |
| Что такое Painting? | Отрисовка пикселей элементов на экране |
| Почему `<script>` блокирует парсинг? | JS может изменить DOM, поэтому браузер ждёт выполнения скрипта |
| Как ускорить загрузку? | CDN, кэширование, сжатие (gzip), async/defer для JS, ленивая загрузка изображений, HTTP/2 |

---

## Кратко для ответа

При вводе URL браузер: парсит URL → проверяет кеш → делает DNS-запрос (домен → IP) → устанавливает TCP-соединение (3-way handshake), для HTTPS + TLS handshake → отправляет HTTP-запрос → получает ответ → парсит HTML (строит DOM), загружает CSS (строит CSSOM), выполняет JS → объединяет DOM + CSSOM в Render Tree → делает Layout (вычисляет позиции) → Painting (рисует пиксели) → показывает страницу.
