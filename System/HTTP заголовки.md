## 📋 Что такое HTTP-заголовки

**HTTP-заголовки (headers)** — это дополнительная информация, которую клиент и сервер передают вместе с запросом и ответом.

Управляют тем, как обрабатываются данные, в каком формате, кем, с какими правами и т.д.

---

### 📦 Основные категории:

| Категория | Кто отправляет | Для чего | Примеры |
|-----------|----------------|-----------|----------|
| **📩 Request** | Клиент → Сервер | Сообщить, что и как нужно получить | `Host`, `Accept`, `Authorization`, `User-Agent`, `Accept-Language`, `Cookie` |
| **📤 Response** | Сервер → Клиент | Сообщить, что и как получено | `Content-Type`, `Content-Length`, `Set-Cookie`, `Cache-Control`, `Access-Control-Allow-Origin`, `Server` |
| **📦 Entity** | Описание тела | Дать метаинформацию о данных | `ETag`, `Content-Encoding`, `Last-Modified` |
| **🛡️ Security** | В обе стороны | Защита и контроль доступа | `Content-Security-Policy`, `X-Frame-Options`, `Strict-Transport-Security` |

---

### 🔍 Популярные заголовки:

#### Request:

- **`Host`** 🌐 — Указывает домен сервера (`api.example.com`)
- **`Accept`** 📦 — Какие типы данных клиент принимает (`application/json`, `text/html`)
- **`Authorization`** 🔐 — Токен доступа (`Bearer <jwt>` или `Basic <base64>`)
- **`User-Agent`** 💻 — Кто делает запрос (браузер, приложение и т.д.)
- **`Accept-Language`** 🌍 — Язык ответа
- **`Cookie`** 🍪 — Отправляет сохранённые cookies для авторизации или сессии

#### Response:

- **`Content-Type`** 📄 — Формат данных (`application/json`, `text/html`)
- **`Content-Length`** 📏 — Размер тела ответа (в байтах)
- **`Set-Cookie`** 🍪 — Устанавливает cookie у клиента
- **`Cache-Control`** ⏱️ — Как и сколько кэшировать (`no-cache`, `max-age=3600`)
- **`Access-Control-Allow-Origin`** 🌍 — Разрешает кросс-доменные запросы (CORS)
- **`Server`** 🏗️ — Информация о сервере (например, `nginx/1.24`)

#### Entity:

- **`ETag`** — Уникальный идентификатор версии ресурса (для кэширования)
- **`Content-Encoding`** 🗜️ — Как сжаты данные (`gzip`, `br`)
- **`Last-Modified`** 🕓 — Когда ресурс последний раз изменялся

#### Security:

- **`Content-Security-Policy`** 🛡️ — Ограничивает источники контента (JS, изображения и т.д.)
- **`X-Frame-Options`** 🚫 — Запрещает встраивание сайта в iframe
- **`Strict-Transport-Security`** 🔐 — Разрешает доступ только по HTTPS
