## 🌐 Что такое CORS и зачем он нужен

**CORS (Cross-Origin Resource Sharing)** — это механизм браузера, который позволяет серверу контролировать, кто и с каких доменов может обращаться к его ресурсам, предотвращая несанкционированный доступ.

---

### 🎯 Для чего нужен:

Чтобы браузер по умолчанию не позволял скриптам с чужих сайтов обращаться к твоему API или читать его ответы.

---

### 🛡️ От чего защищает:

От кражи данных и выполнения нежелательных запросов из чужих сайтов (например, чтобы вредоносный сайт не смог через твой браузер незаметно дернуть защищённое API и получить ответ с личными данными).

## 🛡️ **Что защищает CORS**

### **Защищает пользователя от:**

- **Кражи сессий** (cookie, tokens)
    
- **Кражи личных данных**
    
- **Неавторизованных действий от его имени**
    
- **Межсайтовых атак (CSRF)**
  
  ### **НЕ защищает сервер от:**

- **DDoS атак**
    
- **SQL инъекций**
    
- **Прямых запросов** (curl, Postman)
    
- **Ботов**

---

### 🔄 Как работает:

**Origin** (`<протокол>://<домен>:<порт>`) — комбинация протокола, домена и порта (например, `https://example.com:443`).

- Если JS делает запрос на другой origin, браузер проверяет заголовки ответа сервера (`Access-Control-Allow-Origin` и др.).
- Если разрешено — отдаёт ответ JS-коду.
- Если запрещено — блокирует доступ на уровне браузера.

---

### 📦 Типы запросов:

#### 🔹 Простой запрос (simple request)

Браузер сразу отправляет запрос (например, GET или POST с `application/x-www-form-urlencoded`) и проверяет заголовки в ответе.

Если заголовков нет или они не подходят — ответ блокируется для JS.

#### 🔹 С предварительной проверкой (preflight request)

Если запрос не «простой» (например, PUT, DELETE, PATCH или с кастомными заголовками), браузер сначала отправляет запрос **OPTIONS**.

**Зачем?** Чтобы спросить у сервера: «Можно ли с моего origin выполнять такой метод и такие заголовки?»

---

### 💡 Пример заголовков:

```
Access-Control-Allow-Origin: https://example.com
Access-Control-Allow-Methods: GET, POST, PUT
Access-Control-Allow-Headers: Content-Type, Authorization
```
