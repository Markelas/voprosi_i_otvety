## 💾 LocalStorage, sessionStorage, cookie, IndexedDB

---

### 🔸 localStorage

**Хранилище в браузере для ключ–значение.**

- **Объём:** ~5–10 МБ (зависит от браузера)
- **Время жизни:** до явного удаления (не очищается при закрытии вкладки)
- **Доступ:** только на стороне клиента (JS в браузере)
- **Область действия:** Домен и протокол

**API:**

```js
localStorage.setItem(key, value);
localStorage.getItem(key);
localStorage.removeItem(key);
localStorage.clear();
```

**Использование:** Кэш настроек, тема сайта, сохранение формы и т.д.

---

### 🔸 sessionStorage

**Хранилище в браузере для ключ–значение, как localStorage, но живёт только в рамках одной вкладки.**

- **Объём:** ~5 МБ
- **Время жизни:** пока открыта вкладка (исчезает при закрытии)
- **Доступ:** только на стороне клиента

**Использование:** Временные данные в рамках сессии пользователя (например, шаги формы).

---

### 🍪 Куки (Cookies)

**Маленькие фрагменты данных, которые браузер хранит и отправляет на сервер при каждом HTTP-запросе к этому домену.**

- **Объём:** ~4 КБ на куку
- **Время жизни:** определяется параметром `Expires` или `Max-Age`

**Использование:** Авторизация, трекинг, хранение настроек.

**Параметры безопасности:**

- **`HttpOnly`** — недоступно из JS (защита от XSS)
- **`Secure`** — только по HTTPS
- **`SameSite`** — защита от CSRF

**API (в JS):**

```js
document.cookie = "key=value; path=/; expires=...";
```

**Минус:** Передаются на сервер при каждом запросе → увеличивают трафик.

---

### 🗄️ IndexedDB

**Асинхронная база данных в браузере для хранения больших объёмов структурированных данных.**

- **Объём:** Десятки или сотни МБ
- **API:** Асинхронный, событийный (или через библиотеки вроде `idb`)

**Особенности:**

- ✅ Поддерживает транзакции
- ✅ Можно хранить объекты, файлы, бинарные данные
- ✅ Работает только в браузере, без автоматической отправки данных на сервер

**Использование:** Кэш оффлайн-приложений, сохранение больших данных (фото, оффлайн-версии страниц).

---

### 📊 Сравнение:

| Хранилище | Объём | Время жизни | Доступ из JS | Отправка на сервер |
|-----------|-------|-------------|--------------|-------------------|
| **localStorage** | 5–10 МБ | До удаления | ✅ | ❌ |
| **sessionStorage** | ~5 МБ | До закрытия вкладки | ✅ | ❌ |
| **Cookies** | ~4 КБ | По Expires/Max-Age | ✅ | ✅ При каждом запросе |
| **IndexedDB** | Десятки/сотни МБ | До удаления | ✅ | ❌ |
