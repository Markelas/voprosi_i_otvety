## 💼 Вопросы по архитектуре на собеседовании

---

### 🔹 Что такое композиция vs наследование?

**Композиция** — сборка сложных объектов из простых компонентов. **Наследование** — создание новых классов на основе существующих.

**В React:** используем композицию (компоненты собираются из других компонентов) вместо наследования классов.

---

### 🔹 Что такое избыточная абстракция?

Создание абстракций там, где они не нужны, усложняющих код без реальной пользы.

**Пример:** создание сложной системы классов для простой задачи, которую можно решить одной функцией.

---

### 🔹 Как избежать prop drilling?

- **Context API** — для глобального состояния
- **State management** (Redux, Zustand) — для сложного состояния
- **Композиция компонентов** — передача компонентов через children

---

### 🔹 Что такое Feature-based структура папок?

Организация кода по фичам, а не по типам файлов.

```
features/
  users/
    components/
    hooks/
    api/
    types/
```

**Преимущества:** все связанное с фичей находится в одном месте, легче навигация.

---

### 🔹 Зачем нужны константы и конфигурация?

- Централизованное управление значениями
- Легче изменять поведение без изменения логики
- Избегание магических чисел/строк в коде

---

### 🔹 Что такое инверсия зависимостей в React?

Компоненты не зависят от конкретных реализаций, а получают зависимости через props.

**Пример:** компонент получает функцию `onSubmit` через props, а не вызывает `fetch` напрямую.
