### КОРОТКО

<font color="#de7802">**Принципы ООП**</font>
 
<font color="#de7802">Объектно-ориентированное программирование (ООП)</font> — это парадигма программирования, основанная на концепции объектов, которые содержат данные (свойства) и код (методы).


1. <font color="#0070c0">**Инкапсуляция**</font>
    Скрытие внутренней реализации объекта и предоставление доступа к данным через публичные методы.
      **Пример:** Объект "машина" имеет закрытые переменные **`скорость`** и **`топливо`**, доступ к которым возможен только через методы **`увеличитьСкорость()`** и **`проверитьТопливо()`**.
    
2. <font color="#c00000">**Абстракция**</font>
    Сокрытие сложных деталей и представление только важных характеристик.
       **Пример:** Класс "машина" имеет методы **`ехать()`** и **`останавливаться()`**, но детали работы двигателя скрыты.
    
3. <font color="#76923c">**Полиморфизм**</font>
    Возможность использовать один интерфейс для разных типов объектов.
      **Пример:** Метод **`рисовать()`** может быть применен к объектам классов **`Круг`**, **`Квадрат`**, каждый из которых реализует этот метод по-своему.
    
4. <font color="#7030a0">**Наследование**</font>
    Механизм, позволяющий создать новый класс на основе существующего.
      **Пример:** Класс **`Электромобиль`** наследуется от класса **`Машина`** и добавляет свою функциональность, например **`зарядкаБатареи()`**.
---

### 🏗️ Четыре основных принципа ООП:

#### 1. **Инкапсуляция**

Объединение данных и методов в одном объекте, скрытие внутренней реализации.

**В JavaScript:**

```js
class BankAccount {
  #balance = 0;  // приватное свойство
  
  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
    }
  }
  
  getBalance() {
    return this.#balance;
  }
}
```

**Суть:** Внутренние детали (`#balance`) скрыты, доступ только через публичные методы.

---

#### 2. **Наследование**

Способность объекта наследовать свойства и методы от другого объекта.

**В JavaScript:**

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
  
  speak() {
    return `${this.name} makes a sound`;
  }
}

class Dog extends Animal {
  speak() {
    return `${this.name} barks`;
  }
}
```

**Суть:** `Dog` наследует от `Animal`, может переопределять методы.

---

#### 3. **Полиморфизм**

Способность объектов разных типов обрабатываться через единый интерфейс.

**В JavaScript:**

```js
class Shape {
  area() {
    throw new Error('Must be implemented');
  }
}

class Circle extends Shape {
  constructor(radius) {
    super();
    this.radius = radius;
  }
  
  area() {
    return Math.PI * this.radius ** 2;
  }
}

class Rectangle extends Shape {
  constructor(width, height) {
    super();
    this.width = width;
    this.height = height;
  }
  
  area() {
    return this.width * this.height;
  }
}

// Полиморфизм: разные объекты, один интерфейс
const shapes = [new Circle(5), new Rectangle(4, 6)];
shapes.forEach(shape => console.log(shape.area()));
```

**Суть:** Разные классы реализуют один метод по-своему, но вызываются одинаково.

---

#### 4. **Абстракция**

Упрощение сложных систем через выделение только важных характеристик.

**В JavaScript:**

```js
class Car {
  start() {
    this.#checkEngine();
    this.#ignite();
    console.log('Car started');
  }
  
  #checkEngine() { /* сложная логика */ }
  #ignite() { /* сложная логика */ }
}

const car = new Car();
car.start(); // Пользователю не нужно знать про #checkEngine и #ignite
```

**Суть:** Скрываем сложность, показываем только нужный интерфейс.

---

### 📊 Сравнение с функциональным подходом:

| Параметр | ООП | Функциональный |
|----------|-----|----------------|
| **Основа** | Объекты с состоянием | Функции без состояния |
| **Данные** | Инкапсулированы в объектах | Передаются как параметры |
| **Изменение** | Мутация состояния | Иммутабельность |
| **Фокус** | Что объект делает | Что функция вычисляет |

---

### 💡 Когда использовать ООП:

✅ **Подходит для:**
- Сложных систем с множеством сущностей
- Когда нужно моделировать реальный мир
- Когда важна инкапсуляция и переиспользование кода

❌ **Не всегда нужно:**
- Простые задачи можно решить функциями
- Не стоит усложнять там, где не требуется

---

### 🎯 Итог:

ООП помогает организовать код через объекты, которые инкапсулируют данные и поведение, наследуют друг от друга и могут использоваться полиморфно.
